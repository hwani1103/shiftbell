{
  "name": "shiftbell-dashboard-sync",
  "private": true,
  "version": "1.0.0",
  "description": "GA4 Data API -> Firestore(dashboard/*) 동기화. 관리자 대시보드가 읽는 집계 문서를 만든다.",
  "type": "module",
  "scripts": {
    "sync": "node sync.mjs",
    "sync:mock": "node sync.mjs --mock",
    "test": "node --test",
    "admin-user": "node admin-user.mjs",
    "seed:emulator": "node seed-emulator.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "google-auth-library": "^10.0.0"
  }
}
