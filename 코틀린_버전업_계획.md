# Kotlin 버전업 계획 (보류 중)

> 2026-08-24 시도했다가 보류. UI 작업 끝난 뒤 다시 진행할 것.

---

## 왜 필요한가

AdMob 배너 광고를 붙이면서 최신 `google_mobile_ads`(9.x)를 넣었더니, 그게 끌고 오는
네이티브 `play-services-ads(25.4.0)`가 이 프로젝트의 Kotlin 컴파일러(2.1.0)보다
최신 metadata(2.3.0)로 컴파일돼 있어서 `compileDevDebugKotlin`이 실패했다.

지금은 광고 플러그인을 호환 버전(`google_mobile_ads: 5.1.0`, 네이티브
`play-services-ads: 23.0.0`)으로 고정해서 우회해둔 상태 - 테스트 광고 자리를
확보하는 데는 문제없지만, 영구히 쓸 방법은 아니다 (이유는 `ad_config.dart`,
`pubspec.yaml`의 관련 주석 참고 - SDK 버전이 오래될수록 구글의 최소 버전 정책에
걸릴 수 있고, 개인정보/동의 관련 최신 기능도 못 받음).

## 실제로 시도해본 것

`android/settings.gradle.kts`의 Kotlin 플러그인을 `2.1.0 → 2.3.0`으로 올리고
`google_mobile_ads`를 다시 `^9.1.0`으로 돌린 뒤 `compileDevDebugKotlin`을 돌려봄.

**결과: 한 군데 더 걸림.** `android/app/build.gradle.kts:35`의

```kotlin
kotlinOptions {
    jvmTarget = JavaVersion.VERSION_11.toString()
}
```

이 예전 방식(`kotlinOptions` DSL)이 Kotlin 2.3.0부터는 **에러**로 처리됨
("Using 'jvmTarget: String' is an error. Please migrate to the compilerOptions DSL").
새 `compilerOptions` DSL로 바꿔야 함:

```kotlin
kotlin {
    compilerOptions {
        jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_11)
    }
}
```

(정확한 문법은 실제 진행 시 `android/app/build.gradle.kts`의 현재 구조를 보고
공식 마이그레이션 가이드 https://kotl.in/u1r8ln 참고해서 맞출 것 - 위는 대략적인 형태)

이 외에 다른 컴파일 에러가 더 있는지는 이 지점에서 멈췄기 때문에 확인 못 함 -
이 하나를 고친 뒤 다시 돌려봐야 함.

## 리스크 평가 (2026-08-24 기준 판단)

- 이 프로젝트의 네이티브 Kotlin 코드(19개 파일)는 코루틴, `@OptIn`, 최신 문법
  (`sealed interface`/`data object`/`context receiver`/`value class`)을 전혀 안 씀 -
  전부 `SQLiteOpenHelper`/`BroadcastReceiver`/`Service`/`Activity`/`AlarmManager` 같은
  오래되고 안정적인 안드로이드 표준 API만 사용. Kotlin 2.x 안에서는 소스 호환성이
  매우 강하게 보장되므로, 문법 자체가 깨질 가능성은 낮음.
- 실제로 걸린 건 문법 문제가 아니라 **빌드 스크립트(Gradle DSL) 마이그레이션**
  문제 - `kotlinOptions` → `compilerOptions`. 흔한 종류의 이슈고 고칠 수 있음.
- 다만 이 코드가 **알람을 직접 실행하는 네이티브 코드 그 자체**이므로, Kotlin
  버전을 바꾸는 건 전체가 다시 컴파일되는 변경임. 진행할 땐 `flutter analyze`만
  보고 끝내지 말고 **전체 빌드(모든 flavor) + 실기기 설치 + 알람/위젯 실제 동작
  확인**까지 거칠 것 - DB 스키마 바꿀 때와 같은 기준
  (`DB_스키마_변경_가이드.md` 참고, 알람/위젯이 "조용히" 죽는 사고가 이 앱에서
  세 번 있었던 전례가 있어 이 프로젝트는 특히 보수적으로 검증함).

## 진행 순서 (재개할 때)

1. `android/settings.gradle.kts`: Kotlin `2.1.0 → 2.3.0`
2. `pubspec.yaml`: `google_mobile_ads: 5.1.0` → `^9.1.0`
3. `lib/services/ad_service.dart`: `AdSize.getAnchoredAdaptiveBannerAdSize` →
   `AdSize.getLargeAnchoredAdaptiveBannerAdSizeWithOrientation` (v9의 비-deprecated API)
4. `android/app/build.gradle.kts`: `kotlinOptions` → `compilerOptions` DSL 마이그레이션
5. `cd android && ./gradlew :app:compileDevDebugKotlin` 로 컴파일만 먼저 확인
   (또 다른 에러가 나올 수 있음 - 하나씩 잡을 것)
6. 전체 빌드: `flutter build apk --debug --flavor dev`,
   `flutter build appbundle --release --flavor prod`
7. `./gradlew :app:checkDartKotlinSync` - Dart↔Kotlin 상수 가드 통과 확인
8. 실기기에 `--flavor dev`로 설치 (⚠️ flavor 반드시 지정 - `CLAUDE.md` 참고),
   알람 등록/울림, 홈 위젯, 10일치 갱신이 평소대로 동작하는지 확인
9. 문제없으면 이 문서는 지우거나 "완료"로 표시

## 언제 하나

- **UI 작업(테마/메모 등)이 끝난 뒤**, 그리고 **실제 AdMob 광고 ID로 전환하기 전**
  둘 사이 어느 시점. 지금 당장 급한 건 아님 - 테스트 ID로 자리만 확보하는 데는
  5.1.0 고정으로 충분함.
