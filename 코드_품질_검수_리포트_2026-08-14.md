# 코드 품질 검수 리포트 (2026-08-14)

> ✅ **후속 갱신**: 아래 A2, A3, E2(+E1도 같이) 세 항목은 사용자와 리스크를 상세히 논의한 뒤
> 실제로 수정 완료함(트랜잭션 범위를 좁게 잡아서, 다른 화면/흐름엔 영향 없게 함).
> `flutter analyze` + `gradlew compileDevDebugKotlin` 둘 다 통과. 실기기 검증은 아직 안 함.
> B1/B2(4번, `onReceive()` 동기 호출/500ms 지연)는 실기기 검증 없이는 손대지 않기로 결정 -
> 그대로 둠. 자세한 내용은 각 항목 아래에 남겨둠.

사용자 요청으로 "업데이트 전 마지막 점검" 목적의 전면 코드 리뷰. **코드는 건드리지 않고 검토만
수행함.** 알람 생성/스케줄링(A), 알림 생명주기(B), 갱신/부팅/자정 로직(C), UI 반응형(D), 데이터
무결성(E) 다섯 갈래로 나눠서 메서드 단위로 훑었음. 심각도는 재현 가능성×영향도 기준 상대평가.

## 커버리지 고지 (정직하게)

- **매우 깊게 봄**: 알람 관련 네이티브 전체(Custom/Guard/RefreshEngine/ActionHelper/Overlay/
  Activity/Player/RefreshUtil/DirectBoot/RefreshLockManager), `database_service.dart`의 핵심
  메서드들, `DatabaseHelper.kt` 전체, `alarm_provider.dart`, settings/all_teams/all_shifts/
  work_hours/all_alarms_history/memo_list 화면들.
- **이번 세션에 새로 만들었거나 고친 코드**(친구공유 Firestore 전환, 위젯 되돌리기)는 작성하며
  이미 검증됨 - 이 리포트에서 다시 반복 안 함, 이전 대화 내용 참고.
- **가볍게 훑거나 이전 세션 감사에 의존**: `calendar_tab.dart`(방대하고 이미 여러 라운드 손봐온
  파일), `calendar_theme_lab/picker`(별도 UI 감사 완료 - `project-ui-robustness-audit` 메모리
  참고), `models/*.dart`, `memo_provider.dart`/`overtime_provider.dart`/
  `work_hours_settings_provider.dart`, `onboarding_screen.dart`(입주 화면만 샘플 확인),
  `splash_screen.dart`/`permission_intro_screen.dart`. 이 파일들에 숨은 버그가 없다고
  100% 장담은 못 함 - "전혀 안 봄"과는 다르지만 "메서드 하나하나 다" 수준은 아님.

---

## A. 알람 생성/스케줄링 신뢰성

### A1. [MEDIUM] `RefreshLockManager`의 락이 TTL 만료 시 실제 완료 여부를 확인 안 함
`RefreshLockManager.kt:40` — `tryAcquire`의 기본 TTL 30초는 "이 정도면 충분히 길겠지"라는
가정일 뿐, `AlarmRefreshEngine.doRefresh()`가 실제로 끝났는지는 확인하지 않음. 만약 어떤 이유로
(기기 성능 저하, 발열 스로틀링, 디스크 I/O 지연 등) 갱신이 30초를 넘기면, 그 사이 들어온 다른
트리거가 "락 만료됨"으로 판단하고 두 번째 `doRefresh()`를 동시에 실행할 수 있음 - 원래 이 락이
막으려던 바로 그 race condition이 부활하는 경로. 발생 확률은 낮지만(정상적인 SQLite 트랜잭션은
수십~수백 ms), 락 메커니즘 자체의 설계 결함이라 기록해둠.

### A2. [MEDIUM → ✅ 수정됨] `AlarmPlayer`가 겹쳐 울리면 시스템 알람 볼륨이 영구히 절반으로 고정될 수 있음
`AlarmPlayer.kt:174-183`(`playCustomSound`), `221-232`(`playDefaultSound`) — 재생 시작할 때마다
"원래 볼륨"을 `originalAlarmVolume`에 저장하고 재생 후 `stopAlarm()`이 복원함. 그런데 알람 B가
울릴 때 알람 A가 아직 재생 중(=아직 `stopAlarm()` 안 됨)이면, B의 `playAlarmWithSettings`가 A의
MediaPlayer를 정지시키지만(44-53행) **볼륨 복원 없이** 그 시점의(이미 50%로 낮아진) 볼륨을 다시
"원래 볼륨"으로 잘못 저장함. 이후 B가 `stopAlarm()`되면 "복원"이 실제로는 50%로 굳혀버림 - 사용자가
수동으로 시스템 알람 볼륨을 다시 올리기 전까지 계속 낮은 채로 남음. 두 알람이 근접한 시간(같은 근무에
템플릿 여러 개, 스누즈가 다음 알람과 겹침 등)에 울리면 재현 가능.

### A3. [LOW → ✅ 수정됨] `DatabaseService.deleteAlarm()`이 트랜잭션으로 안 묶여 있음
`database_service.dart:558-605` — 이력 insert(577-590행)와 알람 delete(600-604행)가 별개의 DB
호출. 프로젝트 전반에 이 정확한 패턴(이력+삭제/생성 묶음)이 여러 번 트랜잭션 버그로 발견/수정된
전례가 있는데(메모리 참고: `regenerateFixedAlarms`, `replaceAllAlarmTemplates` 등), 이 함수만
비어있음 - `alarm_provider.dart`의 `deleteAlarm()`을 거쳐 앱에서 알람 취소할 때마다 타는 실제
경로. 그 사이에 프로세스가 죽으면 "이력엔 삭제됐다고 나오는데 alarms 테이블엔 그대로 남아있는"
상태가 될 수 있음.

### A4. [정보] `deleteAllAlarmsCompletely()` 실제 사용처 미확인
`alarm_provider.dart:286` - 코드는 멀쩡하지만 호출부를 못 찾음(별도로 완전 검증은 안 함, grep
샘플 기준). 죽은 코드라면 무해하지만, 살아있는 진입점이라면 최근에 위 A3와 동일한 트랜잭션
부재 패턴이 반복돼 있진 않은지 한 번 더 볼 가치 있음.

---

## B. 알림(Notification) 생명주기

### B1. [MEDIUM] 알람 소리 재생이 `BroadcastReceiver.onReceive()` 안에서 동기(블로킹) 호출됨
`CustomAlarmReceiver.kt:61` — `AlarmPlayer.playAlarmFromDB(id)`를 `onReceive()`에서 직접,
동기적으로 호출함. 그 안에서 `MediaPlayer().prepare()`(비동기 `prepareAsync()`가 아니라 블로킹
버전, `AlarmPlayer.kt:205`/`253`)까지 호출됨. `BroadcastReceiver.onReceive()`는 시스템이 짧은
시간 안에 반환되길 기대하는(ANR 타임아웃 대상) 콜백이라, `prepare()`가 느려지는 기기/상황(저사양
기기, 저장소 I/O 지연, 드문 DRM/코덱 초기화 지연 등)에서는 이론상 알람 자체가 ANR로 지연되거나
표시 자체가 실패할 수 있음. 평소엔 `prepare()`가 수십 ms 안에 끝나서 거의 안 드러나는 문제지만,
"알람 신뢰성"이 최우선인 앱 특성상 구조적으로 가장 리스크가 큰 지점 중 하나.

### B2. [MEDIUM] 알람 UI 표시가 `Handler.postDelayed(500ms)`로 지연되는데, 그 시점에 이미
`onReceive()`가 반환된 뒤임
`CustomAlarmReceiver.kt:72-86` — 화면 깨우기/잠금 확인 후 `Handler(Looper.getMainLooper())
.postDelayed({ ... }, 500)`로 `AlarmActivity`/`AlarmOverlayService` 표시를 예약함. 이 콜백이
실행되는 시점은 이미 `onReceive()`가 반환되고 500ms가 지난 뒤 - 매니페스트 등록 리시버는
`goAsync()` 없이 `onReceive()`가 끝나면 시스템이 프로세스를 살려둘 의무가 없어짐. 실제로는 직전에
잡은 10초짜리 WakeLock(`wakeUpScreen()`)이 대부분의 상황에서 프로세스 생존을 지켜주지만, 이론적
안전망(OS가 프로세스를 정말 죽이면 알람 소리는 나는데 화면(Activity/Overlay)은 영영 안 뜸)은
아님. B1과 함께, "알람이 울리긴 하는데 어떤 상황에서 화면이 안 뜬다"는 보고가 들어온다면 가장
먼저 의심할 지점.

### B3. [LOW/정보] `snoozeOverlay` MethodChannel + `ACTION_SNOOZE_OVERLAY` 경로는 죽은 코드
`MainActivity.kt:252-261`, `AlarmOverlayService.kt:56-59` - 정의는 돼 있지만 Dart 쪽에서
`snoozeOverlay`를 호출하는 곳이 전혀 없음(`dismissOverlay`만 `next_alarm_tab.dart`/
`calendar_tab.dart`에서 씀). 실제로 문제를 일으키진 않지만, 나중에 "왜 스누즈가 앱 UI에서
반영이 안 되지" 하고 이 경로를 의지해서 디버깅하면 시간 낭비함 - 지금은 앱 안에 스누즈를
직접 트리거하는 UI 자체가 없어서 애초에 필요가 없는 상태.

### B4. [정보] `AlarmActivity`/`AlarmOverlayService`의 `alarmDuration` 기본값은 3분, DB
`alarm_types.duration`을 정상적으로 읽어옴
CLAUDE.md에 "테스트용 1분 하드코딩"이라고 적혀있는데(AlarmOverlayService.kt:154 등 지목)
**현재 코드엔 그 하드코딩이 없음** - 이미 실제 DB 설정값을 쓰도록 바뀐 상태로 보임. CLAUDE.md가
낡은 상태일 가능성이 높음 - 다음에 CLAUDE.md 갱신할 기회가 있으면 이 항목부터 사실 확인 후
정리 권장.

---

## C. 갱신/부팅/자정 로직

### C1. [LOW] `MainActivity`의 `refreshReceiver`가 `onCreate`~`onDestroy` 전체 동안 등록됨
`MainActivity.kt:41` 주석에 "onResume으로 이동 예정"이라고 스스로 적어놓고 아직 안 옮김 - Activity가
백그라운드(paused, not destroyed)인 동안에도 `FLUTTER_REFRESH` 브로드캐스트를 받으면 300ms 뒤
`methodChannel?.invokeMethod(...)`를 시도함. 대부분 무해하지만(Flutter 엔진이 계속 붙어있는
구조라), 의도한 설계와 실제 구현이 다르다는 걸 팀 스스로 알고 있었던 미완료 리팩터링.

### C2. [LOW] 재부팅 시 첫 알람은 `setAlarmClock`, 그 직후 재조정은 `setExactAndAllowWhileIdle` -
서로 다른 API가 같은 알람 ID를 놓고 밀어냄
`DirectBootReceiver.kt:160-161`이 `AlarmManager.setAlarmClock()`으로 긴급 등록한 직후, 곧바로
`AlarmRefreshEngine.refresh(context)`(55행)가 같은 PendingIntent 조합으로 `setExactAndAllowWhileIdle`
재등록을 실행해서 사실상 덮어씀. 두 호출 사이 시간이 극히 짧아 실질적 위험은 거의 없지만, "왜
굳이 두 API를 섞어 쓰는지" 의도가 불명확하고, `setAlarmClock`은 시스템 UI에 "다음 알람" 아이콘을
잠깐 노출시켰다가 사라지는 부작용이 있음 - 인지적 노이즈에 가까움, 기능적 버그는 아님.

### C3. [정보] 재부팅 복구 경로가 `LOCKED_BOOT_COMPLETED` 하나뿐
`AndroidManifest.xml`에 일반 `BOOT_COMPLETED` 리시버가 없음(권한은 선언돼 있는데 실제 리시버가
없음) - Device Protected Storage만 쓰는 이 앱 구조상 의도적 설계로 보이나, 특정 제조사(배터리
최적화가 공격적인 일부 OEM)에서 `LOCKED_BOOT_COMPLETED` 전달이 지연/누락되는 사례가 보고된 적
있는 브로드캐스트라, 유일한 복구 경로가 여기 하나뿐이라는 점은 "이중 안전장치가 없다"는 의미에서
기록해둠. 지금 당장 문제는 아니지만 "재부팅 후 가끔 알람이 하나도 안 잡혀있다"는 보고가 들어오면
1순위 용의선상.

### C4. [정보] `lib/services/alarm_refresh_service.dart`의 `AlarmRefreshService` 클래스는
호출부가 전혀 없는 죽은 코드
`main.dart:242` 주석에 "여기서 AlarmRefreshService도 따로 호출해서 중복 실행되던 문제 → 제거"라고
스스로 적어놨음 - 의도적으로 안 쓰게 된 것 확인. `AlarmRefreshHelper`(이전에 삭제됨, 메모리 참고)와
같은 계열의 정리 후보. 지금 있어도 해는 없음, 다음에 알람 리팩터링할 때 같이 지우면 좋음.

---

## D. UI 반응형 / 오버플로우

### D1. [MEDIUM, 기존에 이미 플래그됨·아직 미해결] `calendar_tab.dart`의 `TableCalendar`가 고정
`rowHeight`를 씀
`project-ui-robustness-audit` 메모리에 이미 기록된 항목, 이번 세션에도 재확인만 함(코드 안 바뀜) -
폴더블 메인 화면(정사각형에 가까운 비율)이나 분할화면/멀티윈도우처럼 세로 공간이 부족한
상황에서 6주 그리드가 실제 화면보다 커질 수 있음. `friend_calendar_view.dart`는 이번 세션에
`LayoutBuilder` 기반 반응형으로 고쳤지만(친구공유 작업 중), **정작 진짜 메인 달력탭은 아직 고정
값 그대로**. 후속 작업 후보 1순위.

### D2. [LOW] `AllShiftsView`(전체 근무표)의 조별 테이블이 좁은 화면에서 셀당 폭이 매우 좁아질 수 있음
`all_shifts_view.dart:351-354` - 조 이름 열은 `FixedColumnWidth(22.w)`, 나머지 최대 11개
날짜열은 `FlexColumnWidth(1)`로 균등분배. 폴더블 커버 화면처럼 극단적으로 좁은 폭에서는 칸당
폭이 아주 좁아짐 - `Text`에 `overflow`/`maxLines` 지정이 없어서(406-444행, 480-487행) 이론상
줄바꿈되면 고정 `height: 34.h/36.h` Container를 넘칠 수 있음. 실사용 폭 범위에서는 문제 안 될
가능성이 높지만 방어 코드가 없다는 점만 기록.

### D3. [정보] `all_teams_setup_dialog.dart`와 `all_shifts_view.dart`가 기준일
`DateTime(2024,1,1)`을 각자 파일에 독립적으로 하드코딩
공유 상수로 안 묶여 있어서, 나중에 둘 중 하나만 고치면 조용히(에러 없이) 모든 팀 근무 조회가
하루씩 밀리는 사고가 날 수 있음 - 지금 당장은 두 값이 일치하므로 버그 아님, 유지보수 리스크로만
기록.

### D4. [정보] `settings_tab.dart`의 `_showAllTeamsSetupDialog`(1197행)는 진입 버튼이 삭제된
고아 메서드
자체 주석("나중에 재사용할 수 있어 그대로 둠")으로 의도적임이 확인됨 - `all_shifts_view.dart`
쪽의 동일 기능 진입점은 살아있음. 버그 아님, 중복 코드 정리 후보로만 기록.

### D5. [정보/재확인] `friend_list_screen.dart`의 "친구 추가" 버튼이 `Expanded`로 안 감싸여 있음
기존 메모리에 낮은 확신도로 기록된 항목 - 이번 세션에 화면을 다시 만들면서도 동일 패턴 유지됨
("내 근무표 공유하기"만 `Expanded`, "친구 추가"는 자기 콘텐츠 크기). 극단적으로 좁은 화면이 아니면
문제 없음, 재확인만 하고 넘어감.

---

## E. 데이터 무결성 / 기타

### E1. [MEDIUM → ✅ 수정됨] 근무명 변경 시 `alarm_creation_log.shift_type`이 갱신 안 됨
`database_service.dart:updateShiftNames()`(777-812행)가 `alarms`/`shift_alarm_templates`/
`alarm_history` 세 테이블만 이름을 바꾸고 **`alarm_creation_log`는 빠짐**. 이 로그는 정책상
영구 보존(절대 삭제 안 함)인데, 근무명을 한 번이라도 바꾸면 그 시점 이전 생성 기록은 옛날
이름으로 영원히 남아서 `alarm_history`(새 이름)와 대조했을 때 서로 다른 이름을 가리키게 됨 -
"언제 이 알람이 왜 생겼는지" 감사(audit) 목적의 로그인데, 이름이 바뀌면 그 감사 가치가 훼손됨.
쉽게 재현 가능(설정 → 근무명 변경 → 알람 이력 화면에서 생성 로그와 결과 로그를 대조).

### E2. [MEDIUM → ✅ 수정됨] `_applyShiftNameChanges`(settings_tab.dart:1000)가 DB 갱신과 스케줄 저장을
트랜잭션으로 안 묶음
`updateShiftNames()`(DB 3개 테이블) 호출과 `scheduleProvider.notifier.saveSchedule()`(pattern/
shiftColors/assignedDates 등) 사이에 프로세스가 죽으면, "알람 테이블은 새 이름인데 스케줄
패턴은 옛 이름"이거나 그 반대인 상태가 될 수 있음 - 이 정확한 클래스의 버그(관련된 여러 테이블
갱신을 트랜잭션 하나로 안 묶어서 생기는 tearing)가 이 프로젝트에서 최소 세 번 이상 독립적으로
발견/수정된 전례가 있는데(메모리 참고), 근무명 변경 경로만 그 패턴이 아직 안 남아있음.

### E3. [정보] `DatabaseHelper.kt`/`database_service.dart`의 버전 동기화는 이번 세션(v17)
기준으로 정확히 일치 확인됨
과거 두 번이나 실제로 어긋나서 위젯이 망가진 전례가 있는 값이라 재확인함 - 현재는 문제 없음.
**다음에 Flutter DB `version:`을 올릴 때 반드시 같은 커밋에서 `DATABASE_VERSION`도 같이 올릴 것**
(이미 파일 안에 큰 경고 주석 있음, 계속 지켜지고 있는지만 앞으로 매번 확인 필요).

### E4. [정보] `all_alarms_history_view.dart`의 "테스트 전용 전체 이력 삭제" 버튼은 여전히 UI에
연결 안 된 상태
기존에 이미 열린 질문으로 기록돼 있던 것(메모리 참고) - 이번에도 그대로임, 사용자가 아직 "완전
삭제할지 버튼을 연결할지" 결정 안 함. 새로 발견한 건 아니고, 아직 안 끝났다는 걸 재확인.

---

## 요약 (가장 먼저 볼 가치가 있는 순서)

1. **E1/E2** — 근무명 변경 기능을 실제로 쓴다면 가장 확실하게 재현되는 데이터 불일치. 근무명을
   자주 안 바꾼다면 우선순위 낮음.
2. **A2** — 알람이 근접한 시간에 여러 개 겹칠 수 있는 사용자(잔업/커스텀 알람 여러 개)라면
   실사용에서 볼륨이 조용히 절반으로 고정되는 걸 실제로 겪을 수 있음.
3. **B1/B2** — 발생 확률은 낮지만 터지면 "알람이 안 울린 것처럼 보인다"는 최악의 증상으로
   이어질 수 있는 구조적 리스크. 지금까지 실제 보고가 없었다면 당장 안 고쳐도 되지만, 장기적으로
   `prepareAsync()` 전환 + `goAsync()` 검토를 권장.
4. **A1/A3, D1** — 낮은 확률/이미 알려진 항목들. 시간 날 때 정리.
5. 나머지(정보 등급)는 버그라기보다 "알아두면 좋은 상태 기록".

~~**이번 리포트는 코드를 전혀 바꾸지 않았습니다**~~ (최초 작성 시점 기준 - 이후 아래 "수정 내역"
참고, A2/A3/E1/E2는 사용자 승인 하에 실제로 고침).

## 수정 내역 (2026-08-14, 리포트 작성 후 같은 날)

사용자와 리스크(고쳤을 때 다른 곳에 영향 줄 가능성)를 항목별로 상세히 논의한 뒤, 안전하다고
합의된 3건만 진행함. **B1/B2(4번, 알람 재생 동기 호출 + 500ms 지연)는 실기기 검증 없이는 원인을
100% 확신할 수 없어서 의도적으로 그대로 둠** - 폰 연결되면 재검토.

- **A2 (AlarmPlayer 볼륨 복원)**: `AlarmPlayer.kt`의 `playCustomSound`/`playDefaultSound` 둘 다,
  `originalAlarmVolume`이 이미 저장돼 있으면(-1이 아니면) 덮어쓰지 않도록 조건 추가. 겹쳐 울리는
  알람이 있어도 최초 알람의 진짜 원본 볼륨만 보존됨. `private` 인스턴스 변수 하나만 건드리는
  범위라 다른 파일/기능엔 영향 없음.
- **A3 (deleteAlarm 트랜잭션)**: `database_service.dart`의 `deleteAlarm()`을 `db.transaction()`
  으로 묶음. 이력 insert 실패 시 동작(삼키고 삭제는 진행)은 트랜잭션 안에서도 그대로 유지해서
  기존 동작과 100% 동일 - 오직 "이력 insert 성공 직후 ~ 삭제 사이 크래시" 케이스만 새로 막힘.
- **E1+E2 (근무명 변경 원자화)**: `database_service.dart`의 구 `updateShiftNames()`를 삭제하고
  `renameShiftAtomic()`으로 교체 - alarms/shift_alarm_templates/alarm_history/
  **alarm_creation_log**(예전에 빠져있던 것) 4개 테이블 + `shift_schedule` 행까지 전부 하나의
  트랜잭션으로 묶음. 이 메서드는 원래 있던 `saveSchedule`/`updateSchedule`(온보딩 등 다른 여러
  화면이 같이 쓰는 공용 메서드)는 전혀 안 건드리고, 근무명 변경 전용 새 경로만 추가함 - 그래서
  온보딩/테마 적용 등 다른 흐름에 영향 0. `schedule_provider.dart`에 `applyExternallyPersisted()`
  라는 새 메서드를 추가해서 "DB엔 이미 다른 경로로 저장됐으니 Riverpod 상태만 갱신"하는 역할을
  분리함 - 이것도 새 메서드 추가라 기존 `saveSchedule`/`updateSchedule` 호출부엔 영향 없음.
  `settings_tab.dart`의 `_applyShiftNameChanges()`만 이 새 경로를 쓰도록 교체됨(유일한 호출부).
- 검증: `flutter analyze`(에러 없음, 기존 스타일 info만) + `gradlew :app:compileDevDebugKotlin`
  (BUILD SUCCESSFUL) 확인. **실기기 동작 검증은 아직 안 함** - 폰 연결되면 근무명 변경/알람 취소/
  겹쳐 울리는 알람 시나리오를 우선 확인할 것.

## 방금 고친 3건에 대한 잔여 리스크 검토 (2026-08-14)

수정 자체가 새 버그를 유발할 가능성이 있는지 다시 한번 점검함. **"이 수정이 새로 만든 리스크"와
"원래부터 있었는데 이번에 안 건드리고 그대로 넘어간 리스크"를 구분**해서 적음 - 후자를 이번
수정 탓으로 착각하지 않게.

### A2 (AlarmPlayer 볼륨) — 새로 도입된 리스크: 사실상 없음
- `originalAlarmVolume`을 읽고 쓰는 모든 지점(`playCustomSound`/`playDefaultSound`/`stopAlarm`)이
  전부 **메인 스레드**에서만 호출됨(`CustomAlarmReceiver.onReceive`, `Handler(Looper.getMainLooper())`
  콜백들 전부 메인 루퍼) - 안드로이드 메인 스레드는 싱글스레드라 이 호출들이 "거의 동시에" 실행될
  일 자체가 없음(항상 메시지 큐 순서대로 하나씩 완결됨). 그래서 진짜 동시성 레이스는 없음, 걱정한
  시나리오들(스누즈 vs 새 알람 등)은 전부 순차 실행으로 안전하게 처리됨.
- 유일한 이론적 전제: `stopAlarm()`이 재생을 시작한 알람마다 정확히 한 번씩은 불려야
  `originalAlarmVolume`이 다시 -1로 리셋됨. 현재 dismiss/snooze/timeout 세 경로 전부 `stopAlarm()`을
  부르는 걸 확인함 - 이 전제가 깨질 새로운 경로를 이번에 추가하지 않았음.

### A3 (deleteAlarm 트랜잭션) — 새로 도입된 리스크: 낮음, 1가지만 유의
- sqflite는 **트랜잭션 중첩을 지원 안 함** - 만약 나중에 누군가 `deleteAlarm()`을 이미 열려있는
  다른 `db.transaction()` 콜백 안에서 호출하게 되면 예외가 남. 지금은 호출부 2곳
  (`alarm_provider.dart`) 다 트랜잭션 밖에서 단독 호출이라 문제 없지만, **앞으로 이 함수를 다른
  트랜잭션 안에서 재사용하면 안 된다**는 제약이 새로 생긴 셈 - 코드에 이 제약이 명시적으로
  강제되진 않으니, 나중에 잊고 그렇게 쓰면 바로 에러로 드러남(조용히 틀어지는 종류는 아님).
- 예외 전파 방식은 수정 전후 동일하게 확인함(이력 실패는 삼키고 진행, delete 실패는 그대로
  위로 전파) - 동작 변경 없음.

### E1+E2 (renameShiftAtomic) — 새로 도입된 리스크 1가지 + 원래 있던 리스크 1가지(안 고침)

**① 새로 생긴 것(낮음)**: `newSchedule.id`가 혹시라도 null이면 `shift_schedule` 행 업데이트가
"0개 행 매치"로 조용히 아무것도 안 바꾸고 넘어감(에러 안 남) - 그러면 alarms 쪽 4개 테이블은
이름이 바뀌는데 스케줄 패턴만 안 바뀌는 상태가 될 수 있음. 다만 이 흐름에 들어오는 `schedule`은
항상 `scheduleProvider`(DB에서 이미 로드된, id가 항상 채워진 상태)에서 오기 때문에 실제로 null이
될 경로는 지금 없음 - 순수하게 "미래에 이 메서드를 다른 데서 재사용할 때" 조심할 이론적 포인트로만
기록. (참고로 예전 방식은 id가 null이면 오히려 새 행을 하나 더 만들어버렸을 것이라, 이 부분은
전보다 더 나빠진 게 아니라 비슷하거나 살짝 더 안전한 쪽입니다.)

**② 원래부터 있던 것, 이번에 안 고침(낮음~중간)**: 근무명 변경 다이얼로그(`_EditShiftNamesDialog`)
가 **중복/맞바꿈 검증을 전혀 안 함** - 확인해보니 정말로 두 근무명을 서로 맞바꾸는 것도(예: "주간"
필드에 "야간" 입력 + "야간" 필드에 "주간" 입력) 그대로 저장됨. 이 경우 한 근무당 하나씩 순서대로
`UPDATE ... WHERE shift_type = ?`를 실행하는 구조라, 먼저 처리된 변경이 나중 변경의 대상 행까지
잘못 다시 덮어쓸 수 있음(맞바꿈이 제대로 안 되고 한쪽으로 쏠릴 수 있음). **이건 트랜잭션으로 묶기
전 원래 코드(`updateShiftNames`)에도 완전히 동일하게 있던 문제라, 이번 수정이 새로 만든 것도
악화시킨 것도 아님** - 그대로 이식됨. 다만 이번에 살펴보다 발견했으니 기록해둠. 근무명을 "맞바꾸는"
방식으로 잘 안 쓰신다면 실질적 영향은 없을 가능성이 높습니다.

**결론**: 세 수정 다 "새로 만든" 리스크는 낮거나 사실상 없고, 있는 것도 전부 아주 좁은 조건에서만
발현됩니다. 유일하게 실사용 관점에서 짚어드릴 건 위 ②(근무명 맞바꿈) 하나뿐인데, 이건 이번
수정과 무관하게 원래 있던 것입니다.
