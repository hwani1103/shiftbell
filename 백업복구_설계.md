# 사용자 데이터 백업/복구 — 설계 문서

> 2026-08-31, 사전 검토 요청("A번 요구사항")에 대한 분석 결과 + 1차 기반 코드.
> 이후 새 기능(수면/컨디션 데이터 등)을 만들 때는 **이 문서를 먼저 읽고**,
> 원본 데이터 테이블을 추가하는 것으로 끝내거나(대부분 이 경우), 파생 데이터라면
> `kBackupExcludedTables`에 추가할지만 판단하면 됨. 새 SharedPreferences 설정도
> 마찬가지로 아무것도 안 해도 자동 포함됨(3장 참고).

## 0. 결론 요약

- **기존 코드(알람 데이터/근무명/근무패턴/알람 갱신·발생·notification 로직)를
  전혀 건드리지 않고 자동 백업/복구를 구현할 수 있다** — 실제 코드로 만들어
  확인함(`flutter analyze` 통과, 기존 파일은 전부 additive 수정만).
- 2026-08-31 세션에서 레이어 1(메모리상 조립/복원)만 만들었던 것을, 같은 날
  이어진 세션에서 **레이어 2(저장소 연동)~4(설정 탭 진입점 + 최초실행 UX)까지
  전부 이어서 구현 완료** — 이제 기능 전체가 실제로 동작함(단, 실기기 검증은
  아직 없음 - 다른 미검증 항목들과 동일한 상태).
- 자동 백업(레이어 3)도 결정한 범위 안에서 구현함 - 13장 "자동 백업의 범위와
  한계" 참고, "무엇이든 실시간으로" 감시하는 방식이 아니라 **"앱이 백그라운드로
  전환될 때 + 실제로 데이터가 바뀌었을 때만"**으로 의도적으로 제한함.

---

## 1. 현재 데이터 저장 구조

```
┌─────────────────────────────────────────────────────────┐
│  shiftbell.db (SQLite, 단일 파일)                         │
│  경로: Device Protected Storage                           │
│  ⭐ Flutter(sqflite)와 Kotlin(SQLiteOpenHelper)가          │
│     "같은 파일"을 각자 열어서 씀 (파일명 shiftbell.db 고정)  │
│                                                            │
│  alarm_types · shift_schedule · shift_alarm_templates     │
│  alarms(파생) · alarm_history · alarm_creation_log         │
│  date_memos · date_schedules · date_overtime · friends    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────┐   ┌───────────────────────────┐
│ SharedPreferences (일반)      │   │ SharedPreferences          │
│ 앱 전역 설정                  │   │ (Device Protected, "alarm_ │
│ - calendar_theme_id          │   │  state" 파일, 네이티브 전용) │
│ - work_hours_* (급여일 등)     │   │ - 갱신 락, 울리는 중 알람 등  │
│ - 온보딩/권한안내 완료 플래그    │   │ 순수 런타임 상태 - 사용자     │
│ - 업데이트체크 dedupe/쿨다운   │   │ 데이터 아님, 백업 대상 아님    │
│ - 친구동기화 상태 등           │   └───────────────────────────┘
└─────────────────────────────┘

┌─────────────────────────────────────────┐
│ Firestore (클라우드, 이미 원격 저장소)      │
│ friend_schedules: 내가 공유한 근무표 원본  │
│ app_config: 업데이트 안내(사용자 데이터 X) │
│ ⭐ 내 익명 인증 UID는 firebase_auth SDK가  │
│   자체 관리 - 이 앱의 DB/설정 어디에도 없음 │
└─────────────────────────────────────────┘
```

- DB는 **정말로 파일 하나**다(`DatabaseHelper.kt`의 `DATABASE_NAME = "shiftbell.db"`,
  `database_service.dart`가 `getDeviceProtectedStoragePath` 채널로 받는 경로와 동일 파일).
  이게 이번 설계에서 제일 중요한 사실 — "여러 데이터를 하나의 백업 단위로 묶기"(8장 참고)를
  거의 공짜로 만족시킴.
- `android:allowBackup="false"` (`AndroidManifest.xml`) — Android 자체 Auto Backup은
  이미 꺼져 있음. 게다가 Device Protected Storage는 Auto Backup 규칙 대상에도 안 들어감
  (10장 "방식 D" 참고).

---

## 2. 백업 대상 데이터 — 원본 vs 파생

| 데이터 | 저장 위치 | 원본/파생 | 백업 포함 | 비고 |
|---|---|---|---|---|
| 교대 패턴/근무명/색상 | `shift_schedule` | 원본 | ✅ | |
| 근무별 알람 템플릿 | `shift_alarm_templates` | 원본 | ✅ | |
| 알람음/진동/볼륨 커스텀 | `alarm_types` | 원본 | ✅ | 프리셋도 값 수정 가능 |
| **현재 등록된 알람** | `alarms` | **파생** | ❌ | `shift_schedule`+`shift_alarm_templates`로부터 `AlarmRefreshEngine`이 재생성. 복구 후 앱 실행 시 자동으로 다시 채워짐(CLAUDE.md "알람 갱신 트리거 지점 4") |
| 알람 이력 | `alarm_history` | 로그(영구보존) | ✅ | [[history-permanence]] 원칙과 정합 — 재설치해도 이력이 끊기면 안 됨 |
| 알람 생성 로그 | `alarm_creation_log` | 로그(영구보존) | ✅ | 위와 동일 |
| 달력 메모 | `date_memos` | 원본 | ✅ | |
| 일정관리 탭 일정 | `date_schedules` | 원본(+ML 예측 기록) | ✅ | `predicted_category`/`is_user_corrected`는 재학습 피드백 데이터라 그대로 보존 |
| OT 입력 | `date_overtime` | 원본 | ✅ | |
| 친구 목록(누굴 팔로우하는지) | `friends.name/owner_id` | 원본 | ✅ | |
| 친구 근무표 캐시 | `friends.data_json` | 파생(Firestore 캐시) | ✅(무해) | 굳이 뺄 이유 없음 — 복구 후 다음 동기화 때 최신화됨 |
| 월/주별 근로시간 합계, OT 합계 등 통계 | (테이블 없음, `work_hours_calculator.dart`가 매번 계산) | 파생 | 해당 없음 | 애초에 저장을 안 하므로 백업 이슈 자체가 없음 |
| 달력 테마/급여일 등 설정 | SharedPreferences | 원본 | ✅ | 3장 "전체 스냅샷" 방식 참고 |
| 알람 런타임 상태(`alarm_state`) | Device Protected SharedPreferences | 런타임 상태 | ❌ | 앱이 스스로 재계산 |
| 내가 공유한 근무표 원본 | Firestore | 원본(이미 원격) | 해당 없음 | 클라우드가 곧 원본 저장소라 로컬 백업 불필요 |
| 내 익명 인증 UID | firebase_auth SDK 내부 | 원본(계정 식별자) | ⚠️ 로컬 백업 범위 밖 | **알려진 제약**: 재설치 시 새 UID가 발급됨 → 이전에 내 공유코드를 등록해둔 친구 쪽에서 내 근무표가 끊김. 이 문서의 백업 범위로는 해결 안 됨(별도 과제, 5장 "다음 단계"에 명시) |
| (향후) 수면기록/수면패턴/컨디션 | 아직 테이블 없음 | 원본(수면기록), 파생(컨디션 점수 등 통계) | 자동 포함 예정 | 테이블만 추가하면 `exportAll()`이 자동으로 스윕함(7장) |

---

## 3. 어떻게 "새 테이블/설정을 자동으로" 백업 대상에 포함시켰나

이번에 만든 `BackupService.exportAll()`(`lib/services/backup_service.dart`)은 테이블
이름을 하드코딩하지 않는다. 대신:

```dart
SELECT name FROM sqlite_master WHERE type = 'table'
```

로 **그 순간 DB에 존재하는 모든 테이블**을 읽고, `_kSystemTables`(sqlite 내부용)와
`kBackupExcludedTables`(파생 데이터로 확정된 테이블 — 지금은 `alarms` 하나뿐)만 뺀
나머지를 통째로 백업한다. 마찬가지로 SharedPreferences도 `prefs.getKeys()`로 **키를
하드코딩하지 않고 전체를 스냅샷**한다(런타임 쿨다운 값 같은 게 같이 백업돼도
무해함 — 최악의 경우 업데이트 안내 쿨다운이 며칠 어긋나는 정도).

**결론**: 향후 수면기록 테이블(`sleep_records` 등)을 추가해도 `backup_service.dart`를
고칠 필요가 없다 — 그게 파생 데이터(예: "이번 주 평균 수면시간" 캐시 테이블)일
때만 `kBackupExcludedTables`에 한 줄 추가하면 됨. 이게 10장 질문 ⑤(확장성)의
실제 근거.

---

## 4. 가능한 백업/복구 방식 비교

| 방식 | 자동화 | 앱 삭제 후 복구 | 사용자 개입 | 구현 난이도 | 리팩토링 규모 |
|---|---|---|---|---|---|
| **A. Export/Import 레이어 추가** (이번에 채택) | ✅ (외부 관찰 기반 트리거) | ✅ (저장 위치가 앱 샌드박스 밖이면) | 최소(자동 탐지 가능) | 중 | **거의 없음(0)** |
| B. Repository/Data Layer 정리 후 추가 | ✅ | ✅ | 최소 | 높음 | 큼 — 알람 크리티컬 코드까지 손댈 위험 |
| C. 저장구조 자체를 통합 재설계 | ✅ | ✅ | 최소 | 매우 높음 | 매우 큼 — "알람 로직 불가침" 요구사항과 정면 충돌 |
| D. Android Auto Backup(`allowBackup`) | 시스템이 하루 1회 자동 | 설치 시점에만, 즉시 제어 불가 | 낮음(Google 계정 필요) | 낮음(플래그+룰 XML) | 없음 | 
| D'. Google Drive 등 클라우드 연동 | ✅ | ✅ | 로그인 필요 | 높음(인증/토큰/동기화) | 중~큼 |
| **E. A + MediaStore(앱 소유 파일, SAF 폴더선택 없음)** | ✅ | ✅ | **거의 0**(폴더 선택 UI 자체가 없음) | 중 | 거의 없음(0) |

**D가 낮은 우선순위인 이유**: Device Protected Storage(`shiftbell.db`가 사는 곳)는
애초에 Auto Backup 대상 경로가 아님(`getFilesDir()`/`getDatabasePath()` 같은 일반
컨텍스트만 대상). 포함시키려면 별도 backup rule XML + 검증이 필요하고, 그래도
"하루 1회, Google 계정 로그인 상태, 설치 시점에만 트리거"라 사용자가 원한
"즉시·명시적 복구" UX와 안 맞음. `allowBackup="false"`로 이미 꺼둔 것도 참고.

**D'가 낮은 우선순위인 이유**: 사용자가 명시적으로 "외부 네트워킹/구글 연동/로그인
로직은 피하라"고 함(요청 사항). 앱에 로그인 개념 자체가 없는데 이것 때문에 새로
만들면 배보다 배꼽이 큼.

**C가 기각인 이유**: "알람 데이터/근무명/근무패턴/알람 갱신·발생·notification
로직을 1도 손대면 안 됨"이라는 제약과 직접 충돌 — 저장구조를 통합하려면 그
로직들이 읽는 테이블 스키마 자체를 바꿔야 함.

---

## 5. 추천 방식과 다음 단계

### 추천: **A + E** (Export/Import 레이어, 저장은 MediaStore 앱 소유 파일)

- **왜 MediaStore인가**: Android 10+(scoped storage)부터, 앱이 `MediaStore.Downloads`/
  `MediaStore.Documents`에 **자기 이름으로 만든 파일**은 별도 저장소 권한 없이 쓰고
  다시 찾을 수 있음. 게다가 **재설치해도 같은 패키지명이면 그 앱이 이전에 만든
  MediaStore 항목을 다시 조회할 수 있음**(MediaStore 메타데이터는 앱 설치 여부와
  무관하게 기기에 남아있고, "소유 패키지"로 필터링해서 접근 가능) — 이게 바로
  "사용자가 폴더 위치를 몰라도 자동으로 찾는" 요구(스펙 5장)를 SAF 폴더 피커
  없이 만족시키는 표준 패턴. 로그인/네트워킹 전혀 불필요.
- **자동 백업 트리거**: 어떤 provider/insert 경로도 후킹하지 않고, **바깥에서
  관찰**한다 — `PRAGMA data_version`(같은 DB 파일을 다른 커넥션에서 열어도 값이
  바뀌면 그 사이 누군가 썼다는 뜻)을 앱이 background로 전환되는 시점에만 확인하고,
  마지막 백업 시점과 다르면 그때만 실제로 파일을 씀. 별도 주기 폴링은 안 함(앱이
  background로 전환되는 빈도 자체가 이미 충분히 잦음). **모든 변경마다 파일을
  다시 쓰지 않음** — 배터리/저장장치 부담을 이렇게 줄임. (실제 구현: 아래 레이어
  3, `backup_watcher.dart`)
- **복구 UX**: 최초 실행 시(`shift_schedule`이 비어있을 때) MediaStore에서 이 앱
  소유의 백업 파일이 있는지 조회 → 있으면 "기존 데이터를 찾았어요, 복구할까요?"
  다이얼로그 하나만 노출 → [복구] 누르면 `BackupService.restoreAll()` 호출.

### 레이어 1 — 메모리상 조립/복원 (2026-08-31 1차 세션)

- `lib/models/backup_payload.dart` (신규) — 백업 JSON의 "봉투"(스키마 버전,
  내보낸 시각, 앱 버전, 테이블 데이터, 설정 스냅샷) + 인코딩/디코딩
- `lib/services/backup_service.dart` (신규) — `exportAll()`(DB 전체 테이블 자동
  스윕 + SharedPreferences 전체 스냅샷 → `BackupPayload`), `restoreAll()`(빈 상태
  가드 + 트랜잭션으로 원본 테이블 복원 + 설정 복원)
- alarms 테이블/네이티브 알람 등록/AlarmRefreshEngine/notification 관련 코드는
  이 두 파일 어디에서도 import되지 않음 — 알람 크리티컬 로직 불가침 요구사항을
  코드 수준에서 지킴.

### 레이어 2~4 — 저장소 연동/자동 트리거/UI (2026-08-31 2차 세션, 완료)

1. **저장소 연동(레이어 2)** ✅
   - `MainActivity.kt`에 `writeBackupFile`/`readBackupFile` 두 `when` 케이스 추가
     (기존 케이스는 안 건드림) + `deleteBackupFile()`(내부용, 덮어쓰기 전 항상
     기존 파일부터 지워서 "백업 = 파일 하나" 유지) - `MediaStore.Downloads`,
     경로 `Download/ShiftBell/`, 파일명 `shiftbell_backup.json` 고정.
   - Android 10(Q, API 29) 미만은 스코프드 스토리지 이전이라 미지원 처리(그 밑
     버전은 legacy `WRITE_EXTERNAL_STORAGE` 권한 기반 경로가 따로 필요한데,
     이 앱의 minSdk(24) 대비 비중이 낮다고 판단해 1차 범위에서 제외 - 필요해지면
     추가).
   - `lib/services/backup_storage_service.dart`(신규) — 그 두 채널 호출을
     감싸는 얇은 래퍼. 새 채널을 만들지 않고 기존 `kAlarmChannel` 하나만 재사용.
2. **자동 트리거(레이어 3)** ✅
   - `lib/services/backup_watcher.dart`(신규) — `PRAGMA data_version`으로 "저장
     이후 실제로 데이터가 바뀌었는지"만 가볍게 확인, 바뀐 경우에만 실제로 씀.
     수동/자동이 완전히 같은 함수(`backupNow`)를 씀(수동은 `force: true`로
     변경 여부 확인을 건너뛸 뿐) - "마지막 백업 시각" 표시가 항상 둘 다 정확함.
   - **폴링이 아니라 앱이 background로 전환되는 시점(`AppLifecycleState.paused`)
     에만** 확인함 - `main.dart`의 기존 `didChangeAppLifecycleState`에 `else if`
     분기 하나만 추가(기존 `resumed` 분기·알람 갱신 로직은 안 건드림), `await`
     없이 fire-and-forget이라 백그라운드 전환 자체를 지연시키지 않음. 13장 참고.
3. **최초 실행 UX(레이어 4)** ✅
   - `lib/screens/restore_backup_screen.dart`(신규) — "이 기기에서 이전 백업을
     찾았어요" 화면. `main.dart`의 `InitialRouter._navigate()`가 `schedule ==
     null`(신규 설치 또는 초기화 직후)일 때 곧장 `OnboardingScreen`으로 보내는
     대신, 먼저 `BackupStorageService.read()`로 기기에 백업이 있는지 확인해서
     있으면 이 화면으로, 없으면 기존과 동일하게 `OnboardingScreen`으로 보냄
     (기존 분기 구조는 그대로, 그 안에 조건 하나만 더 갈라짐).
   - [복구하기] → `BackupService.restoreAll()` 호출 후, **`permissions_requested`
     플래그를 무조건 `false`로 강제 리셋**(백업 안의 값을 그대로 믿지 않음) →
     `PermissionIntroScreen`으로 이동. 재설치하면 알림/정확한 알람/오버레이 같은
     OS 권한은 실제로 전부 초기화된 상태라, 이 강제 리셋이 없으면 사용자가 권한을
     다시 허용할 기회 자체를 놓쳐서 알람이 조용히 안 울리는 사고로 이어질 수 있음.
   - [새로 시작하기] → 기존과 동일하게 `OnboardingScreen`(발견한 백업은 그대로
     기기에 남아있음 - 지우지 않음).
   - **(2026-09-01 수정)** 처음 만들 때는 이 확인 로직을 `InitialRouter._navigate()`
     에만 넣었는데, 실제 재설치 흐름에서는 그 지점이 거의 안 쓰였다 — 진짜
     "권한 화면 다음"의 실제 분기점은 `PermissionIntroScreen._navigateToOnboarding()`
     (권한 처리 완료 시 `Navigator.pushReplacementNamed('/onboarding')`으로
     `InitialRouter`를 거치지 않고 곧장 감)이었음. 그래서 "복구했는데 온보딩
     어디에도 그 화면이 안 나온다" 버그가 남아있었음 - 백업 확인 로직 자체는
     맞게 짜여 있었지만 도달 불가능한 코드였던 것. 그 함수를 백업 유무까지
     확인하도록 고쳐서 실제 트리거 지점에 옮김(`InitialRouter` 쪽 로직은
     "권한은 이미 처리됐는데 스케줄/복구를 못 끝내고 앱을 닫았다가 다시 여는"
     별개의 시나리오를 위해 그대로 남겨둠 - 둘 다 유효하고 겹치지 않음).
     같은 김에 반대 방향 버그도 발견해서 같이 고침: 복구 성공 후 권한 화면으로
     돌아왔을 때 그 함수가 무조건 온보딩으로 보내고 있었는데, 이미 스케줄이
     복구돼 있으므로 온보딩이 아니라 `MainScreen`으로 가야 정상 - 그것도
     같이 반영함.
   - **(2026-09-01 추가 수정)** 위 수정 이후에도 "설정 탭 백업 버튼은 토스트가
     뜨며 성공하는데, 재설치 후 온보딩엔 여전히 아무 안내도 안 뜬다"는 재현이
     남아있었음 - Dart 쪽 라우팅이 아니라 `MainActivity.kt`의 네이티브 파일
     읽기 자체가 실패하고 있었던 것. `readBackupFile()`/`deleteBackupFile()`이
     `DISPLAY_NAME`과 `RELATIVE_PATH`를 둘 다 정확히 일치시키는 selection으로
     MediaStore를 조회했는데, `RELATIVE_PATH`는 insert 시 넘긴 문자열과 실제
     저장되는 값이 기기/OS 버전에 따라 트레일링 슬래시 등으로 미묘하게 어긋날
     수 있음(Android 자체 CTS 테스트 이력에도 같은 종류의 불일치가 남아있어
     실제 존재하는 사례로 확인). write는 성공하는데 read만 항상 0건으로
     실패하는 증상과 정확히 들어맞음. 조치: `RELATIVE_PATH`는 selection에서
     빼고 `DISPLAY_NAME LIKE 'shiftbell_backup%'`로만 조회 - 스코프드 스토리지가
     이미 "내 앱이 만든 파일만" 결과를 격리해 주므로 이름 하나로도 안전함.
     겸사겸사 이전 빌드가 테스트 중 남겼을 수 있는 `shiftbell_backup (1).json`류
     중복 파일도 LIKE 패턴으로 같이 잡아서 최신 순으로 읽고, `deleteBackupFile()`도
     동일 패턴으로 전부 정리하도록 맞춤. **실기기 재현 로그로 확진한 게 아니라
     문서화된 Android 버그 패턴에 근거한 조치라, 다음 실기기 테스트에서 여전히
     안 되면 `adb logcat -s MainActivity`로 write/read 각각의 Log.d/Log.e 출력을
     확인해야 함**(둘 다 이미 로그를 남기도록 되어 있음). 또한 만약 사용자가
     실제 "삭제 후 재설치"가 아니라 앱 내 설정 탭의 "초기화"(`_resetSchedule()`,
     `settings_tab.dart`)로 테스트했다면 이 경로는 여전히 백업 확인 없이 곧장
     `OnboardingScreen`으로 직행함 - 의도적으로 다른 흐름(재설치 복구 유도가
     아니라 사용자가 스스로 초기화를 선택한 경우)이라 그대로 뒀지만, 혼동
     가능성이 있어 기록해 둠.
   - **(2026-09-01 재조사 - 진짜 원인)** 위 LIKE 수정 이후에도 안 되길래 실기기
     logcat을 직접 떠서 확인한 결과, RELATIVE_PATH 문제가 아니었음 - **재설치
     여부와 무관하게, 같은 프로세스 안에서 방금 쓴 파일조차 selection(LIKE 포함)을
     건 쿼리로는 못 찾았고 delete도 매번 "0개 삭제"** 였음(그 결과 실제로 파일이
     11개까지 쌓여있었던 걸 파일관리자로 직접 확인). 테스트 기기가 삼성(로그에
     `DownloadStorageProvider` 등장)이라, **OEM 스토리지 프로바이더가 `Downloads`
     컬렉션에 대한 selection(WHERE절) 쿼리를 AOSP와 다르게 처리하는 것으로
     추정**됨. 조치: `MainActivity.kt`의 `readBackupFile()`/`deleteBackupFile()`을
     selection을 아예 안 쓰는 방식으로 다시 작성 - `Downloads` 컬렉션 전체를
     selection 없이 가져와서 파일명 매칭은 코틀린 코드에서 직접 하고, 삭제도
     `delete(collectionUri, selection, args)` 대신 개별 row URI
     (`ContentUris.withAppendedId`)로 하나씩 지움. 이 앱이 만드는 백업 파일
     개수는 항상 소수라 전체 목록을 가져오는 성능 부담은 없음. **교훈: MediaStore
     selection/LIKE 쿼리는 OEM 프로바이더 구현에 따라 신뢰할 수 없을 수 있으니,
     가능하면 selection 없는 전체 조회 + 클라이언트 필터링과 row-URI 단위 조작이
     더 안전함.**
   - **(2026-09-01 최종 - 진짜 진짜 원인 + 근본 대응)** selection 제거 후에도
     여전히 실패해서 `adb shell content query`로 MediaStore 행 자체를 직접
     열어봤더니, 방금 쓴 백업 행의 **`owner_package_name`이 `NULL`** 이었음
     (정상이면 이 앱 패키지명이 insert 시점에 MediaProvider가 자동으로 채워야
     하는 값). 권한 없는 일반 앱의 MediaStore 쿼리는 `owner_package_name`이
     자기 패키지와 일치하는 행만 볼 수 있는데, 그 값 자체가 NULL로 찍히니
     **우리 쪽 쿼리 코드를 아무리 고쳐도 원천적으로 그 행을 볼 수 없는 상태**
     였음 - 이 테스트 기기(삼성, `DownloadStorageProvider`)의 MediaProvider
     구현 버그/차이로 추정. 앱 코드로 고칠 수 있는 문제가 아니라서, 자동 탐지가
     실패했을 때를 위한 **수동 대안**을 추가함: 시스템 파일 선택기(SAF,
     `ACTION_OPEN_DOCUMENT`)로 사용자가 직접 `Downloads/ShiftBell/
     shiftbell_backup.json`을 고르는 경로(`MainActivity.kt`의 `pickBackupFile`
     MethodChannel, `BackupStorageService.pickAndRead()`) - 사용자가 명시적으로
     고른 파일은 owner_package_name 같은 소유권 메타데이터와 무관하게 그 자리에서
     새로 접근 권한을 받기 때문에 이 버그의 영향을 받지 않음. `PermissionIntroScreen`
     (신규 설치 시 항상 첫 화면)에 "이전에 백업한 파일이 있나요? 직접 불러오기"
     링크로 노출 - 자동 탐지는 그대로 "되면 편한" 경로로 남겨두고, 안 될 때의
     탈출구를 항상 열어둠. 고른 뒤 로직은 자동 탐지가 찾았을 때와 완전히 동일한
     `RestoreBackupScreen`을 그대로 재사용(빈 백업 가드 포함).
   - **(2026-09-01 후속 수정)** 수동 불러오기 진입점은 이후 `PermissionIntroScreen`
     에서 온보딩의 근무명 지정 화면("다음" 버튼 위, 카드 형태)으로 옮김 -
     근무 데이터를 실제로 입력하려는 시점에 물어보는 게 더 자연스럽다는 판단.
     파일명도 `shiftbell_backup.json` 고정에서 `ShiftBell_Backup_YYMMDD.json`
     (그날 날짜로 매번 다시 이름 붙임, 파일은 여전히 1개만 유지)으로 변경 -
     파일관리자로 직접 열어봤을 때 최신 여부를 바로 알 수 있게. 설정 탭에도
     반대 방향 진입점("백업 데이터 불러오기" - 이미 쓰던 데이터를 다른 백업으로
     덮어씀, 2단계 확인 다이얼로그 + 복구 후 앱 프로세스 재시작) 추가함.
   - **(2026-09-01 - 복구 직후 "다음 알람"이 한참 있다가 나타나는 버그)**
     `restoreAll()`은 DB 테이블만 직접 갱신하는데, 네이티브 알람 갱신 엔진
     (`AlarmRefreshUtil.checkAndTriggerRefresh`)은 "하루 1번"만 자동으로 도는
     쿨다운이 있음 - 복구 시점에 이미 오늘 한 번 갱신된 상태였다면 `alarms`
     (파생 테이블) 재생성이 그 쿨다운에 막혀 다음 날/재부팅 등 다른 계기가
     생길 때까지 안 일어남(근무표·알람이력은 원본 데이터라 `restoreAll()`이
     바로 채워 넣지만, "다음 알람" 탭이 읽는 `alarms`만 옛 상태로 남아있다가
     한참 후에야 갱신되는 것으로 보였음). 조치: 복구 성공 직후
     `AlarmRefreshService.instance.forceRefresh()`로 그 쿨다운을 무시하고
     즉시 재생성시킴(`restore_backup_screen.dart`/`settings_tab.dart` 양쪽
     복구 플로우 모두에 추가).
4. **설정 탭 진입점** ✅
   - "이력/데이터" 섹션(알람 이력·메모 모아보기와 같은 구역)에 "데이터 백업"
     항목 추가 - 탭하면 `BackupWatcher.backupNow(force: true)` 실행, "마지막
     백업: YYYY.MM.DD HH:mm" 또는 "아직 백업한 적 없어요" 표시.
5. `restoreAll()`의 "상위 버전 백업 거부" 가드는 이미 구현됨 — `kBackupSchemaVersion`을
   올릴 실제 상황이 생기면 그 자리에 마이그레이션 분기를 추가.
6. (범위 밖, 별도 과제, 미해결) Firebase 익명 인증 UID 재설치 시 유실 문제 —
   내가 팔로우하는 친구 목록(`friends` 테이블)은 백업되지만, 반대로 "내 공유
   코드를 등록해둔 다른 사람" 쪽에서는 재설치 후 내 UID가 바뀌어 내 근무표가
   끊김. firebase_auth가 임의 UID로 재로그인하는 걸 공식 지원하지 않아 조사가
   더 필요함 - 이번 백업 범위에는 포함하지 않음.

---

## 6. 예상 리팩토링 규모

**작음.** 기존 파일 리팩토링 없음 — 100% 신규 파일 추가 + 각 진입점(설정 탭
메뉴, 스플래시/온보딩 분기, `MainActivity.kt`의 새 채널 케이스, `main.dart`의
옵저버 등록)에 짧은 훅만 추가. **알람 데이터/근무명/근무패턴 데이터, 알람
갱신·발생·notification 로직 파일은 0개 변경**(이번 세션 실적 기준, 다음 단계도
동일 원칙으로 설계됨).

---

## 7. 향후 데이터 확장 대응 (수면/컨디션/개인화 등)

수면기록·수면패턴·컨디션 데이터가 추가될 때 이 백업 시스템이 그대로 쓰일 수 있는지:

- **원본 데이터**(예: 실제 수면 시작/종료 시각, 근무별 평소 수면패턴 설정)를 새
  테이블로 추가하면 `exportAll()`이 **코드 변경 없이 자동으로** 백업에 포함함(3장).
- **파생 데이터**(예: 수면 평균, 컨디션 점수, 각종 통계)는 원칙대로 저장하지 않고
  원본으로부터 그때그때 계산하는 걸 권장(`work_hours_calculator.dart`가 근로시간
  통계를 다루는 방식과 동일 패턴). 저장한다면(캐시 목적) `kBackupExcludedTables`에
  추가해서 백업 용량을 아낄 것.
- DB `DATABASE_VERSION`이 오를 때(새 컬럼/테이블) 백업 JSON의 "봉투" 구조
  (`schemaVersion`)는 그대로 둬도 됨 — 테이블 내부 컬럼 구성 변화는 `tables` 맵에
  자연스럽게 반영됨. 구버전 백업을 새 스키마에 복원할 때 새 컬럼은 DB의
  `DEFAULT` 값으로 채워짐(대부분의 마이그레이션이 이미 그렇게 설계돼 있음,
  `DB_스키마_변경_가이드.md` 참고).
- SharedPreferences에 새 설정이 추가돼도 "전체 스냅샷" 방식 덕분에 자동 포함됨.

---

## 8. "하나의 백업 파일" 개념

`BackupPayload.encode()`가 JSON 문자열 하나를 만들고, 저장소 연동(다음 단계)도
그 문자열 하나를 파일 하나로 저장하는 걸 전제로 설계함 — 사용자 입장에서도,
구현 입장에서도 "ShiftBell 백업 = 파일 하나"로 취급 가능. 현재 데이터 규모(DB
전체 + 설정)를 감안하면 JSON 하나가 수백 KB~수 MB 선일 것으로 예상되고, 이
정도는 MediaStore/SAF 어느 쪽으로 저장해도 크기 문제가 없음.

---

## 9. 버전 관리

- `kBackupSchemaVersion`(`lib/models/backup_payload.dart`) — 백업 **JSON 봉투**의
  구조 버전. DB의 `DATABASE_VERSION`(현재 20)과는 별개 개념 — 테이블이 느는
  것 자체는 이 값을 안 올려도 됨(3장). 봉투 구조(`tables`/`preferences` 필드
  의미 등) 자체가 바뀔 때만 올림.
- `restoreAll()`은 이미 "백업의 `schemaVersion`이 이 앱이 아는 것보다 높으면
  거부"하는 가드를 갖고 있음(더 새 버전 앱에서 만든 백업을 구버전 앱이 잘못
  해석하는 사고 방지). 실제로 봉투 구조를 바꾸는 날, 그 버전 분기(예: v1→v2
  변환 함수)를 추가하면 됨.

---

## 10. "현재 구조를 최대한 보존" 목표에 대한 결론

이번 세션에서 실제로 방법 A를 코드로 만들어봄으로써, **기존 저장 구조(단일
DB 파일 + Device Protected Storage + SharedPreferences)를 하나도 안 바꾸고도**
백업/복구가 가능하다는 걸 실증함(`flutter analyze` 통과, 기존 파일 변경 0개).
따라서 **전면 리팩토링은 필요 없고, 기존 구조 위에 얇은 백업 계층을 추가하는
것으로 충분함**.

---

## 11. 핵심 질문 답변 (요청 스펙 11장)

① **가장 적은 리팩토링으로 안정적인 자동 백업/복구를 구현할 수 있는 방법은?**
→ 방법 A(Export/Import 레이어) — 이번에 만든 `backup_service.dart` 방식 그대로.
DB/설정 레이어는 그대로 두고 그 위에 읽기/쓰기 전용 레이어 하나만 얹음.

② **사용자가 백업 파일의 존재/위치를 거의 신경 안 써도 되는 방법은?**
→ MediaStore(앱 소유 파일, Downloads/Documents) 자동 저장 — 폴더 선택 UI 자체가
없음. 저장 위치를 사용자가 정할 필요도, 기억할 필요도 없음.

③ **앱 삭제 후 재설치 시 최소 조작으로 복구하는 방법은?**
→ 최초 실행 시 자동 탐지 다이얼로그 하나("기존 데이터를 찾았어요, 복구할까요?")
+ [복구] 버튼 한 번.

④ **자동 백업을 항상 최신으로 유지하면서 배터리/성능 부담을 최소화하는 방법은?**
→ 매 변경마다 파일을 쓰지 않음. `PRAGMA data_version`으로 "바뀐 사실"만 가볍게
확인(다른 커넥션에서 값만 읽는 거라 거의 공짜)하고, 실제 파일 쓰기는 앱이
background로 전환되는 시점 + 디바운스된 주기로만 수행.

⑤ **수면/컨디션/개인화 데이터가 크게 늘어도 같은 시스템을 계속 쓸 수 있는가?**
→ 그렇다. `exportAll()`이 DB 테이블을 그때그때 자동으로 스윕하는 설계라, 새
원본 데이터 테이블은 코드 변경 없이 자동 포함됨(7장).

⑥ **현재 구조를 전면 리팩토링해야 하는가, 아니면 계층만 추가하면 되는가?**
→ 계층 추가로 충분함 — 실제로 만들어서 검증함(기존 파일 0개 변경).

---

## 12. 알람 크리티컬 로직 불가침 확인

이 문서와 이번에 추가된 코드(`backup_payload.dart`, `backup_service.dart`)는
다음을 **한 줄도** import하거나 수정하지 않음:

- `alarm_generation_service.dart`, `alarm_refresh_service.dart`, `alarm_service.dart`
- `AlarmRefreshEngine.kt`, `CustomAlarmReceiver.kt`, `AlarmGuardReceiver.kt`,
  `AlarmActivity.kt`, `AlarmOverlayService.kt`, `AlarmPlayer.kt`, `NotificationHelper.kt`
- `alarms` 테이블 쓰기 경로(`DatabaseService.insertAlarm`/`insertAlarmsInBatch`/
  `deleteAllAlarms` 등)

`restoreAll()`이 `alarms` 테이블을 아예 건드리지 않는 것도 같은 이유 — 복구
직후 앱이 정상적으로 시작되면(또는 재시작되면) 기존 알람 갱신 로직이
`shift_schedule`/`shift_alarm_templates`를 읽어 알아서 다시 채움. 이 원칙은
다음 단계(저장소 연동/자동 트리거/UI)에도 그대로 유지할 것 — 새 코드가 알람
등록/취소를 "직접" 하는 일은 앞으로도 없어야 함.

---

## 13. 자동 백업 — 범위와 한계 (2026-08-31 2차 세션, "수동으로만 할 건지" 질문에 대한 답)

**결론: 수동 전용이 아니라 자동+수동 둘 다 구현함.** 다만 "자동"의 의미를
명확히 해둘 필요가 있음 — 아래가 실제로 구현한 범위와, 일부러 안 한 것들.

### 구현한 자동 백업의 정확한 동작

- **트리거 시점**: 앱이 background로 전환되는 순간(`AppLifecycleState.paused`)
  단 한 곳. "몇 분마다" 같은 별도 주기 폴링은 안 함 - 사용자가 앱을 쓰다가
  다른 앱으로 전환하거나 홈으로 나가는 매 순간이 자연스러운 체크포인트가 되므로
  이걸로 충분하다고 판단.
- **변경 감지**: `PRAGMA data_version`으로 "저장 이후 이 DB 파일에 뭐라도
  쓰기가 있었는지"만 확인. 어떤 테이블이 바뀌었는지, 얼마나 바뀌었는지는 안 봄 -
  "바뀌었나 안 바뀌었나"의 이진 판단만.
- **실제 파일 쓰기**: 위 두 조건(백그라운드 전환 + 변경 있음)이 같이 만족될
  때만 실행. 즉 "근무표를 열어서 보기만 하고 아무것도 안 바꾼 채 나가면" 자동
  백업이 파일을 다시 쓰지 않음(배터리/저장장치 부담 최소화 - 4장 질문 ④ 참고).

### 왜 이 정도로 제한했는가 (더 "즉시"로 만들지 않은 이유)

- **매 변경마다 즉시 쓰기**(예: 메모 하나 저장할 때마다 파일도 같이 씀)는 기술적으론
  가능하지만, 이 앱이 근로시간 계산·메모 저장 등을 상당히 빈번하게 하는 걸
  감안하면 디스크 쓰기가 너무 잦아짐 - 배터리/저장장치 마모 부담이 "자동
  백업"이 주는 이득 대비 과함. "데이터를 잃는 시나리오"는 결국 "앱을 지우기
  직전"인데, 그 시점 이전에 background 전환이 최소 한 번은 있었을 가능성이
  매우 높아서(앱을 지우려면 먼저 앱을 벗어나야 함) 실용적으로 차이가 거의 없음.
- **진짜 실시간(예: 종료 신호를 받아 그 순간 마지막으로 한 번 더 쓰기)**은
  Android가 앱 프로세스 종료 시점에 안정적으로 코드를 실행시켜주는 보장이
  없어서(OS가 언제든 즉시 죽일 수 있음, `onStop`/`onDestroy`도 항상 호출된다는
  보장이 없음) 애초에 신뢰할 수 있는 훅이 아님 - `paused`(백그라운드 전환)가
  이 앱이 안정적으로 잡을 수 있는 가장 늦은 시점.

### 자동 백업이 커버 못 하는 경우 (알아둬야 할 한계)

- 스케줄/알람을 바꾼 **직후, 앱을 배경으로 보내지 않고 바로 강제 종료**(최근
  앱 목록에서 스와이프해서 끔)하면 그 변경분은 자동 백업에 안 잡힐 수 있음 -
  `paused` 콜백 자체가 아예 안 불릴 수 있는 경로라서. 이 경우를 위해 설정 탭의
  "지금 백업" 수동 버튼(4장 진입점)이 항상 확실한 대안으로 남아있음.
- 여러 값을 짧은 시간 안에 연달아 바꾸는 중간에는(예: 온보딩 마법사 진행 중)
  각 단계마다 백업하지 않음 - 어차피 온보딩 도중엔 background 전환도 잘 안
  일어나고, 끝나고 나서 다음 background 전환 때 한 번에 잡힘.

### 검토했지만 채택 안 한 대안

| 대안 | 기각 이유 |
|---|---|
| 매 DB 쓰기마다 즉시 백업 | 배터리/저장장치 부담 과함, 실질적 이득 미미 |
| WorkManager로 주기적(예: 1일 1회) 백업 | 이 앱은 이미 AlarmManager 인프라가 있고, "background 전환 시점"이 더 정확한 트리거라 별도 백그라운드 실행 프레임워크를 새로 들여올 이유가 없음(알람 자동 감지 기능에서도 같은 판단을 내림, 수면기록_자동추정_조사.md 참고) |
| 서버/클라우드에 실시간 동기화 | 로그인/네트워킹 요구사항과 배치(0장 이전 결정 유지) |

**한 줄 요약**: "완전히 실시간"은 아니지만, 사용자가 일상적으로 앱을 쓰는
패턴(열었다 나갔다) 안에서는 사실상 최신 상태를 유지하는 수준의 자동 백업이고,
그 사이 빈틈은 수동 버튼으로 메꿀 수 있게 설계함.
