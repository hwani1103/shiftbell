# Shiftbell 업데이트 가이드

앱 업데이트할 때마다 직접 고쳐야 하는 것들을 정리한 문서입니다. 크게 두 가지입니다.

1. 버전 올리기 (업데이트할 때마다 매번)
2. 빨간날(공휴일) 데이터 추가하기 (연말마다, 늦어도 그 다음 해 되기 전까지)

---

## 1. 버전 올리기

**고쳐야 하는 파일 2개:**

### ① `pubspec.yaml` (프로젝트 최상위 폴더)

```yaml
version: 1.0.13+15
```

`1.0.13` = 버전 이름 (사람이 보는 버전, 예: "1.0.13")
`+15` = 버전 코드 (내부 관리용 숫자, 반드시 이전보다 커야 함)

### ② `android/app/build.gradle.kts`

```kotlin
versionCode = 15
versionName = "1.0.13"
```

**⚠️ 이 두 파일의 값은 서로 짝이 맞아야 합니다:**
- `pubspec.yaml`의 `+15` ↔ `build.gradle.kts`의 `versionCode = 15` → **반드시 같은 숫자**
- `pubspec.yaml`의 `1.0.13` ↔ `build.gradle.kts`의 `versionName = "1.0.13"` → **반드시 같은 문자열**

**업데이트할 때 규칙:**
- `versionCode`(=`+숫자`)는 배포 때마다 **무조건 1 이상 증가**. Play Store가 이전 버전보다 작거나 같으면 업로드를 거부함.
- `versionName`(=`X.Y.Z`)은 사람이 보는 숫자라 자유롭게 정하면 됨. 보통 작은 수정/버그 픽스는 마지막 자리(Z)만, 기능 추가는 가운데 자리(Y)를 올림.

**예시 (다음 업데이트가 자잘한 수정이라면):**
```
pubspec.yaml:        version: 1.0.14+16
build.gradle.kts:    versionCode = 16 / versionName = "1.0.14"
```

**참고**: `lib/services/update_service.dart`의 `_releaseNoteVersion` 상수는 "업데이트 후 첫 실행 안내 팝업" 기능용입니다. 이번 버전(1.0.13)은 이 값을 안 건드려서 팝업이 안 뜨게 해뒀습니다. 다음에 또 사용자에게 알릴 내용이 생기면, 그때 이 값을 새 버전 문자열로 바꾸고 `_releaseNoteTitle`/`_releaseNoteBody`도 새로 쓰면 됩니다. (건드리지 않으면 계속 조용히 스킵됩니다 - 실수로 예전 안내가 다시 뜰 걱정은 없습니다.)

---

## 2. 빨간날(공휴일) 데이터 추가하기

**위치**: `lib/screens/calendar_tab.dart` 파일 맨 위, `_lunarHolidays` 라는 Map

**현재 데이터가 커버하는 범위**: 2025년 ~ 2029년까지. **2030년이 되기 전(즉 2029년 안)에 2030년 데이터를 추가해야** 2030년 달력에도 빨간날이 표시됩니다. 그 이후 해도 마찬가지로 매년 미리 채워둬야 합니다.

### 왜 자동으로 계산이 안 되나요?
`_fixedHolidays`(신정, 삼일절, 어린이날 등 매년 날짜가 똑같은 공휴일)는 이미 자동으로 매년 적용됩니다 - 이건 안 건드려도 됩니다.

문제는 `_lunarHolidays` 쪽인데, 여기 들어가는 건:
- **설날/추석** (음력 기준이라 매년 양력 날짜가 다름)
- **부처님오신날** (역시 음력 기준)
- **대체공휴일** (공휴일이 주말과 겹치면 평일로 하루 미루는 제도 - 정부가 그때그때 정함, 계산식이 없음)
- 가끔 있는 **임시공휴일/선거일** (그해마다 정부가 별도 지정)

이런 건 프로그램으로 계산할 수 없고, **정부가 공식 발표한 날짜를 그대로 받아써야** 합니다.

### 데이터 구하는 곳
- 한국천문연구원(KASI) 홈페이지의 "음력 변환/특일 정보" - 설날/추석/부처님오신날의 양력 날짜를 몇 년 뒤까지 미리 계산해서 제공합니다.
- 행정안전부 또는 포털(네이버/구글)에 "20XX년 공휴일" 검색 - 대체공휴일까지 포함해서 정리된 표가 매년 나옵니다. 보통 그 전해 하반기~말에 다음 해 공휴일이 확정 발표됩니다.

### 추가하는 방법
`_lunarHolidays` Map 안에 아래 형식으로 한 줄씩 추가하면 됩니다 (기존 2029년 블록 바로 아래에 이어서 쓰면 됨):

```dart
  // 2030년
  '2030-02-02': '설날',       // 예시 - 실제 날짜로 바꿀 것
  '2030-02-03': '설날',
  '2030-02-04': '설날',
  '2030-05-05': '부처님오신날',  // 어린이날과 겹치면 대체공휴일이 생길 수도 있음
  '2030-09-11': '추석',
  '2030-09-12': '추석',
  '2030-09-13': '추석',
```

**주의할 점:**
- 날짜 형식은 반드시 `'YYYY-MM-DD'` (하이픈 포함, 월/일 두 자리).
- 설날/추석은 원래 3일 연휴라 보통 3줄(전날-당일-다음날).
- 대체공휴일이 생기는지는 그해 실제 요일을 확인해야 함 (설날/추석/어린이날/부처님오신날이 토·일요일과 겹치거나, 설날·추석 연휴가 다른 공휴일과 겹치면 발생).
- 다 채운 뒤 [1. 버전 올리기]도 같이 해서 새 버전으로 빌드/배포해야 사용자에게 반영됩니다.

### 작업 체크리스트 (매년 말)
- [ ] KASI 또는 포털에서 다음 해 공휴일 표 확인
- [ ] `_lunarHolidays`에 설날/추석/부처님오신날/대체공휴일 추가
- [ ] `flutter build appbundle --release`로 다시 빌드
- [ ] 버전 올리기 (위 1번)
- [ ] Play Console에 업로드
