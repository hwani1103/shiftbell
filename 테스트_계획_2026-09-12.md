# 배포 전 전수 테스트 계획 (2026-09-12)

> 이 문서는 **계획 문서**입니다. 실제 테스트 실행/코딩은 여기서 세운 계획대로
> 이후 단계에서 진행합니다. 진행 경과/결과는 바탕화면의 별도 메모장
> (`테스트_진행상황.txt`)에 계속 최신화하며 기록합니다 - 이 문서(계획) 자체는
> 안 바뀝니다(계획이 바뀌면 이 문서를 개정).

## 전체 구성

- **A. 에뮬레이터 전수 테스트** - 이미 구축된 에뮬레이터로 앱의 살아있는 모든
  기능을 단위별로 나눠서 수동/반자동으로 전부 트리거해봄.
- **B. 자동화 테스트 환경 구축** - A로 못 잡는(또는 반복재현이 비효율적인)
  영역을 `integration_test`(Flutter) + Kotlin/Robolectric(Native)으로 코드화.
  B는 **실제로 코딩부터 시작**하는 작업 - 계획만이 아니라 지금 이 프로젝트에
  없는 테스트 인프라 자체를 새로 만드는 것.
- **C. 결과 기록 형식** - PASS/FAIL 기록 규칙.

---

## A. 에뮬레이터 전수 테스트

### A-0. 환경

- **사용 에뮬레이터**: `Pixel_3_XL_API_33` (이미 구축된 두 개 중 선택)
  - 판단 근거: Pixel 3a(API 33, x86_64)보다 화면이 크고(6.3" vs 5.6") 표준
    해상도(3120×1440)라 이 앱이 다뤄온 기기별 스케일 보정 이슈
    (`schedule_management_tab.dart`의 `.h/.w/.r` 보정 이력 등)가 더 잘 드러남.
    Pixel 3a 쪽 화면(5.6", 2220×1080)은 필요하면 A 통과 후 추가 확인용으로만
    보조 사용.
  - 실행: `flutter emulators --launch Pixel_3_XL_API_33` →
    `flutter run --flavor dev -d emulator-5554` (또는 release APK 설치 후 구동)
  - 시스템 이미지에 Google Play 포함 여부 확인 필요(Firebase/AdMob 테스트를
    위해 Google Play 서비스가 있어야 함) - 없으면 Play 포함 이미지로 AVD를
    다시 만들어야 함(현재 이미지가 그런지 먼저 확인).

- **Dart 유닛테스트(`test/` 9개 파일)에 대한 판단**: 폐기하지 않고 유지함.
  다만 이번 "전수 테스트"의 핵심 수단은 아님 - 컨디션 룰엔진/오늘의 예측/
  수면 로직/메모 분류/패턴 계산 등 **순수 계산 로직만** 커버하고 UI·네이티브
  연동·실제 알림 발생 여부는 전혀 못 봄. 회귀 방지용 안전망으로 계속
  `flutter test`로 같이 돌리되, "100% 전수 테스트"의 실제 작업량은 A(에뮬레이터)
  + B(신규 자동화)가 담당.

### A-1. 테스트 데이터 기준값 (전 항목 공통으로 이 값 사용)

- **근무 패턴**: 규칙적, 8일 주기 - `주간 - 주간 - 휴무 - 휴무 - 야간 - 야간 - 휴무 - 휴무`
- **근무 시각**: 주간 07:00~19:00(12시간), 야간 19:00~07:00(익일, 12시간)
- **급여 정산 기준일**: 21일~다음 달 20일
- **OT**: 여러 날짜에 걸쳐 반복적으로 기록/수정/삭제(단발성 확인 아님)
- **근무 변경**: 달력에서 임의 날짜를 여러 번 재배정(패턴과 다르게)

### A-2. 테스트 단위 (전부 이 순서로 진행, 각 단위가 완료돼야 다음 단위로)

1. **온보딩/최초 설치**
   - 앱 삭제 후 재설치(에뮬레이터라 반복 쉬움) → 웰컴 팝업 1회 노출 확인
   - 규칙적 패턴 생성 플로우로 위 A-1 패턴 입력
   - 권한 요청 화면(알림/오버레이/정확한 알람) 전부 허용 플로우 확인

2. **근무 패턴 / 달력**
   - 달력에 A-1 패턴이 8일 주기로 정확히 반복되는지(최소 2달치 스크롤)
   - 임의 날짜 근무 변경(불규칙 예외 지정) 여러 건 - 변경 후 알람도 그에 맞게
     갱신되는지
   - 캘린더 테마 9종 전환 확인(레이아웃 깨짐 없는지)
   - 공휴일 표시 확인

3. **OT(초과근무) / 근로시간 계산**
   - 여러 날짜에 OT 기록 반복 입력(다른 날짜, 다른 시간으로 최소 5건 이상)
   - 기존 OT 수정, 삭제도 각각 확인
   - 설정 → 근무시간 및 OT 설정에서 **"OT 합산" / "OT 비합산" 두 설정 모두**
     켜고 각각:
     - 일별 근로시간 표시
     - 주별 근로시간 합계
     - 월별 근로시간 합계(위 A-1의 21~20일 정산 기준으로 월 경계가 정확히
       나뉘는지 - 특히 21일/20일 당일 근무가 어느 달에 집계되는지 정밀 확인)

4. **급여 정산 기준일(21~20일)**
   - 설정에서 이 기준일 지정
   - 21일 근무와 20일 근무가 서로 다른 정산월로 갈리는 경계 케이스 집중 확인

5. **알람 시스템**
   - 근무별 알람 템플릿 최대 5개까지 등록(주간/야간 각각)
   - day_offset(전날/당일/다음날) 조합 확인 - 특히 야간 근무 "전날 저녁 알람"
   - 같은 실제 시각에 여러 알람이 겹치는 케이스(우선순위: 당일>전날 기여>
     다음날 기여) 확인
   - **알람 실제로 울리는지**: 화면 꺼짐+잠금 상태에서 알람 시각 도달 →
     잠금화면 액티비티 정상 표시/소리/스와이프 동작
   - 화면 켜짐+잠금해제 상태에서는 오버레이로 뜨는지
   - 스누즈, 끄기, 타임아웃 자동 종료 각각 확인
   - 겹쳐 울리는 알람(A 응답 전 B 도착) 케이스
   - 앱 재부팅(에뮬레이터 재시작) 후 알람 재등록 확인

6. **일정관리 탭**
   - 일정 생성(오늘 새로 고친 분할카드: 지속시간 없음 ↔ 제안시각 확정 전환)
   - 일정 수정/삭제
   - 일정 알림 켜기/끄기, N분전 옵션 전부
   - **과거 시각에 알림 켜면 경고 뜨는지**(오늘 고친 부분)
   - 알림 실제 발생 확인(정시/N분전 각각) + 잠금화면 상태에서도 오는지
   - 내용 자동분류 아이콘 배정 확인(카테고리 여러 개 입력해서 검증)

7. **컨디션 탭**
   - 근무시간 미입력 상태 안내 문구
   - 근무시간 입력 후 판정(NORMAL/ATTENTION/HIGH_LOAD) 각 상태 유도해서 확인
     (예: 연속 야간 3일 이상 만들어서 ATTENTION 트리거)
   - 오늘의 컨디션 예측 문구 - 무난한 상황/문제 상황 둘 다 확인
   - 컨디션 탭 최초진입 튜토리얼 팝업(오늘 스크롤 버그 고친 부분) - 재설치
     후 재노출 + 버튼이 항상 보이는지

8. **수면 기록**
   - 홈 화면 위젯 추가, 수동 취침/기상 기록
   - 자동 추정 후보 발생 확인, "맞아요"/"기록하지 않기" 각각
   - 거부학습(같은 시간대 반복 거부 시 다음부터 덜 잡히는지)
   - 최근 수면 기록 미니 달력 - 직접 추가/수정/삭제

9. **백업/복구**
   - 수동 백업("지금 백업")
   - 자동 백업(백그라운드 전환)
   - 앱 삭제 후 재설치 → 복구 화면 진입 → 복구 → 데이터 원상복구 확인
   - 복구 후 권한 재요청 플로우 확인

10. **친구 공유**
    - 공유 시작 → 코드 발급
    - 다른 계정(에뮬레이터 2대 또는 같은 기기 다른 프로필)에서 코드로 추가
    - 상대 근무표 렌더링 확인, "마지막 동기화 시각" 표시 확인
    - 공유 중지

11. **설정 탭 전반**
    - 근무명 변경(rename) - 컨디션 시각 데이터가 같이 이동하는지(M1 수정 확인)
    - 알람음 7종 + 볼륨 보정
    - 언어 전환(한국어/영어) - 컨디션 탭만 한국어 유지되는지 등 전반 확인

12. **업데이트 안내(소프트만, 강제 업데이트는 이번에 제거함)**
    - Firestore `app_config/android`의 `latestVersionCode`를 임의로 올려서
      "새 버전이 있어요!" 다이얼로그 노출 확인, "나중에"/"업데이트" 각각 동작

13. **Firebase Analytics / AdMob**
    - 에뮬레이터에서 앱 사용 후 Firebase 콘솔 실시간 리포트에 이벤트 잡히는지
    - AdMob 테스트 배너 정상 노출

### A-3. 라운드 기반 진행 방식 (반복 테스트 설계)

한 번에 "모든 경우의 수"를 다 트리거하는 건 현실적으로 불가능하므로,
**여러 라운드로 나눠서 라운드마다 커버리지를 넓혀가는 방식**으로 진행한다.

- **라운드 1**: A-2의 모든 단위를 위 A-1 기준값으로 한 차례 전부 진행(지금
  계획대로). 이때 "그냥 해봤다"가 아니라, **어떤 구체적 조건 조합을
  테스트했는지**를 하나하나 기록으로 남긴다(예: "OT 테스트함"이 아니라
  "OT: 09-15 야간 근무에 3시간, 합산 모드, 21~20일 정산 기준 → 월 집계
  정상" 처럼 구체적 튜플 단위로).
- **라운드 2 이후**: 새 라운드를 시작하기 전에 지금까지 쌓인 커버리지
  기록을 먼저 확인하고, **이미 명확히 통과 확인된 조합은 건너뛰고, 그
  단위 안에서 아직 안 건드린 새로운 조건 조합만 골라서 테스트**한다.
  예: 라운드 1이 "OT 합산 모드"만 다뤘다면 라운드 2는 "OT 비합산 모드"부터,
  라운드 1이 "규칙적 패턴"만 다뤘다면 라운드 2는 "불규칙 패턴 + 수동 예외
  지정"부터 등.
- **라운드가 4~5회 이상 누적되어 새로 테스트할 미답 조합이 거의 안 남으면**,
  그때부터는 이미 통과했던 조합을 다시 골라 반복 테스트(회귀 확인)하는
  것도 허용한다 - 이 시점부터는 "새 영역 개척"보다 "안정성 재확인"이 목적.
- 이 커버리지 기록은 바탕화면 `테스트_진행상황.txt`에 라운드별로 남긴다
  (아래 C번 형식에 라운드 구분을 추가해서 관리 - "몇 라운드에 뭘 다뤘는지"가
  한눈에 보여야 다음 라운드에서 정확히 겹치지 않게 고를 수 있음).

### A-4. 에뮬레이터의 한계 (여기서 확정적으로 검증 불가 - B/잔여 리스크로 이관)

- OEM 특이 버그(예: 삼성 MediaStore `owner_package_name` NULL) - AOSP
  에뮬레이터에선 재현 자체가 안 됨
- 제조사 자체 배터리 최적화/Doze 강제종료 - 순정 에뮬레이터엔 없음
- 실제 네트워크 지연 하의 알림 전달 타이밍
- H2(트랜잭션 중 OS 콜) 같은 정밀 타이밍 경쟁 상태 - 사람이 손으로도,
  에뮬레이터로도 정확한 타이밍 재현 불가(→ B의 Robolectric 담당 영역)

---

## B. 자동화 테스트 환경 구축 (신규 코딩)

### B-1. `integration_test` 패키지 (Flutter, UI/E2E)

- **목적**: A에서 사람이 손으로 눌러야 했던 반복 시나리오(일정 생성, 근무
  변경, 설정 값 토글 등)를 코드로 스크립트화해서 언제든 재실행 가능하게 함.
  에뮬레이터/실기기 둘 다에서 동일하게 돌릴 수 있음.
- **설정**: `pubspec.yaml`에 `integration_test` 의존성 추가,
  `integration_test/` 디렉토리 신설.
- **1차로 코드화할 시나리오**(A-2 단위 중 자동화 우선순위 높은 것):
  1. 온보딩 → 규칙적 패턴(A-1 값) 생성 → 달력에 정확히 반영되는지
  2. 일정 생성 → 분할카드 상호작용(지속시간없음↔확정) → 저장 → 재진입 시
     값 유지 확인
  3. OT 입력 → 월별 근로시간 합계 값 assert(21~20일 경계 케이스 포함)
  4. 설정 → OT 합산/비합산 토글 → 계산 결과 값이 실제로 달라지는지 assert
  5. 근무명 rename → 컨디션 시각 데이터 보존 확인(M1 회귀 테스트)
  6. 컨디션 탭 튜토리얼 팝업 - 버튼이 항상 화면 안에 렌더링되는지(오늘 버그
     회귀 테스트)

### B-2. Kotlin/Robolectric 유닛테스트 (Native, 결정론적 정밀 검증)

- **목적**: 에뮬레이터/실기기로는 타이밍상 재현이 사실상 불가능한 케이스를
  가짜(mock/fake) `AlarmManager`/DB로 결정론적으로 재현.
- **설정**: `android/app/build.gradle.kts`에 `testImplementation`으로
  JUnit + Robolectric(+ 필요시 Mockito) 추가. `android/app/src/test/kotlin/...`
  디렉토리 신설(이 프로젝트 최초의 Kotlin 테스트).
- **1차로 코드화할 대상**:
  1. `AlarmRefreshEngine.doRefresh()` - toAdd 배치 중 N번째 항목에서
     `scheduleNativeAlarm()`이 예외를 던지도록 mock → 그 이전 항목들의 DB
     상태/OS 등록 상태가 의도대로 되는지(H2 관련 검증용 - 지금은 안 고치기로
     했지만, 나중에 고치기로 결정하면 이 테스트가 바로 "제대로 고쳐졌는지
     100% 검증" 역할을 함)
  2. `ScheduleNotificationScheduler.triggerMillisFor()` - 날짜/시각 경계값
     (자정, 22시대 알림 등) 계산 정확성
  3. day_offset 알람 우선순위 계산(당일>전날 기여>다음날 기여) 로직

### B-3. 실행 순서

1. `integration_test` 셋업 + B-1 시나리오 코드화
2. Kotlin 테스트 인프라 셋업 + B-2 대상 코드화
3. 완료 후 **A의 전 단위를 다시 한번(이번엔 자동화된 부분은 스크립트로,
   나머지는 수동으로) 재실행** - "다시 한번 빡세게 진행"

---

## C. 결과 기록 형식

바탕화면 `테스트_진행상황.txt`에 **라운드 단위로** 구획을 나눠서 계속 갱신한다.

```
## 라운드 N (날짜)
다룬 조건 조합:
- [단위번호-항목] 구체적 조건 튜플 → PASS/FAIL

FAIL 상세(있으면):
- [단위번호-항목] 조건: ...
  원인: ...
  수정 방법: ...
  수정 리스크: ...
```

- **PASS**: `[단위번호-항목] 구체적 조건 → OK` — 이유/설명 불필요, 조건+OK만.
  단, "구체적 조건"은 반드시 남긴다 - 다음 라운드가 뭘 건너뛰어도 되는지
  판단하는 유일한 근거이기 때문(A-3 라운드 방식 참고).
- **FAIL**: 아래 4가지를 전부 기록
  1. **정확한 재현 환경/조건**: 어떤 설정값/시나리오/기기 상태에서 실패했는지
     (예: "OT 비합산 설정 + 21일 야간근무 + 월 경계 케이스에서 근로시간 합계가
     이전 달로 잘못 집계됨")
  2. **원인**: 코드 레벨로 특정한 근본 원인
  3. **고칠 수 있는 방법**: 구체적 수정 방향
  4. **그 수정으로 인한 리스크**: 다른 기능에 영향 있는지(H2/M4 리스크
     분석과 동일한 수준의 전수조사 - "0.000000001까지" 검토)

라운드가 끝날 때마다 이 파일 맨 위에 "지금까지 커버한 조건 요약"을 한 번
갱신해둬서, 다음 라운드 시작할 때 전체를 위에서부터 다시 읽지 않아도
어디가 비어있는지 바로 보이게 한다.
