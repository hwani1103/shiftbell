# 전체 코드/사용성 점검 리포트 (2026-09-04)

> 사용자 요청으로 진행한 전면 점검. 알람/수면기록/컨디션매니저/백업·DB마이그레이션/
> 주요 UI 화면/친구공유(Firestore 포함) 6개 영역을 병렬로 나눠서 코드 수준
> 버그 + UX 관점 양쪽에서 검토함. 형식은 `코드_품질_검수_리포트_2026-08-14.md`,
> `백업_수면_컨디션_기능_검토_2026-09-01.md`와 동일(심각도 태그 + 파일:라인 +
> 재현 시나리오). **이번 리포트는 검토만 수행 - 코드는 건드리지 않음.**

## 커버리지 고지 (정직하게)

- **깊게 봄**: 알람 네이티브 전체(AlarmRefreshEngine/AlarmGuardReceiver/
  CustomAlarmReceiver/AlarmActivity/AlarmOverlayService/AlarmPlayer/
  VolumeCalibration/DirectBootReceiver/AlarmActionReceiver·Helper/
  NotificationHelper/DatabaseHelper) ↔ `alarm_generation_service.dart` 대조,
  수면 감지 전체(SleepDetectionReceiver/Scheduler/SleepScheduleResolver/
  SleepWidgetProvider·ActionReceiver) ↔ `sleep_opportunity.dart` 대조,
  컨디션 매니저 전체(`shift_pattern_analyzer`/`condition_rule_engine`/
  `evidence_database`/`today_forecast_engine`/`sleep_condition_extras` 등),
  백업/복구 전체(`backup_payload`/`backup_service`/`backup_watcher`/
  `backup_storage_service`/`restore_backup_screen` + MainActivity.kt의 백업
  메서드 3개), DB 마이그레이션 체인(`DatabaseHelper.kt` ↔ `database_service.dart`
  v2~v22), 주요 UI 화면(`calendar_tab`/`schedule_management_tab`/
  `next_alarm_tab`/`work_hours_settings_screen`/`all_shifts_view`/
  `all_alarms_history_view`/`memo_list_view`/`work_hours_calculator`),
  친구공유 전체(`friend_share_service`/`friend_sync_service`/
  `friend_provider`/`friend_list_screen`/`friend_calendar_view`/
  `my_share_code_screen`) + `firestore.rules` + `web_main.dart`.
- **가볍게 훑음**: `calendar_tab.dart`/`settings_tab.dart`는 각각 3000줄 이상이라
  전체를 줄 단위로 다 보지는 못했고, rename/색상변경 핸들러 등 타겟팅된 구간
  위주로 봄. 남은 부분에 이번에 못 찾은 버그가 있을 가능성은 있음.
- **아예 안 봄**: 캘린더 테마 9종 각각의 픽셀 단위 렌더링 코드, l10n arb 파일
  자체(문자열 내용), 광고(AdMob) 관련 코드.

## 종합 결론 (먼저 요약)

핵심 기능(알람 생성/갱신)은 Dart↔Kotlin 이중구현이 실제로 동일한 로직을
유지하고 있고(day-offset 우선순위, DST-safe 날짜 diff 등 라인 단위로 대조해서
확인함), 과거 3번 재발했다던 종류의 사고는 이번엔 안 보였음. 반면 **최근에
빠르게 추가된 기능들(컨디션 매니저/수면기록/백업)에서 진짜 심각한 버그가
나왔고**, 그중 하나(백업 자동 감지)는 "쓰고 있다고 믿었는데 사실 거의 항상
안 되고 있었다"는 최악의 실패모드라 최우선 확인이 필요함.

---

## 🔴 HIGH

### H1. [✅ 2026-09-04 수정됨 - 2026-09-13 재확인] 컨디션 탭 무한루프 / ANR 위험
`lib/services/condition/shift_pattern_analyzer.dart:137-171`
(`consecutiveNightStreakEndingAt`/`consecutiveWorkStreakEndingAt`/
`consecutiveLongShiftStreakEndingAt`)

세 함수 모두 하루씩 뒤로 훑으며 연속 야간/연속근무/연속 장시간근무 일수를
세는데 **최대 조회일수 캡이 없음**(같은 파일의 `recoveryWindowContaining`은
`lookbackDays: 14`로 캡이 있는데 이 3개만 없음). 규칙적 패턴이면
`ShiftSchedule.getShiftForDate`가 과거 어느 날짜든 modulo 인덱싱으로
답을 내주기 때문에, **패턴 안에 쉬는 날이 하나도 없는 스케줄**(예: 매일 야간만
도는 근무자)에서는 이 루프가 영원히 끝나지 않음. `ConditionRuleEngine.evaluate()`가
`condition_tab.dart` 빌드마다 동기 호출하므로 컨디션 탭을 열 때마다 앱이
멈춤(ANR).

**설계문서(`컨디션매니저_설계.md` 812번 줄 근처)는 이 "무제한 역탐색"을 오히려
장점으로 서술**하고 있는데, 검증은 6~7일짜리(쉬는 날에서 끝나는) 케이스만
해본 것으로 보임 - 쉬는 날이 없는 병적인 케이스는 테스트된 적 없음.

**재현**: 매일 야간 근무만 배정된 규칙적 스케줄을 만들고 컨디션 탭 진입.

**수정 방향**: 세 함수에 `maxLookbackDays`(예: 90~180) 캡 추가, 초과 시 카운트
확정하고 반환.

### H2. [사용자 결정(2026-09-13) - 그대로 두기로 함, 미수정] 알람 갱신이 DB 트랜잭션 안에서 실제 OS 알람 등록을 호출
`android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:142-172`

`db.beginTransaction()` 안의 루프에서 `cancelNativeAlarm()`/`scheduleNativeAlarm()`
(진짜 `AlarmManager` 호출)을 DB insert/delete 사이사이에 부르는데, 이 루프에는
개별 try/catch가 없음(같은 파일 183-192번의 rearm 루프는 있음). 만약 루프
중간에 예외(가장 유력: 사용자가 세션 중 "정확한 알람" 권한을 꺼서
`setExactAndAllowWhileIdle()`가 `SecurityException`)가 나면 **트랜잭션 전체가
롤백**되지만, 그 전까지 이미 걸어놓은 실제 OS 알람 등록은 취소되지 않음. 그
알람이 나중에 울리면 `CustomAlarmReceiver.alarmExistsInDb()`가 DB에서 매칭되는
행을 못 찾고 그냥 조용히 무시함 - `alarm_history`/`alarm_creation_log`에 아무
기록도 안 남음. **"알람 이력은 절대 자동 삭제/누락 금지"라는 프로젝트 원칙과
정면 충돌**하는 지점이고, `alarm_generation_service.dart:138-140`의 주석("트랜잭션
안에서 플랫폼 채널을 부르지 않음")이 지키고 있는 원칙을 이 Kotlin 엔진만 깨고
있음.

**수정 방향**: `scheduleNativeAlarm`/`cancelNativeAlarm` 호출을 개별 try/catch로
감싸거나(185-191번과 동일 패턴), OS 호출 자체를 트랜잭션 커밋 이후로 미룸.

### H3. [✅ 2026-09-04 수정됨(+2026-09-05 회귀 수정, 2026-09-11 재시도 로직 추가) - 2026-09-13 재확인] 자동 백업의 "변경 감지"가 사실상 대부분 상황에서 안 먹힘
`lib/services/backup_watcher.dart:34-43`

`backupNow()`가 `PRAGMA data_version`을 `DatabaseService.instance.database` -
**앱이 평소 쓰기에도 쓰는 바로 그 커넥션**에서 조회함. SQLite 공식 문서 기준
"`data_version`은 같은 커넥션이 만든 커밋에 대해서는 안 바뀐다" - 즉 이
카운터는 **다른 커넥션**(예: Kotlin의 별도 `DatabaseHelper` 커넥션)이 그 사이
DB를 건드려야만 변함. `백업복구_설계.md:151-153`과 이 파일 자신의 주석은
"Dart든 Native든 쓰면 카운터가 올라간다"고 가정하는데 **그 전제 자체가
틀림**.

**구체적 실패**: 사용자가 Flutter UI에서만 데이터를 바꾸고(근무 스케줄 수정,
메모 추가, OT 기록 등 - 가장 흔한 사용 패턴) 그 사이 알람이 울리거나 자정
갱신 같은 네이티브 쓰기가 한 번도 안 일어나면, 앱을 백그라운드로 보내도
`currentVersion == lastVersion`으로 오판해서 **백업이 조용히 스킵됨**. 사용자는
"자동 백업이 되고 있다"고 믿지만 실제로는 계속 안 되고 있을 수 있음.

**수정 방향**: `data_version`을 앱의 쓰기 커넥션과는 별도의 짧은 임시 커넥션으로
조회.

---

## 🟠 MEDIUM

### M1. 근무명 rename 시 컨디션 매니저 데이터가 고아가 됨
`lib/services/database_service.dart:1010-1066`(`renameShiftAtomic`)이
`alarms`/`shift_alarm_templates`/`alarm_history`/`alarm_creation_log`/
`shift_schedule`은 마이그레이션하면서 `condition_shift_times`(v21, 근무명이
키)는 빠뜨림. 설정에서 근무명을 하나 바꾸면, 그 근무명에 입력해둔 출퇴근시각이
사라진 채로 남고 - 그게 유일하게 채워져 있던 근무명이었다면 컨디션 탭 전체가
"미설정" 안내문구로 돌아감(원인이 rename인지는 사용자가 알 방법이 없음).

### M2. 복구가 DB 커밋 이후 실패해도 "복구 실패"로 보고됨
`lib/screens/restore_backup_screen.dart:58-133` - `restoreAll()`의 DB
트랜잭션은 이미 커밋됐는데, 그 뒤 `AlarmRefreshService.forceRefresh()`/
`PermissionService().checkPermissions()`/재시작 채널 호출 중 하나가 예외를
던지면 바깥 `catch`가 "복구 실패" 토스트를 띄움. 사용자는 실패로 알고 다시
시도하지만 데이터는 이미 되돌릴 수 없이 덮어써진 상태.

같은 경로에서 `permissions_requested` 리셋도 `!osPermissionsGranted`일
때만 조건부로 이루어지고(106-121번), catch 블록(127-133)에서는 전혀
안 건드림 - 실패가 커밋 이후에 나면 새 기기에서 알림/정확한알람 권한을
다시 요청하는 절차 자체가 조용히 스킵될 수 있음(2026-09-01에 고쳤던 것과
같은 종류의 문제가 이 경로로 재발 가능).

### M3. 백업 파일을 "삭제 후 새로 쓰기" 순서
`MainActivity.kt:546-572`(`writeBackupFile`) - 549번에서 기존 백업을 먼저
지우고 556-560번에서 새로 씀. `resolver.insert()`가 null을 반환하거나 쓰기
도중 실패(저장공간 부족 등)하면 옛 백업은 이미 없고 새 백업도 없는 상태가 됨 -
호출자는 `false`만 받고 사용자에게 경고 없이 넘어감(`backup_watcher.dart:67-70`).

### M4. 친구공유: 재설치 시 "끊기는" 게 아니라 옛 데이터를 계속 정상처럼 보여줌
`lib/services/friend_sync_service.dart` - 상대방이 재설치하면 새 익명 UID가
발급되지만 옛 `friend_schedules/{oldOwnerId}` 문서는 안 지워지고 영원히
Firestore에 남음(`stopSharing()`을 호출하는 유일한 경로인데, 재설치 흐름에서는
안 불림). 내 앱은 `fetchByOwnerId(oldOwnerId)`를 계속 성공적으로 호출해서
**재설치 시점에 멈춘 스냅샷을 영원히 최신인 것처럼 보여줌** - CLAUDE.md에
적힌 "연결이 끊긴다"보다 실제로는 더 나쁜 상태(에러도 없이 그냥 낡은 데이터를
계속 보여줌). `updatedAt` 필드는 모델까지 전달되지만 화면 어디에도
"마지막 동기화 시각"으로 렌더링되지 않음.

### M5. 공유 브라우저에서 이전 방문자의 친구 근무표가 다음 사람에게 노출될 수 있음
`lib/web_main.dart:246-254` - URL에 `code` 파라미터가 없으면 localStorage에
저장된 마지막 owner_id로 조용히 폴백함(PWA 홈화면 재실행 버그 대응용으로
넣은 것). 문제는 이 폴백이 **PWA standalone 모드인지 확인 없이** 아무 방문에나
적용됨 - 공유/키오스크 브라우저에서 A가 친구 링크를 봤다가 닫고, B가 나중에
그냥 루트 도메인으로 들어오면 A가 마지막으로 본 친구의 근무표가 그대로 뜸.
민감도는 낮음(근무 패턴/색상 + 자기가 정한 표시이름 정도만 노출, 그 외 PII
없음)지만 실제 노출 경로임.

### M6. 일정관리탭 00:00 슬롯에서 시간 미세조정 시 음수 시각 저장 가능
`lib/screens/schedule_management_tab.dart:2859-2875`(`_adjustActiveCard`) -
시작 시각이 0분(자정)일 때 "−" 방향으로 5분씩 내리면 하한 클램프가
`_fineAdjustBoundCenter - _fineAdjustRangeMinutes`(음수 가능)라서
`_preciseMinutes`가 음수가 됨. `_to12Hour(-5)`가 "12:55 AM"처럼 깨진 시각을
표시하고, 그 음수 값이 `date_schedules`에 그대로 저장되어 이후 슬롯 계산과
영구히 어긋남.

### M7. 연속 야간근무 사이 회복수면이 "낮잠"으로 오분류
`lib/services/condition/sleep_day_slots.dart:77-102`(`_isMainSleep`) - 전날
야간근무 끝나고 자는 회복수면인데, 그날 저녁에 또 야간근무가 있으면
"그 근무 시작 전이니까 낮잠"으로 강제 분류됨(당일 근무만 보고 전날 근무를
고려 안 함). **UI 표시(미니달력/전체보기)에만 영향** - 컨디션 예측 엔진
(`sleep_history.dart`)은 이 케이스에 대한 별도 안전장치가 이미 있어서 수치
자체는 안 틀림.

### M8. 수면 최소 2분 규칙이 Dart 쪽엔 구현 안 됨
Kotlin(`SleepWidgetActionReceiver.kt`)에만 최소 시간 검증이 있고,
`sleep_edit_dialog.dart`/`sleep_record_provider.dart`/`database_service.dart`의
Dart 경로(수동 수정, AUTO_DETECTED 후보를 몇 초로 줄여서 저장 등)에는 검증이
전혀 없음.

### M9. 컨디션 점수(0~100점)가 CLAUDE.md 원칙과 문서상 충돌
`lib/services/condition/condition_score.dart` + `condition_tab.dart`의
"컨디션 점수 : N점" 표시 - CLAUDE.md는 "숫자 점수·%·질병 진단 절대 금지"를
하드 제약으로 못박고 있는데, 실제로는 사용자 요청으로 예외가 허용된 기능(설계
문서 `컨디션매니저_설계.md` 19장에 기록 있음)임. **CLAUDE.md 자체에는 이 예외가
반영 안 돼 있어서**, 이 원칙만 보고 판단하면 오해하기 쉬움 - 문서 갱신 필요.

### M10. 백업 복구가 부분 실패 격리 없이 전체 롤백
`lib/services/backup_service.dart:157-171` - 테이블 하나라도 지금 스키마와
안 맞는 행이 있으면(예: NOT NULL 컬럼이 새로 추가된 경우) 트랜잭션 전체가
롤백되어 모든 테이블 복구가 무산됨. DB 다운그레이드(최신 백업을 구버전 앱에
복구) 시나리오에 대한 처리도 없음(`onDowngrade` 핸들러 부재).

---

## 🟡 LOW

- 알람 볼륨을 정수 나눗셈으로 절반 감소시키는 로직(`AlarmPlayer.kt:188-190,242-244`) -
  최대볼륨 단계가 1인 기기에서 `maxVolume / 2 == 0`이 되어 알람이 완전
  무음될 수 있음.
- 스누즈 시간 충돌 회피(`AlarmActionHelper.kt:125-133`)가 최대 `minutes+5`회
  시도 후 포기하고 충돌을 그냥 허용 - 낮은 확률이지만 원치 않는
  `superseded_by_next_alarm` 이력이 남을 수 있음.
- 알람 끄기 처리 중 예외 발생 시 DB에 좀비 행이 남을 수 있음(무해하지만
  이력 누락) - `AlarmActionHelper.kt:47-76`.
- `all_shifts_view.dart`가 `shift_schedule.dart`의 `shiftColors` 캐시를 직접
  읽음(`calendar_tab.dart`는 항상 라이브 계산) - 지금 당장 재현되는 버그는
  아니지만, 향후 어떤 쓰기 경로가 캐시 갱신을 빼먹으면 전체근무표만 색이
  다르게 보이는 구조적 위험.
- `ShiftTimeRange`(`lib/models/shift_time_range.dart:25-29`) - 출퇴근시각을
  실수로 똑같이 입력하면 0시간이 아니라 24시간으로 계산되어 근로시간/OT/
  컨디션 판정에 그대로 들어감.
- `RefreshLockManager`의 락 TTL(30초)이 만료됐을 때 실제 갱신 완료 여부를
  확인 안 함 - 기기 성능 저하 등으로 갱신이 30초를 넘기면 이 락이 막으려던
  race condition이 부활할 이론적 여지.
- `test/widget_test.dart`가 `ProviderScope` 없이 `MyApp()`을 pump해서 항상
  실패하는 깨진 스모크 테스트(`No ProviderScope found`) - 이번 점검과 무관,
  기존부터 이랬을 가능성이 높음.

---

## firestore.rules 보안 점검 (문제없음, 참고용 기록)

`friend_schedules`(`firestore.rules:16-19`): `allow get: if true; allow list:
if false; allow create, update, delete: if request.auth != null &&
request.auth.uid == ownerId;` - 쓰기 권한이 `request.auth != null`만이 아니라
`ownerId` 일치까지 요구하고, `list: false`로 전체 컬렉션 열람도 막혀 있어서
"문서 ID(공유코드)를 아는 사람만 읽을 수 있다"는 설계가 실제로 지켜짐.
`app_config`/`health_tips`는 get/list만 허용, write는 전부 차단 - CLAUDE.md
설명과 일치. 유일한 아쉬운 점은 자기 문서에 쓰는 내용의 필드 타입/크기 검증이
전혀 없다는 것(방어심도 부족) - 하지만 다른 사용자 데이터를 침해하는 실제
악용 경로는 못 찾음.

---

## 우선순위 제안

1. **H2 → H3 → H1** 순으로 먼저 (모두 "조용히 실패해서 사용자가 못 알아채는"
   유형이라 위험도가 큼)
2. M1(컨디션-rename 연동), M2(복구 실패 오보고), M4(친구공유 스테일 데이터)는
   비교적 작은 수정으로 큰 신뢰성 개선
3. LOW 항목들은 급하지 않지만 한 번에 몰아서 처리하기 좋은 목록
