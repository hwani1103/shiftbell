# 친구 공유 기능 v1 스펙 (2026-08-12 확정)

베타가 아니라 정식 v1 스코프로 재정의한 문서. 이전에 만들어둔 "트랙1/트랙2" 초안(스냅샷 코드 방식)에서
Firestore 연동으로 전환하기로 하면서 기능 범위도 같이 좁힘. 다른 대화 세션에서 이 기능을 다시 만질 때
이 문서부터 읽을 것.

## 배경 / 결정 사항

- 기존 구현(commit `b674c73`)은 코드/링크 안에 스케줄 전체를 base64로 박제하는 "서버 없는 스냅샷" 방식이었음.
  → 문제: 친구가 링크를 다시 열거나 새로고침해도 내가 바꾼 근무일정이 반영 안 됨(코드/링크 자체가 그 순간
  데이터 그 자체라서). 이 문제를 풀려면 서버/저장소가 구조적으로 반드시 필요함.
- **Firestore 무료(Spark) 티어로 충분함**: 하루 읽기 5만/쓰기 2만/삭제 2만 회, 저장 1GiB, 카드 등록 불필요.
  실시간 스트리밍이 아니라 "열 때/새로고침할 때 1회 fetch"라서 개인 앱 규모에서는 사실상 무기한 무료.
  Firebase Hosting도 같은 Spark 플랜으로 커버됨.
- **메모 기능은 이번 스코프에서 완전히 제거** (예전엔 옵션 토글이었음 — 이제 아예 코드/UI에서 안 만듦).
- 스코프에 넣지 않는 것: OT, 근로시간, 알람 설정, (달력) 테마. 테마를 스냅샷으로 고정할지 나중에 변동
  가능하게 할지는 Firestore 데이터 구조 잡을 때 재검토 — **아직 미결정, 보류 항목으로만 기록**.
- 핵심은 오직 "근무 스케줄 그 자체" (패턴/근무변경/근무별 색상).

## 기능 스펙

### 기능 1 — 앱 미사용 친구에게 웹으로 공유
- 설정 → 친구 공유 탭 → "웹으로 공유하기" 버튼 → OS 공유시트(카톡/SMS/기타)로 웹 링크 전달
- 링크는 Firestore 문서 ID를 담을 뿐, 데이터를 담지 않음 → **영구 링크**. 친구가 언제 열어도 그 시점
  최신 근무 스케줄이 자동 반영됨 (매 로드마다 Firestore에서 fetch)
- 공유되는 데이터: 근무 패턴 / 근무변경(assignedDates) / 근무별 색상. **메모·OT·알람·테마는 아예 안 보냄**
- PWA: 홈 화면에 저장 가능해야 하고, 홈 화면 아이콘으로 재실행해도 정상적으로 내 스케줄이 다시 떠야 함
  (기존 `manifest.json`의 `start_url`이 코드 없는 루트라 재실행 시 깨지는 버그 있음 → 수정 필요)
- 웹페이지 하단 "앱 설치" 버튼: 플레이스토어 페이지로 이동만 함 (기능 3 참고, 그 이상 없음)

### 기능 2 — 앱 사용자끼리 코드로 공유
- 기능 1과 데이터/의미 전부 동일. 차이는 전달 수단뿐: 링크 대신 코드 텍스트를 직접 교환 → 서로 자기
  앱의 "친구 추가"에 붙여넣음
- 코드도 기능 1의 링크와 동일하게 Firestore 문서 ID만 담음 (전체 데이터 X)
- 친구 목록에서 열람 시 최신 데이터를 fetch (실시간 스트리밍 아님, 요청 시점 기준 최신 — "새로고침"
  버튼 또는 화면 진입 시 자동 재조회)

### 기능 3 — 웹 → 앱 설치 유도 (딱 여기까지만)
- PWA 지원 + "앱 설치" 버튼으로 플레이스토어 이동, 이게 전부
- **범위 밖(안 만듦)**: 새로 설치한 앱에 내 스케줄을 자동으로 심는 기능(딥링크/App Links 자동 이식).
  필요해지면 그때 "코드로 복원" 같은 별도 기능으로 풀기로 결정함 — 이것도 아직 안 만들어져 있음

## 코드 레벨 작업 목록 (해야 할 일)

1. **Firebase SDK 연동**: `firebase_core`, `cloud_firestore` 패키지 추가. `flutterfire configure`로
   Android + Web 플랫폼용 `firebase_options.dart` 생성. `main.dart`/`web_main.dart` 양쪽에
   `Firebase.initializeApp()` 추가
2. **Firestore 데이터 모델 설계**: `friend_schedules/{ownerId}` 문서 — `pattern`, `todayIndex`,
   `startDate`, `shiftColors`, `assignedDates`, `updatedAt`만 (메모/OT/알람/테마 필드 없음).
   `ownerId` 발급 방식 결정 필요(기기별 로컬 UUID 생성 + SharedPreferences 저장 등)
3. **Firestore 보안 규칙 작성**: 본인 문서만 write 가능, 문서 ID를 아는 사람만 read 가능하되 전체
   목록(list)은 막기 (문서 ID가 사실상 비밀키 역할을 하게 설계)
4. **"내 스케줄 쓰기" 로직**: 스케줄 변경 시(근무변경 등) Firestore 문서 upsert. 최초 "공유 시작" 시
   문서 생성 + ownerId 발급. 최소한의 오프라인/실패 처리
5. **코드/링크 포맷 축소**: `FriendShareService`를 "전체 JSON base64" → "문서 ID(+검증용 접두어)만
   인코딩"으로 변경. `FriendScheduleData`에서 `memos` 필드 및 관련 로직 전부 제거
6. **읽기(fetch) 로직 교체**: `friend_provider.dart` — 로컬 스냅샷 캐시 대신 Firestore fetch,
   친구 목록에 "새로고침" 동작 추가. `web_main.dart` — 로드 시 문서 ID로 Firestore fetch, 삭제된
   문서/오프라인 등 에러 상태 처리
7. **PWA 재실행 버그 수정**: `manifest.json`의 `start_url` 문제. Firestore 전환하면 오히려 쉬워짐 —
   마지막으로 성공 조회한 friendId를 localStorage에 저장해두고, URL에 파라미터가 없을 때 그걸로 폴백
8. **메모 UI 완전 삭제**: `my_share_code_screen.dart`의 "메모도 공유" 토글/안내 문구, `getMemosForDateRange`
   호출부, `friend_calendar_view.dart`의 메모 렌더링 블록 전부 제거
9. **레이아웃 버그 수정**: 짧은 뷰포트에서 설치 유도 배너가 달력 하단 행과 겹치는 문제
   (`friend_calendar_view.dart`, 이전 리뷰에서 발견함)
10. **`DatabaseHelper.kt` 버전 수정 커밋**: 이미 워킹트리에 수정은 돼 있음(`DATABASE_VERSION` 15→16,
    친구공유 v16 마이그레이션 때 위젯이 깨지던 버그) — **아직 커밋 안 됨, 다른 변경사항과 같이 커밋할 것**

## 사용자가 직접 해야 할 일

1. Firebase 콘솔에서 프로젝트 생성 (Spark 무료 플랜으로 충분, 카드 등록 불필요)
2. Firestore 활성화 (Native 모드) + 저장소 루트의 `firestore.rules` 내용을 콘솔 규칙
   탭에 붙여넣고 배포
3. Authentication → Sign-in method에서 **"Anonymous" 로그인 방법 켜기** (무료, Spark
   포함) — ownerId를 로컬 UUID가 아니라 익명 인증 uid로 바꿔서 "본인 문서만 write
   가능"을 실제로 강제하도록 설계했음(아래 2026-08-13 갱신 참고). 이걸 안 켜면
   공유 시작 자체가 실패함.
4. Firebase CLI 설치/로그인 → `flutterfire configure` 실행 (로컬 터미널 인증 필요 — 이 단계는 같이
   진행 가능) → `lib/firebase_options.dart`의 플레이스홀더 값이 실제 값으로 교체됨
5. Firebase Hosting에 `build/web` 배포 → 실제 도메인 발급
6. `my_share_code_screen.dart`의 `kWebViewBaseUrl`을 실제 배포 도메인으로 교체
7. `kPlayStoreUrl`은 이미 실제 패키지 ID로 채워져 있어 추가 작업 불필요
8. 폰 재연결되면: Firestore 연동 후 실제 기기 간 왕복(내 스케줄 변경 → 친구 쪽에 반영되는지) 테스트

## 2026-08-14 갱신 — 4~8번 전부 진행, Track2(앱 내) 검증 완료 / Track1(웹뷰어) 버그 발견

1~3번(프로젝트 생성 `shiftbell-29f31`, Firestore 규칙 배포, 익명 인증 켜기)은 사용자가 이미
완료해둔 상태에서 이어서 진행:

- **4번**: `flutterfire configure` 실행 완료. 전역 `flutterfire_cli`가 낡아서(0.2.7) 먼저
  `dart pub global activate flutterfire_cli`로 재설치(→1.4.1) 필요했음. `lib/firebase_options.dart`
  실제 값으로 교체됨. **부수 발견**: 이 명령이 `google-services` Gradle 플러그인을 자동으로
  추가하는데, prod 패키지명(`com.hwani1103.shiftbell`)에만 매칭돼서 dev 플레이버
  (`.dev` 접미사) 빌드가 깨짐 — 이 프로젝트는 애초에 그 네이티브 플러그인이 필요 없는
  구조(Dart 쪽 `FirebaseOptions`만으로 충분)라 되돌림(`android/app/build.gradle.kts`,
  `android/settings.gradle.kts`, `google-services.json` 삭제 + `.gitignore` 등록).
- **5~6번**: Firebase Hosting에 `build/web` 배포 완료, 실제 도메인
  `https://shiftbell-29f31.web.app` 발급됨. `kWebViewBaseUrl`을 그 도메인으로 교체.
- **7번**: 원래 스펙대로 추가 작업 불필요 확인.
- **8번 - Track2(앱 내 친구보기)**: **완전 검증 완료.** 폰에 dev 빌드 설치 →
  온보딩(근무 패턴 생성) → "공유 시작하기" → 실제 익명 인증(uid 발급) + Firestore
  문서 생성까지 실기기에서 확인. 별도 익명 계정으로 만든 테스트 문서를 "친구 추가"로
  등록 → 실제 Firestore에서 fetch해서 달력에 색상/패턴/공휴일까지 정상 렌더링되는 것
  확인. REST로 보안 규칙 4가지(본인 write 성공/무인증 read 성공/타인 문서 write 거부/
  list 거부)도 재검증함.
- **8번 - Track1(웹뷰어) - 미해결 버그 발견**: 웹뷰어(`https://shiftbell-29f31.web.app/#/?code=...`)를
  열면 `Firebase.initializeApp()`이 `PlatformException(channel-error, ...
  FirebaseCoreHostApi.initializeCore)`로 매번 실패해서 "불러올 수 없어요" 화면만 뜸.
  **원인 특정 실패** - 여러 각도로 좁혀봤지만 결론에 도달 못함:
  - `firebase_core`/`cloud_firestore`/`firebase_auth`를 3.x/5.x대에서 4.13.0/6.8.0/6.5.7로
    메이저 업그레이드해봤지만 동일 증상 (원인 아님).
  - `flutter clean` 후 재빌드해서 캐시된 플러그인 registrant 문제도 배제함(정상적으로
    `FirebaseCoreWeb.registerWith`가 포함된 걸 소스 레벨로 확인).
  - **핵심 재현 조건**: 완전히 동일한(byte-identical) `build/web` 산출물을 로컬
    (`python -m http.server`)로 띄우면 100% 정상 동작(Firestore fetch까지 성공) —
    실제 배포 도메인(`https://shiftbell-29f31.web.app`, Firebase Hosting)에서 열 때만
    100% 재현되는 실패. 즉 코드 문제가 아니라 "그 특정 호스팅 오리진에서 서빙됨" 자체가
    트리거 — CSP/보안 헤더도 직접 diff했지만 특이사항 없었고, gstatic.com에서 수동
    dynamic import는 같은 페이지에서 정상 동작해서 네트워크 차단도 아님. 서비스워커도
    등록 안 돼있어서 그것도 아님.
  - `firebase_bootstrap.dart`의 `initFirebase()`에 3회 재시도(지수 백오프)를 방어적으로
    추가했지만 이 특정 실패는 매번 재현되는 결정론적 실패라 재시도로도 해결 안 됨
    (그래도 일반적으로 안전한 개선이라 남겨둠).
  - **정정 (같은 날)**: 사용자가 실제 폰의 진짜 모바일 브라우저로 같은 링크를 열어보니
    "TestUser님의 달력입니다"로 정상 렌더링됨 + 앱 설치 바로가기 + PWA 홈 화면 추가까지
    전부 정상 동작 확인. 즉 **위 실패는 claude-in-chrome 브라우저 자동화 도구 환경에서만
    재현되는 문제였고, 실제 방문자에게는 아무 문제 없음.** 정확한 원인(그 확장 프로그램이
    firebase_core_web의 동적 스크립트 주입을 방해했을 가능성이 유력하지만 확정은 아님)은
    끝내 못 밝혔지만, 더 이상 추적할 필요 없음 - Track1은 Track2와 마찬가지로 정상 작동.
    다음에 claude-in-chrome으로 페이지 로드/JS 초기화 실패가 보이는데 로컬 재현이
    안 되면, 실기기로 먼저 확인하고 나서 진짜 버그로 취급할 것 (자동화 도구 자체가
    재현 원인일 수 있음).

## 2026-08-13 갱신 — 코드 구현 완료

위 "코드 레벨 작업 목록" 1~9번 전부 구현 완료(10번 DB 버전 수정은 이미 이전 커밋에 포함돼
있었음). 실제로 구현하면서 스펙 대비 확정/변경된 부분:

- **메모는 계획대로 완전히 제거** (모델/UI/코드 전부 — 토글 자체가 없음).
- **ownerId 발급 방식**: 로컬 UUID가 아니라 **Firebase 익명 인증(Anonymous Auth) uid**로
  결정. 이유 — 문서 ID(=코드/링크에 실리는 값)를 그대로 write 권한의 열쇠로도 쓰면, 코드를
  받은 친구도 read만 하라고 준 그 값을 알기 때문에 이론상 같은 문서에 write도 시도할 수
  있는 구멍이 생김(스펙의 "본인 문서만 write 가능" 요구를 로컬 UUID만으로는 지킬 수 없음).
  uid를 문서 ID로 쓰고 `request.auth.uid == ownerId`로 write를 검증하면 read용 열쇠(문서 ID)와
  write용 열쇠(uid)가 실제로 분리됨 — `firestore.rules` 참고. 로그인 UI는 없음(완전히 백그라운드,
  최초 "공유 시작" 시 1회 익명 로그인).
- **코드 포맷**: `SB1:`(구, 전체 스냅샷) → `SB2:<ownerId>`. 구 코드는 더 이상 디코딩 안 됨.
- **UX**: "코드를 누를 때마다 생성" 대신 "공유 시작 → (이후 자동 동기화) → 공유 중지"로
  단순화 — 코드/링크가 영구 주소라 매번 다시 만들 필요가 없어졌기 때문. `schedule_provider.dart`의
  스케줄 저장 지점 3곳(`saveSchedule`/`updateSchedule`/`changeShiftWithAlarms`)에서 공유 중일
  때만 자동으로 Firestore upsert함.
- **friends 테이블(DB v17)**: 스냅샷을 통째로 저장하던 구조에서 `owner_id` + nullable
  `data_json`(오프라인 대비 캐시)로 재설계. 베타 단계라 구 데이터는 마이그레이션 없이 버림.
- `friend_calendar_view.dart`의 rowHeight 고정값(83.h) 문제(짧은 뷰포트에서 설치 배너와
  겹치던 버그)는 `LayoutBuilder`로 남는 높이 ÷ 6을 매번 계산하는 방식으로 근본 수정.
- `firebase_core`/`cloud_firestore`/`firebase_auth`는 pubspec에 추가돼 있지만
  `lib/firebase_options.dart`는 아직 플레이스홀더 — `flutterfire configure` 전까지는
  `Firebase.initializeApp()`이 조용히 실패하고(`firebase_bootstrap.dart`) 친구공유 기능만
  비활성 상태로 남음. 알람 등 앱의 나머지 기능은 지금 이 상태로도 정상 빌드/동작함
  (`flutter analyze`/`flutter build web` 확인 완료, 폰 미연결이라 실기기 왕복 테스트는 미완료).
