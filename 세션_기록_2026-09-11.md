# 세션 작업 기록 — 2026-09-11 (dev 브랜치, 전부 미커밋)

> 이 문서는 그날 세션에서 한 작업과 다음 세션에 이어갈 것을 사람이 읽을 수 있게
> 남겨둔 기록입니다. Claude Code의 영구 메모리에도 같은 내용이 더 상세히 저장돼
> 있지만(`project_2026_09_11_ux_fixes_batch` 등), 이 파일은 저장소 안에서 사람이
> 바로 찾아볼 수 있도록 둔 것입니다.

## 1. 한 번에 처리한 7개 요청 (+ 후속 2개)

전부 `dev` 브랜치에 코드로 구현 완료, **아직 커밋 안 됨**(`git status`로 확인).
`flutter analyze`/`flutter test`(91개 통과)/Kotlin `compileDevDebugKotlin` 전부 통과했지만
**실기기 테스트는 안 함**.

1. 컨디션 탭 최초 진입 안내 팝업 (`onboarding_info_popups.dart`)
2. 수면 자동감지 오탐 완화("거부 학습") — 상세는 아래 3번 섹션
3. 자동 백업 문제 — 상세는 아래 2번 섹션(**아직 미해결로 취급할 것**)
4. 친구 달력 전환 애니메이션 즉시 표시로 변경 (`friend_open_util.dart`)
5. Firestore 비용 점검(결론: 문제 없음, 업데이트 체크 쿨다운만 30분→6시간으로 절약)
6. 달력 6번째 줄 버튼/범례 영역 좌우 스와이프로 월 이동 지원 (`calendar_tab.dart`)
7. AdMob 배너 크기 - 정상 동작 확인(수정 없음)
8. 근무시간 설정 - 퇴근시간 자동 채움 버그 수정 (`work_hours_settings_screen.dart`)
9. 일정관리 - "소요시간 없는 일정" 재생성 불가 버그 수정 (`schedule_management_tab.dart`)

## 2. 자동 백업 — 재점검했지만 100% 확정은 아님

**증상**: 폴더(`Download/ShiftBell/`)에 마지막 백업 시점 파일 하나만 있고 그 뒤로 안 쌓임.
수동 백업은 정상.

**가장 유력한 원인(수정함)**: dev/prod 두 앱이 완전히 같은 파일명 규칙(`ShiftBell_Backup_
YYMMDD.json`)을 같은 폴더에 써서, 개발자가 두 앱을 한 기기에서 같이 쓰는 지금 환경에서
충돌 가능성이 있었음. `MainActivity.kt`의 파일명 접두어에 `packageName`을 넣어 분리함.

**단, 코드 리뷰만으로 100% 확정된 원인은 아닙니다.** `backupNow()`/`writeBackupFile()`
전 분기에 진단 로그를 심어뒀으니, 새 빌드로 며칠 써보고도 안 되면 **폰을 USB로 연결해서
`adb logcat`으로 로그를 직접 확인**하는 게 다음 단계입니다.

## 3. 수면 자동감지 오탐 완화

"거부 학습" 구현 완료(확인 카드에서 2회 이상 거부된 시간대는 자동 감지 스킵). **Activity
Recognition(동작 감지 센서) 추가는 검토 후 사용자가 "현재 방식 유지"로 결정 — 다시 꺼내지
말 것.**

## 4. "교대시계 관리자" 앱(DAU/MAU) — 단계적으로 진행하기로 함

원래 제안한 전체 구조(Cloud Function + Firestore + 별도 관리자 앱)는 **일단 보류**.
**1단계로 본 앱에 Firebase Analytics만 추가**하고, GA4 웹 콘솔로 직접 확인하는 것부터
시작하기로 사용자와 합의함.

**현재 막힌 지점** (다음 세션에서 이어갈 것):
- Play Console → 앱 콘텐츠 → 데이터 보안 설문에서, 최상위 "개인정보 수집하나요?" 질문이
  "아니오"로 돼 있어 세부 항목이 안 보이는 상태 → "예"로 바꾸라고 안내함, 확인 필요
- 개인정보처리방침 외부 URL이 어디 호스팅돼 있는지 아직 확인 못 함(질문했으나 미응답)
- Claude in Chrome 브라우저 확장이 연결 안 되는 문제 — 재시도 필요("Browser extension is
  not connected" 에러, claude.ai 로그인/확장 활성화 확인 요청함)

상세 기술 검토(GA4 Data API 지표, Blaze 플랜 필요성, Firestore 규칙 설계 등)는 대화
히스토리와 Claude 메모리(`project_admin_analytics_plan`)에 남아있음.
