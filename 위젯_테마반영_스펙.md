# 홈 화면 위젯 - 달력 테마 반영 스펙 (작업 #31, **2026-08-13 계획 철회됨**)

> ⚠️ **이 문서 전체가 철회된 계획의 기록입니다.** 아래에 9개 테마를 위젯에도 반영하려고
> 실제로 구현까지 했던 두 라운드의 작업이 남아있지만, 사용자가 최종적으로 "위젯은 테마
> 적용 안 하는 걸로 하자, 무조건 메인·화이트 느낌으로 고정" + "메인·다크도 필요 없다"고
> 결정하면서 코드 레벨에서 전부 되돌렸음. **현재 실제 상태**: `CalendarWidgetProvider.kt`는
> 다크모드 분기(`isDarkMode()`)도 없이 `LIGHT_*` 상수 하나로 완전히 고정 렌더링함 - 이전에
> 있던 라이트/다크 2갈래보다도 더 단순화된 상태(다크 지원 자체가 없어짐). `CalendarWidgetTheme.
> kt`(9테마 리졸버), `setWidgetTheme`/`setThemeOverride`/`pushTheme` MethodChannel 경로,
> 테마별 drawable(`widget_panel_materialcard.xml` 등 3개), `widget_today_soft_yellow.xml`
> 전부 삭제/제거됨. `calendar_widget.xml`/`calendar_widget_full.xml`의 `layout_gravity=
> "center"` 추가(CIRCLE 뱃지용)도 되돌림. **다른 세션에서 이 작업을 다시 꺼내려면** 아래
> 내용을 재구현이 아니라 "이런 시도를 했었고 이런 이유로 접었다"는 참고 자료로만 볼 것 -
> 사용자가 다시 요청하기 전까지는 손대지 말 것.

## (아래는 철회 전 원래 스펙 - 역사적 기록)

`calendar_theme.dart`에서 9개 달력 테마 시스템을 만들면서 앱 안(달력 탭)에는 전부 반영했지만,
네이티브 홈 화면 위젯(`CalendarWidgetProvider.kt`)은 아직 손대지 않음 — 지금은 라이트/다크
2갈래(`isDarkMode()`)로만 그림. 다른 대화 세션에서 이 작업을 다시 만질 때 이 문서부터 읽을 것.

## 배경 / 현재 상태

- 위젯은 이미 "작은 위젯"(`calendar_widget.xml`, 이번 주 한 줄) / "큰 위젯"
  (`calendar_widget_full.xml`, 달력 월 전체 6주)로 자동 전환되는 구조가 있음
  (`CalendarWidgetProvider.buildRemoteViews()`의 `isLarge` 분기, 위젯 리사이즈 시
  `isLargeMode(manager, id)`로 매번 재판단).
- 색상/배경은 현재 `isDarkMode(context)` 하나로만 라이트/다크 리소스(`widget_panel_light.xml` vs
  `widget_panel_dark.xml` 등)를 스왑함. 테마 9개 중 "메인·화이트/메인·다크" 2개에 대응하는
  수준이고, 나머지 7개 테마(심플 라인/소프트 카드/다크 그리드/컬러 뱃지/미니멀 언더라인/
  이벤트 캘린더/매거진)는 위젯에 전혀 반영 안 됨.
- 앱 쪽 근무색 배정은 `lib/models/calendar_theme.dart`의 `assignShiftColorsForTheme()` +
  테마별 팔레트(`kMainLightPalette`/`kMainDarkPalette`/`kVividPalette`/`kBoldGridPalette`)로
  이미 정리돼 있음. 위젯도 같은 로직/같은 결과색을 써야 함 (지금처럼 네이티브에서 독자적인
  색상 규칙을 새로 만들면 앱-위젯 간 색이 어긋남).
- `CalendarThemeId`에 이미 `hasLegend` getter가 있음 — 근무를 "색상으로만" 구분해서 범례가
  필수인 테마만 `true` (현재 `underline`/`editorial` 2개). 이 값을 위젯 쪽 분기에도 그대로
  재사용하면 됨(별도로 다시 정의하지 말 것).

## 요구사항 (2026-08-13 확정)

### 1. 범례가 필요한 테마(`hasLegend == true`: 미니멀 언더라인, 매거진) — 위젯 특화 처리

- **작은 위젯**: 범례를 보여주지 않는다. 대신 필요하다면(공간이 남으면) 다음 달 날짜까지
  이어서 보여주는, 범례가 필요 없는 다른 테마들의 기존 방식을 그대로 따른다. 즉 작은
  위젯에서는 이 두 테마도 "범례 없는 일반 테마"와 동일하게 취급.
- **큰 위젯**: 앱 실제 달력 탭과 동일하게, 6번째 줄에서 다음 달 날짜로 넘어가는 빈 칸
  자리에 범례를 넣는다 (`calendar_tab.dart`의 `_theme8Legend`/`_theme10Legend`가 이미
  하고 있는 방식 — 6번째 줄 중 다음 달로 스필오버되는 칸들을 범례 위젯 하나로 치환).
  큰 위젯은 6주 전체가 다 보이니 이 자리가 항상 확보됨.

### 2. 범례가 필요 없는 나머지 7개 테마 — 위젯도 테마를 따라간다

- 심플 라인 / 소프트 카드 / 다크 그리드 / 컬러 뱃지 / 이벤트 캘린더 + 메인·화이트 / 메인·다크
  전부, 선택된 테마의 배경/그리드선/글자색/뱃지 스타일에 맞춰 위젯도 함께 바뀌어야 함.
  지금처럼 "라이트/다크"만 있는 게 아니라 실제 선택된 `CalendarThemeId` 값을 위젯이 읽어서
  그에 맞는 리소스를 그려야 함.
- 작은 위젯이든 큰 위젯이든 이 7개는 특별 취급 없이, 각 테마의 실제 디자인(그리드 유무,
  구분선 스타일, 뱃지/칩 모양 등)을 위젯 레이아웃 안에서 최대한 그대로 재현한다.

## 코드 레벨 작업 목록 (아직 아무것도 안 함)

1. **선택된 테마 값을 네이티브로 전달**: 현재 `CalendarWidgetProvider.setThemeOverride()`는
   `isDark: Boolean` 하나만 SharedPreferences(`KEY_IS_DARK`)에 저장함. `CalendarThemeId.name`
   (문자열)을 그대로 저장하도록 확장 필요 — Flutter 쪽 `calendar_theme_provider.dart`가 테마
   변경될 때마다 MethodChannel로 네이티브에 알려주는 지점을 찾아서 같이 실어 보내야 함.
2. **네이티브에도 테마별 팔레트 이식**: `kMainLightPalette`/`kMainDarkPalette`/
   `kVividPalette`/`kBoldGridPalette` + 로테이션 규칙(`assignShiftColorsForTheme`)을
   Kotlin 쪽에 복제하거나(중복 유지보수 리스크 있음), 혹은 Flutter가 계산한 최종
   "근무명→색상" 맵을 앱 열 때/테마 바뀔 때 네이티브 DB(`shift_colors` 컬럼, 이미 존재)에
   저장해두고 위젯은 그 값을 읽기만 하도록 하는 게 나을 수 있음 — 방식 선택 필요.
3. **테마별 위젯 레이아웃/리소스 분기**: `buildRemoteViews()` 안의 `layoutRes`/색상/배경
   드로어블 선택 로직을 `isDark` 2갈래에서 `CalendarThemeId` 9갈래(또는 "범례 필요
   2개 vs 나머지 7개"로 우선 단순화)로 확장. 필요한 만큼 `widget_panel_*.xml` 류 드로어블을
   테마별로 추가.
4. **범례 위젯(큰 위젯 전용)**: `calendar_widget_full.xml`에 6번째 줄 다음달-스필오버 칸을
   범례로 치환하는 대체 레이아웃(또는 `RemoteViews.setViewVisibility`로 평소엔 숨겨둔
   범례 뷰를 그 자리에 노출) 추가. 앱의 `_theme8Legend`/`_theme10Legend` 배치를 참고.
5. **작은 위젯의 "다음 달 날짜 이어 보여주기" 로직 재사용 확인**: 이미 범례 없는 테마들이
   작은 위젯에서 다음 달 날짜를 어떻게 채우고 있는지(`CalendarWidgetScheduleResolver.kt`
   쪽 로직으로 추정) 먼저 파악하고, `hasLegend` 테마도 작은 위젯에서는 그 경로를 그대로
   타게만 만들면 됨(별도 로직 새로 안 만들어도 될 가능성 높음).
6. **테스트**: 9개 테마 × (작은 위젯/큰 위젯) × (범례 유무) 조합을 실기기에서 위젯 리사이즈해가며
   확인. 특히 미니멀 언더라인/매거진의 작은↔큰 전환 시 범례가 나타났다 사라지는 게 자연스러운지.

## 우선순위

이전 세션에서 매번 "네이티브 Kotlin 작업이라 이번 라운드는 보류"로 미뤄져 온 항목. 커스텀 알람
시스템 등 다른 HIGH 우선순위 작업과 별도로, 사용자가 명시적으로 요청할 때 별도 세션으로
진행하기로 함.

## 2026-08-13 갱신 — 구현 완료 (실기기 미검증)

폰 미연결 상태로 진행 - 코드는 전부 작성/컴파일 확인했지만 실기기 리사이즈/스크린샷 검증은
못 함(다음 세션에서 폰 연결 시 최우선으로 확인할 것).

- **테마 전달**: `calendar_theme_provider.dart`의 `setTheme()` → `WidgetRefreshService.
  pushTheme(CalendarThemeId)` → MethodChannel `setWidgetTheme`(`isDark` + `themeId` 둘 다 전달)
  → `CalendarWidgetProvider.setThemeOverride()`가 `SharedPreferences`에 `calendar_theme_id`
  (enum.name 문자열)로 저장. `resolveThemeId()`가 미저장 시 `isDarkMode()`로 mainWhite/
  mainDark로 폴백 - "완전히 모르는 테마" 상태 자체가 없음.
- **새 파일 `CalendarWidgetTheme.kt`**: 9개 themeId → `Spec`(배경 drawable, 5가지 텍스트
  색상, 오늘강조 drawable 2종 + 글자색, 뱃지 모양, 범례 여부) 매핑 테이블. 근무별 "색상"은
  여기서 다루지 않음 - 스펙 문서 #2가 정리한 대로 이미 DB(`shift_colors`)에 앱이 최종
  계산값을 써주는 기존 방식을 그대로 사용(팔레트/로테이션을 Kotlin에 다시 복제하지 않음).
- **뱃지 모양은 3종으로 단순화**: `PILL`(둥근 알약 - mainWhite/mainDark/materialCard/
  eventChip), `BAR`(모서리 없는 막대 - minimal/boldGrid), `COLOR_ONLY`(텍스트 없이 얇은
  중앙 바 - underline/editorial). 원래 구상했던 완전한 원(이니셜 뱃지형용)은 RemoteViews의
  ImageView가 셀 너비만큼 늘어나는 구조라 항상 타원으로 찌그러져서(scaleType을 동적으로
  못 바꿈) 뺐고, 대신 `initialBadge`는 `BadgeTextMode.INITIAL`로 "근무명 전체가 아니라
  첫 글자만" 보여줘서 구분함 - 완전한 원보다 훨씬 안전하고, 실제로도 눈에 띄게 다르게 보임.
- **배경/오늘강조**: 대부분 테마는 기존 라이트/다크 패널·오늘강조 drawable을 재사용하고,
  소프트카드/다크그리드 전용으로 `widget_today_soft_yellow.xml`(앱의 해당 테마 오늘-셀
  배경색과 동일 톤) 1개, 패널 배경은 소프트카드/이벤트캘린더/매거진 3개(`widget_panel_
  materialcard.xml`/`widget_panel_eventchip.xml`/`widget_panel_editorial.xml`)만 새로
  추가 - 나머지는 기존 `widget_panel_light/dark.xml` 재사용. 그리드선(칸 구분선)은 이번
  범위에서 뺌 - 42칸 각각에 테두리 ID를 새로 부여해야 해서(레이아웃 XML을 다시 대규모로
  건드려야 함) 배경/글자색/뱃지모양 대비 화면 임팩트가 작다고 판단, 스펙 문서의 "최대한
  그대로 재현" 원칙에서 의도적으로 낮은 우선순위로 뺀 부분 - 필요해지면 후속 작업으로.
- **요구사항 1번(범례)**: `hasLegend`(underline/editorial) + 큰 위젯일 때만, 6번째 줄
  화~토(col 2~6) 자리를 범례로 치환. 새 뷰를 추가하는 대신 이미 있는 셀 뷰(뱃지 자리 1개 +
  메모 자리 3개 = 컬럼당 4슬롯 × 5컬럼 = 최대 20슬롯, 근무 종류는 최대 12개까지라 항상
  충분)를 재활용 - `CalendarWidgetProvider.renderLegend()`. 작은 위젯은 이 분기 자체를
  안 타서 요구사항이 말한 "범례 없는 일반 테마와 동일 취급"이 자동으로 만족됨(별도 분기
  코드 없음). 범례 순서는 `schedule.shiftTypesOrdered`(신규 필드 - `shift_schedule.
  shift_types` 컬럼을 그대로 split, Dart의 `schedule.shiftTypes`와 동일 순서) 기준.
- **작은 위젯의 "다음 달 이어 보여주기"**: 확인 결과 원래부터 모든 테마가 "이번 주 기준
  ±1주 롤링 윈도우"라 월 경계 자체를 안 따짐(이번 달이 아닌 날짜인지 아닌지만 글자 톤으로
  구분) - 요구사항 5번이 우려한 "레거시 로직 분기"가 애초에 없었고, 그래서 손댈 코드도 없었음.
- **검증**: `flutter analyze`/`gradlew compileDebugKotlin`으로 컴파일만 확인(아래 "빌드 검증"
  섹션 참고). 9테마 × (작은/큰) × (범례 유무) 실기기 리사이즈 확인은 폰 재연결 후 최우선 진행.

## 2026-08-13 재작업 — 범례/뱃지 모양을 달력탭 실제 디자인에 맞춤 (실기기 설치 후 피드백 반영)

폰 연결 후 실기기에 설치하고 코드 리뷰하다가, 그리고 사용자 피드백으로 두 라운드에 걸쳐 고침.

**1차 - 실기기 설치 전 자체 리뷰로 발견한 버그**: 범례 뱃지 비트맵 캐시가 색상 하나로만 키가
잡혀 있어서, 같은 색이 일반 날짜 셀(hasLegend 테마는 COLOR_ONLY 모양)과 범례(항상 PILL 고정)에
둘 다 쓰이면 먼저 그려진 모양이 캐시를 선점 - 범례가 가는 바 모양으로 잘못 나오는 버그였음.
캐시 키를 `(모양, 색상)` 쌍으로 바꿔서 수정(`CalendarWidgetProvider.pillBitmapCache`).

**2차 - 사용자 피드백("범례도 획일화된 네모다, 달력탭 실제 디자인이랑 똑같이 만들어")로 전면
재작업**:
- **범례**: 색칠된 배경 박스 방식을 완전히 버림. 대신 근무명 앞에 그 테마의 실제 범례 스와치
  모양을 닮은 도형 글자를 붙이고 `ForegroundColorSpan`으로 그 글자에만 근무색을 입힘
  (언더라인="▬ 야간", 매거진="▲ 야간") - 별도 비트맵/캐시 없이 `SpannableString` 하나로 끝남.
  달력탭의 "작은 색 스와치 + 근무명 나란히" 배치(`_buildThemedLegend`)를 텍스트 한 줄로
  재현한 것 - RemoteViews로 진짜 Row(스와치+텍스트 나란히) 레이아웃을 만들 방법이 없어서
  찾은 대안.
- **뱃지 모양 3종 → 6종으로 세분화**: `PILL`(메인/이벤트캘린더), `BAR`(미니멀/다크그리드),
  `CHIP`(소프트카드/이벤트캘린더 - 작은 반경+좌우 여백, `_theme2Cell`의 margin:4w/radius:3r을
  반영), `CIRCLE`(컬러뱃지 - 진짜 원), `TRIANGLE`(매거진 - 좌상단 삼각형, `_theme10Cell`의
  `_CalendarTriangleClipper` 반영), `COLOR_ONLY`(언더라인 - 가는 중앙 바). `CalendarWidgetTheme.
  badgeBitmap()`이 shape별로 다른 Canvas 경로를 그림.
- **CIRCLE은 API 31(Android 12)+ 전용으로 진짜 원**: RemoteViews의 ImageView가 기본적으로
  셀 너비(match_parent)만큼 늘어나서 원이 항상 타원으로 찌그러지는 문제가 있었음 -
  `RemoteViews.setViewLayoutWidth/Height(id, 14f, COMPLEX_UNIT_DIP)`로 뷰 자체를 14dp
  정사각형으로 동적 리사이즈(이 API가 API31+ 전용이라 `Build.VERSION.SDK_INT` 가드 필수 - 그
  미만 기기는 자동으로 이전처럼 넓게 그려짐, 완전한 원이 아닐 뿐 깨지진 않음). 정사각형으로
  줄어든 뷰가 부모 FrameLayout 안에서 실제로 중앙에 오려면 `layout_gravity="center"`가
  필요한데, 기존 XML의 `pillbg_r_c` 42×2개 선언 전부에 이게 없었어서 두 레이아웃 파일 전체에
  스크립트로 일괄 추가함(`calendar_widget.xml` 21개, `calendar_widget_full.xml` 42개 -
  width/height가 그대로 `match_parent`인 다른 8개 테마는 gravity=center가 있어도 시각적으로
  아무 차이 없어서 안전한 전역 추가였음).
- **컬러뱃지 텍스트 모드는 그대로 유지**: 완전한 원 + 이니셜 한 글자 조합이 이제 API31+에서
  실제로 달력탭(`_theme5Cell`)과 거의 동일하게 보임.
- **여전히 못 맞춘 부분(다음 라운드 후보)**: 셀 자체의 카드 테두리/그림자(소프트카드), 매거진의
  Stack 기반 자유 배치(숫자 우상단 고정, 스와치가 셀 좌상단 모서리에 걸침) 등 "뱃지 모양"을
  넘어서는 셀 전체 레이아웃/크롬은 아직 그대로임 - 지금 위젯은 42칸 전부가 고정된 세로 스택
  구조(뱃지→숫자→메모)라, 테마마다 완전히 다른 배치를 쓰려면 각 셀에 개별 컨테이너 배경/보더
  리소스를 부여하는 더 큰 작업이 필요함. 사용자에게 명시적으로 공유해둔 한계.
- **검증**: `gradlew :app:compileDevDebugKotlin` BUILD SUCCESSFUL, `flutter build apk --debug
  --flavor dev` 성공, 실기기(`com.hwani1103.shiftbell.dev`)에 설치 완료. 실제 위젯 화면
  스크린샷 검증은 사용자가 직접 진행 중("내가 일일이 테스트할테니까").
