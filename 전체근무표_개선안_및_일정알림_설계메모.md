# 전체근무표 설정 개선안 + 일정관리 알림 기능 설계메모

> 2026-09-05 작성. 다음 세션에서 실제 작업할 두 가지를 정리한 메모 - 리뷰/설계만
> 하고 이번 세션에선 코드를 건드리지 않았음. 실제 착수 전에 값(파일 경로/줄
> 번호)이 그대로인지 한 번 더 확인할 것.

---

## 1. 전체근무표 설정 플로우 개선안

### 1-1. 현재 구조

- 진입점: `all_shifts_view.dart`(전체 근무표 화면)의 "미설정" 상태 안내
  화면(`_showAllTeamsSetupDialog()`, 라인 103) → `AllTeamsSetupDialog`
  (`all_teams_setup_dialog.dart`) 3단계 마법사
  1. **패턴 확인** - 내 규칙적 패턴을 카드로 그냥 보여줌(입력 없음)
  2. **전체 조 이름 입력** - 자유 텍스트에 공백으로 구분해 한 글자씩("A B C
     D") 입력 → 정규식으로 파싱, 중복 제거
  3. **조별 오늘 위치 선택** - 조마다 1단계에서 본 패턴 카드 중 "오늘이 몇
     번째 칸인지"를 다시 골라야 함
  4. 완료 시 `SharedPreferences`에 `all_teams_names`/`all_teams_offsets`
     저장(절대기준일 `julianDayNumber` 기반 오프셋 계산)
- 저장된 값은 `all_shifts_view.dart`가 매번 읽어서 전체 조 근무표 그리드를
  렌더링

### 1-2. 발견된 문제

1. **⭐ 설정 후 다시 고칠 방법이 아예 없음(가장 중요)** - 일단 한 번
   완료하면 `all_shifts_view.dart`엔 "다시 설정"/"편집" 진입점이 전혀 없다
   (240번 줄 버튼은 `!_isConfigured`일 때만 보임). 조가 바뀌거나 오타를
   냈어도 되돌릴 길이 없다. 앱 전체 초기화(재onboarding) 말고는 답이 없음.
2. **위 기능은 사실 이미 한 번 만들어졌다가 진입점만 없어진 상태** -
   `settings_tab.dart`의 `_showAllTeamsSetupDialog()`(1314번 줄)는 "이미
   설정된 근무표가 있으면 덮어쓸지 확인" 다이얼로그까지 갖춘 더 완성된
   버전인데, 536번 줄 주석에 "전체 교대조 근무표 작성 진입점 삭제 - 메인
   달력탭에서 직접 탭해서 설정하는 방식으로 옮길 예정(별도 작업)"이라고
   적혀있고 그 뒤로 버튼만 빠진 채 함수가 죽은 코드로 남아있다. → **이 죽은
   로직을 재활용해서 `all_shifts_view.dart`에 편집 진입점을 다시 붙이는 게
   가장 빠른 해법.**
3. **`SharedPreferences` 키 불일치(정리 필요, 당장 버그는 아님)** -
   `settings_tab.dart`의 전체 초기화 로직(237~238번 줄)은
   `all_teams_names`/`all_teams_indices`를 지우는데, 실제 저장 키는
   `all_teams_offsets`다(`all_teams_indices`는 어디서도 안 씀). 초기화해도
   `all_teams_offsets`는 안 지워지고 orphan으로 남는다 - 지금은
   `all_teams_names`가 지워지는 순간 `_isConfigured`가 false가 되고 재설정
   시 값이 통째로 덮어써져서 겉으로 문제는 없지만, 죽은 키를 정리해두는 게
   맞음.
4. **`_myTeam` 필드가 저장은 되는데 고를 UI가 없음** - `_AllTeamsSetupDialogState`에
   `String? _myTeam`이 있고 완료 시 `prefs.setString('all_teams_my_team',
   _myTeam ?? '')`까지 하는데, 3단계 어디에도 "이 중 내 조는?"을 고르는 화면이
   없어서 항상 빈 문자열로 저장된다. 사실 **내 조의 "오늘 위치"는 이미 앱이
   알고 있음**(`schedule.todayIndex`) - 사용자가 조 이름 목록에 자기 조를
   입력했다면, 그 조만큼은 3단계에서 수동으로 다시 고르게 하지 않고
   `schedule.todayIndex` 값으로 자동 채워주고 나머지 조만 입력받는 쪽이
   입력도 줄고 실수(내 조 위치를 잘못 세는 것) 위험도 없앨 수 있음.
5. **디자인 시스템 불일치** - 이 다이얼로그만 플레인 Material
   `Dialog`/`ElevatedButton`/`OutlinedButton`/`Chip`을 그대로 쓰고,
   `Colors.amber.shade50`/`Colors.green` 등 하드코딩 색을 씀. 앱 나머지
   화면(달력탭 등)이 쓰는 `AppButton`/`AppSecondButton` 디자인 시스템과
   따로 놈 - 통일 필요.
6. **`print()` 잔존** - `_complete()`(605~611번 줄), `_loadTeamData()`
   (all_shifts_view.dart 84/88/91번 줄) 전부 `print` 디버그 로그가 배포
   코드에 남아있음.
7. **3단계 플로우 자체의 마찰** - 1단계는 입력이 전혀 없는 "확인만" 페이지라
   굳이 별도 스텝일 필요가 없고, 3단계에서 조마다 "오늘 몇 번째 칸인지"를
   고를 때 1단계에서 본 패턴 카드를 화면에서 다시 볼 수 없어(스크롤도 안 됨)
   기억에 의존해야 함. 팀 이름 입력(2단계)도 자유 텍스트+정규식이라
   오타/중복 조 이름 실수를 유발하기 쉬움.

### 1-3. 개선 방향 (실행 항목 후보)

우선순위 순서 제안:

1. **편집 진입점 복구** - `settings_tab.dart`의 덮어쓰기-확인 로직을
   `all_shifts_view.dart`로 옮기거나 공용 함수로 빼서, 이미 설정된 상태에도
   "다시 설정" 버튼을 노출. 기존 값(`_teams`/`_teamOffsets`)을 다이얼로그
   초기값으로 프리필하는 것까지 고려.
2. **"내 조" 자동 인식** - 2~3단계 어딘가에 "이 중 내 조는?" 선택 추가,
   선택하면 그 조의 3단계 위치는 `schedule.todayIndex`로 자동 채우고
   잠가서(수정 불가) 실수를 원천 차단.
3. **1+3단계 통합 또는 패턴 상시 노출** - 3단계 화면 상단에 1단계 패턴
   카드를 접이식(축소 가능한 헤더)으로 항상 띄워서 스크롤 없이 참조 가능하게.
4. **디자인 시스템 통일** - `AppButton`/`AppSecondButton`, 앱 공용 팔레트로
   교체.
5. **정리** - `print()` 제거, `all_teams_indices` 죽은 키 정리(초기화 로직을
   `all_teams_offsets`/`all_teams_my_team`까지 지우도록 고침).
6. (검토만) 팀 이름 입력을 "조 개수" 스테퍼 + 자동 A/B/C… 배정으로 단순화할지
   - 자유 텍스트 입력의 유연성(조 이름을 알파벳 아닌 것으로 쓰고 싶은 경우
   없는지)과 트레이드오프이므로 사용자 확인 후 결정.

---

## 2. 일정관리 탭 "일정 시간 도래 알림" 기능 설계메모

> ✅ **구현 완료(2026-09-12)** - 아래 내용은 설계 당시의 검토 기록이라 그대로
> 남겨두지만, 실제로는 이 문서의 결론(옵션 (A), 정시+N분전, 일정별 개별
> 토글, 시스템 알림 설정 그대로 따름)대로 구현됐고 §2-4 열린 질문도 전부
> 이 세션에서 확정/구현됨. DB v23(`date_schedules.notify_enabled`/
> `notify_offset_minutes`), Native `ScheduleNotificationScheduler.kt`/
> `ScheduleNotificationReceiver.kt`, Dart `schedule_notification_service.dart`.
> 상세는 `DB_스키마_변경_가이드.md`의 v23 항목과 `CLAUDE.md`의 일정관리 탭
> 절 참고.

### 2-1. 요구사항 원문

사용자 요청: "일정관리에 그냥 알림(notification)" - 일정관리 탭
(`schedule_management_tab.dart`, `date_schedules` 테이블)에 등록해둔 일정의
**시각이 되면 알림**을 주는 기능. "그냥 알림"이라는 표현 그대로, 기존
알람(전체화면 잠금해제 벨소리 알람)과는 다른, **가벼운 일반 알림**을
의미하는 것으로 해석함 - 다음 세션 착수 전에 이 해석이 맞는지 확인할 것.

### 2-2. 기존 알람 시스템과의 의도적 차이

| | 기존 알람(`alarms` 테이블) | 일정 알림(신규) |
|---|---|---|
| 트리거 대상 | 근무 배정일 기준 반복 패턴(10일 롤링) | `date_schedules`의 개별 1회성 일정 |
| 실행 방식 | 잠금화면 전체화면(`AlarmActivity`)/오버레이, 벨소리, 스누즈, 타임아웃 | (제안) 일반 트레이/헤즈업 알림 1건, 탭하면 앱으로 이동 |
| 갱신 필요성 | 매일 자정/알람 20분 전/근무변경 시 재계산 필요(`AlarmRefreshEngine`) | 일정 생성 시점에 그 일정 하나만 1회 예약하면 끝 - 반복 재계산 개념 자체가 불필요(설계가 훨씬 단순함) |

**"일정관리는 알람이 아니라 알림"이라는 원칙을 지킬 것** - 기존 알람의 무거운
인프라(볼륨보정, 스누즈, 잠금화면 오버레이, 알람이력 영구보존 테이블 등)를
그대로 끌어오면 과설계가 됨. 다만 "정시에 정확히 울려야 한다"는 요구 자체는
같으므로, 이미 검증된 **정시성 인프라**(정확한 알람 권한, `AlarmManager`
`setExactAndAllowWhileIdle` 패턴)는 재사용할 수 있음.

### 2-3. 필요한 변경 (현재 상태: 관련 필드/트리거 전혀 없음 - 완전 신규)

**DB 스키마 (v22 → v23)** - `DB_스키마_변경_가이드.md` 절차를 반드시 먼저
읽고 그대로 따를 것(Dart `onUpgrade`와 Kotlin `DATABASE_VERSION`을 같은
커밋에서 올리는 것 포함). `date_schedules`에 최소 아래 컬럼 추가 검토:

- `notify_enabled INTEGER` (0/1) - 이 일정에 알림을 켤지
- `notify_offset_minutes INTEGER` - 정시(0)뿐 아니라 "N분 전" 옵션을 줄지는
  아래 열린 질문 참고

**등록/취소 트리거 지점** (기존 "알람 갱신 트리거 지점" 4곳과 유사한 고민이
필요하지만 범위는 훨씬 좁음):

1. 일정 생성/수정(시각 변경)/삭제 시(`schedule_management_tab.dart`의 CRUD
   경로) - 그때그때 그 일정 하나의 알림만 등록/재등록/취소하면 됨
2. 지난 일정(오늘 이전 날짜) 알림은 자동 정리 필요 - 언제 청소할지(앱 실행
   시? 그날 자정?) 결정 필요
3. ⚠️ 기존 알람처럼 "10일치 롤링 재생성" 개념은 **불필요** - `date_schedules`는
   반복 패턴이 아니라 각 날짜에 독립적으로 만든 개별 일정이므로, 생성 시점에
   그 일정의 정확한 1회용 트리거만 걸면 끝. 이 점에서 설계가 알람보다
   근본적으로 단순함.

**Native 구현 필요 여부** - `date_schedules`는 지금 Kotlin이 전혀 안 읽는
Dart 전용 테이블(`condition_shift_times`와 같은 부류). 앱이 완전히 종료된
상태에서도 정시에 안 죽고 울려야 한다면(기존 알람이 Native로 간 이유와 동일한
이유), 다음 둘 중 하나 검토:

- (A) 기존 알람과 같은 방식 - MethodChannel로 (트리거 시각, 일정 id, 내용
  텍스트)를 Kotlin에 넘겨 `AlarmManager`에 예약 + 수신 시 `NotificationHelper.kt`류의
  가벼운 알림만 띄우는 신규 Receiver 하나 추가(잠금화면 UI는 안 만듦)
- (B) `flutter_local_notifications` 같은 순수 Dart 패키지로 예약 - 구현은
  더 간단하지만, 딥 도즈/제조사 배터리 최적화에 대한 신뢰성이 (A)보다
  낮을 수 있음(이 앱이 애초에 알람을 Native로 짠 이유가 바로 이 신뢰성
  문제였다는 점을 참고 - `수면기록_자동추정_조사.md`에도 비슷한 조사가 있음)

→ 다음 세션에서 "일정 알림도 알람만큼 안 울리면 안 되는 기능인지, 아니면
어느 정도 느슨해도 되는지"부터 정해야 (A)/(B) 중 선택 가능.

**권한** - 알림 권한(`POST_NOTIFICATIONS`)은 이미 앱 전체가 요청/보유 중
(`permission_service.dart`) - 이 기능 때문에 새 권한 프롬프트를 추가할
필요는 없음.

### 2-4. 열려있는 질문 (착수 전 사용자 확인 필요)

1. 정시 알림만 지원할지, "N분 전" 옵션도 줄지?
2. 일정마다 개별 on/off 토글인지, 일정관리 탭 전체 알림 켜기/끄기 하나로
   퉁칠지?
3. 알림 탭했을 때 어디로 이동? (그 날짜의 일정관리 탭?)
4. 알림음/진동은 기존 "알람"과 확실히 구분되는 조용한 기본값으로 할지?
5. 지난 일정 알림 자동 정리 타이밍(위 2-3 참고)
