# 출시 전 수정작업 — 그룹별 실행계획 및 새 세션 인계

작성: 2026-09-14 / 대상: Google Play 배포용 교대근무 달력·자동알람 앱 교대시계(shiftbell)

개정: 2026-09-14 2차 — 사용자 결정 반영. D5~D9·D11·D12 권장안으로 확정(§4). 두 AI 동시 작업 대신 교대 실행으로 전환(§0-3, §2.2, **§11 작업 목록 T00~T26**).

**이 문서는 수정 작업의 인계·분담·검증 기준이다. 구현 완료 보고서나 출시 승인서가 아니다. 문서 작성 시 앱 코드는 수정하지 않았고, 아래 테스트도 실행하지 않았다.**

## 0. 새 세션에서 가장 먼저 읽을 요약

1. 목적은 기능 확장이 아니라 출시 전 감사에서 확인한 결함과 승인된 정책 변경을 수정하는 것이다. 예외적으로 불규칙 근무의 자동 롤링 생성(P1), 개별 알람 예외 테이블(DB v24)은 이번 릴리스에 도입하기로 확정했다.
2. **G0 공통 기반 → G1/G2/G3 수정(그룹별 worktree) → 수정 동결·연결·그룹별 테스트·통합 → G4 백업/복원 수정 및 테스트 → G5 출시 설정/UI 수정 및 테스트 → G6 최종 통합 검증** 순서로 진행한다.
3. **2026-09-14 사용자 결정: 두 AI(Claude Code, ChatGPT Codex)는 동시에 작업하지 않고 교대로 작업한다.** 한 AI가 토큰 한도로 멈추면 그동안 다른 AI가 **§11 작업 목록**에서 다음으로 가능한 작업을 한다. 멈춘 작업은 원래 AI가 복귀해서 마무리한다. 동시에 실행하지 않더라도 **그룹별 Git worktree·브랜치 분리와 파일 소유권은 유지**한다(토큰 소진으로 갑자기 끊긴 미커밋 변경이 다른 AI의 작업과 섞이지 않게 하기 위함). 공통 파일은 해당 단계의 단 한 명만 수정한다.
4. **사용자 지시: 수정과 테스트를 동시에 진행하지 않는다.** 교대 실행이어도 단계 순서는 같다. 수정 묶음 전체가 동결된 다음 그룹별 테스트를 한 번에 하나씩 한다. 테스트 실패로 소스 수정이 필요하면 테스트 단계를 중단하고 수정 단계로 돌아간다.
5. v4의 모든 번호 #1~#31을 아래에 배정했다. 원 감사 39건, 보류 항목, 추가 확인 V1~V7, 시나리오 S1~S18도 별도 대조표로 남겼다. 보류는 누락이나 해결 완료가 아니다.
6. **D5~D9·D11·D12는 2026-09-14 사용자가 전부 권장안으로 확정했다(§4).** 단 D7·D8은 확정된 내용 자체가 “조사·테스트 결과를 본 뒤 다음 판단”이라는 절차이므로, 그 결과(S9, S12)는 다시 사용자에게 보고하고 후속 선택을 받는다.
7. P1의 정확한 의미는 **실제 울림일이 오늘부터 오늘+9일까지인 알람을 자동 준비**하는 것이다. “모든 배정일의 정확히 10일 전 생성”으로 되돌리지 않는다.
8. DB는 Device Protected Storage에 있다. **네이티브가 첫 잠금 해제 전에도 SQL 마이그레이션**한다. 헤드리스 Flutter·구스키마 호환 대기 큐 설계는 폐기했다.
9. 복원 중 울리는 알람·미래 스누즈는 **기존 ID와 복원 전 재생 설정을 보존**한다. ID 충돌 시 새 ID를 받는 쪽은 가져오는 백업 custom 행이다.
10. 자동 테스트 통과만으로 실제 기상 알람의 전달·재생 생존·Doze·OEM 동작을 보장할 수 없다. G1/G4/G6의 필수 실기기 검증 전에는 출시 가능으로 표시하지 않는다.

## 1. 기준 자료, 우선순위, 현재 상태

### 1.1 기준 시점

| 항목 | 문서 작성 시 확인값 |
|---|---|
| 작업 폴더 | `C:\Users\Administrator\Desktop\shiftbell` |
| 브랜치 / HEAD | `dev` / `5fdbc880bb80835752fa3c404ba5278396e27ada` |
| 추적 소스 변경 | `git diff --stat` 기준 없음 |
| 기존 감사 문서 | 원 감사와 최종 v4는 현재 미추적 파일. 새 worktree에 저절로 들어가지 않음 |
| 앱 / DB | 작업 브랜치 `1.0.23+25`, DB v23. 목표 DB v24. 운영 main 기준 `1.0.22+24`, DB v18 |
| Android 테스트 | 프로젝트 Robolectric 4.14.1, 테스트 SDK는 `robolectric.properties`에서 34로 고정. 앱 targetSdk 36과 구분 |
| 현재 권한 | 프로젝트 파일 읽기·문서 쓰기 가능. 이번 세션의 `.git`은 읽기 범위이므로 브랜치·커밋·worktree 생성 가능까지 확인한 것은 아님 |

운영 버전·구버전 DB 배포 이력은 v4의 Git 이력 조사 기준이다. Play Console의 실제 배포·서명 상태를 이번 문서 작성으로 확인한 것은 아니다. 다음 세션은 HEAD와 작업 트리 차이를 먼저 확인한다.

### 1.2 근거 문서

- [원 감사 39건](출시전_코드감사_2026-09-13.md)
- [최종 v4 검토결과](출시전_코드감사_검토결과_v4_2026-09-13.md): 파일명은 09-13이나 본문은 09-14의 7차 정정까지 포함한다.
- 원 감사 SHA256: `E3B50ACE2B699A5F49B79E79A9CFCE86BFF3B072166C131FD75A94B9572B9F87`
- v4 SHA256: `DED66494A5586D77FA31461B9449412C6147EF9DB56F9B3B6291CB2793C6477E` (2026-09-14 T03 후 #1·#8 구현·자동 테스트 상태와 REPAIR-REJECT 사용자 결정 A를 추가한 뒤 값. 그 전: `5EC787E8…94513C18D0`, 결정표 갱신 후 `C888C6C5…FDC8F55D`, 최초 `4E4DF7DF…6807B78`)

판정 우선순위: **이후의 명시적 사용자 결정 → 최종 v4의 최신 정정·결정표 → 이 문서의 분담·테스트 운영 규칙 → 과거 설계·초기 감사의 제안**. 이 문서는 최신 정정을 다시 풀어 적었으며, 새로운 제품 정책을 몰래 확정하지 않는다. 모순이 발견되면 작업자가 한쪽을 임의로 골라 구현하지 말고 담당 작업의 인계 기록에 남긴다.

특히 `CLAUDE.md`의 “네이티브는 Flutter 마이그레이션 완료 후에만 DB 사용” 및 과거 `TEST_CHECKLIST.md`의 “불규칙은 자동 생성 안 함”은 **이번 변경으로 교체할 과거 규칙**이다. 영구 이력 보존, Dart/Kotlin 상수 동기화, 저장소 구분 등 나머지 규칙은 유지한다.

필독 자료는 담당 기능에 맞게 추가한다: `CLAUDE.md`, `DB_스키마_변경_가이드.md`, `백업복구_설계.md`, `친구공유_v1_스펙.md`, `일정관리탭_UI_스펙.md`, `컨디션매니저_설계.md`, `컨디션매니저_근거자료.md`, `수면기록_자동추정_설계.md`, `수면기록_자동추정_조사.md`, `테스트_계획_2026-09-12.md`.

## 2. 병렬화의 단위와 순서

### 2.1 그룹

| 그룹 | 책임 | 번호 / 추가 범위 | 병렬 가능 여부 | 가장 중요한 검증 |
|---|---|---|---|---|
| G0 | DB v24·공통 계약·초기화 실패 복구 기반 | #1, #8, #31 스키마, V4 | 최초 단독 | 실제 SQLite 마이그레이션 + 업데이트/잠금 해제 전 실기기 |
| G1 | 알람·달력·일정 알림·근무 편집 | #3, #4, #5, #10, #11, #12, #13, #14, #16, #17 핵심, #20, #23, #26, #27, #28, #31 실행, V6/V7, H03 출시 게이트 | G0 이후 G2/G3와 가능 | Dart/Kotlin 동일 fixture + 경합 실패 주입 + 실기기 울림 |
| G2 | 친구 공유·Firestore·Firebase 분리 조사 | #21, #24, #29, #30, V5, 조건부 D7 웹 뷰어 | G0 이후 G1/G3와 가능 | rules emulator + 두 클라이언트 offline/재시작 |
| G3 | 수면 후보·컨디션 갱신 | #15, #22, #17 수면 파일, M02/M03 조건부 검증 | G0 이후 G1/G2와 가능 | 계산·provider 테스트 + 화면 꺼짐 실기기 |
| G4 | 자동백업·파일 저장·복원 트랜잭션 전체 | #2, #9, #18, #19, #25, #17 백업 파일, V1/V2/V3 | G1/G2/G3 통합 후 단독 | 단계별 중단·재실행 + 진행 중 알람 실기기 |
| G5 | 출시 설정·개인정보·UI·외부 설정 확인 | #6, #7, S10/M12, D2/D3, D8 연결 마무리, V1/V5/V7 확인 | G4 이후 단독 | prod release·화면 매트릭스·Console 대조 |
| G6 | 통합 회귀·출시 판정 | 모든 그룹 경계, S1~S18 최종 상태, 보류·잔여 위험 | 수정 동결 후 단독 테스트 | 최종 빌드 실기기·증적·미해결 항목 판정 |

**왜 G1이 큰가:** 알람 종료·스누즈·갱신·개별 예외·근무명 변경은 `MainActivity.kt`, `database_service.dart`, `alarm_provider.dart`, `calendar_tab.dart` 등을 함께 수정한다. 작게 나눠 동시 배정하면 같은 파일뿐 아니라 DB/OS 계약까지 충돌한다. G1 안에서는 하위 작업 순서를 나누되 소유자는 한 명이다.

**왜 G4를 뒤로 미루는가:** 복원은 G1의 activeRing·스누즈·예약, G2의 공유 상태, G3의 수면 쓰기를 모두 조정한다. 파일을 나누더라도 의미적으로 독립할 수 없어 병렬 작업으로 취급하지 않는다.

### 2.2 두 AI의 실행 방식 — 교대 실행

2026-09-14 사용자 결정으로 두 AI를 **동시에 쓰지 않고 교대로** 쓴다. 누가 어떤 작업을 하고, 한쪽이 멈췄을 때 다른 쪽이 무엇을 이어받는지는 **§11 작업 목록과 교대 규칙**이 기준이다. 아래 단계 순서와 통과 조건은 그대로 유효하다.

| 단계 | 내용 | 통과 조건 |
|---|---|---|
| 0 | 준비 + G0 수정 | G0 코드 동결 |
| 1 | G0 테스트 (필요한 수정은 별도 단계로 되돌림) | G0 검증 기준 충족, 공통 계약 확정, 기준 커밋 |
| 2 | G1 / G2 / G3 수정 — 그룹별 worktree, 두 AI가 교대로 | G1/G2/G3 모두 코드 동결. 이 단계에서 테스트·빌드 실행하지 않음 |
| 3 | 통합 담당자가 공통 연결 요청 반영 | 연결 코드 포함 동결 커밋 생성 |
| 4 | G1·G2·G3 그룹 테스트 — 한 번에 하나 | 각 그룹 결과 기록. 테스트와 앱 코드 수정 병행 금지 |
| 5 | 검증된 G1/G2/G3 순차 통합 | 경계 회귀 통과, G4의 새 기준 커밋 |
| 6 | G4 수정 → 동결 → G4 테스트 | 복원 중단/스누즈/공유 상태 검증 통과 |
| 7 | G5 수정 → 동결 → G5 테스트 | prod release 후보 및 Console 확인 |
| 8 | G6 통합 테스트 | 출시 게이트 판정 |

**G3는 G2의 미통합 소스를 이어받아 시작하지 않고, G0 공통 기준에서 만든 자기 브랜치**에서 진행한다. 실제 실행 시간을 줄이는 것보다 파일·계약 격리가 우선이다.

G0의 업데이트 실기기 장기 검증을 기다리는 동안 설계 읽기는 가능하다. 필수 증적이 없으면 G0를 PASS로 바꾸지 않는다. 부득이하게 후속 코딩만 진행한다면 `PROVISIONAL / G0 실기기 미통과`를 각 그룹에 상속하고, G0에서 계약 변경이 발생하면 해당 그룹을 동결해 재기준화한다.

### 2.3 worktree와 통합 규칙

1. 통합 담당자 1명을 정한다. 최종 문서·원 감사·v4·G0 결과를 기준 커밋에 포함한 뒤 그 커밋에서 작업 브랜치를 만든다. 현재 미추적 문서는 커밋하지 않으면 다른 worktree에 없다.
2. 사용자 또는 Git 쓰기 권한이 있는 담당자가 별도 경로를 준비한다. 예: `shiftbell-g1`, `shiftbell-g2`, `shiftbell-g3`. 현재 작업 루트 밖 경로 생성 권한을 당연하게 가정하지 않는다. 이번 문서 작성에서 생성하지 않는다.
3. 각 터미널 시작 시 `Get-Location`, `git branch --show-current`, `git status --short`, `git rev-parse HEAD`를 확인하고 인계 기록에 적는다. 서로 다른 cwd여야 한다.
4. 각 그룹은 자기 파일만 수정한다. 다른 그룹 파일은 읽을 수 있다. 수정이 필요하면 자기 `integration_requests.md`에 파일·이유·필요 API·호출 시점·실패 의미를 적는다. **직접 수정하거나 임시 복사본으로 우회하지 않는다.**
5. `pubspec.yaml/lock`, Gradle, Android Manifest, 공통 ARB/생성 파일, `main.dart`, `MainActivity.kt`, DB 서비스는 아래 소유권을 따른다. 전역 format, 의존성 일괄 업그레이드, 대규모 파일 이동, 전체 코드 생성은 금지한다.
6. 공통 ARB·진입점 연결 요청은 수정 묶음 동결 후 통합 담당자가 한 번 반영한다. 이 연결 전에는 `IMPLEMENTED_NEEDS_WIRING`이며 그룹 완료가 아니다. 테스트는 **연결까지 포함한 빌드 가능한 커밋**에서 실행한다.
7. 두 그룹이 같은 공통 변경에 의존하면 통합 담당자가 공통 커밋을 먼저 만들고, 각 그룹이 그 동일 커밋을 받아 테스트한다. 상대 그룹의 기능 전체를 몰래 cherry-pick하지 않는다. 모든 영향 그룹의 기준 SHA를 기록한다.
8. merge는 통합 담당자만 순차 진행한다. 텍스트 충돌이 나면 자동으로 양쪽을 합치지 말고 소유권 위반/계약 변경부터 확인한다. merge 직전 변경 경로 교집합을 검사한다.
9. 다른 worktree도 Git 저장소 일부와 도구 캐시를 공유하므로 테스트·빌드는 한 번에 하나만 한다. 하나의 AVD/ADB 기기에 두 터미널이 동시에 명령하지 않는다.
10. 별도 worktree와 파일 소유권은 텍스트 충돌을 예방하는 수단이다. **의미적 충돌까지 무조건 0건이라고 보장하지 않는다.** 공통 계약·통합 테스트가 함께 필요하다.

## 3. 파일 소유권 및 공통 계약

아래 경로의 Kotlin 접두사는 `android/app/src/main/kotlin/com/hwani1103/shiftbell/`이다. 목록은 쓰기 허용 범위다. 목록 밖 파일이 필요하면 먼저 인계 요청에 추가하고 통합 담당자가 단독 소유자를 정한다. `lib/services/**`처럼 다른 그룹을 포함하는 포괄 소유권은 사용하지 않는다.

### 3.1 병렬 구간의 소유권

| 소유자 | 쓰기 허용 파일/영역 | 금지·연결 조건 |
|---|---|---|
| G0 완료 후 동결 | `DatabaseHelper.kt`, 신규 SQL 자산·실행기·스키마 검증기, DB 스키마 가이드, `CLAUDE.md`의 DB 규칙 | G1은 DB v24를 다시 설계/증분하지 않는다. 스키마 결함은 G0 담당자에게 반환 |
| G1 | `lib/main.dart`, `MainActivity.kt`, `lib/services/database_service.dart`의 CRUD/트랜잭션, `alarm_service.dart`, `alarm_refresh_service.dart`, `alarm_generation_service.dart`, `schedule_notification_service.dart` | DB migration/repair 영역은 G0 계약 유지. G2/G3는 진입점·DB 수정 요청만 제출 |
| G1 | `Alarm*.kt`, `CustomAlarmReceiver.kt`, `RingingAlarmTracker.kt`, `DirectBootReceiver.kt`, `ScheduleNotificationScheduler.kt`, `ScheduleNotificationReceiver.kt`, 필요한 알람 레이아웃 | `Sleep*.kt`, 백업 저장 컴포넌트는 포함하지 않음 |
| G1 | `alarm_provider.dart`, `schedule_provider.dart`, `date_schedule_provider.dart`, `tab_visibility_provider.dart`, `work_hours_settings_provider.dart`, `condition_shift_time_provider.dart` | `condition_provider.dart`는 G3 소유. 근무시간 저장은 G1, 소비·재계산은 G3 |
| G1 | `calendar_tab.dart`, `schedule_management_tab.dart`, `next_alarm_tab.dart`, `settings_tab.dart`, `onboarding_screen.dart`, `work_hours_settings_screen.dart`, 관련 alarm/schedule 모델·`lib/constants/alarm_*` | 복원 화면 G4, 친구 화면 G2, 수면/컨디션 화면 G3. 다른 화면의 문자열 요청은 공통 연결 단계 |
| G2 | `friend_sync_service.dart`, `friend_share_service.dart`, `firebase_bootstrap.dart`, `firebase_options.dart`, `friend_provider.dart`, `health_tip_provider.dart`, `friend_list_screen.dart`, `friend_calendar_view.dart`, `friend_schedule.dart`, `friend_open_util.dart` | `main.dart`·DB·Gradle·공통 설정 화면은 수정하지 않음 |
| G2 | `lib/web_main.dart`, 관련 `web/` 파일, `firestore.rules`, 그룹 전용 Firestore 테스트 설정 | SDK/서비스 설정 파일은 통합 담당자 단독 반영. 운영 배포는 별도 승인·실행 기록 |
| G3 | `condition_provider.dart`, `sleep_record_provider.dart`, `sleep_condition_provider.dart`, `Sleep*.kt`, `condition_tab.dart`, `sleep_calendar_full_screen.dart`, `sleep_edit_dialog.dart`, `sleep_record.dart`, `sleep_format_util.dart` | SQL·`MainActivity.kt`·근무시간 원본 provider는 수정하지 않음 |
| G3 | 필요한 `lib/services/condition/`의 수면/컨디션 계산 파일 | 의학적 규칙·가중치·팁을 변경하는 작업은 이번 결함 수정으로 확대하지 않음. 근거 자료 준수 |
| 통합 담당자 | `pubspec.yaml/lock`, `android/*.gradle*`, `android/app/build.gradle.kts`, `android/gradle.properties`, 각 Manifest, Firebase 플랫폼 설정, `lib/l10n/` 및 생성물 | 수정 묶음 중에는 동결. 필요한 변경은 요청을 모아 연결 단계에서만 반영 |

공유 위젯 resolver·광고 공통 위젯 등 미기재 파일은 임의로 가져가지 않는다. 예를 들어 `CalendarWidgetScheduleResolver.kt`의 실제 수정이 필요하면 G1로 추가 배정하고 다른 그룹의 쓰기를 막는다.

### 3.2 순차 구간의 소유권 이전

- G4 시작 전 G1/G2/G3 작업을 모두 종료·통합한다. G4는 `backup_service.dart`, `backup_watcher.dart`, `backup_storage_service.dart`, `backup_payload.dart`, `restore_backup_screen.dart`, 네이티브 백업 I/O와 함께 **복원 잠금/재생 스냅샷에 필요한 기존 공통 파일의 쓰기 소유권을 인수**한다. G1/G3 담당자가 동시에 추가 수정하지 않는다.
- G5는 최종 설정·ARB·Manifest·개인정보 자료·광고/UI 필요한 파일의 소유권을 인수한다. 수정을 끝내고 전부 동결한 다음 검증한다.
- G6는 테스트와 결과 기록만 한다. 결함 발견 시 해당 그룹 소유권을 다시 열고, 수정 후 새 빌드로 영향 테스트를 재개한다.

### 3.3 G0에서 고정할 계약

계약의 실제 함수명은 기존 구조를 읽고 최소 변경으로 정한다. 아래 의미를 `docs/release_audit/contracts.md`에 확정한 뒤 병렬 작업을 시작한다. 이 파일은 통합 담당자만 수정한다.

| 계약 | 반드시 고정할 내용 |
|---|---|
| DB | v24 스키마, 단일 SQL 자산 위치/포맷, migration과 repair 분리, 오류 전파, 트랜잭션 진입 방식 |
| 시간/알람 | 실제 울림일 10일 창, 공통 고정 now fixture, 슬롯 문자열 형식, day_offset 우선순위, custom/snoozed/activeRing 보호 |
| 변경 통지 | G1의 OT/근무시간/근무배정 커밋 성공 후 G3가 재계산할 revision/알림 계약. 실패 시 성공 통지하지 않음 |
| 공유 | G2 서비스의 시작/중지/dirty/retry 및 복원 후 재업로드 진입점. main lifecycle 호출은 G1이 연결 요청 반영 |
| 복원 연동 | G4가 사용할 native lock owner token, 각 writer가 읽을 gate의 의미와 저장소. 실제 전체 복원 구현은 G4. 빈 함수로 동작 완료를 가장하지 않음 |
| 저장소 | Flutter SharedPreferences와 native Device Protected `alarm_state`의 키·소유자·복원 대상 여부를 구분 |
| 초기화 | 필수 DB 실패와 선택 서비스 실패 구분, 재시도 경로, 무한 시작 화면 방지, release 오류 원문 노출 금지 |
| 테스트 경계 | now/OS 예약/파일 쓰기/Firestore 제출/단계 중단을 주입할 최소 지점. 제품 동작과 동일 경로를 검증할 수 있어야 함 |

새 prefs 키는 소유자·자료형·기본값·백업 포함 여부·실패 시 동작을 등록한다. 같은 문자열을 각 AI가 다른 의미로 정의하지 않는다. 데이터 모델/API의 기존 호출자는 유지하거나 공통 연결 단계에서 일괄 변경한다.

## 4. 확정 결정과 아직 남은 결정

| ID | 상태 / 내용 | 영향 그룹·진행 조건 |
|---|---|---|
| D1 | **확정**: 불규칙 근무 P1 자동 롤링 | G1/G4 |
| D1-a | **확정**: 배정일 D-10이 아닌 실제 울림일 오늘~+9일 | G1, Dart/Kotlin 동일 fixture |
| D2 | **확정**: 광고 운영. 실제 광고 ID·틴트 제거·Manifest 광고 앱 ID 교체는 최종 배포 직전 | G5. 그 전 테스트 ID 유지, 분리 구조 준비 가능 |
| D3 | **확정**: ko/en 개인정보 안내와 Play 데이터 보안 양식 갱신, 실제 수집 흐름 확정 후 최종 배포 직전 | G5. 현재 초안을 최종 사실로 쓰지 않음 |
| D4 | **폐기**: 헤드리스 Flutter migration 시제품 | G0에서 구현 금지. native SQL로 대체 |
| D5 | **확정(2026-09-14, 권장안)**: 백업 파일에 snoozed 제외 | G4 백업 포맷. **현재 기기의 진행 중 snoozed 보존은 별개 필수** |
| D6 | **확정(2026-09-14, 권장안)**: 기존에 저장된 잘못된 근무명은 자동 변환하지 않고 로그만 남김 | G1 #11. 신규 입력 검증은 그대로 구현 |
| D7 | **확정(2026-09-14, 권장안)**: 앱 측 상태 머신 보완을 먼저 구현하고, S9 결과로 tombstone/rules 채택 여부를 판단 | G2 앱 상태 머신 먼저. S9 결과를 사용자에게 보고한 뒤 서버 강제 정책·웹 뷰어 수정 여부를 받는다. 미채택이면 잔여 부활 위험 명시 |
| D8 | **확정(2026-09-14, 권장안)**: S12(실제 appId/DebugView 목적지) 확인 후 dev 전용 Firebase 프로젝트 분리 여부 결정 | G2 조사, G5 설정 마무리. S12 결과를 사용자에게 보고. 분리 시 Firebase 프로젝트 생성·Console 작업은 사용자가 수행하며 AI가 임의 생성/운영 설정 덮어쓰기 금지 |
| D9 | **확정(2026-09-14, 권장안)**: 이번 출시에는 백업 암호화 대신 안내 | G4/G5. 암호화 자체를 필수 구현으로 확대하지 않음 |
| D10 | **확정**: alarm_overrides 테이블, DB v24 | G0 스키마 / G1 기능 / G4 백업 |
| D11 | **확정(2026-09-14, 권장안)**: 30일 이상 지난 override만 정리 | G1 정리 로직. 영구 이력(`alarm_history`/`alarm_creation_log`)에는 절대 적용 금지 |
| D12 | **확정(2026-09-14, 권장안)**: 원 배정일/템플릿 변경 시 관련 예외 삭제 | G1 #31 실행 정책. `A→B→A` 되돌림 검사는 그대로 필수 |

2026-09-14 사용자가 D5~D9·D11·D12를 **모두 권장안으로 확정**했다(사용자 지시: “권장안대로”). v4 9장 결정표도 같은 날 같은 내용으로 갱신했다. 이후 결정이 바뀌면 결정일·정확한 선택·영향 그룹을 통합 담당자가 이 표와 v4 9장에 함께 기록한다. 이 결정은 제품 정책의 확정일 뿐, 운영 Firebase 배포·rules 배포·Play 게시·push를 승인한 것이 아니다.

## 5. 그룹별 구현 내용과 완료 후 테스트

각 표의 `F`는 Flutter/Dart 단위·widget 테스트, `R`은 JVM Robolectric, `E`는 Android 에뮬레이터의 실제 앱, `D`는 실기기, `FS`는 Firestore rules emulator를 뜻한다. `D 필수` 항목은 에뮬레이터 PASS로 대체하지 않는다. 아래 테스트명/디렉터리는 **추가할 계획**이며 현재 모두 구현돼 있다는 뜻이 아니다.

### G0 — 마이그레이션·초기화·공통 기반

**하위 작업 순서:** 기존 v1~v23 SQL/스키마 추출 → v24 예외 스키마 → 공통 SQL 실행/repair → native 최초 접근/부팅 경로 기반 → Dart 오류 복구 UI → 공통 계약/문서 → 코드 동결 → 테스트.

| 작업 | 발생 조건·증상 / 수정해야 할 내용 | 수정 후 가장 명확한 검증 |
|---|---|---|
| #1 [Critical], H16 native [High] | 업데이트 후 앱을 열지 않거나 잠금 해제 전 부팅하면 구 DB로 재생 설정/스누즈/이력을 못 읽는다. 모든 native DB 진입점에서 v1→v24 순차 SQL migration. 네이티브 빈 onCreate/onDowngrade는 예외로 막고 버전 확인 실패를 최신으로 간주하지 않음 | R: 역사 fixture/실제 SQLite 롤백, E: 동시 open, **D 필수 S1**: 업데이트 후 미실행·잠금 해제 전 실제 울림 |
| #8 [High] | ALTER 실패를 삼켜 최신 버전인데 컬럼 누락 상태가 생김. 존재 확인 후 필요한 SQL 실행, 실패 전파. Dart/native onOpen의 비파괴 repair 분리 | R/F DB harness: 중간 SQL 실패·최신 버전 누락 컬럼·잘못된 repair 거부, friends/설정 데이터 보존 |
| #31 스키마 [High] | 개별 예외를 저장할 원본이 없음. v24 alarm_overrides와 키·origin 참조·최종 schema/repair를 함께 도입 | F/R: 신규/업그레이드 DB의 테이블·컬럼·기본값·인덱스·제약·버전 비교 |
| V4 필수 | DB/날짜초기화/알람초기화/Firebase await 실패·지연 시 runApp 이전 정지, release 오류 원문 노출 가능. 재시도 가능한 초기화 실패 UI, 필수/선택 실패 분리, unawaited 오류 처리 | F widget: 각 단계 throw/지연·재시도·중복 초기화; E + D release: 네트워크 차단/DB 실패 화면, 원문·사용자 데이터 미노출 |

G0 테스트 fixture는 최소 v12·v14·v15·v17·v18·v23, 지원 범위 검증용 v1과 중간 단계 전 구간을 포함한다. 과거 데이터가 들어 있는 fixture를 쓰고 마이그레이션 SQL을 그대로 복사해 expected를 만드는 자기검증을 피한다. native migration 결과와 **Dart 신규 onCreate를 실제 실행해 만든 v24 DB**를 비교한다. Robolectric만으로 Dart onCreate를 실행했다고 표시하지 말고, Flutter DB harness/디바이스에서 생성한 독립 결과를 사용한다.

중간 SQL 실패 시 user_version과 스키마/데이터가 함께 롤백돼야 한다. `PRAGMA table_info`만 같고 인덱스·제약이 다른 상태도 실패다. SQLite mock의 성공 반환만 검증하지 않는다. native와 Dart 동시 open은 실제 앱/E에서도 반복한다.

**회귀 금지:** repair에 과거 v17 DROP friends/v11 UPDATE를 재실행하지 않는다. native onCreate로 빈 새 DB를 만들지 않는다. 정상 최초 설치는 Dart가 만든다. SQL 자산의 release APK 포함·Device Protected 접근 가능 여부는 실기기로 확인한다. 향후 SQL로 표현할 수 없는 데이터 변환까지 이 설계가 자동 지원한다고 주장하지 않는다.

**G0 산출물:** v24 계약, 두 언어 SQL 실행 결과, fixture/실패 주입 테스트, 문서 규칙 수정, 공유 파일/키/API 목록, S1 중 통과·미검증 범위. H03 재생 생존 전체와 G1 최종 권한 수신 경로는 G1/G6에서 다시 확인한다.

### G1 — 알람·달력·일정 알림의 단일 소유 그룹

**하위 작업 순서:** #3/#14/#23 재생 회차·자원 → #4/#13 제어·권한 → #16/#27 DB/OS 경합 → #26/#31 생성/예외 → #11/#12/#28 원본 편집 → #5/#10/#17/#20/V6 일정·시간·예약 통일 → 연결·문서 → 동결 → 테스트. 한 작업자가 순서대로 진행하며 G1 내부를 다른 AI에게 동시 배정하지 않는다.

| 작업 | 발생 조건·증상 / 수정 계약 | 수정 후 테스트·합격 조건 |
|---|---|---|
| #3 [High] | 알람 화면에서 홈 이동 시 화면 타이머 소실. native 회차별 종료 예약, 누적 ringCounter와 activeRing 분리, 토큰 직렬화/저장 실패 메모리 fallback | R: 이전 회차 종료/버튼이 새 스누즈를 못 끔, commit 실패; **D S4 필수**: 홈·잠금·Doze의 지속시간 |
| #4 [High] | overlay/FSI 실패 시 끄기 수단 상실. 울림 내내 ongoing 제어 알림 유지, 종료에만 정리 | E 권한 UI + **D S2**: 화면 표시 후 홈에서도 끄기/스누즈. 알림 권한·채널까지 막힌 조합은 별도 잔여 위험 판정 |
| #5 [High] | 탭 숨김 후 reboot 재예약, 같은 ID 일정 변경 후 옛 시각에 새 내용 표시. DP toggle + receiver DB 재확인 + expected trigger 정확 비교 | R 트리거 대조·삭제/disabled/변경; E/D S11 탭 숨김·재부팅·채널 실패 |
| #10 [High] | timezone 변경 후 일정 알림은 옛 절대시각. timezone/time/date 변경에 최신 DB 기준 재계산 | R/F 날짜 fixture + E S7. D에서 기상 알람과 동시 확인 |
| #11 [High] | 쉼표·reserved sentinel 근무명이 저장 직렬화를 손상. 신규 UI/import 검증, 기존 값 D6 처리 | F 입력/모델, E S8: `,`, `미설정`, 빈 값, 기존 잘못된 데이터 로드. 기존 근무명 임의 자동변환 금지 |
| #12 [High] | A↔B 맞교환·중복 rename으로 다른 근무 참조 합쳐짐. 임시 고유 이름을 쓰는 2단계 rename, 관련 참조 같은 transaction | F/R 실제 DB: 맞교환·3개 순환·중복 최종명 거부·중간 실패 롤백. override의 shift_type과 origin_shift 모두 변경 |
| #13 [High] | 빈 실패 목록/전부 실패 판정 오류, MethodChannel 실패가 성공·권한 허용으로 둔갑 | F/R: 0개·전부·부분 실패, native true/false/throw, permission unknown. E/D S2 재허용 후 회복 |
| #14 [High] | 다른 알람 삭제가 현재 재생 중단, 진동/무음 탐지 누락 | R 회차/ID 판별; **D S4**: 서로 다른 ID 삭제, 진동·무음·스누즈 중복 동작, 현재 회차만 종료 |
| #16 [High] | DB rollback과 OS 예약이 불일치. transaction 안에서는 읽기/계산/쓰기만, OS 반영은 commit 후 | R 실제 DB + OS 실패 주입: rollback이면 OS 호출 0, commit 후 실패는 복구 대상 기록. E S13 |
| #17 [Medium] | locale별 저장 날짜가 Dart/native 조회 키와 불일치 | F/R Locale.US 고정·슬롯 문자열 바이트 일치, E 아랍어 등 기기 locale/연말. 사용자 표시 locale까지 고정하지 않음 |
| #20 [High, 전달 지연 잠재 위험] | Doze에서 가까운 기상 알람·스누즈 지연 가능. 기상 알람 등록 경로 setAlarmClock 통일, showIntent는 사용자 화면 | R 모든 호출 경로; **D S3 필수**: 수신 실제시각/5분 스누즈/다음 알람 표시 각각 기록 |
| #23 [Medium] | MediaPlayer 준비 중 예외면 아직 전역 필드에 대입되지 않은 객체가 누수 | R 초기화 단계별 throw와 local player release; D 반복 재생 실패 후 재생 가능·자원 추세 확인 |
| #26 [High] | 창 밖 불규칙 배정이 창 안에 들어와도 자동 생성되지 않음. P1·0템플릿 정리·정책 첫 갱신·매일 Guard 유지 | F/R 동일 fixture + 멱등성; E/D S15 앱 미실행·자정·재부팅·중단 재개 |
| #27 [High] | Flutter 수정/native 갱신/늦은 OS 반영이 서로 덮어씀. DB transaction 안 최신값, OS 실행 직전·수신 시 DB 대조, 실패 재조정 | R 결정적 순서 주입 + E/D S13. 40초 조기 수신 금지, 삭제 후 취소 실행, 늦은 취소가 스누즈 제거 금지 |
| #28 [Medium] | 출퇴근시각 두 테이블 부분 저장/오래된 provider map 덮어쓰기 | F/R transaction 안 DB 최신 map의 해당 키만 변경, 두 테이블 rollback, E S8 연속 초기화/수정 |
| #31 실행 [High] | 개별 fixed 삭제/무음 변경이 네이티브 및 Flutter 재생성으로 원복 | F/R 규칙·불규칙 동일 fixture, E/D S16 자정·인접 배정·A→B→A·템플릿 되돌리기. D12 확정 필요 |
| V6/V7 | BOOT_COMPLETED/MY_PACKAGE_REPLACED/정확한 알람 권한 재허용 누락, 복합 권한 동작 | E OS별 수신·재예약 + D S1/S2. 선언 중복 자체를 버그로 단정하지 않음 |
| H03-GATE | receiver 반환 후 재생 프로세스 생존은 보장 확인 필요 | **D S4 필수**: 정상 background 회수/메모리 압박에서 재생 유지 측정. 실패하면 G1에서 서비스 구조 보완 후 재검증. 코드 미변경이어도 결과 없이 출시 후로 넘기지 않음 |

v4 6장의 `G1 — H03`은 이번 작업 그룹 이름과 우연히 같으므로 이 문서에서는 **H03-GATE**로 부른다.

#### G1에서 반복 정정을 보존할 상세 규칙

**P1·예외:**

- 울림 날짜 R에 대해 R의 당일 템플릿, R+1의 전날 템플릿, R-1의 다음날 템플릿을 계산한다. 같은 울림 슬롯 충돌 우선순위는 당일 > 전날 기여 > 다음날 기여이다.
- 실제 생성 범위는 R=오늘…오늘+9일이며 지금 이전 시각은 제외한다. 창 밖 배정일도 창 안 울림일에 기여할 수 있다. 배정일 D가 바뀌면 D-1/D/D+1의 창 안 결과를 재계산한다.
- 불규칙의 빈 패턴은 곧바로 손상 데이터가 아니다. `assignedDates` 경로를 안전하게 읽는다. 정규 패턴 가정의 조기 return만 무작정 제거하지 않는다.
- 템플릿 조회 성공 후 0개는 유효한 빈 desired 집합이다. 옛 fixed를 정리한다. 조회 실패를 빈 집합으로 바꿔 전부 삭제하지 않는다.
- custom·미래 snoozed·activeRing을 보호한다. activeRing 행이 fixed일 수도 있으므로 `type='fixed'` 필터만으로 보호 완료라고 판단하지 않는다.
- 입력/now 동일한 재갱신은 추가/삭제/생성이력 0, ID 불변이어야 한다. 알람 0개여도 자정 Guard가 유지돼야 한다.
- 정책 버전은 **첫 강제 갱신 성공 후에만** 완료 기록한다. 당일 이미 갱신했다는 과거 플래그로 P1 최초 적용을 건너뛰지 않는다.
- override는 skip 또는 set_type 원본이다. 슬롯 키는 `(울림시각, shift_type, day_offset)`, 시각은 두 언어 모두 `yyyy-MM-dd'T'HH:mm:ss`, 초 00. Dart의 밀리초 접미사 혼용 금지. 변경할 alarm_type_id를 키에 넣어 스스로 매칭을 깨지 않는다.
- `origin_date`, `origin_shift`를 보관한다. rename은 shift_type/origin_shift 모두 transaction으로 변경한다. D12 선택대로 원 배정일/템플릿 변경 시 예외를 처리한다. `A→B→A` 되돌림을 반드시 검사한다.
- `alarms.type`에 fixed_skip/fixed_user를 추가하는 우회는 기각됐다. 파생 fixed 행·이력만으로 개별 삭제 의도를 추정하지 않는다.

**DB와 OS의 경합:**

- SQLite transaction은 AlarmManager까지 원자적으로 묶지 않는다. 모든 원본 읽기와 DB 변경은 transaction, OS 작업은 commit 이후다.
- OS **예약** 직전 행이 없어졌거나 결과가 낡았으면 그 예약은 버린다. OS **취소** 직전 행이 없으면 정상 삭제 결과이므로 반드시 취소한다. “행이 없으면 모든 작업 무시”는 재발 금지다.
- 늦은 취소 시 같은 ID가 현재 스누즈/유효 미래 알람으로 남아 있으면 그 최신 DB 시각으로 재예약한다. 살아 있는 행이라는 이유만으로 과거 알람을 무조건 다시 울리지 않는다.
- 수신 Intent의 expected timestamp와 DB를 정확 비교한다. 새 DB 시각이 미래면 조기 재생하지 않고 그 시각에 재예약한다. 불일치하지만 DB 예정시각도 지난 경우의 지연 울림은 v4의 허용 정책이다. 1분 오차 허용으로 40초 일찍 울리게 만들지 않는다.
- 구버전 Intent에 timestamp가 없거나 DB 시각 파싱이 실패하는 호환 경로는 v4의 fail-open 한계를 기록하고 따로 시험한다. 일정 알림 #5의 오래된 예약 차단 규칙과 무분별하게 통합하지 않는다.
- 실패 목록은 ID/작업종류를 남기되 재시도는 최신 DB에서 재조정한다. 옛 payload를 그대로 재생하지 않는다.
- `FLAG_NO_CREATE`로 PendingIntent가 있다고 OS 예약도 있다고 판단하지 않는다. 기존 20분 전 Guard에서 대상 미래 알람을 재등록한다. 이는 전달 보장이나 DB commit 직후 프로세스 종료의 모든 공백 제거를 뜻하지 않는다.

**회차·권한·시간:**

- 누적 ringCounter는 activeRing 정리 시 초기화하지 않는다. native 소유자에서 증가/활성화/종료 비교를 직렬화한다. prefs commit 실패 시 ID 단독 비교로 후퇴하지 않고 메모리 회차 토큰을 사용한다.
- 화면·overlay 타이머는 S4에서 OS 종료 예약의 실제 정확도를 확인한 뒤 제거 여부를 결정한다. 설계만 보고 미리 제거하지 않는다.
- ongoing 알림은 화면 표시 성공 직후 지우지 않는다. POST_NOTIFICATIONS/채널까지 막힌 조합에서 제어 수단을 무조건 보장한다고 쓰지 않는다. startActivity 예외가 없다는 것만으로 실제 화면 표시를 확인했다고 보지 않는다.
- MainActivity·Engine·ActionHelper·DirectBoot·Guard 등 **모든 기상 알람 예약 경로**를 조사한다. 일정 알림/수면 감지를 기상 알람과 같은 setAlarmClock 정책으로 무조건 바꾸지 않는다.
- 저장 문자열만 Locale.US로 고정한다. 배포 국가로 기기 locale/timezone을 추정하지 않는다. 이미 잘못 저장된 값은 명시적 실패 처리/보고를 하고 형식 변경만으로 복구됐다고 주장하지 않는다.

### G2 — 친구 공유·Firebase·비용

**하위 작업 순서:** 실제 Firebase 옵션 조사 → 로컬 공유 상태/제출 직렬화 → 캐시 확인 상태 → 변경 기반 동기화/읽기 절감 → D7 결정 시 rules·viewer → 진입점 연결 요청 → 동결 → 테스트.

| 작업 | 발생 조건·증상 / 수정 계약 | 수정 후 테스트·합격 조건 |
|---|---|---|
| #21 [Medium] | 공유 중지 후 친구 캐시가 계속 표시. 서버 확인된 revoked/삭제와 단순 offline·오류를 구분 | F 상태 전이; E 두 클라이언트 S9: 서버 확인 후 제거, offline은 확인 불가로 표시하고 삭제로 단정하지 않음 |
| #24 [High + Medium] | 중지 실패를 성공 표시, 이전 upload가 공유 부활, offline Future 무한 대기, 재시도 누락 | F 지연/역순/실패/재시작, E/기기 두 클라이언트 S9. 로컬 off·stop_pending·dirty·generation 보존 |
| #29 [Medium, 설정 위험] | dev/prod 프로젝트·appId 불일치 가능. 파일 이름만 보고 분리됐다고 판단하지 않음 | 실제 Firebase.app().options/appId와 DebugView 목적지 대조 S12. D8 선택 후 설정 변경·재검증 |
| #30 [Medium] | 동일 내용 전체 업로드·반복 친구 읽기·무제한/잘못된 문서 입력 | F 변경키/debounce/refreshAll 호출량; FS 소유권·형식·크기·legacy 경계; 테스트 환경에서 read/write 전후 수량 기록 |
| V5 / 조건부 M15 | 저장소 rules와 운영 배포 일치 미확인. D7이면 웹 viewer가 tombstone을 정상 처리해야 함 | FS S18, 웹 빌드/브라우저 revoked 표시. 운영 rules는 사용자/권한 있는 담당자의 실제 대조 증적 |

**공유 상태 계약:**

- 사용자가 중지하면 로컬에서는 즉시 off, 서버 중지는 성공 확인까지 stop_pending으로 영속 보관한다. 재시작 후 임의로 active로 복원하지 않는다.
- generation은 새로운 공유 시작 회차에 증가한다. 이름/근무표 정상 수정마다 증가시키지 않는다. 각 진입점뿐 아니라 **실제 set 제출 직전**에도 현재 공유 의도·회차를 검사한다.
- 네트워크 제출 순서는 단일 큐로 통제하되, 앞선 offline write의 서버 ACK를 무한정 기다려 뒤의 stop 제출까지 막지 않는다. SDK가 이미 받은 offline write와 인증/검증 실패로 접수되지 않은 write를 구분한다. Future timeout은 이미 제출된 write를 취소하지 않는다.
- 실패한 시작/이름 변경/수정은 실패 또는 pending 상태를 사실대로 표시하고 dirty를 유지한다. 앱 시작/재개/재연결에서 현재 상태로 재시도한다.
- 앱 측 gate만으로 과거 SDK queue나 다른 클라이언트의 늦은 쓰기를 완전히 차단했다고 선언하지 않는다. D7 미선택이면 이 잔여 위험을 출시 판정에 남긴다.
- 공유할 필드가 바뀌었을 때만 업로드한다. 서로 다른 의미의 근무·메모·수면 정보를 추가 공유하지 않는다. `refreshAll`은 한 번 로드한 결과를 재사용한다. 비용 경보는 지출 상한 보장이 아니다.
- 공개 공유 코드의 get 허용은 현재 제품 의도다. 그것만으로 보안 결함이라고 단정하지 않는다. 소유자만 write하는 인증 규칙, 필드 자료형/크기/허용 범위, 불필요한 list 노출을 따로 검사한다. Firebase 공개 클라이언트 API key를 서버 비밀키처럼 취급해 무작정 교체하지 않는다.

**D7 선택 시 rules 상태표(앱·웹 동시 호환):**

| 기존 문서 | 요청 | 결과 |
|---|---|---|
| 없음 | active, generation >= 1 | 소유자·필드 검증 후 허용 |
| active(g) | active(g), 정상 수정 | 허용. `g > g`를 요구해 정상 수정을 거절하지 않음 |
| active(g) | revoked(g) | 허용 |
| revoked(g) | revoked(g) 재전송 | 허용 |
| 없음 | revoked(g), 유효 회차 | 중지 재시도 허용 |
| revoked(g) | active(g) 또는 더 낮은 회차 | 거부 |
| revoked(g) | active(g2), g2 > g | 새 시작 허용 |
| 새 회차 문서 | 이전 회차 upload/stop | 거부 |
| legacy, generation 없음 | 기존 형식 정상 수정 | 기존 사용자 호환 규칙으로 허용 |
| legacy | 유효한 새 generation 문서 | 전환 허용 |
| legacy, generation 없음 | 구버전 앱 delete | 허용 |
| generation 있는 문서 | delete | 거부. tombstone을 삭제해 과거 upload가 recreate하는 통로 차단 |

모든 허용은 기존 소유권/자료형 검증을 만족할 때만이다. tombstone은 **공유 payload를 제거**하고 최소 회차·중지 상태만 남긴다. 앱과 웹은 이를 빈 근무표로 파싱하지 말고 중지 상태로 처리한다. generation은 순서 제어 값이지 인증 비밀이 아니다. TTL·익명 계정 연결은 H14 근본 해결의 별도 보류 과제다.

**G2 인계:** G4가 호출할 복원 후 dirty/현재 상태 유지 API, 공유 관련 backup 제외 키 목록, D7/D8 결정 상태, 로컬 테스트 rules와 운영 배포 상태를 구분해 전달한다. 운영 문서에 테스트 쓰기·rules 배포를 자동 실행하지 않는다.

### G3 — OT 반영·자동 수면 후보

| 작업 | 발생 조건·증상 / 수정 계약 | 수정 후 테스트·합격 조건 |
|---|---|---|
| #15 [Medium] | OT 변경이 컨디션 cache key/의존성에 안 들어가 점수·판정 유지 | F provider: OT만 변경·삭제·실패·동일값 재저장, 재계산 횟수/최신 결과. E 탭 이동·resume 후 확인 |
| #22 [Medium] | 화면이 오래 꺼져 자동 수면 후보가 미종료 | F/R 동일 now fixture: 종료=min(now,start+9h), LOW·확인 대기; **D S14 필수** 화면 꺼진 채 trigger와 앱 실행 경로 비교 |
| #17 수면 부분 [Medium] | native 수면 기록 날짜 locale 불일치 | R + E locale·자정 fixture. G1 공통 문자열 계약 사용 |
| M02/M03 보류·조건부 | 자정/비활성 탭 cache와 DST 영향 재확인 | F/E 결과 기록. v4 보류 범위는 자동 확장하지 않고 결함 영향/출시 판단으로 올림 |

**#22 상세:** 9시간 기한은 행의 start에서 파생하고 별도 prefs deadline을 두지 않는다. 앱 경로와 native 경로에서 같은 end_time을 만들어야 한다. 9시간이 됐다는 이유로 사용자 확인 완료 처리하지 않는다. 화면을 다음 근무 때 반드시 켠다고 가정하지 않는다. 감지 창이 9시간 전에 닫혀 추가 trigger가 없을 가능성과 실제 9시간 초과 수면이 잘리는 한계는 기록하고 사용자가 수정할 수 있어야 한다.

기존 `condition_rule_engine_v2_test`, `condition_score_test`, `today_forecast_engine*`, `sleep_history_test`, `sleep_opportunity_test`, `sleep_day_slots_test` 등 관련 회귀를 실행한다. OT 데이터 commit 통지는 G1 계약을 사용하며 G3가 두 테이블 저장 로직을 별도로 만들지 않는다. notifier/listener 추가 시 dispose·중복 구독·mounted도 검사한다. 의학적 임계값/근거를 바꾸는 기능 확장은 하지 않는다.

### G4 — 자동백업·복원(순차 전용)

**하위 작업 순서:** D5/D9 확인 → 백업 대상/검증 규칙 → canonical snapshot·single flight·파일 완결성 → 모든 writer와 잠금 연동 목록 → 단계 기록·사본·ID 매핑·재생 snapshot → DB/prefs/OS 복원 및 공유 상태 → 복구 UI → 동결 → 테스트.

| 작업 | 발생 조건·증상 / 수정 계약 | 수정 후 테스트·합격 조건 |
|---|---|---|
| #2 [Critical + Medium] | data_version 비교로 변경 감지 누락, 비일관 snapshot·중복 백업/UI 정지. canonical hash·일관 DB snapshot·single flight·성공 후 hash 기록·실행 중 변경 재확인 | F 실제 데이터 변경/키 순서/동시 호출; E S6 대량 데이터·홈복귀·백업 중 수정. main thread 멈춤/완료 파일 내용 측정 |
| #9 [High] | custom/불규칙 복원 누락, 옛 OS 알람·일정 예약 잔존 | F/R export/import 대상, **E/D S5/S17** fixed 재생성 + 미래 custom/snoozed 실제 OS 등록 |
| #18 [Medium] | 중간 쓰기 실패 파일이 최신 정상 백업을 가림 | 파일 I/O 실패 주입 + E/D S6: 저장공간 부족·중단·부분 파일. 직전 완성본 유지, 미완성본 후보 제외 |
| #19 [High] | DB 성공/prefs 실패 등 부분 복원, 재시도 막힘, 복원 중 제어/갱신 경합 | 실제 DB + 파일/prefs/OS 단계 실패 주입, **D S17 필수** 진행 중 울림·스누즈·프로세스 재시작 |
| #25 [High의 완화] | 다른 설치의 공유키/상태 복원으로 현재 소유권과 불일치 | F 상태별 import, E/D S9: 새 설치 off, 기존 active는 현재 UID/회차 유지 후 dirty, stop_pending 유지 |
| #17 백업 부분 [Medium] | native 백업 파일/데이터 날짜 locale 의존 | F/R/E 고정 저장 형식·정상 백업 탐색, 기존 파일 호환 |
| V1/V2/V3 | 평문 백업 잔존, 재설치 자동 탐색 한계, API29 미만 미지원 | **D 재설치/MediaStore/SAF** + E API28 안내. 지원 범위를 UI에 사실대로 표시 |

#### 자동백업의 정확한 원인·수정 경계

- `PRAGMA data_version`은 같은 연결에서 관찰해야 의미 있는 값이다. 다른 연결끼리 숫자만 비교하는 변경 판단은 신뢰할 수 없다. 원 감사의 “영원히 스킵”은 과장된 표현이므로 쓰지 않는다.
- 테이블명·행 PK(없으면 안정적인 rowid 대안)·객체 키·prefs map의 재귀 키 순서를 정규화한다. exportedAt/상태용 시각 같은 휘발 필드는 내용 hash에서 제외한다.
- DB는 읽기 transaction snapshot, prefs도 일관된 복사본으로 export한다. DB와 prefs는 별도 저장소이므로 transaction 하나로 양쪽 전체가 원자적이라고 주장하지 않는다. snapshot 중 변경 감지/재확인 계약을 정한다.
- 백업 실행은 single flight. 실행 중 변경을 pending dirty로 기억하고 종료 후 재확인한다. **완성 파일 저장 성공 후**에만 마지막 성공 hash를 갱신한다.
- JSON 처리 isolate만 옮기고 native 파일 I/O가 UI thread에 남은 상태를 성능 해결로 처리하지 않는다. DB 읽기·직렬화·저장 각각 시간을 확인한다.
- 완성 전 파일을 최신 정상 백업으로 노출하지 않는다. 실패해도 이전 정상본을 먼저 지우지 않는다.

#### 복원 설계 — 이전 정정으로 돌아가면 안 되는 항목

1. **검증이 모든 변경보다 먼저다.** 백업 버전·자료형·허용 테이블/컬럼·시각 범위·참조·근무명·수면 시각을 검증한다. 임의 SQL/테이블명을 그대로 실행하지 않는다. **버전별 유효 데이터**를 기준으로 한다. 불규칙 모드의 빈 pattern, 종료 전 후보의 null end_time을 무조건 손상으로 거부하지 않는다. 정규 모드에서는 pattern/today_index 관계를 검사한다.
2. 검증 통과 내용을 **Device Protected 작업 사본 + hash/작업 ID**로 저장한다. 다운로드 원본이 지워져도 사본으로 재시도한다. 사본 생성 실패면 기존 DB/prefs/OS를 바꾸지 않는다.
3. 잠금은 prefs 토큰과 **native 프로세스 메모리의 소유 토큰**으로 판정한다. Dart에서 MethodChannel로 소유권을 전달한다. 현재 단일 프로세스 전제이며 향후 android:process 추가 시 다시 설계한다. 시간 경과만으로 살아 있는 복원 잠금을 무시하지 않는다.
4. 갱신 엔진·Guard·수면 쓰기·일정 재예약·자동백업·앱 UI 쓰기를 막거나 다음 기회로 미룬다. **알람 재생·끄기·스누즈·타임아웃은 막지 않는다.** 모달 UI만으로 background writer 차단이 끝났다고 보지 않는다. 구현 전 실제 DB/prefs/OS writer 전수 목록을 작성한다.
5. 복원 owner 자신이 수행하는 최종 OS 재조정은 gate를 통과해야 한다. 일반 background 작업과 구분하지 않아 복원 자신까지 막히는 교착을 테스트한다. 같은 프로세스에서 예외/취소된 Dart Future도 finally 등으로 소유권을 정리해야 한다. 프로세스 재시작 검사만으로 이 경우가 해결되지는 않는다.
6. DB 교체 transaction 안에서 **현재 미래 snoozed 및 activeRing 행**을 읽어 기존 ID로 이월한다. 스누즈 쓰기는 같은 SQLite 쓰기 잠금의 전/후에 커밋되도록 조정한다. OS 일괄 취소에서 이월 대상 ID를 제외한다.
7. **ID 충돌 시 현재 진행 중 행의 ID를 바꾸지 않는다.** 백업 custom 행을 새 ID로 매핑한다. v4 S17의 짧은 “새 ID로 이월” 표현은 7차 정정에 따라 **백업 custom이 새 ID, 기기 진행 알람은 원 ID**로 읽는다. 버튼·activeRing·PendingIntent·종료 예약 모두 원 ID를 유지한다.
8. 새 ID는 이월/백업 ID와 sqlite_sequence를 고려해 할당하고 `{백업ID: 새ID}`를 DB 단계 전 영속 기록한다. 재시도에서는 같은 매핑을 먼저 재사용한다. 새 충돌만 추가한다. 명시적 삽입 후 sequence와 다음 신규 ID를 검사한다. 역사 이력의 alarm_id는 과거 참조이므로 이 매핑으로 고치지 않는다.
9. 이월 행의 alarm_type_id가 복원 후 다른 설정/없는 타입을 가리킬 수 있다. **DB 교체 전** 알람 ID별 `sound_file/volume/vibration_strength/duration`을 DP prefs snapshot으로 저장한다. 재시도에서 첫 snapshot을 덮어쓰지 않는다. 재생과 duration 조회는 snapshot을 우선 사용한다. 끄기/타임아웃/인계에서 정리하고, 스누즈로 계속 살아 있는 회차의 다음 재생 전에 삭제하지 않는다. startup의 고아 snapshot 정리도 실제 행/진행 상태와 대조한다.
10. 사용자 타입 목록에 임의의 복원 전 타입을 추가해 해결하는 안은 기각됐다. 이월 행의 참조 제약·UI 조회에서도 누락 타입 때문에 crash하지 않는지 테스트한다.
11. `alarm_history`/`alarm_creation_log`는 절대 비우고 교체하거나 자동 삭제하지 않는다. 실제 컬럼으로 자연키를 확정해 중복 제외 병합한다. 서로 다른 기기의 alarm_id가 전역 고유라는 가정을 하지 않는다.
12. 덮어쓰기 시 구버전 백업에 없는 **원본 테이블**은 버전별 기본/빈 상태로 처리해 현재 값이 몰래 남지 않게 한다. `alarm_overrides` 없는 백업이면 기존 override를 비운다. 영구 이력은 예외로 병합한다. 오래된 백업의 수면/설정 누락을 사용자 설명 없이 임의의 유효 데이터로 꾸미지 않는다.
13. 단계는 `validated → locked → os_cleared → db_applied → prefs_applied → os_reconciled → 완료 정리`다. DB commit 직후 단계 기록 전에 죽어도 같은 사본·매핑으로 재실행 가능해야 한다. DB·prefs·OS 전체가 하나의 transaction이라고 표현하지 않는다.
14. `forceRefresh`만 호출하면 custom/snoozed가 복구됐다고 볼 수 없다. 마지막 단계에서 **미래 custom·이월 snoozed를 명시 재예약**, fixed는 #26/#31 계산, 일정 알림은 복원된 toggle/시간 기준으로 재예약한다.
15. 프로세스가 재시작되어 메모리 토큰이 없으면 죽은 작업의 차단을 해제하고 **현재 DB** 기준 fixed/custom/snoozed/일정 알림을 재조정한다. 작업 사본/단계 기록은 보존하고 미완료 안내 후 앱에서 이어간다. 그동안 생긴 스누즈도 다시 이월한다.
16. 사본까지 없거나 hash가 틀리면 안전 종료한다. 현재 DB 기준 위의 모든 예약을 재조정하고 미완료 상태와 다시 복원/현재 유지 선택을 보여 준다. 예약이 취소된 채로 단순 오류 토스트만 내고 끝내지 않는다.
17. 중단 후 현재 DB로 동작하는 사이 사용자가 새 데이터를 수정하는 경로도 검사한다. 자동 이어하기가 그 변경을 덮어쓸 수 있는 조건을 UI/작업 잠금·확인 흐름으로 명시하고 테스트한다. 잠금이 해제됐다고 복원이 완전 성공한 것은 아니다.
18. 공유 상태 키는 백업에서 제외한다. 신규 설치는 공유 off. 기존 설치 덮어쓰기는 현재 UID·generation·active/stop_pending 상태를 유지한다. active이면 복원 완료 후 dirty 재업로드, stop_pending이면 중지 재시도 유지. **이 조치로 재설치 이전 익명 계정의 서버 문서를 회수할 수는 없다.**

**G4 테스트는 G1 실기기 결과를 무효화할 수 있다.** AlarmPlayer/ActionHelper/CustomAlarmReceiver/Guard를 건드렸다면 G1 S2/S4/S13의 영향 항목을 새 빌드로 다시 실행한다. sleep gate 변경이면 G3 S14도 재실행한다.

### G5 — 출시 설정·개인정보·화면

| 작업 | 수정 범위·주의 | 수정 후 테스트·합격 조건 |
|---|---|---|
| #6 [Low, 이번 출시 필수] | dev/prod 광고 ID·Manifest 광고 앱 ID·debug tint 분리. 실제 운영값 전환은 D2대로 최종 직전 | dev는 테스트 광고 유지, prod release 리소스/화면에 테스트 ID·틴트 없음. 광고 로드 실패/미노출도 UI 정상. 실광고 클릭 테스트 금지 |
| #7 [High, 이번 출시 필수] | 실제 Analytics/공유/백업 수집·전송·보관·삭제 범위에 맞춰 ko/en 안내·Play 양식 대조 | S12 문구/SDK/Console 대조 증적. 평문 백업 잔존·수동 삭제·익명 공유 삭제 한계 누락 없음 |
| M12 조건부 | S10에서 360/600/840dp·글자 1.0/1.5/2.0·접힘/펼침·가로·키보드·inset 검사 후 재현되는 레이아웃 수정 | F widget/골든은 보조, E 실제 화면·스크롤·버튼 접근. D prod release 광고/복원 화면 최종 확인 |
| V2/V3 안내 | 재설치 자동 탐색 제한/SAF 수동 경로/API29 미만 미지원 상태 | E API28, D 삼성 재설치·수동 파일 선택. “자동 탐색 항상 가능” 문구 금지 |
| #29/V5/V7 외부 확인 | D8 플랫폼 설정 연결, 실제 운영 rules·Play 정확한 알람/FSI 관련 신고·자격 대조 | 저장소 값과 실제 Console 값 구분. 확인자/일시/배포 버전 기록. 외부 확인 없으면 미검증 |
| L02 제품 결정 | 새 Analytics 이벤트는 버그 수정으로 임의 도입하지 않음 | 계측 추가 선택 시 이벤트 정의/중복 방지 테스트. 근무명·메모·수면 상세 원문 전송 금지 |

M15는 Android 출시 자체의 필수 수정이 아니다. D7로 웹 뷰어를 바꾸거나 웹을 배포한다면 웹 빌드/실행 검증은 필요하다. D2/D3 최종 변경 뒤 다시 release 산출물을 만들고 검증한다. 변경 전 빌드의 PASS를 최종 빌드 PASS로 복사하지 않는다.

### G6 — 최종 통합 검증과 출시 판정

이 그룹은 새로운 기능을 구현하지 않는다. 아래 대조표의 각 항목을 `PASS / FAIL / NOT_RUN / DEFERRED / ACCEPTED_RISK`로 판정한다. Critical·A 필수 항목 또는 H03-GATE의 필수 검증 실패/미실행이 있으면 출시 가능으로 표시하지 않는다. B 권장 항목을 미루려면 결함·증상·이유·사용자 수용 결정을 명시한다. 테스트 자원이 없다는 이유로 PASS로 바꾸지 않는다.

최종 빌드에서 업데이트→앱 미실행→잠금 해제 전 울림, 불규칙 P1→개별 예외→근무변경→백업/복원, 복원 중 스누즈→프로세스 재시작, 공유 stop_pending→복원→online, OT/수면→자정→timezone 경계를 연결해서 검증한다. 개별 그룹 단위 PASS만으로 통합을 대체하지 않는다.

## 6. 테스트 도구·환경·운영 원칙

### 6.1 어떤 도구가 무엇을 입증하는가

| 수단 | 가장 명확하게 확인할 것 | 이것만으로 입증할 수 없는 것 |
|---|---|---|
| Flutter/Dart 단위 테스트 | 날짜/우선순위/override/hash/검증기/상태 전이, 같은 fixture 결과 | Android 실제 전달·권한·DB 플러그인 실제 동작 |
| Flutter widget 테스트 | 초기화 오류·재시도·폼·큰 글자·provider 구독/해제 | 실제 시스템 권한창·알림·OEM 화면 |
| **Robolectric** | JVM에서 native 로직·SQLite·Intent/회차·예약 요청·예외 주입 | 실제 AlarmManager 전달 정확도·Doze/OEM 프로세스 생존·소리/진동 |
| Android 에뮬레이터 | 실제 앱/SQLite/MethodChannel, OS별 권한 UI·재시작·레이아웃·timezone | 제조사 배터리 정책·실제 오디오/진동·일부 MediaStore 재설치 차이 |
| Firestore rules emulator | 실제 rules의 허용/거부·owner·generation·legacy·크기 경계 | 운영 배포 일치, 실제 앱 SDK offline queue/persistence 전체 |
| **실기기** | 실제 울림·스누즈·종료·locked boot·Doze·OEM 제한·release·재설치 저장소 | 모든 제조사/OS에서 무결함이라는 보장 |

사용자가 언급한 도구 이름은 **Robolectric**이다. 현재 프로젝트에 이미 기반이 있다. 최신 버전이 별도로 있어도 이번 수정 때문에 무조건 업그레이드하지 않는다. 현재 설치된 4.14.1/SDK34 테스트 결과는 targetSdk36 검증이 아니다.

도구의 역할 근거: [Robolectric 공식 설명](https://robolectric.org/), [Flutter 테스트 구분](https://docs.flutter.dev/testing/overview), [Firebase rules 단위 테스트](https://firebase.google.com/docs/rules/unit-tests). Doze/standby의 실제 상태 전환 테스트는 [Android 공식 테스트 절차](https://developer.android.com/training/monitoring-device-state/doze-standby)를 따른다. 이 문서의 앱별 합격 조건은 해당 공식 문서가 아니라 감사 결과와 제품 요구에서 도출했다.

### 6.2 이 프로젝트의 제약

- `integration_test/`는 호스트 RAM 6GB 환경에서 두 번 메모리 부족을 겪은 뒤 2026-09-13 제거됐다. 과거 테스트 계획만 보고 되살리지 않는다. 기본은 F/R + 에뮬레이터 수동/ADB 재현이다.
- `android/gradle.properties`의 현재 JVM 설정은 `-Xmx8G`, metaspace 4G다. 문서상의 6GB 호스트와 맞지 않으므로 실제 가용 메모리를 확인해 **통합 담당자가 테스트 준비 시** 조정한다. 모든 worktree에서 동시에 Gradle/에뮬레이터를 실행하지 않는다.
- 기존 Kotlin 테스트: `AlarmDayOffsetPriorityTest.kt`, `AlarmRefreshEngineH2Test.kt`, `ScheduleNotificationSchedulerTest.kt`. 기존 Flutter 테스트는 `test/`에 있다. 기존 테스트 통과 이력을 현재 수정 결과로 재사용하지 않는다.
- 초기 감사의 Flutter 102건 통과·analyze 640 diagnostics 등은 **과거 기록**이다. 이번 문서 작성에서는 앱 테스트를 실행하지 않았다. 다음 실행에서 baseline을 기록한다.
- `unitTests.isReturnDefaultValues=true` 때문에 Android mock의 기본 반환값이 오류를 숨길 수 있다. 필요한 OS/DB 경로가 실제 실행됐는지 assertion을 둔다. 함수 호출 횟수만 검사하는 테스트로 사용자 결과를 대체하지 않는다.

### 6.3 OS·기기 매트릭스

| 환경 | 필수 초점 |
|---|---|
| API28 | 백업 미지원 상태와 안내, 기존 앱 핵심 실행 |
| API29/30 | scoped storage/백업 파일 기본 경로 |
| API31/32 | 정확한 알람 권한 거부·재허용 |
| API33 | notification permission 거부·재허용 |
| API34 | 전체화면/백그라운드 실행·알림 채널·정확한 알람 경로 |
| API35/36 | 최종 target과 최신 OS 실제 앱/레이아웃/권한 회귀 |
| 실기기 AOSP 계열 + 삼성 우선 | 잠금 해제 전 부팅, Doze·배터리 제한, 실제 재생/진동, MediaStore/재설치 |

보유하지 않은 OS/기기는 `NOT_RUN`으로 적고 범위를 사용자에게 알린다. 폴더블은 먼저 에뮬레이터의 접힘/펼침·폭으로 레이아웃을 검증하고, 실기기 확보 시 실제 전환을 확인한다. 모든 모델을 확보해야만 단위 작업을 시작할 수 있다는 의미는 아니다.

`force-stop`/설정의 강제 종료는 일반 background 프로세스 회수와 다르다. 강제 종료 상태의 플랫폼 제한을 정상 메모리 회수 시 결함과 혼동하지 않는다. 프로세스 회수·화면 swipe·재부팅·업데이트·Doze를 별도 케이스로 기록한다. 실제 대기 10일 검증과 시계 주입/날짜 가속 검증도 구분한다.

### 6.4 실행 명령 예시

다음은 **수정 동결 후 실행할 예시**다. 신규 테스트 경로·패키지는 담당자가 실제 추가한 이름으로 바꾸며, 존재하지 않는 테스트를 실행한 것처럼 보고하지 않는다. dev 테스트 데이터만 사용한다.

```powershell
# 프로젝트 루트: 수정 전후 상태·기준 확인
git status --short
git rev-parse HEAD

# 그룹별로 추가할 Dart 테스트 경로 예시
flutter test test/release_audit/g1

# 프로젝트 루트: 기존 테스트 회귀 (관련 그룹 검증 후 최종 통합 때 전체)
flutter test
flutter analyze

# android 디렉터리에서 실행
.\gradlew.bat :app:checkDartKotlinSync --max-workers=1
.\gradlew.bat :app:testDevDebugUnitTest --max-workers=1

# 프로젝트 루트: 에뮬레이터용 dev debug
flutter build apk --debug --flavor dev

# 실기기용 release 확인 / 최종 prod 산출물 (게시 아님)
flutter build apk --release --flavor dev
flutter build appbundle --release --flavor prod

# 웹 변경/배포가 있는 경우
flutter build web -t lib/web_main.dart
```

Firestore는 G2가 별도 로컬 테스트 설정과 rules test runner를 만든 뒤 `firebase emulators:exec --only firestore`로 실행한다. **명시적으로 `demo-` 테스트 프로젝트와 테스트용 설정을 지정**하고, 실제 운영 프로젝트의 읽기/쓰기나 rules deploy를 테스트 명령에 섞지 않는다. 예시를 복사하기 전에 설정 내 rules 경로·포트·SDK emulator 연결·필요 도구 설치를 검증한다.

ADB는 항상 `adb -s <SERIAL>`로 대상 지정. dev 패키지는 `com.hwani1103.shiftbell.dev`, prod는 `com.hwani1103.shiftbell`이다. 실행 직전 실제 flavor/applicationId를 대조한다. 테스트 스크립트가 운영 앱 데이터를 clear/uninstall하지 않도록 고정한다.

**S1 업데이트 주의:** 설치된 앱과 업데이트 APK의 패키지·서명이 같아야 `adb install -r`가 성립한다. Play App Signing 배포본과 로컬 서명이 다르면 내부 테스트 트랙 등 일치하는 서명 경로를 사용한다. 서명 오류를 우회하려고 운영 앱을 삭제하면 업데이트 검증이 아니다. dev의 충실한 v18 fixture 업그레이드는 유용하지만 실제 prod 업데이트 증적과 구분한다.

### 6.5 테스트 단계와 결과 기록

그룹별 상태는 `PLANNED → CODING → IMPLEMENTED_NEEDS_WIRING(필요 시) → CODE_FROZEN → TESTING → PASS/FAIL/BLOCKED → INTEGRATED`다. 테스트 코드·fixture는 동결된 제품 코드에 대해 테스트 단계에서 완성/실행한다. 코드 단계에서는 필요한 주입 지점/예상 결과를 설계할 수 있지만 테스트를 병행 실행하지 않는다.

실패 수정 시 `TESTING → CODING → CODE_FROZEN`으로 되돌아간다. 실패한 빌드와 고친 빌드의 결과를 섞지 않는다. unrelated 테스트를 무한 반복하기보다 변경 영향과 미확인 위험에 맞게 재실행한다.

그룹별 독립 기록 위치(생성 예정):

```text
docs/release_audit/contracts.md                 # 통합 담당자 단독
docs/release_audit/g0/{handoff,test_results,integration_requests}.md
docs/release_audit/g1/{handoff,test_results,integration_requests}.md
docs/release_audit/g2/{handoff,test_results,integration_requests}.md
docs/release_audit/g3/{handoff,test_results,integration_requests}.md
docs/release_audit/g4/{handoff,test_results,integration_requests}.md
docs/release_audit/g5/{handoff,test_results,integration_requests}.md
docs/release_audit/g6/release_gate.md
```

한 개의 공통 진행 로그를 두 AI가 동시에 수정하지 않는다. 결과에는 최소 다음을 남긴다: 그룹/작업/S 번호, baseline·테스트 대상 SHA, flavor/빌드, 기기·API·locale·timezone, 권한/채널/잠금/배터리/네트워크 상태, fixture, 재현 절차, expected/actual, PASS 여부, 로그/영상/스크린샷 경로, 미검증·잔여 위험.

알람은 목록 스크린샷만으로 PASS하지 않는다. DB 예정시각·OS 예약·실제 수신·재생 시작/종료·ID/회차를 대조한다. 복원은 DB/prefs/OS와 진행 중 제어를 함께 확인한다. 공유는 로컬 의도/SDK 접수/서버 확인/다른 기기 표시를 나누어 기록한다. 증적에는 실제 사용자 근무표·메모·수면 상세·공유 토큰 등 개인정보를 남기지 않는다.

## 7. S1~S18 누락 방지용 최종 체크리스트

| 시나리오 | 담당 | 반드시 포함할 재현과 판정 | 필수 환경 |
|---|---|---|---|
| S1 | G0 → G1/G6 | v12/14/15/17/18/23→24, 신규 동일 schema, 중간 실패 rollback, native create/downgrade 차단, latest 누락 repair+friends 보존, SQL whitelist, 업데이트 앱 미실행, 첫 잠금 해제 전 boot, 동시 open, release SQL 자산, 10일 창 연장/수면 위젯 | R+실제 Dart DB 결과+E+**D** |
| S2 | G1/G6 | 알림·채널·overlay·FSI·exact 권한 각각 허용/거부/재허용, 잠금/해제 수신·끄기·스누즈, 화면→홈 제어 알림 | E+**D** |
| S3 | G1/G6 | Doze 5분 간격 기상 알람·5분 스누즈·수면·일정 동시, 실제 수신시각 비교, 다음 알람 표시 별도 | **D**, E 보조 |
| S4 | G1 → G4/G6 | 홈/잠금/다른 ID 삭제/진동·무음/정상 프로세스 회수, 이전 회차 종료 무시, commit 실패 memory token, Doze 자동 종료, H03 재생 유지 | R+**D** |
| S5 | G4/G6 | 규칙·불규칙/custom/현재 snoozed/skip/set_type, 신규 및 덮어쓰기 복원, DB·OS·공유상태 비교 | F/R+E+**D** |
| S6 | G4/G6 | 저장 중 종료·공간 부족·malformed import·prefs 실패, 정상 파일 보존, 수동백업→메모 수정→resume 자동백업, 실행 중 변경 재확인 | F/R+E/D 저장소 |
| S7 | G1/G3/G6 | timezone 변경 후 과거가 되는 알람, 자정·연말·D-1/D/D+1·일정 동시. 조건부 DST 양방향 New_York 2026-03-08/11-01. 로케일 변경은 timezone과 별개 | F/R+E, 실제 전달 D |
| S8 | G1/G6 | 쉼표·sentinel·맞교환·중복, 연속 배정과 native refresh 경합, 출퇴근 두 테이블 초기화 연타/실패 rollback | F/R+E |
| S9 | G2 → G4/G6 | 두 클라이언트 공유→수정→offline→stop→프로세스 종료→restart→근무수정→online, 부활 여부, 시작/이름 실패, 캐시 서버확인과 offline 구분, reinstall 옛 링크, restore stop_pending | F+E/기기 두 클라이언트, 재설치 D |
| S10 | G5/G6 | prod release 360/600/840dp × 글자 1/1.5/2, 접힘/펼침·광고 실패·복원 스크롤·키보드/가로. 모든 버튼 도달 가능 | F 보조+E+**D release** |
| S11 | G1 → G4/G6 | 탭 숨김→boot→알림 없음, 탭 복원, 복원 후 native toggle 일치, 일정 변경 후 채널 실패→옛 trigger 표시 금지 | R+E/D boot |
| S12 | G2/G5/G6 | dev 실제 appId/DebugView 목적지, 운영 rules와 저장소, ko/en 안내·SDK·Play 데이터 보안 일치 | 앱+Console 실제 확인 |
| S13 | G1 → G4/G6 | PI 유지/OS만 취소 후 20분 Guard 복구, 옛 예약 40초 조기 수신 차단, OS 예외 재시도, commit 후 삭제 시 schedule 폐기/cancel 실행, 같은 ID snooze에 늦은 cancel→최신 재예약 | R 결정적 주입+E+**D 전달** |
| S14 | G3 → G4/G6 | 후보 생성→9h 이상 화면 꺼짐, 화면 켠 앱 경로/꺼진 native trigger 경로 종료시각 동일·LOW·확인 대기 | F/R+**D** |
| S15 | G1 → G4/G6 | 한 달 뒤 배정→앱 없이 창 진입 생성, 창 끝의 ±1 기여, 배정 삭제·변경, 마지막 template 삭제, 0알람 Guard, boot, 중단 재시도, 동일 입력 ID/이력 불변, 정책 첫 강제갱신 | F/R+E+**D** |
| S16 | G1 → G4/G6 | 규칙/불규칙 개별 삭제·무음→자정/Flutter 재생성에도 보존, 원 배정 변경, A→B→A/템플릿 시각 되돌림 D12, 두 언어 슬롯 문자열 동일 | F/R+E, 실제 무음/울림 D |
| S17 | G4/G6 | 아래 별도 상세 표 전부. 특히 진행 중 ID 유지·백업 custom ID 변경·재생 snapshot·custom/snoozed OS 예약 | 실제 DB/실패 주입+E+**D** |
| S18 | G2/G6, D7 선택 시 | rules 상태표 모든 행: 정상 같은 회차, stop/retry/missing stop, 늦은 쓰기 거부, 재시작 회차, legacy write/delete, 새 문서 delete 거부 | **FS**, 앱/웹 호환 E/브라우저 |

### S17 세부 실패 지점 — 하나라도 생략하면 복원 검증 미완료

| 주입 조건 | 기대 결과 |
|---|---|
| 사전 검증 실패 / 사본 저장 실패 | DB/prefs/OS 변화 없음, 정상 백업 유지 |
| os_cleared 직후 종료 | 단계와 사본으로 이어서 최종 예약 복구 |
| DB commit 직후 단계 기록 전 종료 | 동일 사본/ID 매핑 재실행, 이력 중복·새 ID 재발급 없음 |
| prefs 일부 쓰기 실패 | 재시도 후 단번 성공 결과와 일치, 성공으로 오표시 없음 |
| 복원 중 Guard·수면·일정·자동백업 | 허용되지 않은 writer는 미룸, 다음 trigger/최종 재조정으로 복구 |
| 복원 중 울림·끄기·타임아웃 | 제어 유지, 진행 ID 보존, 이력 병합 |
| 복원 중 snooze, DB 교체 전/후 각각 | 같은 ID의 최신 snoozed 보존, 미래 OS 예약 일치 |
| 현재 진행 ID와 백업 custom ID 충돌 | 현재 ID 유지, 백업 custom만 새 ID, 해당 제어/예약 모두 올바른 행 |
| 매핑 기록 후 종료·재실행 | 같은 새 ID 재사용, sqlite_sequence 이후 새 알람 충돌 없음 |
| 같은 타입 ID의 다른 설정 / 백업에 타입 없음 | 재울림 소리·음량·진동·지속시간은 복원 전 snapshot 유지 |
| 원본 백업 삭제 | 작업 사본으로 정상 완료 |
| 사본까지 삭제/손상 | 안전 종료, 현재 DB의 fixed/custom/snoozed/일정 재예약, 미완료 안내 |
| 구버전 백업에 overrides 없음 | 덮어쓰기 후 옛 overrides 남지 않음, 이력은 보존 |
| 프로세스 재시작, 메모리 owner 없음 | stale lock 해제, 현재 DB 알람 동작, 기록/사본 유지, 앱에서 재개 |
| 같은 프로세스의 예외/작업 취소 | 살아 있지 않은 작업의 lock이 영구 잔존하지 않음 |
| 잠금 중 복원 owner 최종 reconcile | 자기 gate에 막히지 않고 완료 |
| 최종 forceRefresh 뒤 목록 확인 | 목록뿐 아니라 미래 custom/snoozed OS 예약과 실제 수신 확인 |

## 8. 원 감사 39건과 수정 번호 전체 대조

심각도는 원 감사 식별자의 등급을 유지하되, 확정 버그와 잠재 위험/제품 결정을 구분한다. A/B/C는 심각도와 별개인 v4의 작업 우선순위다. 신규 #26/#31은 High·A다.

| 원 ID | 원 등급 | 최종 처리 / 담당 |
|---|---|---|
| C01 | Critical | #1 G0, 업데이트·부팅 통합 G1/G6 |
| C02 | Critical | #2 G4 |
| H01 | High | #3 G1 |
| H02 | High | #4 G1 |
| H03 | High / 생존 잠재 위험 | H03-GATE G1/G6, 실기기 결과에 따라 출시 전 보완 |
| H04 | High / Doze 전달 잠재 위험 | #20 G1 |
| H05 | High | #9 G4, #26 G1의 생성 결과 사용 |
| H06 | High | #9 G4 + #5 수신 검증 G1 |
| H07 | High | #10 G1 |
| H08 | High | #5 G1 |
| H09 | High | #27 G1 |
| H10 | High | #16 G1 |
| H11 | High | #11 G1 |
| H12 | High | #12 G1 |
| H13 | High | #24 G2 |
| H14 | High | #25 G4는 완화. 익명 소유권 회수/계정 연결/TTL 근본 해결은 보류 |
| H15 | High | #13 G1 |
| H16 | High | #1 native + #8 Dart, G0 |
| H17 | High | #19 G4 |
| H18 | High | #7 G5 |
| H19 | High | #14 G1 |
| M01 | Medium | #15 G3 |
| M02 | Medium | 자정/비활성 탭 cache: v4 출시 후 보류, G3/G6 검증 결과 보관 |
| M03 | Medium | DST 조건부 S7 G1/G3/G6. 해외 사용 조건 확인 후 범위 결정 |
| M04 | Medium | 근무명으로 휴무 판정: 보류. G1이 이름 검증 중 휴무 의미까지 바꾸지 않음 |
| M05 | Medium | #22 G3 |
| M06 | Medium | #17 G1 공통, G3 수면, G4 백업 |
| M07 | Medium | #28 G1 |
| M08 | Medium | #24 G2 |
| M09 | Medium | #21 G2 |
| M10 | Medium | #2 G4 |
| M11 | Medium | #18 G4 |
| M12 | Medium / 레이아웃 잠재 위험 | G5 S10 후 재현된 결함 수정, 미재현/미검증 구분 |
| M13 | Medium / 설정 위험 | #29 G2 조사/구현, G5 설정 연결·실제 확인 |
| M14 | Medium | #30 G2 |
| M15 | Medium / 웹 전용 | Android 필수 아님, D7/웹 변경 시 G2/G5 검증 |
| M16 | Medium | #23 G1 |
| L01 | Low | #6 G5, D2로 이번 릴리스 필수 |
| L02 | Low / 제품 결정 | Analytics 이벤트 추가 여부 미정, G5/G6. 버그 해결로 자동 계측 추가 금지 |

총 **2 Critical + 19 High + 16 Medium + 2 Low = 원 감사 39건**이다. 다음 표는 수정 번호 자체의 누락 방지용이며 숫자순이다.

| 수정 번호 | 주 담당 | 함께 확인할 항목 |
|---|---|---|
| #1 | G0 | C01/H16 native/S1 |
| #2 | G4 | C02/M10/S6 |
| #3 | G1 | H01/S4 |
| #4 | G1 | H02/S2 |
| #5 | G1 | H08/H06 수신/S11 |
| #6 | G5 | L01/D2/S10 |
| #7 | G5 | H18/D3/S12 |
| #8 | G0 | H16 Dart/V4/S1 |
| #9 | G4 | H05/H06/S5/S17 |
| #10 | G1 | H07/S7 |
| #11 | G1 | H11/D6/S8 |
| #12 | G1 | H12/#31/S8 |
| #13 | G1 | H15/V6/V7/S2 |
| #14 | G1 | H19/S4 |
| #15 | G3 | M01/G1 commit 통지 |
| #16 | G1 | H10/#27/S13 |
| #17 | G1 | M06; G3 수면/G4 백업 파일 분담 |
| #18 | G4 | M11/S6 |
| #19 | G4 | H17/S17 |
| #20 | G1 | H04/S3 |
| #21 | G2 | M09/S9 |
| #22 | G3 | M05/S14 |
| #23 | G1 | M16/MediaPlayer |
| #24 | G2 | H13/M08/D7/S9/S18 |
| #25 | G4 | H14 완화/S5/S9 |
| #26 | G1 | 신규 High/D1/D1-a/S15, G4 복원 |
| #27 | G1 | H09/#16/S13 |
| #28 | G1 | M07/S8 |
| #29 | G2 | M13/D8/S12, G5 최종 설정 |
| #30 | G2 | M14/D7/S18 |
| #31 | G1 | 신규 High; G0 v24, G4 백업, D10/D11/D12/S16 |

### V1~V7 별도 확인

| 항목 | 담당·판정 증거 |
|---|---|
| V1 평문 백업/삭제 후 잔존 | G4 실제 파일, G5 안내·D9 결정 |
| V2 재설치 후 이전 백업 자동 탐지 한계 | G4 삼성 등 실기기 MediaStore/SAF, G5 안내 |
| V3 Android10 미만 백업 미지원 | G4 상태 처리, G5/E API28 안내 |
| V4 초기화 지연·실패·release 오류 원문 | G0 #8과 함께 필수, G6 release 확인 |
| V5 운영 Firestore rules | G2 로컬 검증, G5 사용자/권한 있는 담당자 Console 대조 |
| V6 boot/update/exact 재허용 | G0 native migration 기반, G1 receiver·강제갱신, G6 실제 OS |
| V7 exact 권한 선언·Play 자격 | G1 API31 권한 경로, G5 실제 Console 신고 확인 |

## 9. 새 AI 세션에 넘길 작업 지시문

**교대 실행(§11)에서는 §11.4의 짧은 지시문을 쓴다.** 아래 문장은 그 지시문이 가리키는 공통 금지사항이며, 그룹 전체를 한 세션에 처음 배정할 때도 쓸 수 있다. 그룹/기준 커밋/worktree만 채운다. 현재 문서 하나로 작업을 시작하되 해당 그룹의 근거 문서·실제 코드를 읽는 과정은 생략하지 않는다.

> 이 프로젝트의 `출시전_수정작업_그룹별_실행계획_및_세션인계_2026-09-14.md`를 먼저 읽어라. 담당은 **G__**, worktree는 **___**, 기준 커밋은 **___**이다. 사용자가 이번에 승인한 수정 범위는 해당 그룹이다. 최종 v4 최신 정정과 §4 확정 결정만 구현하라. D7·D8의 후속 판단(S9·S12 결과 이후)은 임의로 정하지 말고 사용자에게 보고하라. 먼저 cwd/branch/status/기준 SHA와 읽기·쓰기 범위를 확인하라. 다른 그룹 소유 파일·공통 설정은 직접 수정하지 말고 자기 그룹 integration_requests.md에 구체적으로 요청하라. 제품 코드 수정 완료 후 동결 상태로 인계하고, 사용자가 정한 전체 수정/테스트 단계에 따라 테스트하라. 수정과 테스트를 동시에 진행하지 마라. 통과하지 않은 실기기·Console 항목은 NOT_RUN으로 남겨라. 임의 merge/push/운영 rules 배포/Play 게시를 하지 마라. 최종 보고에는 변경 파일, 해결 번호, 계약 변경 요청, 테스트 대상 SHA와 증적, 남은 결정·잔여 위험을 적어라.

그룹 handoff에는 최소 다음을 작성한다.

```text
그룹 / 담당 / worktree / branch:
공통 기준 SHA / 최종 소스 SHA:
상태: CODING | CODE_FROZEN | TESTING | PASS | FAIL | BLOCKED | INTEGRATED
수정한 감사 번호:
실제 변경 파일(소유권 범위 검사 결과):
공통 파일/API/ARB 연결 요청 및 반영 SHA:
사용자 결정 확인(D 번호·선택·일시):
최종 동작과 회귀 금지 규칙 준수:
추가/변경 테스트와 실제 실행 명령:
테스트 결과(실행 빌드·환경·expected/actual·증적):
미실행 실기기/Console 항목:
잔여 위험·다음 그룹 주의사항:
진행 기록(끊김 대비 — 하위 작업마다 한 줄: 일시 / 담당 AI / T 번호 / 감사 번호 / 커밋 SHA / 다음에 할 일):
```

## 10. 문서 확정과 구현 완료의 구분

이 문서는 **작업 배정용으로 사용할 수 있는 최종 인계 기준**이다. D1~D12는 2026-09-14에 모두 결정됐지만(§4), D7·D8의 후속 판단, 실기기 검증, 운영 Console 사실 확인까지 끝났다는 뜻은 아니다. G0는 확정된 기반으로 시작할 수 있고, 각 후속 그룹은 자신의 선행조건과 결정 상태를 확인한다.

모든 그룹 통합 뒤에는 수정 범위 밖의 보류 항목까지 포함해 최종 판정표를 남긴다. “원 감사에서 빠진 항목 없음”은 위 대조표의 **추적 가능성**을 뜻하며, 모든 결함이 수정·검증됐다는 뜻이 아니다. 최초 작성 때는 이 계획 문서만 추가했다. 2026-09-14 2차 개정에서는 사용자 결정(D5~D9·D11·D12 확정, 교대 실행)만 이 문서·v4 9장에 반영했고 앱 코드는 수정하지 않았다.

## 11. 교대 실행 — 작업 목록과 규칙 (2026-09-14 사용자 결정)

두 AI(Claude Code, ChatGPT Codex)는 **동시에 작업하지 않는다.** 사용자가 한 번에 한 AI에게만 작업을 준다. 한 AI가 토큰 한도로 멈추면 리셋을 기다리는 동안 다른 AI가 다음으로 가능한 작업을 한다. 멈춘 작업은 원래 AI가 복귀해 마무리한다.

### 11.1 역할

| 역할 | 담당 |
|---|---|
| 통합 담당자 (`contracts.md`, worktree 생성, 공통 파일 연결, merge) | Claude Code |
| G0 / G1 / G4 수정, G5 설정 수정 | Claude Code (다른 AI에게 넘기지 않음) |
| G2 / G3 수정, 준비 문서, 교차 리뷰·테스트, 최종 판정 | ChatGPT Codex |
| AI 교대 지시, 실기기 테스트, Play/Firebase Console 작업, push·배포 승인 | 사용자 |

**왜 이렇게 나누는가:** G0·G1·G4는 DB 마이그레이션·알람 핵심·복원 잠금이 서로 얽혀 있어 §2.1대로 한 소유자가 끝까지 맡는다. G2·G3는 다른 그룹과 가장 덜 얽혀 따로 맡기기 좋다. G1은 코드 규모(약 12,500줄, 감사 번호 16개)가 다른 그룹보다 훨씬 커서 여러 번 끊길 것을 전제로 한다. 그 사이 Codex는 G2·G3와 준비 문서를 진행하고, 작업이 없으면 기다린다. **G1이 오래 걸린다는 이유로 G1을 Codex에게 나눠 주지 않는다.**

### 11.2 교대 규칙

1. **작업 단위는 11.3의 T 번호다.** 각 작업에는 주 담당이 있다. 주 담당이 멈춰 있으면 다른 AI는 `대체` 칸에 자기 이름이 있고 선행 조건을 충족한 작업만 한다.
2. **시작한 수정 작업(CODING)은 끝날 때까지 시작한 AI의 것이다.** 다른 AI는 그 worktree를 읽기만 한다. 사용자가 명시적으로 넘기라고 할 때만 넘기며, G0·G1·G4는 넘기지 않는다.
3. **동시에 실행하지 않아도 그룹별 worktree·브랜치를 유지한다.** 토큰 소진으로 예고 없이 끊기면 미커밋 변경이 남는다. 다른 AI가 같은 폴더에서 다른 그룹을 작업하면 그 변경과 섞인다. 경로 예: `C:\Users\Administrator\Desktop\shiftbell-g0`, `-g1`, `-g2`, `-g3`, `-g4`.
4. **언제든 끊길 수 있다고 보고 작게 나눠 일한다.**
   - 감사 번호 하나(또는 의미 있는 하위 단계)를 끝낼 때마다 자기 그룹 `handoff.md`의 진행 기록에 한 줄을 적는다. 그다음 자기 그룹 브랜치에 로컬 커밋한다(예: `wip(G1): #3 회차별 종료 예약`).
   - 그룹 브랜치의 로컬 커밋은 이 계획으로 허용한다. merge는 통합 담당자만 하고, push는 사용자 승인 후에만 한다.
5. **복귀(이어하기) 절차.** 먼저 해당 worktree의 `git status`, `git diff`, `git log -3`을 확인한다. `handoff.md` 마지막 기록과 `task_board.md`도 읽는다. 마지막 기록 이후 **기록 안 된 변경**이 있으면 끊기기 직전 반쯤 고친 파일일 수 있다. 그 변경부터 검토해 완성하거나 되돌린 뒤 진행한다.
6. **작업판.** 작업을 시작·중단·완료할 때 `docs/release_audit/task_board.md`의 해당 줄을 갱신한다(상태·담당 AI·마지막 SHA·한 줄 메모). 두 AI가 동시에 실행되지 않으므로 이 파일 하나를 공유한다.
7. **공유 문서의 위치.** 이 계획 문서, v4, `docs/release_audit/` 전체(`contracts.md`, 작업판, 각 그룹 handoff/test_results/integration_requests)는 **메인 폴더 `C:\Users\Administrator\Desktop\shiftbell`의 것만** 읽고 쓴다. worktree 안의 사본은 커밋 시점 것이라 낡을 수 있다. worktree에는 제품 코드와 테스트 코드만 쓴다. 메인 폴더의 이 문서들은 통합 담당자가 단계가 끝날 때 `dev`에 커밋한다.
8. **메모리(호스트 RAM 5.9GB, 2026-09-14 확인).** 교대하기 전에 빌드·테스트를 한 AI는 Gradle 데몬(`gradlew --stop`)과 에뮬레이터를 종료한다. 앞 AI가 남긴 프로세스 때문에 다음 AI의 빌드가 메모리 부족으로 실패하지 않게 하기 위함이다.
9. **테스트 작업이 끊기면** 끝난 케이스까지만 결과를 기록하고 나머지는 `NOT_RUN`으로 둔다. 테스트 작업(제품 코드 수정 없음)은 `대체` 칸의 AI가 같은 대상 SHA에서 이어받을 수 있다.
10. **테스트 FAIL로 제품 코드 수정이 필요하면** 다음 순서로 되돌린다.
    1. 진행 중인 다른 테스트는 끝난 케이스까지 기록하고 멈춘다.
    2. 해당 그룹 소유 AI가 수정한다(CODING).
    3. 재동결한다.
    4. 통합 담당자가 연결 커밋을 갱신한다.
    5. 영향받는 테스트를 새 SHA로 재개한다.

    §0-4의 “수정과 테스트를 동시에 진행하지 않는다”는 교대 실행에서도 **단계 단위**로 지킨다.
11. **작성자와 검증자를 나눈다.** 가능하면 코드를 짜지 않은 AI가 기대값·리뷰·최종 판정을 맡는다. 작성자가 불가피하게 스스로 테스트하면 `test_results.md`에 “작성자 자체 검증”이라고 적는다.

### 11.3 작업 목록

| T | 단계 | 작업 | 주 담당 | 대체 | 선행 조건 | 완료 기준 |
|---|---|---|---|---|---|---|
| T00 | 0 | 준비: 이 문서·원 감사·v4를 `dev`에 커밋, `docs/release_audit/` 뼈대와 `task_board.md` 생성, `android/gradle.properties` JVM 메모리를 실제 RAM에 맞게 조정 | Claude | — | 없음 | `dev` 커밋 SHA를 작업판에 기록 |
| T01 | 1 | G0 fixture 초안: git 이력에서 v12·14·15·17·18·23의 onCreate/onUpgrade를 찾아 현실적 샘플 데이터가 든 과거 DB 생성 SQL 작성. 현재 마이그레이션 SQL을 복사해 expected를 만들지 않음. 제품 코드 수정·실행 없음 | Codex | Claude | 없음 | `docs/release_audit/g0/fixtures_draft/` |
| T02 | 1 | G0 수정 (#1, #8, #31 스키마, V4) — worktree `shiftbell-g0` | Claude | — | T00 | CODE_FROZEN, handoff |
| T03 | 1 | G0 테스트 코드 작성·실행 (T01 fixture 사용, S1의 R/F/E 부분) | Claude | Codex | T01, T02 동결 | `g0/test_results.md` |
| T04 | 1 | 실기기 S1 (업데이트 후 앱 미실행·첫 잠금 해제 전 울림) | 사용자 | — | T03 | 결과 기록. 미완이면 §2.2의 `PROVISIONAL` 상속 |
| T05 | 1 | `contracts.md` 확정, G0를 `dev`에 병합해 기준 커밋 생성, `shiftbell-g1/g2/g3` worktree 생성 | Claude | — | T03 PASS | 기준 SHA를 작업판에 기록 |
| T06 | 2 | G1 수정 (§5 G1 하위 작업 순서대로). 여러 번 끊겨도 Claude가 이어감 | Claude | **없음** | T05 | CODE_FROZEN |
| T07 | 2 | G2 수정 (D7 앱 측 상태 머신, D8은 S12 조사까지) | Codex | Claude | T05 | CODE_FROZEN |
| T08 | 2 | G3 수정 (G0 기준 커밋에서 시작, G2 소스 미사용) | Codex | Claude | T05 | CODE_FROZEN |
| T09 | 2 | 읽기 전용 준비 문서: ① G4용 DB/prefs/OS writer 전수 목록 ② S17 실패 주입 테스트 설계 ③ G5용 실제 데이터 수집·전송 목록(Analytics·Firestore·백업). ①은 G1·G3 동결 후 한 번 더 대조 | Codex | Claude | T05 | `g4/prep_*.md`, `g5/prep_*.md` |
| T10 | 3 | 그룹별 연결 요청 반영(`main.dart`, ARB, Gradle 등), 연결 동결 커밋, 빌드 가능 확인 | Claude | — | T06·T07·T08 동결 | 연결 SHA |
| T11 | 4 | G1 교차 리뷰(§5 G1 회귀 금지 규칙 대조) + P1·override의 Dart/Kotlin 공통 fixture 기대값 작성 | Codex | — (작성자 분리) | T10 | `g1/review.md`, fixture |
| T12 | 4 | G1 테스트 실행 (F/R/E) | Claude | Codex | T10, T11 | `g1/test_results.md` |
| T13 | 4 | G2 테스트 (F, Firestore rules emulator, E) | Claude | Codex (자체 검증 표시) | T10 | `g2/test_results.md` |
| T14 | 4 | G3 테스트 (F/R/E) | Claude | Codex (자체 검증 표시) | T10 | `g3/test_results.md` |
| T15 | 4 | 실기기 S2·S3·S4·S14, 두 기기 S9 | 사용자 | — | T12~T14 | 결과 기록 |
| T16 | 4 | S9·S12 결과를 사용자에게 보고하고 D7(tombstone/rules)·D8(dev Firebase 분리) 후속 선택 받기. 채택 시 G2를 CODING으로 되돌림 | Codex | Claude | T13, T15 | §4 표에 후속 선택 기록 |
| T17 | 5 | G1 → G2 → G3 순차 통합, 경계 회귀, G4 기준 커밋, `shiftbell-g4` 생성 | Claude | — | T12~T16 | G4 기준 SHA |
| T18 | 6 | G4 수정 (§5 G4 하위 작업 순서대로) | Claude | **없음** | T17 | CODE_FROZEN |
| T19 | 6 | G4 테스트 코드 작성·실행 (T09 설계 기반 S17 실패 주입, S5·S6) | Codex | Claude | T18 동결 | `g4/test_results.md` |
| T20 | 6 | 실기기 S5·S17, 재설치 V2 | 사용자 | — | T19 | 결과 기록. G1 영향 항목 재실행 여부 판단(§5 G4) |
| T21 | 7 | G5 설정 수정: 광고 ID dev/prod 분리 구조, Manifest/Gradle, S10에서 재현된 레이아웃 결함, T22 초안의 앱 내 반영. 실제 광고 ID 교체는 최종 배포 직전 | Claude | — | T20 | CODE_FROZEN |
| T22 | 7 | G5 문서 초안: 개인정보 안내 ko/en, Play 데이터 보안 양식 대조표 (T09 ③ 기반). `docs/`에만 작성 | Codex | Claude | T20 | `g5/privacy_draft.md` |
| T23 | 7 | G5 테스트: S10 화면 매트릭스, prod release 산출물 확인 | Codex | Claude | T21, T22 동결 | `g5/test_results.md` |
| T24 | 7 | Play Console·Firebase Console 실제 값 확인 (S12, V5, V7) | 사용자 | — | T21 | 확인자/일시 기록 |
| T25 | 8 | G6 통합 시나리오 실행 (§5 G6의 연결 시나리오) | Claude | Codex | T23, T24 | `g6/` 결과 |
| T26 | 8 | G6 최종 판정 `release_gate.md` | Codex | Claude (판정자 분리 불가 표시) | T25, 최종 실기기 | 출시 게이트 판정 |

선행 조건이 충족된 작업이 둘 이상이면 번호가 앞선 것부터 한다. 사용자 작업(T04·T15·T20·T24)은 AI 교대와 무관하게 언제든 진행할 수 있다.

### 11.4 교대 지시문

**새로 시작할 때** — 사용자가 어느 AI에게든 T 번호만 바꿔서 준다.

> `C:\Users\Administrator\Desktop\shiftbell\출시전_수정작업_그룹별_실행계획_및_세션인계_2026-09-14.md`를 먼저 읽어라. 최소한 §0, §2~§4, §11, 그리고 담당 그룹의 §5 절을 읽어라. 이번 작업은 **§11.3의 T__**이다. `docs/release_audit/task_board.md`와 해당 그룹 `handoff.md`를 읽고 선행 조건이 충족됐는지 확인하라. 충족되지 않았거나 이 작업의 주 담당/대체 담당이 네가 아니면 작업하지 말고 보고하라. §11.2 교대 규칙과 §9 공통 금지사항을 따르라. 끝나거나 멈출 때 작업판과 handoff를 갱신하라.

**끊긴 작업을 이어갈 때** — 위 문장 끝에 다음을 붙인다.

> 이 작업은 이전에 토큰 한도로 중단됐다. §11.2-5 복귀 절차부터 수행하라.

**Codex에게 무엇을 줄지 모를 때** — Codex에게 “작업판을 보고 네가 주 담당 또는 대체 담당이고 선행 조건이 충족된 가장 앞 번호 작업을 찾아 보고하라”라고 먼저 묻는다. 그 결과를 보고 T 번호를 지정한다.
