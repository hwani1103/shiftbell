<?xml version="1.0" encoding="utf-8"?>
<!-- WaveGradientView.kt 전용 커스텀 속성. -->
<resources>
    <declare-styleable name="WaveGradientView">
        <!-- 아래쪽 모서리만 둥글게(오버레이 카드용). 0이면 전부 각짐(잠금화면용). -->
        <attr name="bottomCornerRadius" format="dimension"/>
    </declare-styleable>
</resources>
