<!-- android/app/src/main/AndroidManifest.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- 기존 권한들... -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.RECEIVE_LOCKED_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        tools:replace="android:label">
        
        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:directBootAware="true">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- AlarmActivity -->
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity="com.hwani1103.shiftbell.alarm"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait"
            android:noHistory="true"
            android:finishOnTaskLaunch="true"
            android:directBootAware="true"/>
        
        <!-- AlarmOverlayService -->
        <service
            android:name=".AlarmOverlayService"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"/>
        
        

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- CustomAlarmReceiver -->
        <receiver 
            android:name=".CustomAlarmReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"/>
        
        <!-- DirectBootReceiver -->
        <receiver
            android:name=".DirectBootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <!-- ⭐ 2026-09-14 (출시전 감사 V6, T10 연결) - 잠금 해제 전 부팅만이 아니라 잠금 해제 후 부팅(LOCKED_BOOT가
                 안 오는 기기/경로), 앱 업데이트 직후, 정확한 알람 권한 허용 순간에도 알람·일정 알림을 즉시 다시 맞춤.
                 수신 처리: DirectBootReceiver.kt (g1/integration_requests.md #1). 세 액션 모두 시스템이 보내므로
                 receiver의 RECEIVE_BOOT_COMPLETED 발신자 권한 조건을 만족함. -->
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>
        <!-- ⭐ 신규: AlarmRefreshReceiver 추가 -->
    <receiver
        android:name=".AlarmRefreshReceiver"
        android:enabled="true"
        android:exported="false"
        android:directBootAware="true">
        <intent-filter>
            <action android:name="com.hwani1103.shiftbell.REFRESH_ALARMS"/>
        </intent-filter>
    </receiver>
        
        <!-- ⭐ AlarmGuardReceiver 신규.
             TIME_SET/TIMEZONE_CHANGED는 OS만 보낼 수 있는 protected broadcast라
             exported="false"여도 정상 수신됨 (서드파티 앱이 위조 불가). 시스템
             시계/시간대가 바뀌어도 이미 예약된 알람은 "그 당시" 계산된 절대 시각
             그대로 유지되므로, 다음 자정/20분전 트리거를 기다리지 않고 즉시
             재계산하기 위해 추가함. -->
        <receiver
            android:name=".AlarmGuardReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
            </intent-filter>
        </receiver>
        
        <!-- ⭐ AlarmActionReceiver 신규 -->
        <receiver
            android:name=".AlarmActionReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"/>

        <!-- ⭐ 홈 화면 캘린더 위젯. exported=true 필요(런처가 APPWIDGET_UPDATE를 보냄) -
             표준 위젯 요구사항이고 커스텀 액션을 안 받으니 위조돼도 위험한 동작 없음. -->
        <receiver
            android:name=".CalendarWidgetProvider"
            android:enabled="true"
            android:exported="true"
            android:label="교대시계 달력">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/calendar_widget_info"/>
        </receiver>

        <!-- ⭐ 실제 수면 기록 위젯("C번 요구사항", 캘린더 위젯과 완전 별개).
             exported=true 필요(런처가 APPWIDGET_UPDATE를 보냄) - 표준 위젯 요구사항. -->
        <receiver
            android:name=".SleepWidgetProvider"
            android:enabled="true"
            android:exported="true"
            android:label="교대시계 수면기록">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sleep_widget_info"/>
        </receiver>

        <!-- ⭐ 수면 위젯의 취침/기상 버튼 처리 - 앱 내부에서만 보내는 커스텀 액션이라
             exported=false. 2026-09-01 - 토글 액션 1개 → 명시적 액션 2개(SLEEP/WAKE)로 재설계. -->
        <receiver
            android:name=".SleepWidgetActionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hwani1103.shiftbell.SLEEP_WIDGET_SLEEP"/>
                <action android:name="com.hwani1103.shiftbell.SLEEP_WIDGET_WAKE"/>
            </intent-filter>
        </receiver>

        <!-- ⭐ 실제 수면 기록 자동 추정 - SleepDetectionScheduler가 예약하는 정확한
             알람 수신 전용. 앱 내부(AlarmManager)에서만 발생시키는 알람이라 exported=false. -->
        <receiver
            android:name=".SleepDetectionReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"/>

        <!-- ⭐ 일정관리 탭 "일정에 맞춰서 알림받기" - ScheduleNotificationScheduler가
             예약하는 정확한 알람 수신 전용. 앱 내부(AlarmManager)에서만 발생시키는
             알람이라 exported=false. directBootAware - 재부팅 직후(DirectBootReceiver의
             재예약)에도 그 예약이 실제로 울릴 수 있어야 함. -->
        <receiver
            android:name=".ScheduleNotificationReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true"/>

        <!-- ⭐ 2026-08-24 추가 - AdMob 앱 ID.
             이 meta-data가 없으면 AdMob SDK가 앱 시작 시점에 의도적으로 크래시시킴.
             지금 값은 구글이 공개한 "테스트 전용" 앱 ID로, 계정 없이 쓸 수 있음.
             ⚠️ 실제 배포 전 본인 AdMob 앱 ID로 교체할 것
             (lib/constants/ad_config.dart의 kBannerAdUnitId도 같이 교체). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}"/>

        <!-- ⭐ 2026-09-14 (출시전 교차 검토 X-14) - Firebase Analytics 초기 수집 여부. dev flavor는 false, prod는 true
             (build.gradle.kts manifestPlaceholders). Dart의 setAnalyticsCollectionEnabled(firebase_bootstrap.dart)보다
             먼저 적용돼 첫 실행·초기화 실패·Native만 뜨는 경로에서도 dev 설치가 운영 통계에 섞이지 않게 함. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="${analyticsCollectionEnabled}"/>

    </application>

    <queries>
        <intent>
            <action android:name="android.settings.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
        </intent>
    </queries>
</manifest>