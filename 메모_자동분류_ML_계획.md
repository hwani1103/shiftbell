# 메모 자동 범주화(온디바이스 ML) 계획

> 2026-08-27 갱신. **Phase 0~5 완료.** Phase 6(근무 스케줄 피처)/Phase 7(배포 후
> 재학습 루프)은 **사용자 결정으로 보류** — 필요해지면 아래 "Phase 6~7 (보류)"부터
> 다시 시작. 상세는 "Phase 4~5 완료 기록(2026-08-27)" 섹션 참고.

---

## 최종 결과 (Phase 0~3, 2026-08-26)

### 데이터
최종 **5,464개** (목표 5,000 초과 달성). 500(Claude 합성) → 5,000(Claude 합성, 4라운드) →
**5,464(사용자 직접 작성 500개 반영)** 순으로 확장. 전부 사람이 직접 작성/검수
(대량 LLM 생성 아님), 중복 0건.

| 파일 | 개수 | 용도 |
|---|---|---|
| `ml/data/pilot_dataset.jsonl` | 5,230 | 카테고리별 순수 예문 (사용자 제공 500개 포함) |
| `ml/data/boundary_cases.jsonl` | 174 | 6개 경계쌍(운동↔여가, 가족↔사교, 식사↔사교, 업무↔공부, 쇼핑↔가족 등) + 판단 근거 |
| `ml/data/eval_holdout.jsonl` | 60 | 학습에 안 섞는 독립 문체 평가셋 |
| `ml/data/user_batch1.jsonl` / `user_batch1_raw.txt` | 500 | 사용자가 직접 작성한 원본(추적용, pilot_dataset에 병합됨) |

카테고리별 분포는 448~566개로 고르게 분포(여가/휴식만 의도적으로 더 두껍게 — 초기
파일럿에서 제일 약한 범주였음).

### 모델 아키텍처 — 3단계
1. **키워드 하드매핑** (`ml/keyword_router.py`) — 카테고리_가이드.md의 우선순위 규칙을
   코드로 그대로 옮긴 캐스케이드:
   - 가족 관계어(직계는 무조건, 인척은 "친구/동료/지인"과 같이 있으면 사교로 양보)
   - 사람 만남 키워드 → 사교
   - **당구/볼링처럼 실제로 애매한 종목은 일부러 뺌** — 처음엔 수영/요가/헬스/등산도
     "명확하다"고 판단해 넣었다가, ML이 이미 학습한 강도/뉘앙스 구분(그냥 물놀이 vs
     왕복 20바퀴)을 하드매핑이 덮어써서 정확도가 오히려 떨어지는 걸 확인하고 제외함
     (이 실패 자체가 유의미한 교훈 — "명확해 보이는 키워드"도 실측 없이 하드매핑하면 안 됨)
   - 구매 동사(사기/사러/구매/주문)가 가족 키워드와 같이 있으면 쇼핑이 이김
   - "여자친구/남자친구"가 "친구"의 부분문자열로 오탐되는 것도 별도 처리
2. **TF-IDF(char 2~3gram) + LogisticRegression** (`ml/train.py`, 학습된 모델은
   `ml/models/pilot_v1.joblib`) — 하드매핑에 안 걸린 것만 여기로.
3. **margin(1위-2위 확률차) 기반 기타 폴백** (`ml/predict.py`) — 확신 없으면 기타로.

### 성능
- 내부 테스트(1,081개, 학습 풀에서 분리): **정확도 94%**, 전 카테고리 precision/recall 83%+.
  데이터 500→1,274→2,408→4,964(Claude 합성)→**5,464(사용자 실데이터 반영)**로 늘 때마다
  70%→79%→92%→96%→94%로 개선되다가 실데이터 반영 후 살짝 내려감 — 실사용 어휘가
  합성 데이터보다 다양해서 생기는 자연스러운 현상(더 정직한 수치로 봄, 후퇴 아님).
- 키워드 하드매핑 자체(margin 필터 제외)만으로 boundary_cases 89.1%→90.8%(→실데이터
  반영 후 재측정 89.7%) — 여러 라운드에 걸쳐 순수 이득 유지.
- margin threshold 실측(오답 캐치율 vs 정답 희생률, 정확도 숫자만으론 이 필터의 가치가
  왜곡되어 보여서 따로 측정 — 5,000개 데이터 기준):

  | margin 기준 | 오답 캐치율 | 정답 희생률 |
  |---|---|---|
  | 0.05 | 29% | 7% |
  | **0.08(현재 기본값)** | **57%** | **9%** |
  | 0.10 | 67% | 11% |
  | 0.15 | 86% | 19% |

  실데이터 반영 후 eval_holdout/boundary_cases 정확도(하드매핑만 vs +margin 0.08):
  eval_holdout 88.3%→78.3%, boundary_cases 89.7%→87.4%(margin 필터가 정답을 기타로
  돌리는 비용이 예상대로 반영됨). 0.08~0.10을 잠정 기본값으로 유지하되, 데이터가 더
  쌓이거나 재학습하면 이 수치도 달라지므로 **Phase 4~5에서 재튜닝 전제**.

### 비정제 실사용 문체 테스트 (총 35문장, 2026-08-26)
사용자가 직접 던진 오타/은어/다중활동 문장으로 확인:
- 확신도(margin)가 정오답을 실제로 잘 구분함을 재확인 — 명백한 오답들은 margin이
  거의 0에 가까웠음("엄마한테 전화" 오분류, "로또 구매" 오분류 등, 하드매핑 이전 기준).
- **"운동 하러가기 전에 엄마한테 전화하기"** 같은 복합 문장에서 활동 키워드보다 가족
  키워드가 우선하는지가 쟁점이었고, 우선순위 캐스케이드로 해결 확인.
- 테니스/골프/축구 등 흔한 종목은 하드매핑 없이도 ML이 13/15 정확히 잡음(단
  당구·볼링은 설계대로 애매하게 나옴 — 실제로 학습 데이터에서도 상황에 따라 갈리게
  만들어놨기 때문).

### 2026-08-26 추가 — 사용자 제공 실사용 데이터 500개 반영

사용자가 카테고리당 정확히 50개씩(총 500개) 직접 작성한 실사용 문체 데이터를 받아
병합. **최종 데이터셋 5,464개** (pilot 5,230 + boundary 174 + holdout 60) — 목표 5,000 초과.
내부 테스트(1,081개) 정확도 94%(합성 데이터만일 땐 96%였는데, 실데이터가 섞이며 살짝
내려간 건 자연스러움 — 실사용 어휘가 제 합성 데이터보다 다양해서 오히려 더 정직한 수치).

**병합 전 사용자 라벨 vs 하드매핑 규칙 크로스체크로 진짜 버그를 여러 개 찾음** (실사용
데이터가 아니었으면 못 잡았을 것들):
- `아이` 접두어 매칭이 "아이디어/아이언"과, `아내`가 "닦아내기"와, `형`이 "정형외과"와,
  `동기`가 "비동기"와 충돌 → 어절 전체가 그 단어(+흔한 조사/복수형)일 때만 매칭하도록
  전면 수정(`keyword_router.py`의 `_word_matches_token`)
- "골프공"(발마사지 도구)이 "골프" 운동 키워드와 충돌 → 예외 처리 추가
- **가장 중요한 발견**: "형/누나/언니/오빠/동생"이 혈연이 아니라 **친한 사람을 부르는
  호칭으로도 매우 흔하게 쓰임**("친한 형 가게 오픈해서 축하금 송금"을 사용자는 사교로
  분류 — 친형이 아님). 이 4개는 하드매핑 대상에서 완전히 빼고 ML 판단에 맡기도록 변경.

**버그를 다 고친 뒤에도 남은 9개는 코드 문제가 아니라 규칙 자체의 판단 차이** — 우선순위
규칙(가족 > 사교 > 활동특정형)을 다시 논의해볼 지점으로 기록만 해둠(아직 규칙 변경 안 함):
- "동호회 참석해서 경기 뛰기"류 → 사용자는 운동으로 봄(활동이 뚜렷하면 사교보다 우선?)
- "동료랑 점심/술자리"류 → 사용자는 식사로 봄(이전에 저희가 정한 규칙과는 반대)
- "본가 어머니가 싸주신 김치로 찌개 끓이기" → 가족 언급이 배경 서사일 뿐 핵심 행위가
  아닌 경우, 하드매핑은 이 구분을 못 함(키워드 존재만으로는 근본적 한계)
- "보드게임 카페 예약해서 친구들과" → 사용자는 여가/휴식으로 봄(활동 구체성이 사교보다
  우선하는 것으로 보임)

→ 이 4개 패턴이 반복되는 걸 보면, "활동이 구체적이고 뚜렷할수록 사교보다 그 활동
쪽으로 판단하는" 경향이 실사용자 직관에 있는 것 같음. 우선순위 규칙을 이 방향으로
조정할지는 데이터가 더 쌓인 뒤 사용자와 논의해서 결정.

### 알려진 한계 (문서화만, 지금 당장 고치는 항목 아님)
- **짧은 문장(1~2단어)은 구조적으로 margin이 낮다** — "넷플릭스"처럼 짧으면 정답이어도
  다른 후보와 확률 차이가 잘 안 벌어져서 기타로 더 자주 빠짐. 실사용자가 짧게 쓰는
  경향이 있다면 체감 기타 비율이 이 문서의 수치보다 높을 수 있음.
- **인척 호칭(매형/사위/동서 등)은 학습 데이터에 각 단어가 2~4번씩만 등장** — 데이터
  희소성으로 여전히 불안정. 실사용 피드백이 쌓이면 우선 보강 대상.
- **개인 사이드프로젝트/사업 메모는 전부 기타로 감** — "일정관리 UI 확정하기",
  "Shiftbell 사용자 확보 전략 수립" 등 4개 테스트 전부 기타. 버그는 아니고 "업무"를
  처음부터 "회사 근무 중 이벤트"로 좁게 정의했기 때문(근무 스케줄 타임라인과 중복 방지
  목적). 다만 Shiftbell 자체가 1인 개발 앱이라 개발자 본인이 쓰기엔 사각지대일 수 있음 —
  실사용 후 이 범위를 넓힐지 재검토 여지 있음.
- 여전히 반복되는 미세 오분류: "부서 회식"류(식사 vs 사교), "요가원 명상 수업"류
  (운동 vs 여가) — 데이터를 더 넣어도 몇 라운드째 완전히 안 없어짐. 우선순위 규칙
  자체의 미묘한 지점으로 보이며, 100% 해소보다는 margin 필터로 안전하게 기타 처리하는
  쪽으로 받아들임.

---

## 목적

사용자가 날짜별 메모(`date_memos`)를 입력할 때, 별도 선택 없이 앱이 문장을 분석해
카테고리를 자동 판단하고 아이콘으로 표시. 서버/LLM API 호출 없이 **온디바이스**로
1초 이내 처리하는 것이 목표. 배경 및 대안 검토 내용은 이 대화 로그 참고
(별도 문서화 안 함 — 결론만 아래에 반영).

**핵심 제약 인지**: TF-IDF+Logistic Regression 계열은 표면 토큰 통계 기반이라
"명시적 키워드 없는 완전히 새로운 표현"에 대한 일반화 능력이 없다. 학습 데이터에
없던 참신한 간접 표현은 계속 틀릴 수 있다는 걸 전제로 두고, 데이터 다양성으로
커버리지를 넓히는 전략을 쓴다. (Phase 3에서 정확도가 기대 이하면 소형 임베딩
모델로 확장 검토 — 아래 "예상 용량" 참고)

---

## 범주 설계 (9개 + 기타 = 10개)

| 범주 | 아이콘 | 정의 |
|---|---|---|
| 업무 | 💼 | 구체적 업무 이벤트(회의/보고/마감/출장). 근무 시간대 자체는 스케줄 타임라인 기능이 이미 표시하므로, 이 범주는 "근무 중 특정 이벤트"로 좁게 정의 |
| 공부 | 📚 | 자격증/시험/강의/독서/스터디 |
| 운동 | 🏃 | 신체 활동 자체가 목적(러닝/헬스/요가/구기/등산) |
| 병원·건강관리 | 🏥 | 진료/약/검진/치과/헌혈 등 관리·치료 목적 (운동과의 구분 기준: 단련이 목적이면 운동, 관리/치료가 목적이면 이쪽) |
| 식사 | 🍽 | 끼니 관련, 사람을 만나는 게 목적이 아닌 경우 |
| 약속/사교 | 🤝 | **사람을 만나는 것 자체**가 목적. 활동이 곁들여져도 만남이 핵심이면 우선 (예: "친구랑 볼링"→사교, "혼자 볼링"→여가) |
| 가족 | 👨‍👩‍👦 | 가족 구성원 언급 시 최우선 |
| 쇼핑 | 🛒 | 구매/장보기 |
| 여가/휴식 | 🌿 | 취미·영화·카페·나들이(능동) + 낮잠·늘어지기(수동) 통합. 짧은 메모에서 둘을 구분할 신호가 약해 v1은 병합, 데이터 쌓이면 분리 재검토 |
| 기타 | ❔ | 위에 명확히 속하지 않거나 모델 확신도 낮음 |

**우선순위(라벨 충돌 해소):** 가족 > 약속/사교 > (운동/공부/식사/업무/쇼핑 등 활동 특정형) > 여가·휴식 > 기타

이 표를 데이터 생성 프롬프트 + 사람 검수 기준으로 그대로 재사용한다.

---

## 근무 스케줄 데이터 활용

- **지금 스키마로 바로 가능**: 메모가 달린 날짜의 근무일/휴무일/근무 종류(`shift_schedule`)를
  범주형 피처로 만들어 TF-IDF 벡터에 concat, 같은 LogisticRegression에 입력. 모델 구조
  변경 없이 가능. Phase 3에서 텍스트 단독 vs 피처 추가 버전을 **비교 평가**해서 실제
  이득이 있는지 수치로 확인 후 채택 여부 결정.
- **근무명별 실제 시각(07~19 등) 타임라인 기능이 붙은 뒤 확장 가능**: 메모에 시각이
  붙으면 "그 시각이 근무 블록 내부인지"까지 피처화 가능. 단, 반드시 **메모가 가리키는
  일정 시각** 기준이어야 하며 **메모를 작성한 시각** 기준이면 안 됨(근무 중에 적은
  개인 용무 메모가 업무로 오분류되는 걸 방지). 현재는 `date_memos`가 날짜 단위라
  이 문제 자체가 발생하지 않음 — 시각 필드가 추가될 때 이 구분을 명시적으로 설계에 반영할 것.
- 부가 이점: 타임라인이 근무 블록을 이미 시각적으로 보여주므로, "업무" 카테고리 정의를
  좁게 유지해도 정보 손실이 없음(중복 표시 방지).

---

## 개발 플랜

### Phase 0 — 범주/가이드라인 확정 ✅ 완료
- `ml/카테고리_가이드.md`에 정리. 진행 중 쇼핑↔가족 규칙 등 계속 보완(총 6개 경계쌍 정의).

### Phase 1 — 파일럿 ✅ 완료
- 500개로 baseline(char n-gram TF-IDF + LogisticRegression) 학습, 내부 테스트 70%.
  여가/휴식이 제일 약한 범주로 확인 → Phase 2에서 우선 보강 결정.

### Phase 2 — 본 데이터셋 ✅ 완료 (목표 대비 99%)
- 4라운드로 나눠 500→1,274→2,408→4,964로 확장. 여가/휴식은 다른 범주의 1.5배로 두껍게.
- `ml/data/eval_holdout.jsonl`(60개, 독립 문체) 확보, 학습에 안 섞음.

### Phase 3 — 모델 학습/평가 ✅ 완료 (근무일 피처 비교는 미실시, "결정해야 할 것" 참고)
- char n-gram TF-IDF + LogisticRegression, 내부 테스트 96%.
- 경계쌍 confusion matrix 분석 → 키워드 하드매핑 레이어 추가 설계/검증(위 "최종 결과" 참고).
- margin 기반 threshold 실측 완료. 임베딩 확장은 **불필요 판단**(96%면 충분, 용량 트레이드오프
  고려해 1차 목표 TF-IDF+LogReg에서 멈추기로 확정 — 사용자 결정, 2026-08-26).

### Phase 4 — 온디바이스 추론 구현 (Dart) ✅ 완료 (2026-08-27)
- `ml/export.py` 작성 — `ml/models/pilot_v1.joblib`(vocab 20,071개 + idf + LogReg
  coef/intercept)를 `assets/ml/memo_category_model.json`(약 2.28MB)으로 export.
  export 직후 `ml/verify_export.py`로 "JSON만으로 sklearn 없이 재현한 예측"이
  원본 파이프라인과 확률 오차 1e-6 이내로 일치하는지 회귀 검증(재학습/재export
  때마다 재실행할 것).
- `ml/keyword_router.py`(하드매핑 캐스케이드) + `ml/predict.py`(TF-IDF→LogReg→
  softmax→margin 폴백)를 `lib/services/memo_category_classifier.dart`로 그대로
  이식. TFLite 등 런타임 의존성 없이 순수 Dart(char_wb n-gram 직접 구현).
- `test/memo_category_classifier_test.dart`로 `ml/predict.py` 실제 출력값과
  1:1 대조하는 19개 테스트 작성 — 전부 통과.
- **2026-08-27 추가 - 하이브리드 하드매핑 확장(짧은 단어 오분류 대응)**:
  실측(`운동`/`독서`/`병원`/`쇼핑`/`휴식` 등 24개 단일 단어 테스트)에서 6개
  (25%)가 '기타'로 빠지는 걸 확인 - `ml/keyword_router.py`에
  `ACTIVITY_PREFIX_KEYWORDS`(공부/독서→공부, 병원→병원·건강관리, 출장→업무,
  식사/외식→식사, 여가→여가/휴식) + `ACTIVITY_CONTAINS_KEYWORDS`(쇼핑→쇼핑,
  "온라인쇼핑" 같은 복합어 대응) 추가. 이걸로 "공부하러가기", "병원가야됨" 같은
  동사결합형과 "병원", "쇼핑" 같은 카테고리 이름 그 자체가 모두 하드매핑됨.
  `ml/check_keyword_regressions.py`(신규)로 전체 라벨 데이터(5,464개) 검증 -
  baseline(하드매핑 히트 1182건, 정답 1037) 대비 hits +86, 정답 +83, 오답 +3만
  늘어남(순증 +80). "업무"/"휴식"은 단독 키워드로 넣었다가 실측에서 충돌 다수
  발견해서 뺐음("은행 업무"→기타인데 강제로 업무, "헌혈 후 휴식"→건강관리인데
  강제로 여가 등 - 수영/헬스처럼 맥락에 따라 갈리는 유형으로 재분류). "독서"는
  "모임" 신호가 같이 있으면 하드매핑을 보류하도록(`ACTIVITY_DEFER_MARKERS`)
  추가 - 실측 데이터에서 "독서모임"이 사교로 분류된 경우가 더 많았음. 부수적으로
  가족 존칭 접미사("님"/"어른"/"댁") 인식 버그도 같이 고침("장모님"이 가족
  하드매핑에 안 걸리던 것). Dart 쪽(`memo_category_classifier.dart`)도 동일하게
  포팅, 테스트 5개 추가(총 19개).
- **DB 스키마 변경함(v20)** — 착수 시점엔 `schedule_management_tab.dart`가 영구
  저장이 없어서 불필요하다고 판단했으나, 같은 날 사용자가 "일정관리도 실제
  DB에 저장"을 요청해서 `date_schedules` 테이블을 신설(v19→v20, 한 버전에
  통합). `predicted_category`/`is_user_corrected` 컬럼도 이때 같이 넣음 - 아래
  Phase 5 참고. `메모_일정기능_설계메모.md`가 계획 중인 `start_time`/`end_time`
  확장은 이 테이블에 아직 없음 - 그 기능 착수 시 이 테이블에 컬럼을 추가하는
  형태가 될 것(테이블 자체는 이미 있으니 새 버전 하나로 끝남).

### Phase 5 — UI 연동 ✅ 완료 (2026-08-27)
- **적용 화면**: 원래 계획은 `date_memos`(달력 탭 메모)였으나, 실제로는
  `schedule_management_tab.dart`(일정관리 탭)의 일정 생성 시트에 연결함 —
  사용자가 아이콘을 직접 안 고르고 저장하면 내용 텍스트로 자동분류해서
  `_kScheduleCategoryIcons` 중 하나를 대신 골라줌(`_CreateBlockSheetState.
  _handleSave`). 아이콘을 직접 고르면 항상 그 선택이 우선.
- **"사용자가 항상 카테고리 수정 가능"**: 만족함 — 아이콘 칩을 언제든 다시 탭해서
  바꿀 수 있음(생성 시점/수정 시점 둘 다).
- **재학습용 "수정 로그 캡처"도 구현함** — `date_schedules.predicted_category`
  (분류기가 처음 제안한 카테고리 키, 사용자가 처음부터 직접 고르면 null)와
  `is_user_corrected`(최종 아이콘이 그 예측과 다르면 true)로 기록. UI엔 아직
  안 보여줌(내부 데이터만) - Phase 7 재개 시 이 필드로 오분류 패턴을 분석하면 됨.
- 모델 로드는 `main.dart`에서 앱 시작 시 `await` 없이 미리 시작해둠(약 2.28MB
  JSON, 첫 프레임을 막지 않음) — 실제 분류 시점에 안 끝났으면 그쪽에서 다시
  `ensureLoaded()`를 await해서 안전하게 기다림.

### Phase 6~7 (보류 — 2026-08-27 사용자 결정)
당장은 불필요 판단, 이 계획만 남겨두고 실제 착수는 안 함. 필요해지면 아래
그대로 재개.

#### Phase 6 — 근무 스케줄 연동
- 위 "근무 스케줄 데이터 활용" 방안을 실제로 통합.

#### Phase 7 — 배포 후 루프
- 사용자 수정 데이터 누적 → 오분류 패턴 분석 → hard example 추가 → 재학습.
- 실사용 메모를 학습에 쓰려면 opt-in 동의 + 익명화 UI 필요
  (이 앱은 지금까지 Analytics도 안 쓸 만큼 외부 전송에 보수적 — 그 기조에 맞출 것).
- Phase 5에서 "수정 로그 캡처"(`predicted_category`/`is_user_corrected`)를 이미
  넣어뒀으므로(2026-08-27 갱신), Phase 7 재개 시 바로 그 데이터를 분석하면 됨.

---

## Phase 4~5 완료 기록 (2026-08-27)

- **정확도 재검증은 안 함** — Phase 3에서 측정한 내부 테스트 94%/boundary_cases
  87~90%는 그대로. Dart 포팅은 "같은 수식을 다른 언어로 옮긴 것"이라 정확도
  자체가 달라질 이유가 없고(실제로 14개 샘플에서 확률 오차 1e-6 이내로 확인),
  포팅 정확성 검증(=같은 입력에 같은 출력)만 했음.
- **모델 정확도를 더 높이는 방법** (텍스트로만 정리, 착수 안 함 — 사용자 요청):
  1. **실사용 데이터 추가 축적** — 지금 5,464개 중 사용자 직접 작성분은 500개뿐.
     특히 인척 호칭(매형/사위/동서 등)은 학습 데이터에 단어당 2~4번만 등장해서
     여전히 불안정 — 우선 보강 대상.
  2. **짧은 문장 문제** — 1~2단어 메모는 구조적으로 margin이 낮아서(다른 후보와
     확률차가 잘 안 벌어짐) '기타'로 자주 빠짐. 일정관리 탭처럼 원래 학습
     데이터(완전한 문장 위주)보다 더 짧고 축약된 텍스트("병원", "회의"처럼)가
     입력될 가능성이 있는 화면일수록 이 문제가 더 크게 체감될 수 있음 — 실사용
     후 짧은 텍스트 전용 규칙/사전 보강을 검토.
  3. **margin threshold 재튜닝** — 지금 0.08은 5,000개 데이터 기준 잠정값. 데이터가
     더 쌓이거나 재학습하면 boundary_cases/eval_holdout으로 다시 스윕해서 최적값
     재확인할 것 (원본 "결정해야 할 것 #5"와 동일 항목).
  4. **우선순위 규칙 재검토** — 실데이터 500개 중 9개가 "가족>사교>활동특정형"
     규칙과 반대로 판단됨("동료랑 점심"→식사, "동호회 경기"→운동 등, "활동이
     구체적일수록 그 활동 우선"하는 경향). 데이터가 더 쌓이면 규칙 자체를
     개정할지 결정(원본 "결정해야 할 것 #7"과 동일).
  5. **Phase 6(근무 스케줄 피처)** — 아직 비교평가 안 함. 텍스트만으로 94%가
     나와서 급하지 않지만, 이득이 있는지는 실측해봐야 앎.
  6. **Phase 7(피드백 루프)** — 가장 확실한 개선 경로. 실사용 오분류를 모아
     hard example로 추가하고 재학습하는 것 — 지금은 보류 상태라 이 경로 자체가
     아직 안 열려 있음(위 Phase 7 항목의 "수정 로그 캡처 먼저" 참고).
  7. **임베딩 모델로 교체** — 마지막 수단. TF-IDF는 표면 토큰 통계라 "학습
     데이터에 없던 완전히 새로운 표현"에 근본적으로 약함. 실사용 후에도 이
     한계가 자주 체감되면 소형 다국어 문장 임베딩(양자화 MiniLM 계열)으로
     교체 검토 — 단 앱 용량이 20~90MB 늘어나는 트레이드오프가 있음(아래
     "예상 용량 증가" 참고, 사용자가 2026-08-26에 "1차 목표(TF-IDF)로 충분,
     임베딩 확장 불필요"로 이미 결론 낸 사안이라 재검토 근거가 확실할 때만).
  8. **"업무" 범위 재검토** — 지금은 회사 업무로 좁게 정의(개인 사이드프로젝트
     메모는 전부 '기타'로 감). 일정관리 탭처럼 실사용자(=개발자 본인)가 직접
     쓰는 화면이 늘어날수록 이 사각지대가 체감될 수 있음.

---

## 예상 용량 증가

현재 릴리스 빌드 실측(2026-08-25 기준, `prodRelease`): AAB **약 60MB**, 단일 APK 약 68MB.
아래는 그 기준선 대비 추가되는 양.

| 구성 | 내용 | 추가 용량(대략) |
|---|---|---|
| **Phase 1~4 (TF-IDF + LogisticRegression)** | vocabulary(~5,000~10,000개 char n-gram) + IDF + 가중치 행렬(10 카테고리 × vocab), 압축/포맷에 따라 편차 | **약 0.3MB ~ 1MB** |
| ㄴ Dart 추론 코드 | 순수 로직, 외부 런타임 없음 | 수십 KB 이하, 무시 가능 |
| **Phase 3에서 정확도 부족 → 소형 임베딩 모델로 확장 시** | 양자화된 다국어 문장 임베딩(int8, 예: MiniLM 계열) + TFLite/ONNX Runtime Mobile 런타임 | **약 20MB ~ 90MB** (모델 선택/양자화 수준에 따라 크게 갈림) |

정리:
- **1차 목표(TF-IDF+LogReg)로 끝나면** 현재 60MB 기준선 대비 **1% 남짓**만 늘어나는 수준 — 무시 가능한 증가.
- **임베딩 모델까지 가야 한다면** 이야기가 다름 — 현재 앱 크기의 **1/3~1.5배**에 해당하는 무게라
  스토어 다운로드 이탈률에 영향을 줄 수 있는 수준. 그래서 Phase 3에서 "TF-IDF로 충분한가"를
  수치로 먼저 확인하는 게 이 결정에서 특히 중요하다 (감으로 판단하지 말 것).
- Play Store는 AAB 기반으로 기기별 최적화 APK를 배포하므로, 위 추가분은 사용자가 실제
  받는 다운로드 용량에 거의 그대로 반영된다(아키텍처 스플릿과 무관한 asset이라).

---

## 관련 파일

| 계층 | 경로 | 상태 |
|---|---|---|
| 카테고리 가이드 | `ml/카테고리_가이드.md` | ✅ |
| 데이터 | `ml/data/pilot_dataset.jsonl`, `boundary_cases.jsonl`, `eval_holdout.jsonl`, `README.md` | ✅ |
| 학습/평가 스크립트 | `ml/train.py`, `ml/evaluate.py` | ✅ |
| 하드매핑 규칙 | `ml/keyword_router.py` | ✅ |
| 최종 예측 파이프라인 | `ml/predict.py` (하드매핑 → ML → margin 폴백) | ✅ |
| 학습된 모델 | `ml/models/pilot_v1.joblib` | ✅ |
| Python venv | `ml/.venv/` | ✅ (scikit-learn 1.9.0) |
| Dart 내보내기 스크립트 | `ml/export.py` | ✅ |
| export 검증 스크립트 | `ml/verify_export.py` (재학습/재export 때마다 재실행) | ✅ |
| 모델(Dart용 JSON) | `assets/ml/memo_category_model.json` (~2.28MB, pubspec 자산 등록됨) | ✅ |
| 추론 엔진(Dart) | `lib/services/memo_category_classifier.dart` | ✅ |
| Dart 테스트 | `test/memo_category_classifier_test.dart` (predict.py 실제 출력과 대조, 19개) | ✅ |
| 활동 키워드 하드매핑 회귀 검사 | `ml/check_keyword_regressions.py` (전체 라벨 데이터 5,464개) | ✅ |
| 화면(적용처) | `lib/screens/schedule_management_tab.dart`(`_CreateBlockSheetState._handleSave`) | ✅ |
| DB 테이블 | `date_schedules`(v20) — `lib/models/date_schedule.dart`, `lib/providers/date_schedule_provider.dart` | ✅ |
| 수정 로그 캡처(재학습용) | `date_schedules.predicted_category`/`is_user_corrected` | ✅ (UI 노출은 아직 안 함) |

---

## 결정해야 할 것

1. ~~여가/휴식 병합~~ → **v1 확정으로 유지.** Phase 1에서 제일 약했으나 Phase 2에서
   데이터를 1.5배로 늘려 해결 확인(recall 66%→93%). 분리 필요성 없음.
2. **근무일 피처는 아직 비교 평가 안 함** — Phase 3에서 하기로 했었는데 실측을
   못 함. Phase 4~6 사이 여유 있을 때 재검토(근본적으로 급한 항목은 아님, 텍스트만으로도
   96% 나왔기 때문).
3. `date_memos`에 시각 필드가 붙는 [[메모_일정기능_설계메모]] 작업과 이 기능의 순서 —
   시각 필드가 먼저 들어오면 "근무 블록 내부 여부" 피처를 바로 쓸 수 있음 (미정)
4. Phase 7 실사용 데이터 수집 동의 UX를 어떻게 넣을지 (별도 opt-in 화면 필요 여부) (미정)
5. **(신규) margin threshold 최종값** — 지금 0.08~0.10을 잠정값으로 썼지만 실측
   기반 최종 확정은 안 함. Phase 4 구현 시점에 boundary_cases/eval_holdout으로 다시
   스윕해서 정할 것.
6. **(신규) "업무" 범위를 개인 사이드프로젝트까지 넓힐지** — 위 "알려진 한계" 참고.
   지금은 회사 업무로 좁게 유지, 실사용 후 판단.
7. **(신규, 2026-08-26) 사교 우선순위를 "활동이 구체적이면 그 활동 우선"으로
   완화할지** — 사용자 실데이터 500개 중 9개가 "동료랑 점심"(식사로 봄),
   "동호회 가서 경기 뛰기"(운동으로 봄) 등 지금 규칙(가족>사교>활동특정형)과
   반대로 판단함. 지금은 규칙을 안 바꿨고 ML 판단에 맡긴 상태. 데이터가 더
   쌓이면(특히 사용자가 추가로 라벨링해줄 실데이터) 이 패턴이 반복되는지 보고
   우선순위 규칙 자체를 개정할지 결정.
