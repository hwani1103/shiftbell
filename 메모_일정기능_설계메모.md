# 메모 → 일정 기능 확장 설계 메모

> 2026-08-24 논의 내용을 보류해둔 것. **아직 결정된 것 없음, 구현 착수 전.**
> 광고(AdMob) 레이아웃 작업을 먼저 하기로 해서 여기 남겨둠.

---

## 하려는 것

메모를 없애고 일정으로 바꾸는 게 아니라, **같은 데이터를 두 가지 방식으로 보는 것**.

- **달력 탭** — 지금처럼 단순하게. 짧은 텍스트만.
- **일정 관리 뷰(신규)** — 같은 메모에 **시간대**를 붙여서 봄. `00~05 취침` 같은 식.

즉 "메모는 메모인데, 일정 뷰에서 볼 때는 시간까지 설정 가능".

---

## 스키마 영향 — v18 → v19 필요

시작/종료 시각은 **새로 저장할 값**이므로 스키마 변경이 필요하다.
(테마 변경이나 화면 재배치 같은 순수 UI 작업과 달리, 이건 DB를 건드려야 한다)

### 현재 `date_memos` (v18)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | |
| `date` | TEXT NOT NULL | `'YYYY-MM-DD'` |
| `memo_text` | TEXT NOT NULL | |
| `order_index` | INTEGER NOT NULL | 사용자 수동 정렬 (0,1,2) |
| `created_at` | TEXT NOT NULL | |

인덱스: `idx_date_memos_date ON date_memos(date)`

### 예상 변경

`start_time`, `end_time` 두 컬럼 추가. **둘 다 nullable** — 그래야 기존 메모가
"시간 없는 메모"로 그대로 살아남고, 달력 탭의 단순 메모도 계속 시간 없이 쓸 수 있다.

`ALTER TABLE date_memos ADD COLUMN start_time TEXT` 두 줄이면 되는 단순한 마이그레이션.

### ⚠️ 놓치기 쉬운 것

**홈 위젯의 Kotlin 코드(`CalendarWidgetScheduleResolver.readMemos`)도 이 테이블을 읽는다.**

위젯에 시간을 표시할 생각이 없더라도, **`DatabaseHelper.kt`의 `DATABASE_VERSION`은
반드시 같이 올려야 한다.** "새 컬럼을 Native가 안 쓰니까 Kotlin은 안 건드려도 된다"고
생각하기 딱 좋은 지점인데, 그게 정확히 세 번 재발한 사고다.
→ `DB_스키마_변경_가이드.md` 참고. 지금은 빌드 가드가 잡아주긴 함.

---

## 결정해야 할 것

### 1. 자정 넘김 (⭐ 제일 중요)

교대근무 앱이라 **야간 근무자의 취침은 대부분 22~06처럼 날짜를 넘긴다.**
그런데 `date_memos`는 `date` 컬럼이 하나뿐이다. 이걸 어느 날짜에 붙일 것인가.

| 안 | 방식 | 장점 | 단점 |
|---|---|---|---|
| A | 시작일 기준 한 줄로 저장, `end_time < start_time`이면 다음날로 해석 | 저장이 단순, 한 일정 = 한 줄 | 달력에서 "6일 새벽"을 볼 때 5일에 걸린 일정이 안 보임 → 조회 시 항상 전날치도 같이 읽어야 함 |
| B | 날짜 넘기면 두 줄로 쪼개 저장 (5일 22~24, 6일 00~06) | 달력·위젯 조회가 제일 단순 (그날 날짜만 읽으면 됨) | 수정/삭제 시 두 줄을 한 덩어리로 묶어 다뤄야 함 |
| C | 자정 넘김 금지 | 제일 단순 | 야간 근무자한테 이 기능의 핵심 용례를 막는 셈 |

**나중에 바꾸기 어려운 결정**이므로 구현 전에 정할 것. C는 사실상 탈락.

### 2. 알람 연동 여부 (⭐ 이번 버전 크기를 가르는 선)

`07:00 병원` 같은 일정에 알람을 걸 수 있게 할 것인가.

- 넣으면: 근무 기반 알람 생성 로직과 **별개 경로가 하나 더** 생긴다. 10일치 롤링 갱신,
  Native 재등록, 이력 기록까지 전부 얽힘. 기능이 확 커진다.
- 논의 시점 권장: **이번엔 빼고 다음으로 미루기.** "간단한 시간 메모"와 "일정 관리 앱"을
  가르는 선이 여기다.

### 3. 정렬

지금은 `order_index`로 사용자가 직접 순서를 잡는다. 시간이 붙으면 시간순이 자연스러운데,
시간 있는 메모와 없는 메모가 섞이면 애매해진다.

- 시간 있는 것 먼저(시간순) → 없는 것 아래(수동 순서)?
- 아니면 일정 뷰에서만 시간순으로 다시 정렬하고 달력 탭은 기존 수동 순서 유지?

### 4. 하루 3개 제한

현재 달력 셀과 위젯 **둘 다 3개까지만** 표시한다 (`memos.take(3)`, 위젯은 셀당 슬롯 3개
하드코딩). 일정 관리로 쓰려면 하루 3개는 금방 모자란다.

→ **저장 개수와 표시 개수를 분리**할지 정할 것 (저장은 무제한, 달력 셀엔 3개 + "+2" 식)

### 5. 달력 탭에 시간을 보여줄지

"간단하게"라고 했는데, 완전히 숨길지 / `05 취침`처럼 앞에 시각만 붙일지에 따라
셀 폭 압박이 달라진다. 달력 셀은 이미 좁다.

### 6. 위젯 반영 여부

넣으면 Kotlin 작업이 붙는다. 위젯 레이아웃이 **셀마다 메모 슬롯 3개씩 하드코딩**된
구조(`memo1_0_0` ~ `memo3_5_6`, 총 126개 뷰 ID)라, 표시 형식을 바꾸는 게 생각보다 크다.

---

## 관련 파일

| 계층 | 파일 |
|---|---|
| 모델 | `lib/models/date_memo.dart` |
| 상태 | `lib/providers/memo_provider.dart` (변경 시 `WidgetRefreshService.refresh()` 호출함) |
| DB | `lib/services/database_service.dart` (`date_memos` 생성/조회/정렬) |
| 화면 | `lib/screens/memo_list_view.dart`, `lib/screens/calendar_tab.dart` |
| 위젯(Native) | `CalendarWidgetProvider.kt`, `CalendarWidgetScheduleResolver.kt` |

---

## 다음 단계

1. AdMob 배너 자리 확보 + 달력 크기 최적화 (선행 작업)
2. 위 **1번(자정 넘김)** 과 **2번(알람 연동)** 결정
3. 착수 전 `DB_스키마_변경_가이드.md` 절차 확인
4. (선택) 마이그레이션 자동 검증 테스트를 먼저 깔아두기 —
   `_onCreate`와 `_onUpgrade`가 갈라지는 것을 잡아주는 테스트. 현재 `_onUpgrade` 블록이
   17개라 손으로 대조하기엔 이미 많음
