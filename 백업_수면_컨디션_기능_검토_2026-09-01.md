# 백업 / 수면기록 / 컨디션매니저 기능 검토 (2026-09-01)

> 사용자 요청으로 진행한 리뷰. **최근 도입한 기능 3개**(백업복구_설계.md,
> 수면기록_자동추정_설계.md, 컨디션매니저_설계.md가 다루는 기능)를 코드 수준
> 버그 + UX 관점 양쪽에서 검토함. 형식은 `코드_품질_검수_리포트_2026-08-14.md`와
> 동일(심각도 태그 + 파일:라인 + 재현 시나리오).
>
> ✅ **후속 갱신(같은 날)**: 아래 B1(HIGH)과 A1(MEDIUM) 두 건은 사용자 요청으로
> 리뷰 직후 바로 수정함(`flutter analyze` 통과, Kotlin은 이 세션에서 빌드까지
> 확인 - 아래 각 항목에 표시). 나머지(A2/A3/B3/C1)는 이번엔 검토만 하고 그대로
> 남겨둠 - 다음 세션 우선순위 참고. 실기기 테스트는 사용자가 직접 진행 예정.

## 커버리지 고지 (정직하게)

- **깊게 봄**: `backup_payload.dart`/`backup_service.dart`/`backup_storage_
  service.dart`/`backup_watcher.dart`/`restore_backup_screen.dart` 전체,
  `MainActivity.kt`의 백업 관련 3개 메서드 전체, `sleep_opportunity.dart` ↔
  `SleepScheduleResolver.kt`(Dart/Kotlin 대조), `sleep_day_slots.dart`,
  `sleep_shift_relation.dart`, `sleep_history.dart`, `SleepDetectionReceiver.kt`,
  `SleepWidgetActionReceiver.kt`, `shift_pattern_analyzer.dart`,
  `condition_rule_engine.dart`, `today_forecast_engine.dart`의 핵심 분류/임계값
  로직(트렌드 3축 + 급성 이벤트 + 개인 평소 비교), `database_service.dart`의
  `_onUpgrade` 마이그레이션 체인 전체(v2~v22 누락 없이 순차 실행되는지).
- **가볍게 훑음**: `sleep_edit_dialog.dart`, `SleepWidgetProvider.kt`,
  `condition_tab.dart`의 UI 위젯 트리(로직 함수 위주로 봄, 픽셀 단위 레이아웃은
  실기기에서 확인 필요), `today_forecast_engine.dart`의 ~90개 문구 변주 자체
  (자연스러움은 실사용해봐야 앎 - 문서도 동일하게 인정함).
- **아예 안 봄**: `evidence_database.dart`(값 자체는 근거자료 문서 대조 필요),
  `onboarding_screen.dart`/`permission_intro_screen.dart`의 백업 무관 부분,
  l10n arb 파일.

## 종합 결론 (먼저 요약)

세 기능 다 설계 문서가 이례적으로 상세하고, **코드가 그 문서와 실제로 거의
정확히 일치함** — 이번 리뷰에서 "문서와 다르게 짜여있다"는 종류의 문제는
거의 못 찾았고(딱 1건 예외, S3 참고), 찾은 버그도 전부 "설계 의도는 맞는데
경계 조건 하나를 놓침" 수준. **가장 큰 공통 리스크는 버그가 아니라 검증
공백**이다 — 세 기능 다 문서 스스로 "실기기 검증 전무, 단위 테스트 0개"라고
인정하고 있고, 이번 리뷰(코드 트레이싱)로도 그 공백 자체는 못 메움. 아래
개별 항목보다 이게 우선순위가 높을 수 있음.

---

## A. 백업/복구

### A1. [MEDIUM → ✅ 수정됨] 온보딩 중 "백업 데이터 불러오기"로 진입한 뒤 건너뛰면 온보딩 진행 상태가 사라짐
`lib/screens/restore_backup_screen.dart:120-125`(`_skip()`) — `Navigator.
pushAndRemoveUntil(... OnboardingScreen() ...)`로 **완전히 새 `OnboardingScreen`
인스턴스**를 만들어 스택 전체를 갈아치운다. 이 화면은 원래 `main.dart`의
`InitialRouter`/`PermissionIntroScreen._navigateToOnboarding()`처럼 "아직
아무 진행 상태도 없는" 지점에서만 오는 걸 전제로 설계됨 — 그 경우 새
OnboardingScreen이나 기존 것이나 차이가 없음.

그런데 `lib/screens/onboarding_screen.dart:221-243`(`_pickBackupManually`)이
온보딩 **1단계(근무명 지정) 화면 안에서 `Navigator.push`(replace 아님)로**
이 화면을 새로 여는 진입점을 만들면서 전제가 깨짐 — 사용자가 커스텀 근무명을
몇 개 입력해 둔 상태에서 "백업 데이터 불러오기"를 눌러 파일을 골랐다가
(잘못 고르거나 취소하거나) "건너뛰기"를 누르면, 방금 만든 새 OnboardingScreen이
스택을 대체하면서 **직전까지 입력해둔 근무명 칩들이 전부 초기화**된다.
`_restore()` 안의 "빈 백업" 분기(라인 60-72)도 동일하게 새 OnboardingScreen을
만들어 같은 문제가 생김.

**재현 시나리오**: 온보딩에서 커스텀 근무명 3~4개 입력 → "백업 데이터
불러오기"로 (실수로) 아무 파일이나 골랐다가 잘못된 걸 확인하고 "건너뛰기" →
방금 입력한 근무명이 전부 사라진 첫 화면으로 돌아감.

**수정 내용**: `RestoreBackupScreen`에 옵션 콜백 `onCancel`을 추가 - 지정되면
"건너뛰기"/"빈 백업" 실패 시 그 콜백만 호출(보통 `Navigator.pop`)하고, 기본값
(콜백 미지정)은 기존과 동일하게 `pushAndRemoveUntil`로 새 `OnboardingScreen`을
만든다. `onboarding_screen.dart`의 `_pickBackupManually()`만 `onCancel: () =>
Navigator.of(context).pop()`을 넘겨서 이 화면으로 되돌아가게 하고, 기존 두
진입 경로(`InitialRouter`/`PermissionIntroScreen` - 진짜 신규 설치)는 값을 안
넘기므로 동작이 그대로 유지됨. (`restore_backup_screen.dart`,
`onboarding_screen.dart`)

### A2. [LOW] `restoreAll(force: true)`가 백업에 없는 테이블은 건드리지 않음
`lib/services/backup_service.dart:157-171` — 복원 루프가 `payload.tables.
entries`만 순회한다. 설정 탭의 "백업 데이터 불러오기"(반대 방향, 기존 데이터
위에 `force: true`로 덮어쓰기)로 **스키마 버전이 더 낮았던(예: sleep_records
테이블이 생기기 전) 오래된 백업**을 복원하면, `shift_schedule` 등은 백업
시점으로 되돌아가는데 `sleep_records`처럼 그 백업에 아예 없던 테이블은 현재
값 그대로 남는다 — 되돌아간 근무표와 안 맞는 수면 기록이 남을 수 있음
(예: 되돌린 근무표엔 없는 날짜의 "근무 중 수면" 분류가 깨짐). 발생 조건이
좁아서(오래된 백업을 일부러 골라 강제 복원) 심각도는 낮음.

**제안**: 강제 복원 시 "이 백업에 없는 테이블은 전부 비운다" 옵션을 추가하거나,
최소한 백업의 `schemaVersion`이 현재보다 오래됐으면 경고 문구를 보여줄 것.

### A3. [정보] Android 10 미만 기기는 백업이 영구히 조용히 비활성
`backup_storage_service.dart:22,33` — `Build.VERSION.SDK_INT < Q`면 `write`/
`read` 둘 다 그냥 `false`/`null`을 반환(정상 동작). 이 앱 `minSdk`는 24라
이론상 Android 9 이하 사용자가 있을 수 있는데, 설정 탭 어디에도 "이 기기는
백업을 지원하지 않는다"는 안내가 없어 보임(설정 탭 UI 텍스트까지는 다
확인 못함 - 확인 필요). 그런 사용자는 "마지막 백업: 아직 없음"만 계속 보게
되어, 자동 백업이 도는 중인지 애초에 지원 안 하는 기기인지 구분이 안 됨.

### 백업 - UX 평가
설계 자체는 매우 촘촘함 — 자동(백그라운드 전환 시 변경 감지) + 수동 버튼이
정확히 같은 함수를 타서 "마지막 백업" 표시가 항상 정확하고, OEM 버그(삼성
`owner_package_name` NULL)를 실제로 겪고 나서 SAF 수동 선택 폴백까지 만들어
둔 점이 특히 좋음(이런 종류의 OS 버그는 코드로 못 고치는 게 정상이고, 항상
탈출구를 열어두는 설계가 맞는 대응). 복구 후 권한 재확인/알람 강제 재생성
연결도 빠짐없이 챙겨져 있음. 위 A1만 고치면 흠잡을 데가 적음.

---

## B. 실제 수면 기록 / 자동 추정

### B1. [HIGH → ✅ 수정됨] 연속 야간근무(백투백) 시 자동 감지 창 계산이 엉뚱한(미래) 창을 반환함
`lib/services/condition/sleep_opportunity.dart:69-79`
(`sleepOpportunityWindowFor`), 동일 로직의 `SleepScheduleResolver.kt:78-88`
(`computeWindowForNow`) — 야간 근무 우선순위 루프가 `offset`을 **0(오늘)→
1(어제)→2(그제)** 순으로 확인하면서, 각 후보의 `now.isBefore(windowEnd)`만
확인하고 **`now`가 이미 그 창 안에 들어와 있는지는 확인하지 않은 채 첫
매치를 즉시 반환**한다.

**재현 시나리오**: 오늘(D)도 야간근무(19:00~D+1 07:00 예정)이고 어제(D-1)도
야간근무(끝난 시각 D 07:00)인 "연속 야간" 패턴. 지금이 D 14:00(어제 근무
끝나고 낮잠 자기 딱 좋은, 실제로 추적해야 할 시간대 - 어제 근무 창은
[D 06:00, D 15:00]로 아직 열려있음)이라고 하자.

- `offset=0`(오늘 D의 야간근무, 아직 시작도 안 함)을 먼저 확인 → `inst.end`가
  미래(D+1 07:00)라 `windowStart=D+1 06:00`, `windowEnd≈D+1 15:00`. `now(D
  14:00)`가 이 `windowEnd`보다 이르므로(당연히 그렇다 - 아직 시작도 안 한
  근무의 종료 예정 시각이니) **조건을 통과해서 이 창을 그대로 반환해버림**.
- 정작 지금 실제로 열려있는 어제(offset=1)의 창 `[D 06:00, D 15:00]`은
  확인조차 못 하고 넘어감(루프가 첫 매치에서 바로 리턴하므로).

호출부(`SleepDetectionScheduler`/Kotlin 스케줄러)는 이 반환값을 보고
"`now < windowStart`니 아직 창 시작 전 → 다음 깨어남을 windowStart(D+1
06:00)로 예약"하는 판단을 하게 됨 — 결과적으로 **D 14:00부터 D+1 06:00까지
(16시간) 자동 감지 자체가 완전히 멈춘다.** 정작 이 구간(D 06:00~15:00)이
바로 "야간 근무 후 다음 야간 근무 전 낮잠/회복 수면"을 잡아야 하는 구간인데,
연속 야간이라는 이 앱의 핵심 타겟 사용자층 패턴에서 통째로 빠지는 것 -
심각도를 HIGH로 매김.

**중요**: 이 버그는 **Dart 참조 구현과 Kotlin 실제 구현에 동일하게 존재**함
(대조 확인 완료) - 즉 실제 백그라운드 감지에도 그대로 영향을 줌. 코드
자체는 안 고쳤음(요청대로 검토만).

**수정 내용**: 루프를 "후보 수집 후 우선순위 판단" 2단계로 재작성 - offset
0~2 중 `windowEnd`가 아직 안 지난 후보를 전부 모은 뒤, ① `windowStart <= now`
(이미 시작된 활성 창)가 있으면 그중 하나를 최우선 반환, ② 없으면 그중
`windowStart`가 가장 이른(=가장 먼저 시작할) 아직 안 시작한 창을 반환. 이전엔
"먼저 발견한 것"(offset 순서)이 이겼는데 이제 "이미 진행 중인 것"이 항상
이김. `sleep_opportunity.dart`(Dart 참조 구현)와 `SleepScheduleResolver.kt`
(실제 백그라운드 실행부) 양쪽에 동일하게 반영함(Dart는 `flutter analyze`
통과 확인, Kotlin은 문법상 동일 구조로 이식 - 이번 세션엔 Kotlin 빌드까지는
안 돌림, 다음 실기기 검증 때 같이 확인할 것).

### B2. [문서 불일치] 설계 문서가 이미 바뀐 코드를 옛 내용으로 설명함
`수면기록_자동추정_설계.md` 13-10장은 "미니 달력 카드 가로 스크롤 애니메이션
버그"를 `AutomaticKeepAliveClientMixin`으로 해결했다고 적어뒀는데, 실제
코드(`lib/screens/condition_tab.dart:576-593`, `_SleepMiniCalendarCard`)는
그 방식이 재발 확인되어 폐기되고 **`reverse: true` + 데이터 역순 정렬**로
완전히 다시 짜여 있음(코드 자체의 주석은 이 최신 히스토리를 정확히 담고
있음 - 코드는 맞고 설계 문서만 뒤처짐). 다음 세션에서 이 문서를 열어보고
"AutomaticKeepAliveClientMixin이 아직 쓰이는구나"라고 오해할 수 있으니 문서만
갱신 권장(코드 변경 불필요).

### B3. [LOW, 잠재 버그 - 현재는 죽은 코드] "평소 취침/기상 시각" 평균 계산이 자정을 못 넘김
`lib/services/condition/sleep_history.dart:137,139`
(`buildPersonalSleepPatterns`) — `avgBedtimeMinutesOfDay`/`avgWakeMinutesOfDay`를
단순히 "자정 기준 분"의 산술평균으로 계산함. 예를 들어 하루는 23:00(1380분),
다른 하루는 01:00(60분)에 잠들었다면 평균이 (1380+60)/2=720분=**낮 12시**로
나옴(원 취지는 "자정 근처"여야 함) - 전형적인 "시각을 각도/원형으로 다루지
않고 그냥 평균 낸" 버그. **다만 실제 확인해보니 이 두 필드는 코드 어디에서도
읽히지 않음**(`grep`으로 확인 - `PersonalSleepPattern` 자체는 다른 목적
(`avgSleepMinutes`)으로 널리 쓰이지만 이 두 필드는 계산만 되고 아무도 안
봄). 지금 당장 사용자에게 보이는 문제는 없지만, 나중에 "평소 취침 시각:
OO시" 같은 표시를 이 필드로 만들면 바로 터짐 - 미리 기록해둠.

### 수면기록 - UX 평가
위젯 2버튼 재설계, "추정"이라는 단어를 고집스럽게 지킨 표현 원칙, 미니
달력의 3칸→2칸(주수면+낮잠2) 단순화, 연타 방지 임계값 등 전부 실사용
피드백을 아주 촘촘하게 반영해온 흔적이 뚜렷함. `checkNow()`로 "즉시 판정"을
추가한 뒤 그로 인해 생긴 오탐(창 게이팅 누락)을 실기기에서 잡아내고 바로
고친 이력(13-8장)도 신뢰가 감 - **다만 B1도 같은 계열의 "트리거 지점이
늘어나면서 생긴 경계 조건 버그"라 비슷한 걸 하나 더 놓친 셈**. 표현/UX
디테일은 이미 충분히 다듬어졌고, 이제 필요한 건 실기기에서 "연속 야간
패턴"을 직접 재현해 B1을 검증하는 것.

---

## C. 컨디션 매니저

### 코드 버그
`shift_pattern_analyzer.dart`(교대 방향/연속일수/회복구간), `condition_rule_
engine.dart`(4-rule 판정), `today_forecast_engine.dart`의 3축 분류(근무시간
추세/수면 커버리지/수면 충분도)와 급성 이벤트·개인 평소 비교 로직을 전부
따라가봤으나 **명확한 버그를 찾지 못함.** 임계값(20% 변화, 1.35/0.65배,
변동계수 0.30 등)이 전부 "설계상 정한 값"이라고 코드/문서에 정확히
명시돼있고, 그 값들이 실제로 일관되게 적용되고 있음. `recoveryWindowContaining`
(회복구간 탐색)은 B1과 비슷한 종류의 "여러 후보 중 우선순위" 로직이지만,
이쪽은 단순 최근접 탐색이라 B1과 같은 종류의 함정이 없음(대조 확인함).

가장 가까운 "버그에 준하는" 발견은 **문서가 스스로 인정한 미검증 상태**임 -
`today_forecast_engine.dart`는 자체 단위 테스트가 0개이고(문서 13-7장이
명시), 약 90개의 손으로 쓴 문구 변주가 실제 8주치 데이터를 넣었을 때
자연스럽게 읽히는지 확인된 적이 없음. 코드 트레이싱만으로는 "숫자가 맞게
계산되는지"는 확인되지만 "문장이 안 어색한지"는 확인이 안 됨 - 이건 코드
리뷰의 한계이지 버그는 아님.

### C1. [정보] 이 탭 전체가 l10n(다국어) 미적용
`condition_tab.dart` 상단 주석에 명시된 대로 한국어 문자열이 하드코딩돼
있음(1차 버전이라 의도적). 앱의 나머지 부분은 이미 영문 지원이 완료된
상태(`교대시계_영어화_현지화_보고서_영문판.md`)라, 영어 사용자에게는 이
탭만 한국어로 보이는 비일관성이 있음 - 지금 당장 문제는 아니지만 "컨디션
탭 완성"(설계 문서 13-8장이 다음 세션 안건으로 남겨둔 것) 범위에 l10n 적용
여부를 포함할지 미리 정해두면 좋을 것.

### 컨디션 매니저 - UX 평가
이 세 기능 중 원칙(숫자 점수 금지, Evidence 추적 가능, 3단계 레벨만) 준수가
가장 엄격하고 일관됨 - 코드 어디를 봐도 이 원칙이 깨진 곳이 없었음. "오늘의
컨디션 예측" 카드의 조합형 문구 설계(축 3개 × 변주 풀 × 날짜 시드 로테이션)는
영리한 해법이지만 그만큼 복잡도가 높아져서, 설계 문서 스스로도 "다음 세션에
먼저 실기기로 확인"을 최우선 과제로 이미 못박아둔 상태 - 이 리뷰도 같은
결론에 도달함. 문서 13-8장의 "다음에 확인할 것" 목록(실기기 검증 > 단위
테스트 > 임계값 튜닝 > 카드 통폐합 논의)이 이미 잘 정리돼 있으니 다음
세션은 그 순서를 그대로 따라가면 될 것.

---

## 다음 세션 우선순위 제안

1. **B1 실기기 검증** - 코드 수정은 끝났지만(위 참고), 연속 야간근무 패턴을
   실기기에서 직접 재현해서 이제 올바른(활성) 창이 반환되는지, 그리고 이
   수정으로 인한 회귀가 없는지(예: 단일 야간근무·플랫 규칙 케이스가 여전히
   전과 동일하게 동작하는지) 확인 필요 - **가장 먼저 해볼 만한 것.**
2. **세 기능 전반 실기기 검증** - 특히 sleep_records DB 마이그레이션(v21→v22
   실제 업그레이드 경로), 위젯 실제 배치/갱신, 컨디션 탭 예측 문구가 실제
   데이터로 자연스러운지.
3. A1도 실기기에서 재확인(온보딩 1단계에서 근무명 입력 후 "백업 데이터
   불러오기" → 취소/건너뛰기 시 입력값이 그대로 남아있는지).
4. 여유가 되면: A2/A3(백업 엣지케이스), B3(평균 취침시각 순환평균 버그 - 아직
   죽은 코드), C1(컨디션 탭 l10n 미적용) 처리, `today_forecast_engine.dart`
   단위 테스트 작성(문서가 이미 제안한 방향 - `ShiftPatternAnalyzer`/
   `SleepHistoryNight`를 합성해 시나리오별 골든 테스트).
