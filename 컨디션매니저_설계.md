# 컨디션 매니저 — 아키텍처/UI 설계 (Phase 1~4, v1)

> 2026-08-31 작성. "B번 요구사항"(컨디션 매니저 기능 개발 명세) 1차 구현.
> 근거자료는 `컨디션매니저_근거자료.md`, 백업 연동 검토는 이 문서 8장 참고.
> **이 기능은 100% 신규 파일 + `main.dart` 탭 등록 한 줄 + DB 마이그레이션
> 한 블록만 추가**하는 방식으로 만들었다 — 기존 알람/근무패턴/위젯 관련 파일은
> 단 한 줄도 수정하지 않았다(9장 "기존 코드 영향 범위" 참고).

---

## 0. 왜 이렇게 만들었나 (요약)

- 사용자 지시: "일정관리 탭 옆에 새 탭 하나" — 기존 UI/UX를 갈아엎지 않고 독립된
  탭(`ConditionTab`)으로 시작. 설정 탭도 건드리지 않음.
  근무 시작/종료 시각 입력도 이 신설 탭 안에서 처리(새 테이블 `condition_shift_times`).
- 원칙(스펙 1~2장, 17~19장): 숫자 점수·%·질병 진단 절대 금지. 상태는 딱 3단계
  (`NORMAL`/`ATTENTION`/`HIGH_LOAD`)로만, 그 판정은 전부 이름 붙은 Rule +
  Evidence ID로 추적 가능해야 함(AI가 임의 판단 금지).
- 기존 데이터 재사용(스펙 30장): 근무 패턴은 `ShiftSchedule`(기존 모델)을 그대로
  읽는다. 새로 만든 건 "근무명별 출퇴근 시각"(`condition_shift_times`) 하나뿐 —
  이건 진짜로 앱에 없던 정보라 새로 받을 수밖에 없음.

---

## 1. 데이터 흐름 (스펙 5장 그대로)

```
ShiftSchedule(기존) + condition_shift_times(신규, 근무명→출퇴근시각)
        │
        ▼
ShiftPatternAnalyzer   ← lib/services/condition/shift_pattern_analyzer.dart
  - 특정 날짜의 ShiftInstance(시작/종료 DateTime, 카테고리) 계산
  - 연속 야간/연속 근무일 스트릭
  - 근무 사이 회복시간(분)
  - 교대 방향(정/역방향) 판정
  - 주간 패턴 요약
        │
        ▼
ConditionRuleEngine    ← lib/services/condition/condition_rule_engine.dart
  - 위 분석 결과에 "이름 붙은 rule"을 적용해 ConditionLevel + Finding + Tip 생성
  - 모든 Finding/Tip은 evidenceIds(EVIDENCE-XXX)를 반드시 가짐
        │
        ▼
ConditionResult
        │
        ▼
ConditionTab (UI)      ← lib/screens/condition_tab.dart
  - "오늘의 컨디션" 카드 + "오늘의 추천"(우선순위) + "왜 이런 조언을 하나요"
```

## 2. 새 파일 목록

| 파일 | 역할 |
|---|---|
| `lib/models/shift_time_range.dart` | 근무명 1개의 출퇴근 시각(분 단위, 자정 넘김 지원) |
| `lib/services/condition/shift_time_category.dart` | 시작 시각만으로 주간/오후/야간 분류(이름 무관 휴리스틱, 근거자료 아님) |
| `lib/services/condition/shift_pattern_analyzer.dart` | Phase 1 — 패턴 분석 엔진 |
| `lib/services/condition/evidence_database.dart` | Phase 2 — Evidence 상수(컨디션매니저_근거자료.md와 1:1) |
| `lib/services/condition/condition_rule_engine.dart` | Phase 2~3 — Rule Engine + 수면 추천 |
| `lib/providers/condition_shift_time_provider.dart` | `condition_shift_times` 테이블 CRUD를 Riverpod으로 노출 |
| `lib/providers/condition_provider.dart` | 위 셋을 조합해 "오늘"/"이번 주" 결과를 만드는 Provider |
| `lib/screens/condition_tab.dart` | Phase 4 — 화면 |

## 3. DB 변경 (v20 → v21)

**신규 테이블 하나만 추가. 기존 테이블은 0개 변경.**

```sql
CREATE TABLE IF NOT EXISTS condition_shift_times(
  shift_name TEXT PRIMARY KEY,
  start_minutes INTEGER NOT NULL,  -- 자정 기준 분 (0~1439)
  end_minutes INTEGER NOT NULL,    -- 자정 기준 분 (end<=start면 자정 넘김으로 해석)
  updated_at TEXT NOT NULL
)
```

- `DB_스키마_변경_가이드.md` 절차대로 `database_service.dart`의 `_onCreate`/
  `_onUpgrade` 둘 다에 추가, `version: 21`.
- `DatabaseHelper.kt`의 `DATABASE_VERSION`도 21로 동기화(`checkDartKotlinSync`
  통과 필요) — 단, **Kotlin 쪽은 이 테이블을 읽는 코드를 전혀 추가하지 않았다**.
  `DatabaseHelper.kt`의 `onCreate()`는 원래 비어있고(Flutter가 스키마를 관리,
  주석 참고) 게이트(`isDatabaseReady()`)는 오직 버전 숫자 일치만 확인하므로,
  Kotlin 파일에서 실제로 고친 줄은 `DATABASE_VERSION = 20` → `21` 단 한 줄이다.
- **백업 레이어 자동 포함 확인** — `백업복구_설계.md` 3장/7장에서 이미 설계된
  대로, `BackupService.exportAll()`은 `sqlite_master`를 그때그때 읽어 새 테이블을
  자동 포함한다. `condition_shift_times`는 원본 데이터(사용자가 직접 입력한 값)라
  `kBackupExcludedTables`에 넣지 않았다 — **`backup_service.dart`/`backup_payload.dart`
  코드는 이번 작업에서 한 줄도 안 고쳤고, 고칠 필요도 없다.** (8장에서 다시 확인)

## 4. `ShiftTimeCategory` 분류 — 근거자료 아님, 순수 라벨링 규칙

근무명은 사용자가 자유 텍스트로 짓기 때문에("A조", "1번" 등) 이름으로는 주간/야간을
가릴 수 없다. 대신 **시작 시각**만으로 분류한다:

| 시작 시각 | 분류 |
|---|---|
| 05:00 ~ 10:59 | `day`(주간) |
| 11:00 ~ 16:59 | `evening`(오후) |
| 17:00 ~ 04:59(자정 넘김 포함) | `night`(야간) |

이건 연구에서 가져온 임계값이 아니라 **앱 내부 표시용 분류 규칙**이다 — 위험 판정과
무관하고, "이 근무가 몇 시에 시작하는 근무인지"만 나눈다. 휴무는 `off`, 출퇴근 시각을
아직 입력하지 않은 근무명은 `unspecified`로 별도 처리(그 근무가 낀 날은 회복시간/방향
계산에서 자동으로 제외 — 침묵 실패가 아니라 "이 근무는 시각 미입력이라 계산 못 함"을
명시적으로 구분해서 UI가 안내할 수 있게 함).

## 5. Rule ↔ Evidence 매핑 (판정에 실제로 쓰이는 것만)

| Rule ID | 조건 | 결과 | Evidence |
|---|---|---|---|
| `RULE_SHORT_RECOVERY` | 근무 종료 → 다음 근무 시작까지 < 11시간 | Finding 추가, 최소 `ATTENTION` | EVIDENCE-002, EVIDENCE-003 |
| `RULE_LONG_SHIFT` | 해당 근무 실제 근무시간 ≥ 12시간 | Finding 추가, 최소 `ATTENTION` | EVIDENCE-004 |
| `RULE_COMPOUND_HIGH_LOAD` | `RULE_SHORT_RECOVERY` **AND** `RULE_LONG_SHIFT`이 같은 회복구간에서 동시 성립 | `HIGH_LOAD` | EVIDENCE-002, 003, 004 |
| `RULE_EXTENDED_STREAK_SHORT_BREAK` | 연속 3일 이상 ≥12시간 근무 **AND** 그 다음 회복시간 < 48시간 | `HIGH_LOAD` | EVIDENCE-004, EVIDENCE-005 |

**상태 판정에는 이 4개 rule만 관여한다.** 그 외 아래 항목들은 전부 "왜 그런지"
설명용 Finding으로만 나오고 🟢/🟡/🔴 색상에는 영향을 주지 않는다(근거 부족 항목을
판정에 섞지 않기 위한 의도적 분리):

| 설명용 항목(판정 아님) | 근거 | 표현 원칙 |
|---|---|---|
| 교대 방향(정/역방향) | EVIDENCE-001 | "~로 보고됩니다" (단정 금지) |
| 연속 야간근무 N일째 | (근거 없음, 순수 계산) | 사실만 서술, "위험" 표현 금지 |
| 야간근무 다음 날 = 휴무 | EVIDENCE-006 | "회복을 위한 수면·휴식 구간" 재구성 |
| 교대 전환(주간↔야간 등) | EVIDENCE-001 | "적응 기간이 필요할 수 있음" |

Tip(오늘의 추천) 4종 — 전부 판정과 무관, 조건이 맞을 때만 노출:

| Tip | 노출 조건 | Evidence | category |
|---|---|---|---|
| 추천 수면 시간대 | 회복구간을 계산할 수 있을 때 | EVIDENCE-011(7~9h) | `sleep` |
| 카페인 컷오프 | 위 수면 시간대가 나왔을 때 | EVIDENCE-008 | `caffeine` |
| 야간근무 중 식사 | 오늘이 야간근무일 때 | EVIDENCE-010 | `meal` |
| 귀가길 빛 관리(+졸음운전 경고 필수) | 방금/오늘 야간근무가 끝나는 날일 때 | EVIDENCE-009 | `light` |
| 야간근무 전 낮잠(근거 낮음 명시) | 오늘이 야간근무일 때 | EVIDENCE-007 | `fatigue` |

## 6. "추천 수면 시간대" 계산 — 스펙 15장 방식 그대로

```
sleepStart = 직전 근무 종료시각 + 60분(이동/정리 버퍼, 연구값 아님 — 명시)
sleepEnd   = min(sleepStart + 9시간, 다음 근무 시작시각 - 60분(준비 버퍼))
```

- 버퍼(60분)는 근거자료에서 온 값이 아니라 **상식적 여유 시간**이라는 걸 설계
  문서·코드 주석에 명시 — "의학적으로 정확한 값"처럼 보이면 안 됨(스펙 15장).
  화면 문구도 "수면을 확보하기 좋은 시간대"로만 표현하고 "최적 수면시간"이라고
  안 함.
- `sleepEnd <= sleepStart`(가용 시간이 버퍼보다도 짧음)면 시간대 자체를 만들지
  않고 "다음 근무까지 회복시간이 매우 짧습니다"라는 Finding만 보여줌.
- 실제 표시 길이가 7시간 미만이면 "권장 범위(7~9시간)보다 짧게 확보 가능"이라고
  같이 표기 — "이 정도면 충분하다"는 오해를 막기 위함.

## 7. UI 구조 (`condition_tab.dart`) — 스펙 26~28장 반영

```
[헤더] 오늘 날짜 + 오늘 근무 칩 (기존 캘린더 탭 스타일 참고, 새로 안 베낌)

[근무시간 미설정 안내] ← activeShiftTypes 중 하나라도 condition_shift_times에
                          없으면 이 카드만 보이고 아래는 다 숨김
  "먼저 근무별 시작~종료 시각을 입력해주세요" + 근무명별 시간 입력 리스트(시작
  시간/종료 시간 TimePicker 2개, 저장 버튼)

[오늘의 컨디션] 🟢/🟡/🔴 + 레벨 라벨 + Finding 최대 2~3줄
[오늘의 추천] Tip 최대 3개(우선순위 순: sleep > light > caffeine > meal > fatigue)
             "더보기"로 나머지 + 전체 근거 펼치기
[이번 주 패턴] 주간/오후/야간/휴무 횟수, 최대 연속근무, 최대 연속야간, 평균 근무시간
[왜 이런 조언을 하나요?] 펼치면 이번에 적용된 Rule ID + Evidence 원문 요약 + 출처
                         URL 표시(투명성 — 스펙 24장 "추적 가능해야 한다")
```

- 근무시간을 하나라도 안 정했으면 분석 자체가 불가능하므로, 이 화면 안에서
  1회성으로 입력받는다(스펙 지시: "설정 탭은 건드리지 말고 신설 탭에서 진행").
  나중에 설정 탭으로 옮기고 싶으면 `condition_shift_time_provider.dart`만
  재사용하면 되므로 이전 비용은 낮음.
- 기존 디자인 언어 재사용: `app_theme.dart`/`app_colors.dart`의 `kAppMainAccent`,
  `AppTheme.lightTheme` 카드 스타일을 그대로 씀 — 새로운 "건강앱 톤"을 만들지
  않음(스펙 26장).
- 이 탭은 **1차 버전이라 별도 l10n 키를 안 만들고 한국어 문자열을 직접 씀**
  (`AlarmTestScreen` 등 실험적 화면과 같은 관례). 정식으로 자리잡으면 그때
  `app_ko.arb`/`app_en.arb`로 옮길 것 — 지금 옮기면 생성 파이프라인
  (`l10n/generated/`)까지 다시 돌려야 해서 "기존 버전에 영향 0" 원칙에 비해
  얻는 게 적음.

## 8. 백업 레이어 재검토 결과 (스펙 요청 "백업로직 참고해서 설계 필요한지 검토")

**결론: 아무것도 안 고쳐도 된다.** `백업복구_설계.md`가 이미 "새 원본 데이터
테이블은 코드 변경 없이 자동 포함됨"이라고 설계해뒀고, 이번에 실제로 그 설계가
맞는지 확인함:

- `condition_shift_times`는 사용자가 직접 입력한 **원본 데이터**(파생 아님) → 백업
  대상 맞음, `kBackupExcludedTables`에 넣지 않음.
- `BackupService.exportAll()`은 `SELECT name FROM sqlite_master`로 테이블을 그때
  그때 스윕하므로, 이 테이블이 생기는 순간부터 **다음 백업부터 자동으로 포함**됨.
  `backup_payload.dart`/`backup_service.dart` 코드 변경 0줄.
- `ConditionResult`(오늘의 컨디션 레벨, Finding, Tip)는 항상 그 자리에서
  `ShiftSchedule` + `condition_shift_times`로부터 다시 계산하는 **파생 데이터**라
  애초에 저장하지 않는다(=백업 이슈 자체가 없음) — `work_hours_calculator.dart`가
  근로시간 통계를 다루는 것과 동일한 원칙(스펙 30장, 기존 패턴 재사용).

## 9. 기존 코드 영향 범위 (전부 나열)

**수정한 기존 파일 (모두 additive, 기존 줄 삭제/변경 없음):**

| 파일 | 변경 내용 |
|---|---|
| `lib/services/database_service.dart` | `version: 20`→`21`, `_onCreate`/`_onUpgrade`에 `condition_shift_times` 블록 추가, 파일 끝에 CRUD 메서드 추가 |
| `android/.../DatabaseHelper.kt` | `DATABASE_VERSION = 20` → `21` (한 줄) |
| `lib/main.dart` | `_tabs` 리스트에 `ConditionTab()` 한 항목 추가(일정관리 다음, 설정 이전) + `BottomNavigationBarItem` 한 항목 추가 |
| `CLAUDE.md` / `DB_스키마_변경_가이드.md` | 문서 갱신(v21 기록) |

**절대 건드리지 않은 것 (알람/근무패턴/위젯 크리티컬 로직 — 확인 완료):**

- `alarm_generation_service.dart`, `alarm_refresh_service.dart`, `alarm_service.dart`
- `AlarmRefreshEngine.kt`, `CustomAlarmReceiver.kt`, `AlarmGuardReceiver.kt`,
  `AlarmActivity.kt`, `AlarmOverlayService.kt`, `AlarmPlayer.kt`, `NotificationHelper.kt`
- `shift_schedule.dart`(모델 자체는 import만 하고 필드/메서드 추가 없음),
  `work_hours_calculator.dart`, `all_shifts_view.dart`, `calendar_tab.dart`,
  `schedule_management_tab.dart`
- `CalendarWidgetProvider.kt` 등 홈 화면 위젯 관련 전체
- `backup_payload.dart`, `backup_service.dart` (8장 참고 — 수정 불필요 확인됨)

## 10. 다음 단계 (이번 세션 범위 밖)

- **Phase 5 (Firebase Health Tips)**: `healthTips` 컬렉션 연동 — 지금은 Tip 4종이
  Dart 코드에 하드코딩됨(EVIDENCE와 함께). Firestore로 옮기면 `evidence_database.dart`
  구조를 그대로 문서 스키마로 옮기면 됨(스펙 19~21장 필드와 이미 이름을 맞춰둠).
- **Phase 6 (주간/월간 리포트 통합)**: `weeklySummary()`는 이미 있음 — 달력탭의
  월간 통계 팝업과 나란히 노출하는 UI 통합은 보류.
- 실기기 검증 이력 없음(다른 미검증 기능들과 동일 — MEMORY 참고). DB 마이그레이션
  경로(기존 사용자 v20→v21 업그레이드)는 `DB_스키마_변경_가이드.md` 4-3절 절차대로
  기존 데이터 있는 dev 앱에 덮어 설치해서 확인 필요.

## 11. (2026-09-01) 근무시간 입력 이동 + 부분 입력 허용 + 실제 수면 연동

위 9장 "다음 단계"에 있던 "근무시간 입력 UI를 설정 탭으로 옮기고" 항목을 실제로
진행함. 사용자 요청으로 범위가 커져서 별도 장으로 기록.

- **입력 위치 이동**: `condition_shift_times`(근무별 출퇴근 시각) 입력 UI를 이
  탭에서 완전히 제거하고, 설정 → "근무시간 및 OT 설정"(`work_hours_settings_screen.dart`)
  으로 옮김. 그 화면은 원래 근무별 "시간(분)"만 직접 입력받아 `shift_schedule.shiftDurations`
  (근로시간/OT 계산용, 기존)에 저장했는데, 이제 **출퇴근 시각**을 입력받아
  `ShiftTimeRange.durationMinutes`(자정 넘김 포함 자동 계산)로 `shiftDurations`를
  채우는 동시에 `condition_shift_times`(컨디션 탭용)에도 그대로 저장함 - 입력
  한 번으로 두 데이터를 같이 채움. 기존 `work_hours_calculator.dart` 등
  `shiftDurations` 소비자는 코드 변경 없이 그대로 동작(값의 출처만 바뀜).
- **부분 입력 허용**: `conditionSetupNeededProvider`를 "사용 중인 근무명 전부에
  시각이 있어야 통과"에서 "**한 근무라도** 시각이 있으면 통과"로 완화함(이름은
  호환성 위해 유지, 의미만 바뀜). `ShiftPatternAnalyzer.recoveryWindowContaining()`
  등이 이미 시각 미입력 근무를 만나면 앞/뒤로 탐색하도록 짜여 있어서(3장) 부분
  입력도 안전하게 동작함 - 코드 변경 없이 gate 조건만 바꿈.
- **탭 진입 게이트**: 위 gate를 통과 못 하면(사용 중인 근무명 전부 미입력)
  탭 전체가 안내 문구 하나만 보여주고 아무 카드도 안 보여줌(`_SetupNeededNotice`,
  "설정 → 근무시간 및 OT 설정에서 입력해주세요"로 안내) - 입력 UI가 이 탭에
  더는 없으므로 부분적으로라도 보여줄 게 없음.
- **실제 수면 연동(완전 분리 원칙은 유지)**: `sleep_condition_provider.dart`에
  `todayRecoverySleepProvider`(오늘의 회복구간과 겹치는 확정 수면 기록을 찾음)를
  추가하고, `sleep_condition_extras.dart`(신규)의 순수 함수
  `buildActualSleepFinding()`이 그 실제 수면 시간을 권장 범위(7~9시간,
  EVIDENCE-011 재사용)와 비교해 보조 설명 한 줄을 만듦. `_TodayConditionCard`가
  이 결과를 있으면 추가로 보여주고, 없으면(대부분의 경우) 기존처럼
  `RULE_SLEEP_WINDOW` 팁(이론적 권장 수면 시간대)만 보여줌.
  **`ConditionRuleEngine`은 이 작업으로 0줄 수정** - 판정(레벨)에는 절대
  영향을 주지 않는 순수 표시용 보조 라인(5장 "완전 분리" 원칙 그대로 준수).
- 이 참에 "근무별 평균 취침 시각" 입력 기능(자동 수면감지 1순위 앵커) 자체도
  사용자 요청으로 완전히 삭제됨 - 자세한 내용은 `수면기록_자동추정_설계.md`
  12장 참고.

## 13. (2026-09-01) "오늘의 컨디션 예측" — 킬러 기능

사용자가 명시적으로 "이 앱의 킬러 기능이 될 수도 있다"고 지정한 요청. 탭 맨
위에 카드 1개만 새로 추가하고, 기존 카드들은 전부 그 아래로 내림. 최근 최대
**8주(56일)**의 근무시간 추세 + 실제 수면 기록(있는 만큼만)을 종합해서 오늘의
컨디션을 2~3문장으로 예측한다.

### 13-1. (2026-09-01 재설계) 왜 ML/임베딩이 아니라 "조합형(compositional)"
규칙 기반인가

사용자가 "준 AI급 추정"을 요청하면서도 앱 용량 증가(임베딩 등)는 원하지
않는다고 명시함. 게다가 이 앱의 핵심 원칙(0장, 컨디션매니저_근거자료.md)이
"근거 없는 임의 판정 절대 금지"인데, 학습 모델은 태생적으로 "왜 이 판정이
나왔는지"를 근거자료 ID로 역추적할 수 없다 - 이 프로젝트의 제약과 정면으로
충돌한다.

**1차 버전(우선순위 신호 1개 채택 방식)에 대한 사용자 피드백**: "근무시간이
늘어나는 중/줄어드는 중/줄다가 갑자기 늘거나/늘다가 갑자기 줄거나/들쑥날쑥,
수면도 있음/없음/50%/80%, 안정적/최근 나빠짐/최근 좋아짐 등이 전부 곱해지는
경우의 수인데 신호 7개로 감당되겠냐"는 정확한 지적. 그래서 "신호 하나를
통째로 고르는" 방식을 버리고, **독립적인 축(axis) 3~4개를 각각 분류한 뒤
이어 붙이는 조합형 문장 생성**으로 다시 짬:

- 축 1(근무시간 추세 모양): increasing/decreasing/recentSpike/recentDrop/
  volatile/stable/insufficientData — 7가지
- 축 2(수면 기록 커버리지 자체의 변화): none/steadyLow/steadyHigh/
  recentlyStarted/recentlyStopped — 5가지
- 축 3(수면 충분도 추세, 데이터 있을 때만): consistentlyGood/consistentlyPoor/
  recentlyDeclined/recentlyImproved/volatile/insufficientData — 6가지
- 급성 이벤트(어제 하루, 트렌드보다 우선하는 헤드라인 오버라이드)

축들이 조합되고(7×5×6 = 최대 210가지 조합) **실제 숫자(시간/시간대)가 매번
그대로 문장에 박히기** 때문에 "문구 수백 개를 직접 쓴 것"과 비슷한 다양성을
훨씬 적은 코드로 냄. 사용자가 요청한 "어제와 오늘이 조금이라도 달라지면
문구도 달라져야 한다"는 요구도 자연히 충족됨.

**(2026-09-01 추가) "모든 조합의 워딩을 미리 다 만들어둘 수 있지 않냐"는
질문에 대한 결론**: 조합(210가지)을 손으로 다 쓰는 건 오히려 비효율적이라고
판단함 - 조합형 구조가 이미 모든 조합을 자동으로 커버하고 매번 실제 숫자가
박히므로, 조합을 손으로 다 쓰면 같은 조합에 대해 숫자만 다른 문장을 반복해서
복붙하는 꼴이라 유지보수만 폭증하고 다양성은 늘지 않음. 대신 "축-상태 하나당
문구가 1개뿐이라 얇다"는 실제 지적을 반영해, **축-상태마다 문구를 3개(안내
문구 위주로는 2~3개)씩 준비하고, 날짜+절 종류를 시드로 한 결정론적 로테이션**
(`_pickFrom`/`_variantIndex`)으로 고름 - 같은 날 안에서는 항상 같은 문장(화면
재빌드에 안 흔들림), 날짜가 바뀌면 변주도 함께 바뀜. 결과적으로 직접 작성한
문구는 약 90개(축1 OT 추세 29 + 축2·3 수면 절 27 + 원인추론 18 + 급성 6 +
권고 11)로 늘었고, 여기에 조합×숫자×변주가 곱해져 실사용 다양성은 훨씬 큼.

### 13-2. 새 파일 / 변경 파일

- `lib/services/condition/sleep_history.dart` — 최근 최대 **56일**의 "근무일 +
  그 뒤 실제 수면"을 짝지은 시계열(`SleepHistoryNight`)과, 근무명별 "평소
  수면 패턴"(`PersonalSleepPattern`, 표본 3개 미만인 근무명은 제외)을 만드는
  순수 함수(4주→8주로 확장, 아래 13-4 참고).
- `lib/services/condition/today_forecast_engine.dart` — 핵심 로직 전면
  재작성(위 13-1의 조합형 구조). 문장 조립 순서: [급성 있으면 급성, 없으면
  근무시간 추세] → [수면 커버리지/충분도 추세] → [원인·컨디션 추론
  (급성 없을 때만)] → [권고 1개].
- `lib/services/condition/sleep_condition_extras.dart` — (12장에서 이미 추가)
  오늘 하루짜리 "실제 수면 vs 권장" 비교. 이 킬러 기능과는 별개 레이어.
- `evidence_database.dart`에 EVIDENCE-012(EU 근로시간지침 제6조, 주 48시간
  상한) 추가.
- `lib/providers/sleep_record_provider.dart` — 수면 기록 조회 범위를 30일 →
  **60일**로 넓힘(8주 추세를 보려면 원본 데이터부터 그만큼 가져와야 함).

### 13-3. 권고(권고 문구)는 근거 있는 주제만

evidence_database.dart에 실제로 있는 근거만 씀: 수면 목표(EVIDENCE-011),
카페인 컷오프(EVIDENCE-008), 휴식(EVIDENCE-004/005 취지). **"운동"은 이
프로젝트가 조사해둔 근거가 없어서 의도적으로 추천하지 않음** - 나중에 실제로
운동 관련 근거자료를 조사해서 `컨디션매니저_근거자료.md`/`evidence_database.dart`
에 추가하기 전까지는 계속 배제.

### 13-4. 설계상 정한 값(연구값 아님 - ConditionRuleEngine의 3시간 낮잠 기준과
같은 성격, `today_forecast_engine.dart` 상단 주석에도 명시)

- 급성 판정 "6시간 미만" — EVIDENCE-011(7~9시간 권장)보다 낮게 잡아 "짧다"를
  보수적으로만 트리거.
- 평소 대비 부족 "90분 이상 차이" — 우연한 변동과 구분하기 위한 여유.
- 추세 "20% 이상 변화"해야 increasing/decreasing으로 인정, 급증/급감은
  "직전 1주가 전체 평균의 1.35배 이상/0.65배 이하"로 별도 판정(넓은 추세는
  평탄한데 최근 1주만 튀는 경우를 잡기 위함), 변동계수(표준편차/평균) 0.30
  이상이면 volatile.
- **주간 비교는 "완료된 주들을 반으로 갈라 앞 절반 vs 뒤 절반 평균"** - 이번
  주(진행 중)는 항상 제외(안 끝난 주라 총량이 적게 잡혀 오판 위험). 8주 창
  기준 보통 7개 완료 주가 나오므로 4 vs 3으로 갈림(자동 계산, 하드코딩
  아님) - 4주였던 1차보다 표본이 넉넉해져 "최근 이상치 한둘로 추세가
  오염"되는 위험이 줄어듦(사용자가 8주로 늘려달라고 한 이유).
- 수면 충분도 판정 기준은 "밤마다 EVIDENCE-011의 권장 최소(7시간) 이상인가"
  - 개인 평소 패턴이 아니라 일반 기준을 씀(개인 평소 대비 비교는 급성 이벤트
  전용으로 따로 있어서, 두 비교 기준을 섞지 않기 위함).

### 13-5. UI

`condition_tab.dart`의 `_TodayForecastCard` - 탭 맨 위(대기 중인 수면 확인
카드 다음), 강조 테두리/배경으로 기존 카드들과 시각적으로 구분. 하단에 작게
"최근 근무일 N일 중 수면 기록 M일 반영" 캡션으로 데이터 근거를 투명하게 보여줌.
기존 "오늘의 컨디션"/"오늘의 추천"/"이번 주 패턴"/"최근 수면 기록"/"수면과
근무의 관계"/"왜 이런 조언을 하나요" 카드는 전부 그 아래로 이동(내용 변경 없음).

### 13-6. 수면 기록 유도 토스트

`_ConditionBodyState._maybeShowSleepNudge()` - 탭에 처음 들어왔을 때(State
인스턴스당 1회 체크) 어제가 근무일이었는데 위젯/자동감지 어느 쪽으로도 수면
기록이 없으면 `SnackBar`(액션 버튼 "입력하기")로 한 번만 물어봄 - alert
다이얼로그처럼 강제로 닫아야 하는 게 아니라 완전히 무시해도 되는 형태(명시적
요청). "오늘 하루 한 번만"은 SharedPreferences(`condition_sleep_nudge_shown_date`)
로 보장. 대기 중인 자동감지 확인 카드가 이미 떠 있으면(그쪽이 먼저 물어보고
있으므로) 중복으로 안 띄움.

### 13-7. 한계 / 다음에 확인이 필요한 것

- **실기기 검증 전무 + 자동 테스트 없음** - 손으로 몇 가지 경우(추세 증가/
  급증/평탄+급증 구분 등)를 코드 트레이싱으로만 확인했고, `flutter test`에
  이 엔진 전용 단위 테스트는 아직 없음(이 세션 범위 밖으로 미룸). 실제 몇
  주치 데이터가 쌓인 상태에서 문구가 정말 "장황하지 않고 스마트하게"
  느껴지는지, 절 4개(추세/수면/원인/권고) 조합이 자연스러운 한국어 문장으로
  읽히는지는 실사용해봐야 확실히 앎. 13-4의 임계값(20%/1.35배/0.65배/
  변동계수 0.30 등)은 전부 1차 추정치라 조정 가능.
- 급성 이벤트가 있을 때는 원인추론 문장을 생략하는데, 이때도 급성 문장 자체가
  이미 원인을 담고 있어서 자연스러운지 확인 필요.
- 휴무일에 대한 "평소 패턴"은 다루지 않음(근무명 기준으로만 개인 평균을
  잡음) - 휴무 전날 수면 패턴 비교는 다음 단계 후보.
- "운동" 관련 권고는 근거자료가 없어 의도적으로 비워둠(13-3) - 필요하면 먼저
  근거 조사부터.

### 13-8. (2026-09-01) 워딩 1차 다듬기 + 다음 세션 인계 노트

**이번 세션에서 마지막으로 한 것**: `today_forecast_engine.dart`의 문구 90여 개를
전체적으로 재검토해서 어색한 표현 몇 개를 고침 - "다른 주들과 달리 최근 1주
근무시간이 크게 늘었/줄었어요"(급증/급감 main variant 3) → "평균 주별
근무시간에 비해 ~"로, 화살표(`이전 X시간 → 최근 Y시간`) 쓰던 증가/감소
variant 3을 자연스러운 문장(`이전 약 X시간에서 최근 약 Y시간으로 계속 늘고/
줄고 있어요`)으로, 그 외 몇 군데 다소 딱딱했던 표현(`~하는 상태예요`,
`~빈도가 줄었어요`, `~함께 작용해` 등)을 더 대화체에 가깝게 다듬음. 코드
자체 로직(축 분류/우선순위/조합 순서)은 이번엔 안 건드림 - 순수 워딩만 손봄.

**다음 세션에서 하기로 한 것(사용자가 명시)**: ① 이 예측 로직(축 분류·임계값·
조합 방식) 자체를 더 개선 ② 컨디션 탭을 "완성"시키기(1차 완성 이후 후속
단계) - 둘 다 아직 구체적인 To-do로 확정된 건 아니라, 세션 시작 시 사용자에게
방향을 먼저 물어볼 것. 다만 13-7의 한계들이 자연스러운 시작점 후보:

1. **실기기 검증** - 이 세션 전체가 코드 트레이싱으로만 검증됐음. 근무일지
   며칠~몇 주치를 실제로 넣어보고 카드에 뜨는 문장이 자연스러운지/근거
   없이 튀는 판정은 없는지 눈으로 확인 필요. **가장 먼저 해볼 만한 것.**
2. **`today_forecast_engine.dart` 단위 테스트** - 아직 0개. `_classifyOtTrend`
   등이 전부 private이라 지금 구조로는 `buildTodayForecast()`(공개 API)를
   통해서만 간접 테스트 가능 - `ShiftPatternAnalyzer`/`SleepHistoryNight`를
   합성해서 시나리오별(증가/급증/급감/변동성/수면 0%~100% 등) 골든 테스트를
   짜는 게 실기기 검증보다 먼저/병행해서 할 만함.
3. **임계값 튜닝** - 13-4에 나열된 값(20% 변화, 1.35/0.65배 급증·급감,
   변동계수 0.30, 급성 6시간, 평소 대비 90분)은 전부 1차 추정치. 실데이터로
   "너무 예민하다/둔하다" 판단되면 조정.
4. **컨디션 탭 "완성"이 정확히 뭘 뜻하는지 사용자와 먼저 정렬** - 후보로
   생각해볼 만한 것들(전부 미확정, 다음 세션에 확인):
   - 새 `_TodayForecastCard`와 기존 "오늘의 컨디션"(`_TodayConditionCard`,
     4-rule 판정 그대로 노출) 카드가 지금 둘 다 있어서 내용이 일부 겹쳐
     보일 수 있음 - 그대로 둘지, 기존 카드를 접이식으로 축소할지 등.
   - Phase 5(Firebase Health Tips)/Phase 6(주간·월간 리포트 통합)는 여전히
     범위 밖(9장 다음 단계). "완성"에 이게 포함되는지 확인 필요.
   - l10n(다국어) 미적용 상태 계속 유지할지(1차라서 의도적으로 미룸,
     `condition_tab.dart` 파일 상단 주석).

## 14. (2026-09-01 후속) "개인 평소 근무시간" 반영 - 예측 문구 레이어 한정

**문제의식**(사용자 피드백): "기본 근무시간이 8시간인 사람이 12시간 근무하면
힘든 거고, 기본이 12시간인 사람한테 맨날 '12시간이라 위험하다'고 하면 그
사람은 매일 위험 판정이 되어버린다 - 실제로는 12시간 근무자는 휴일이 더
잦아서 보완되는데 그런 디테일이 없다."

**결론 - 두 레이어를 분리해서 처리**:

- **`ConditionRuleEngine.RULE_LONG_SHIFT`(12시간 이상, EVIDENCE-004)는 그대로
  둔다(절대 안 건드림)**. 이건 "습관화 여부와 무관하게 장시간 근무 자체가
  피로/사고 위험을 높인다"는 실제 연구 근거값이라, 개인별 상대 비교로 바꾸면
  근거를 왜곡하게 됨(0장/이 문서 전체가 반복하는 "근거 없는 임의 판정 금지"
  원칙과 정면 충돌). 12시간 근무자에게 매번 이 finding이 뜨는 건 "그 사람은
  매번 이 위험 요인에 노출된다"는 사실을 그대로 보여주는 것이지 오작동이
  아님.
- 대신 **"오늘의 컨디션 예측"(`today_forecast_engine.dart`, 조합형 문구
  레이어 - 원래도 evidence 절대 기준이 아니라 "설계상 정한 값"을 쓰는 곳)에만
  개인 상대 비교를 추가**함:
  - `sleep_history.dart`의 `personalBaselineShiftMinutes()` - 최근 8주 근무일
    (근무명 무관하게 전부)의 평균 근무시간(분). 표본 5건 미만이면 null(신규
    사용자는 "평소"를 아직 모르므로 비교 자체를 건너뜀).
  - 어제 근무가 길었는지 판단하는 급성 이벤트 규칙(13-4/`_findAcuteEvent`)이
    기존 절대 기준(11시간 이상)을 **개인 평소 대비 90분 이상 긴가**로 교체
    (표본 부족 시에만 절대 기준 11시간으로 폴백) - "기본 12시간인 사람이 매번
    이 신호를 트리거하는" 문제를 고침.
  - 새로 `_findTodayLongerThanUsual()` - 급성 이벤트가 없을 때, 오늘 근무
    자체가 개인 평소보다 90분 이상 길면 헤드라인 뒤에 짧게 덧붙임("오늘
    근무는 평소(약 X시간)보다 긴 약 Y시간이에요").
  - 두 임계값(90분) 다 근거자료 값이 아니라 순수 문구 판단용(수면 쪽
    `_kBelowBaselineGapMinutes`와 대칭 맞춤).
- **"12시간 근무자가 휴일이 더 잦아서 보완된다"는 부분은 이번엔 반영 안 함** -
  이건 "근무시간 대비 개인 평소"가 아니라 "회복 구간(휴무 포함) 패턴 자체를
  비교"하는 별도 주제라 범위가 더 큼(예: 주당 실근무일수/연속근무-휴무 비율의
  개인 평소 대비 비교). `WeeklyPatternSummary`가 이미 이번 주 패턴은 계산해두고
  있으니, 다음에 필요하면 이걸 확장하는 방향으로 접근 가능 - 상세 설계는
  다음 세션 후보.
- 상세 구현 기록은 수면기록_자동추정_설계.md 13-7장(같은 세션에서 자동 감지
  창 축소와 함께 진행됨 - 두 작업 다 sleep_history.dart를 건드려서 한 문서에
  몰아 적지 않고 각자 관련 문서에 나눠 기록함).

## 15. (2026-09-01 후속4) "오늘의 컨디션 예측" 개선 + 중복 카드 정리

**"실제로 얼마나 잤는지"가 최우선 신호가 되도록 급성 이벤트 규칙 수정**:
`_findAcuteEvent`(today_forecast_engine.dart)가 원래 "어제 근무가 길었을
때만"(`shiftWasLong` 게이트) 수면 부족을 헤드라인으로 띄웠는데, 그러면 근무
자체는 평소와 비슷한데 이런저런 이유로 수면만 유독 짧았던 날(예: 8시간 근무
후 실제로는 3시간만 잠)을 완전히 놓쳤다. 이제 **수면이 6시간 미만
(EVIDENCE-011 권장 최소 밑)이면 어제 근무 길이와 무관하게 항상** 헤드라인으로
뜨고, 어제 근무가 "이 사람 평소보다도" 길었으면(14장의 개인 평소 근무시간 비교)
그 사실을 문장에 덧붙여 원인을 더 구체적으로 짚어줄 뿐 - 더 이상 게이트
조건이 아니다. (수면기록_자동추정_설계.md 13-8장의 `sleep_history.dart` 매칭
완화와 짝을 이룸 - 짧은 수면도 데이터로 잡혀야 이 신호가 작동하므로.)

**"오늘의 컨디션" 카드 삭제**(사용자 요청): `condition_tab.dart`의
`_TodayConditionCard`(4-rule 판정 그대로 노출하던 카드, 예: "12시간
근무입니다. 관련 연구에서는...")를 화면에서 뺐다 - 맨 위 "오늘의 컨디션
예측" 카드가 이제 실제 수면 데이터까지 종합해서 하고 싶은 말을 다 하므로
중복이라고 판단. `ConditionRuleEngine` 자체(판정 로직/`RULE_LONG_SHIFT` 등)는
0줄 수정 - "오늘의 추천" 카드가 여전히 그 결과(`RULE_SLEEP_WINDOW`,
`RULE_CAFFEINE_CUTOFF` 등 tips)를 그대로 씀. 화면에서 판정 레벨
(NORMAL/ATTENTION/HIGH_LOAD)을 직접 문장으로 노출하던 카드만 없앤 것 -
`ConditionResult`/`ConditionLevel`은 여전히 내부적으로 계산되고 tips 생성에
쓰인다.

**다음에 확인할 것**(사용자가 명시 - 이번엔 손 안 댐): "오늘의 추천" 카드도
"갈무리가 필요할 듯"하다는 의견이 있었음 - 구체적으로 뭘 어떻게 정리할지는
다음 세션에 논의.
5. **"운동" 근거자료 조사** - 필요하다고 판단되면 `컨디션매니저_근거자료.md`
   에 먼저 채워넣고 나서 `evidence_database.dart`/추천 로직에 반영.

**바로 이어서 작업할 때 먼저 읽을 파일**: 이 문서 12~13장(이번 세션 전체),
`lib/services/condition/today_forecast_engine.dart`(핵심 로직),
`lib/services/condition/sleep_history.dart`(데이터 준비),
`lib/providers/sleep_condition_provider.dart`(provider 3개:
`recentWorkNightsProvider`/`personalSleepPatternsProvider`/`todayForecastProvider`),
`lib/screens/condition_tab.dart`의 `_TodayForecastCard`/`_ConditionBodyState`

## 16. (2026-09-01 후속5) "최근 5일 실측 피로도" 축 추가 - 달력 변경이 문구에 반영 안 되는 문제 해결

**문제 발견 경위(사용자 리포트)**: 달력 탭에서 이번 주 휴무를 전부 야간근무로
바꿨는데 "오늘의 컨디션 예측" 문구가 전혀 안 바뀜. 조사 결과 - 헤드라인이었던
축1(`_classifyOtTrend`, OT 트렌드)이 **"완결된 주"만** 트렌드 비교에 씀
(`weeks.sublist(0, weeks.length - 1)`로 진행 중인 이번 주를 항상 제외) - 그래서
이번 주에 뭘 바꾸든 최소 그 주가 다 지나가야 반영됐음. `ConditionRuleEngine`도
"오늘 하루"의 회복구간/근무길이만 보고, 그 결과(`recoveryMinutes`/
`levelFindings`)는 애초에 예측 문구 조합에 안 쓰이고 있었음(폴백 메시지
용도로만 존재). 결과적으로 "달력을 바꿔도 미래 날짜가 실제로 지나가기 전까진
100% 무반응"이었던 것 - 버그가 아니라 설계상 과거/오늘만 보게 되어 있었는데,
그 "과거"의 갱신 단위가 주(週) 단위라 지연이 너무 컸던 게 진짜 원인.

**해결 - 축4(신규) "최근 5일 실측 피로도" 추가**(`_classifyRecentFatigue`):
- **철저히 과거 기반** - 오늘 포함 최근 5일(`_kFatigueWindowDays`)만 봄. 이번
  주 후반(아직 안 지난 미래) 근무 변경은 절대 안 봄 - 실제로 근무하게 될지
  모르고, 수면도 알 수 없으므로("미래는 로직에 넣지 말 것" 사용자 명시 원칙).
  하지만 이 창은 **매일 굴러가므로** 어제 반영된 변경이 오늘 바로 문구에
  드러남(주 단위였던 축1의 지연 문제 해결).
- 신호 4개를 조합: ① 야간근무 빈도(5일 중 3일 이상), ② 개인 평소 대비 근무량
  (근무시간+OT, 축1/`_findTodayLongerThanUsual`과 동일한 "평소 대비 90분"
  기준 재사용), ③ 최근 수면 부족(EVIDENCE-011, 표본 2개 이상일 때만), ④ 오늘의
  회복시간 부족(`ConditionRuleEngine`의 `RULE_SHORT_RECOVERY` 결과를 그대로
  읽어옴 - 임계값 중복 정의 안 함).
- **실제 초과근무(OT, `date_overtime`)가 이번에 처음으로 컨디션 예측에
  반영됨** - 그 전엔 `ShiftPatternAnalyzer`가 `date_overtime` 테이블을 아예 안
  읽어서 존재 자체가 무시됐음. `recentOvertimeMinutesProvider`(신규,
  `sleep_condition_provider.dart`)가 최근 7일치를 직접 조회(달력 탭이 미리
  로드해둔 `overtimeProvider` 캐시에 기대지 않음 - 컨디션 탭을 달력 없이 바로
  열어도 정확하도록). 임계값은 사용자가 직접 지정: **누적 OT 8시간 이상**일
  때만 "초과근무가 많아 보여요" 수준으로 문구에 영향(3시간 안팎은 무시) -
  EVIDENCE-012(EU 근로시간지침, 연장근무 포함 주간 근무시간 기준)로 근거를 댐 -
  이 Evidence는 2026-09-01 최초 세션 때 이미 이 용도로 추가돼 있었는데
  실제로는 이번에 처음 코드에서 씀.
- **신호를 원자료로 나열하지 않음**(사용자 명시 요구 - "최근 5일간 3일
  야간근무했습니다" 식 금지, "너무 AI스럽게/수치식으로 말하지 말 것") - 대신
  신호 개수(0/1/2개 이상)로 3단계(`stable`/`building`/`high`) 강도만 정하고,
  강도별로 톤이 다른 문장을 고름("야간근무가 조금씩 잦아지고 있어요"(building) →
  "피로 누적이 상당한 수준으로 예상돼요"(high, 2개 이상 겹칠 때)). 조합형+변주
  로테이션(`_pickFrom`) 패턴은 기존 축들과 동일하게 유지.
- **헤드라인 우선순위 재정렬**: acute(어제 급성) > 축4(최근 5일) > 축1(OT
  주간 트렌드, fallback). 축4가 뜨면 축1의 헤드라인 역할을 대신하고
  `_combineConcern`(원인 설명 문장)도 생략(축4 문장이 이미 원인을 담고
  있으므로 중복 방지). 축1(`_classifyOtTrend`) 자체는 삭제하지 않음 - 여러 주에
  걸친 더 긴 호흡의 추세 서술은 여전히 유효한 정보라 축4가 조용할 때(flagCount
  0) fallback으로 남겨둠.
- `_pickRecommendation`(팁 선택)도 축4가 `stable`이 아니면 "휴식을 챙기라"는
  팁이 뜨도록 `recentFatigueConcern` 파라미터 추가.
- `ConditionRuleEngine` 자체는 이번에도 0줄 수정 - `RULE_SHORT_RECOVERY` 결과를
  읽기만 함(완전 분리 원칙 유지).
- **미검증**: `flutter analyze` 통과만 확인. 실기기로 "달력에서 근무를 바꾸고
  하루 지난 뒤 문구가 실제로 바뀌는지"는 아직 안 봄 - 다음 세션 후보(13-8의
  "실기기 검증" 항목과 동일 성격).

## 17. (같은 날 후속8) 추천 로직 전면 교체 - "행동 팁"을 요인 기반으로

**문제의식(사용자)**: `_pickRecommendation`(구)이 "수면부족→휴식→카페인→유지"
4갈래 고정 팁이라 어떤 원인이든 같은 문구가 나옴 - "그날의 특화된 추천"이
아니라는 지적.

**해결**: 축4(`_classifyRecentFatigue`)가 이미 우선순위까지 매겨둔
`reasons`(recovery/workStreak/nightFrequency/sleepShortage/overtimeLoad/
generalLoad + 후속9에서 추가된 extendedStreak)를 그대로 재사용해서, 요인마다
전용 행동 팁을 붙임(`_actionTipPrimary`/`_actionTipSecondary`). "운동은 근거
없으니 절대 추천 안 함" 원칙은 **사용자가 명시적으로 완화** - "무리한 운동은
삼가고 가벼운 스트레칭 정도로" 같은 특정 논문 인용 없는 상식 수준 조언은
evidenceIds 없이 허용(파일 상단 원칙 2번 주석 참고). "카드 제목에서 '예측'을
빼고 '오늘의 컨디션'으로 확정"(추천 팁까지 포함하니 예측만은 아니라는 사용자
설명) - `condition_tab.dart`의 `_TodayForecastCard` 제목 텍스트 변경.

## 18. (같은 날 후속9) 실제 시나리오 테스트로 발견한 진짜 버그 2건 + 정제

**요청**: 주간(07-19)/야간(19-07) 12시간 패턴 + 대근 변형(주주주휴야야휴휴 등)
+ 신규 4교대 MENO(M 07-15/E 15-23/N 23-07/O 휴무) 패턴까지 실제 시나리오로
검증. `test/today_forecast_engine_scenarios_test.dart`(신규, 15개 시나리오) -
수면 커버리지 5가지 조합(전무/전부/부분적/최근시작/최근중단), 야간 몰빵
패턴(야야야야휴), 8주 내내 고강도, 휴가 위주 저강도, MENO 패턴 전부 포함.

**발견한 버그 1(중요) - 가장 심각한 신호가 카드에서 아예 빠져 있었음**:
"야야야야휴"(5일 주기, 4연속 12시간 야간+휴무 1일)로 테스트했더니
`ConditionRuleEngine`은 이미 `RULE_EXTENDED_STREAK_SHORT_BREAK`(12시간+ 근무
3일 연속 + 다음 회복 48시간 미만, EVIDENCE-004+005)로 **HIGH_LOAD**까지
판정하고 있는데, 축4는 `RULE_SHORT_RECOVERY`만 읽고 이 rule은 전혀 안 읽어서
"오늘의 컨디션" 카드엔 "요즘 야간근무가 조금씩 잦아지고 있어요"라는 순한
문구만 뜨는 모순이 있었음(workStreak는 5일 이상 기준이라 이 패턴에선 최대
4일에서 항상 끊겨 절대 안 뜸). `_FatigueReason.extendedStreak`를 최우선
순위로 추가해서 고침 - `RULE_LONG_SHIFT`(단순 12시간 하루)는 여전히 안 씀
(습관화된 12시간 근무자를 매일 플래그하면 안 된다는 14장 원칙 유지),
`RULE_EXTENDED_STREAK_SHORT_BREAK`만 "연속+짧은 회복"이라는 실제 이상
패턴이라 반영.

**발견한 버그 2(경미) - "회복 중"이 아닌데 "회복되고 있다"고 서술**:
수면 기록이 처음부터 끝까지 쭉 좋았던(consistentlyGood, 변화 없음) 시나리오
에서 `_combineConcern`이 "전반적으로 컨디션이 회복되고 있을 수 있어요"라고
서술 - `_sleepImproving()`가 "꾸준히 좋음"과 "실제로 좋아지는 중
(recentlyImproved)"을 같은 카테고리로 묶어서 생긴 부정확한 표현. "진짜 개선
추세"(otReallyImproving/sleepReallyImproving - decreasing/recentDrop/
recentlyImproved만)와 "원래도 꾸준히 좋음"을 분리해서, 후자는 그냥 중립
문구로 떨어지게 고침(sleepClause가 이미 "수면이 꾸준히 좋다"고 말한 뒤라
중복 방지 목적도 겸함).

**정제(사용자 요청 - "4~5개 요인 겹쳐도 전부 나열 말고")**: 추가 언급 개수
2 → 1로 축소(헤드라인 + 후속 1개 + 종합 = 최대 3문장), "그 외에도 몇 가지
더" 오버플로 문장 제거(종합 문장 톤만으로 충분). 연속근무일수 문구에서
실제 숫자("5일째")도 빼고 "연속 근무가 잦다/길어졌다" 식 정성 표현으로
통일(raw 데이터 지양 원칙을 이 축에도 동일 적용).

**수면 문구도 세분화**: "전부 다 있으면"(coverage=high) 격려 문구 생략,
"아예 없으면"(coverage=none) 기존 기회 프레이밍 유지, "부분적"(coverage=
low/mid)일 때만 실질 문장 뒤에 "꾸준히 기록해주시면 더 정확해져요" 격려
꼬리를 추가로 붙임(`_composeSleepClause`를 `_composeSufficiencyClause`로
분리).

`test/today_forecast_engine_test.dart`(11개) + 새 시나리오 파일(15개) 전부
통과. 기기 대신 코드 테스트로 진행한 이유는 16-2장과 동일.

## 19. (같은 날 후속10) "오늘의 컨디션 점수"(0~100) - 원칙 의도적 반전

**⚠️ 중요 - 기존 원칙 반전**: `condition_rule_engine.dart`/
`today_forecast_engine.dart` 상단, 그리고 CLAUDE.md에 반복 명시됐던
"숫자 점수(0~100)/%/확률/질병 진단 절대 금지" 원칙을, **사용자가 이 축에
한해 명시적으로 요청**해서 의도적으로 뒤집었다. 사용자 스스로 "가중치는
내가 직접 조정하고, 근거는 나중에 별도로 설명할 계획"이라고 밝힘 - 기존
3단계(NORMAL/ATTENTION/HIGH_LOAD) 판정과 "오늘의 컨디션" 문구 레이어
(today_forecast_engine.dart)는 이번에도 전혀 안 건드림(0줄 수정, 완전
분리 원칙 그대로) - 점수는 `condition_score.dart`(신규)라는 완전히 별개의
추가 레이어.

**계산 방식**: 100점에서 시작해서 요인별로 감점(가중치는 전부
`condition_score.dart` 상단 `_w*` 상수 - 사용자가 나중에 바로 조정 가능):
- 회복시간 부족(EVIDENCE-002/003, 11시간 미만 - 부족량 비례, 최대 -20)
- 오늘 근무 12시간 이상(EVIDENCE-004, Folkard&Lombardi 상대위험 참고) -15
- 장시간근무 연속+짧은 회복(RULE_EXTENDED_STREAK_SHORT_BREAK 재사용) -15
- 최근 5일 야간근무 3일 이상 -8 / 연속근무 5일 이상 -8 / 초과근무 8시간+ -6
- 역방향 교대(EVIDENCE-001, 신규 반영 - 기존엔 판정에 안 쓰이던 context용
  정보였는데 사용자 요청으로 점수 축에 추가) -5
- 수면 부족(EVIDENCE-011 권장 최소 7시간 기준 - 사용자가 제시한 "6시간"은
  실제 근거자료(7~9시간) 기준과 안 맞아서 기존 코드 기준인 7시간으로
  통일함, 이 부분만 사용자 원안과 다르게 조정했다고 보고 필요) - 부족량
  비례, 최대 -20
- "교대근무 자체"의 기본 부담(오늘이 오후/야간이면 -15, 주간이지만
  로테이션 스케줄이면 -8, 휴무면 0) - 특정 논문 수치가 아니라 순수 설계값
  (사용자가 말한 "스케줄이 바뀌는 것 자체가 힘들다"를 반영하려고 새로 만든
  축, 나중에 조정 전제)

수면 데이터가 없는 축은 감점하지 않음(측정 안 된 걸 나쁘다고 단정 안
한다는 기존 철학 유지) - "근거 없이 나쁘다고 단정 안 함" 원칙은 이 점수
레이어에도 그대로 적용.

**1차 캘리브레이션 결과**(`test/condition_score_test.dart`, 7개 시나리오):
완전 휴무+수면 8시간=100점, 평범한 주간근무일=82점, 평범한 야간근무일=75점
(사용자가 말한 "OT 없어도 스케줄 자체 때문에 70점 전후"에 근접), 야간
3연속=52점, 장시간연속+수면부족=18점, 최악 조합=9점 - 대체로 사용자가
말한 감(100/70/낮음)과 방향이 맞음. **정밀 최적화는 안 됨** - 사용자가
나중에 `_w*` 상수를 직접 조정할 걸 전제로 한 1차 설계값.

**아이콘 표시**: "95~100/90~95…처럼 5점 단위로 아이콘"을 문자 그대로
20단계로 만들진 않고, 8단계(`kConditionScoreTiers` - 🌟😄🙂😌😐😕😣😫,
점점 굳어가는 표정 하나의 계열로 "연결성" 확보)로 묶고 **정확한 점수
숫자는 항상 그대로 노출**하는 절충안으로 구현함 - 20개를 억지로 구분하면
아이콘 간 차이가 무의미해질 거라 판단(임의 결정, 사용자가 다른 방식을
원하면 조정 가능). `condition_tab.dart`의 `_TodayForecastCard`에 제목
바로 아래, 기존 워딩 위에 "오늘의 컨디션 점수 NN점 [아이콘] (등급)" 행을
추가.

**미검증**: 실기기 미확인. 가중치는 명시적으로 "1차 추정치, 사용자가
재조정할 것"이라는 전제로 설계함 - 다음에 실사용 데이터를 보고 값을
조정하는 캘리브레이션 세션이 필요.

## 20. (같은 날 후속11) 레이아웃/점수 보정 + "교대근무 건강 & 수면관리 Tip" 신설

**레이아웃**: "오늘의 컨디션" 제목 줄 우측에 "컨디션 점수 : NN점" + 아이콘을
같은 줄로 이동(`_ConditionScoreInline`, 기존 `_ConditionScoreRow`를 대체).

**점수 보정 - "문제 축" 상한 도입**: "아무리 나빠도 최악이 40~50점 선"
요청으로, 회복/장시간/연속스트릭/야간빈도/연속근무/OT/역방향/수면(문제
축 8개) 감점 **합계에만** 상한(`_kMaxProblemDeduction = 45`)을 씌움 -
"교대근무 자체" 기본 부담은 상한과 별개로 항상 그대로 적용. 개별 가중치를
전부 낮추는 방식 대신 이렇게 한 이유: 요인 1~2개짜리 보통 나쁜 날은
세밀하게 감점되고, 여러 개가 한꺼번에 쌓이는 극단적인 날만 상한에 걸려
40점대로 수렴함(1차 버전은 최악이 9점까지 떨어졌었음). 부작용: 이미
상한에 걸린 두 극단 케이스끼리는 서로 구분이 안 됨(트레이드오프로 인지).
8단계 아이콘 경계도 새 분포(대략 40~100)에 맞춰 재등분(95/88/80/72/64/
56/48/그 아래).

**"오늘의 추천" → "교대근무 건강 & 수면관리 Tip"로 전면 교체**: 기존
`ConditionRuleEngine.tips`(오늘 상황에 맞는 걸 전부 나열, 근무시간 계산이
섞여 있던 수면시간대 등)를 화면에서 걷어내고, "오늘의 컨디션"과 완전히
무관한 범용 건강 상식 카드로 교체(사용자 요청). 필드는 아이콘/제목/내용
3개뿐(번호 없음), **하루에 1개만** 순서대로 로테이션. 기존 5개(수면/빛노출/
카페인/식사/낮잠)를 특정 근무 계산값 없는 범용 문장으로 다시 쓰고
(카페인의 "구체적 취침시각"은 제거, 낮잠은 야간근무 전용이 아니라 주간·
휴무일에도 범용으로 확장), 운동/스크린타임/수분/알코올/관절부담/가족시간/
규칙적 식사/스트레칭/눈건강/실내온도까지 10개를 추가로 만들어 총 15개
(`health_tips_catalog.dart`). **Firebase 연동**: `health_tips` Firestore
컬렉션(app_config와 동일한 "읽기 전용 원격설정" 패턴, `firestore.rules`에
추가 - list도 허용한 게 app_config와의 차이점)에서 읽되, 미설정/오프라인/
빈 컬렉션이면 로컬 15개로 조용히 대체(`health_tip_provider.dart`). 사용자가
Firebase 콘솔에서 문서를 추가하면 앱 재배포 없이 자동 반영됨 - 사용자가
"애초에 만들려고 했던 Firebase 기반 팁 리스트"라고 밝힌 것의 1차 구현.
로테이션은 해시 기반 의사난수가 아니라 **단순 나머지 연산**(날짜번호 %
개수) - "골고루 돌아가야 한다"는 요청대로 전부 빠짐없이 균등하게 순환됨.
`_EvidenceSection`("왜 이런 조언을 하나요")도 이제 안 보이는 `result.tips`
대신 실제로 화면에 보이는 `forecast.evidenceIds`를 근거로 삼도록 같이 고침.

## 21. (같은 날 후속12) 수면 측정 로직 정밀 분석 - 낮잠+메인수면 합산 버그 발견·수정

**요청**: "낮잠 한 번/두 번 껴서 총 수면시간이 충분한 경우"까지 고려해서
수면 측정 로직을 정밀 분석. 확인 결과 **진짜 버그 발견**:
`collectRecentWorkNights()`(sleep_history.dart)가 근무 종료 후 매칭되는
수면 기록 중 **가장 이른 것 1건의 길이만** 그 밤의 수면시간으로 썼음 -
짧은 낮잠을 먼저 자고 몇 시간 뒤 진짜 메인 수면을 또 잔 경우(흔한 패턴:
퇴근 직후 잠깐 졸다가 씻고 다시 본격적으로 잠), 실제 총 수면은 충분한데도
"낮잠 길이만큼만 잤다"로 계산돼 축4(poorRecentSleep)와 급성 이벤트
(`_findAcuteEvent`, "어제 O시간밖에 못 주무셨어요")가 **전부 오탐**할 수
있는 구조였음.

**수정**: `SleepHistoryNight`에 `totalSleepMinutes`(매칭되는 모든 수면의
합계) 필드를 추가하고 `sleepMinutes`/`hasSleepData`가 이제 이 합계를
반환하도록 바꿈(호출부 코드는 안 바뀌어도 의미가 정확해짐 - 축4/급성이벤트/
점수의 `avgRecentSleepMinutes` 전부 자동으로 정확해짐). 취침 시각은
여전히 "가장 이른" 수면 기준(`sleep`), 기상 시각은 새로 추가한
"가장 늦게 끝난" 수면 기준(`latestSleep`)으로 분리 - 낮잠 종료 시각이
기상 시각으로 잘못 쓰이던 부수 버그도 같이 고침. `buildPersonalSleepPatterns`
도 이 두 필드를 정확히 반영하도록 수정.

**Dart↔Kotlin 수면 감지 파리티 검증**: `sleep_opportunity.dart` ↔
`SleepScheduleResolver.kt`를 줄 단위로 대조 - 활성/예정 창 우선순위, 캡
계산, 야간 판정 경계(05~16시=day/evening, 그 외=night) 전부 정확히 일치함
확인(불일치 없음). `SleepDetectionReceiver.kt`(2회 연속 꺼짐 확인, 창
게이팅, 2시간 미만 메인수면 폐기)와 `SleepWidgetActionReceiver.kt`(수면/
기상 버튼 상호배제, 2분 미만 폐기, AUTO_DETECTED 확정 시 2시간 기준
적용)도 검토 - 로직 자체는 기존 세션에서 이미 여러 번 다듬어진 상태라
새로운 버그는 못 찾음. 다만 "AUTO_DETECTED가 2시간 미만으로 끝나면
저장하지 않고 통째로 버려짐"(짧은 수면으로라도 안 남고 삭제됨)은 의도된
트레이드오프이되 실사용에서 데이터 유실처럼 느껴질 수 있어 다음 세션에
재검토할 만한 항목으로 남김.

**테스트**: `test/sleep_history_test.dart`(신규 5개) - 낮잠만/메인만/낮잠+
메인/낮잠 2번+메인/통합 시나리오(버그 수정 전이었다면 반드시 오탐했을
케이스) 전부 통과. 기존 38개(11+15+7+5)까지 전부 그린.

### 16-1. (같은 날 후속6) 축4 1차 구현이 "야간 빈도 하나만" 보던 문제 - 신호 확장 + 다문장화

**사용자가 잡은 구멍**: "오늘/전날/전전날 야간 + 전전전날/전전전전날 주간"
예시로 축4를 테스트해보니 - 5일 전부 근무(휴무 0일)인데도 "야간근무 비중이
늘어난 편" 한 문장만 나오고 **"5일 연속 근무" 자체에 대한 언급이 아예 없음**을
지적. "야간이 없어도 5일 연속근무면 그 자체로 무리"라는 지적도 - 원래는
야간·수면·OT·회복 4개 신호만 보고 "연속 근무일수"는 안 봤음. 또한 여러 신호가
겹칠 때 상위 1~2개 조합만 하드코딩된 문장으로 처리하고 나머지 신호는 그냥
버렸던 것도 지적("워딩이 너무 단편적", "여러 측면에서 분석해야지").

**수정**:
- **신호를 4개 → 5개로 확장**: `_FatigueReason.workStreak`(연속 근무일수)
  추가. `ConditionRuleEngine`이 이미 계산해둔 `baseResult.consecutiveWorkDays`를
  그대로 읽음(임계값 재정의 없음) - 5일(`_kFatigueWindowDays`) 이상이면 신호
  켜짐. 이 신호는 EVIDENCE-005(12시간 이상 근무 3일 연속)와 조건이 달라서 그
  근거를 갖다 쓰지 않음 - evidenceIds 없이 순수 서술만.
- **`_RecentFatigueResult.clause`(단일 문자열) → `.sentences`(리스트)로
  구조 변경**: 겹친 요인을 전부 순서대로(우선순위: 회복시간 > 연속근무일수 >
  야간빈도 > 수면부족 > 근무량) 문장으로 짚어준 뒤, 마지막에 요인
  개수(2개/3개 이상)로만 강도를 나누는 종합 문장으로 마무리. "야간 3일
  했습니다"식 원자료 나열은 여전히 안 하지만, "겹치는 요인을 다 얘기해준다"는
  분석적인 느낌을 위해 문장 수를 늘림(최대 헤드라인 1 + 추가언급 2 +
  종합문장 1 = 4문장, 그 이상 겹치면 "그 외에도 몇 가지 더 있어요"로 뭉뚱그림).
- **축1(OT 주간 트렌드)이 축4 때문에 완전히 묵살되는지 재확인** - 아니었음:
  축4가 헤드라인일 때도 `_combineConcern`(원인 종합 문장)만 생략하고,
  축1이 뚜렷한 방향성(증가/급증/감소/급감)을 보이면 그 shortClause를 "몇 주째
  흐름은 어떤가"라는 보충 정보로 계속 덧붙임(완전 대체가 아니라 시야를
  넓히는 정보 추가). 축1 자체(`_classifyOtTrend`)는 여전히 축4가 조용할 때
  fallback 헤드라인으로 그대로 남아있음 - 삭제 안 함.
- **수면 기록 문구 재프레이밍**(사용자 요청): "수면 기록이 없어서 비교가
  어려워요"(결핍 프레이밍) → "수면을 기록해두시면 더 정확한 컨디션 예측을
  받아보실 수 있어요"(기회 프레이밍)로 `_composeSleepClause`의
  `coverage_none`/`sufficiency_insufficient` variant 전부 교체.
- `flutter analyze` 통과 확인. 이번에도 실기기 미검증 - 특히 "요인 4~5개가
  동시에 겹치는" 극단 케이스는 코드 트레이싱으로만 확인함(자연스러운 문장인지
  실제로 화면에서 보고 판단 필요).

### 16-2. (같은 날 후속7) 실제 시나리오 테스트 - 정제/검증 + 팁 버그 하나 발견·수정

**요청**: 주간 07~19시/야간 19~07시(각 12시간), 기본 패턴 "주주휴휴야야휴휴"에
대근으로 "주주주휴야야휴휴"/"주주휴휴야야야휴휴" 같은 변형과 OT 8시간을
섞은 실제 시나리오로 검증. "5일 연속근무"도 5일 창만 보고 판단하면 안 되고
전체(6일/7일)를 봐야 한다는 점, 4~5개 요인이 겹쳐도 전부 나열하지 말고
정제해서 보여달라는 점도 같이 요청받음.

**방법**: 기기 대신 `test/today_forecast_engine_test.dart`(신규) - 순수 함수인
`buildTodayForecast`를 실제 DB 없이 합성 `ShiftSchedule`/`ShiftTimeRange`/
`SleepRecord`로 직접 호출. 이 엔진은 100% 플랫폼 무관 Dart 계산이라(Kotlin/
UI 어디에도 안 걸침) 기기 설치본을 켜서 화면으로 보는 것과 검증 범위가
동일하면서 훨씬 빠르고 반복 가능 - 그래서 기기 대신 이 방법으로 진행함.
`flutter test test/today_forecast_engine_test.dart`로 재실행 가능(11개 시나리오,
전부 통과). 컨디션매니저_설계.md 13-8장이 "0개"라 남겨둔 단위 테스트 숙제의
1차분이기도 함.

**검증된 것**:
- 정성적 야간 3연속(주주휴휴야야[야]휴휴, 대근): "요즘 야간근무가 조금씩
  잦아지고 있어요." 단독 - 정확히 사용자가 처음 검증했던 케이스 그대로 재현.
- 주간 3연속(주[주]주휴야야휴휴, 야간 없음): 아무 신호도 안 뜸(3<5라
  workStreak 문턱 안 넘음) - 과민반응 안 하는지 확인.
- **6일 연속근무**(오프셋 -6=휴무로 스트릭을 정확히 캡, 5일 창보다 하루 더
  김): `baseResult.consecutiveWorkDays == 6`으로 정확히 잡힘(5로 클리핑되지
  않음) - `ConditionRuleEngine.consecutiveWorkStreakEndingAt`가 애초에
  무제한 역탐색이라 이 축의 5일 창 크기와 무관하게 항상 정확함을 실측 확인.
  7일 연속도 동일하게 `== 7`로 확인.
- OT 3시간 vs 8시간: 3시간은 기준 시나리오와 **문자 그대로 동일한 출력**
  (전혀 영향 없음), 8시간은 "평소 근무시간에 비해 초과근무가 많아 보여요."로
  전환 - 사용자가 지정한 두 임계값 그대로 동작.
  - 7일 연속근무+회복시간부족+3일이상야간(3개 요인) 케이스에서 헤드라인
    1문장+후속 1문장+종합 1문장(=3문장)으로 정확히 캡되고, 나머지(야간빈도)는
    문장으로 나열되지 않고 종합 문장의 "여러 부담이 겹치면서..." 톤으로만
    반영됨 - "정제해서 보여달라" 요청대로 동작.
  - 6일 연속 야간+OT 8시간(4개 요인 동시 활성)에서도 전체 메시지가 축4(3문장)
    +수면문구(1)+팁(1) = 5문장으로 유지됨 - 요인이 늘어나도 문장 수가
    비례해서 안 늘어남을 확인.
- 실제 수면기록(4시간/5시간, 2건) 투입 시 축4의 `sleepShortage`가 켜지는 것과
  별개로, 8주 전체 커버리지 관점에서는 여전히 "표본 부족"으로 잡혀 두 신호가
  서로 다른 문장으로 공존하는 것도 확인(설계대로 - 5일 창과 8주 창은 원래
  다른 질문에 답하는 축).

**발견·수정한 버그 1건**: 위 수면부족 시나리오에서 팁이 "무리하지 말고
휴식 시간을 확보해보세요"(일반 휴식)로 나왔는데, 실제로는 "오늘은 평소보다
조금 더 일찍 잠자리에..." 같은 수면 특화 팁이 나와야 맞음 - `_pickRecommendation`이
축4의 `sleepShortage` 신호를 몰랐기 때문(acute/장기 sufficiencyTrend만 보고
있었음). `_RecentFatigueResult`에 `sleepConcern`(활성 요인에 sleepShortage가
있는지) 필드를 추가해서 팁 선택에 반영 - 수정 후 재실행하니 정확히 수면 팁으로
바뀜을 확인.

**테스트 파일은 커밋 대상으로 유지함**(스크래치 아님) - `today_forecast_engine.dart`를
다음에 또 건드릴 때 회귀 확인용으로 계속 쓸 수 있음.
(카드 UI + 수면기록 유도 토스트).

## 22. (2026-09-04) ConditionRuleEngine v2 - "확보한 evidence는 빠짐없이 판정에" (사용자 요청으로 5장 원칙 개정)

**배경**: 도움말 문서화(전체_코드_점검_리포트_2026-09-04.md 후속 세션) 중
사용자가 "판정에 실제로 쓰이는 규칙은 딱 4가지" 문구를 보고 "내가 의도한
설계에서 완전히 벗어나 있다"고 지적 - 교대 방향/연속 야간근무 같은, 앱이
이미 별다른 사용자 입력 없이 계산 가능한 신호를 판정에서 배제한 게 문제라는
지적. 명시적 요청: "evidence에서 요하는 data가 내 앱에 이미 들어있으면
빠짐없이 활용해서 판정 로직을 다시 짜라, 무지성 나열이 아니라 워딩을
가감해가며 스마트하게, 점수판정에도 반영."

**조사 결과 - 이미 되어 있던 부분**: `condition_score.dart`(19장)는 사실
이미 direction(EVIDENCE-001, -5점)/야간빈도/연속근무일수/OT(EVIDENCE-012,
-6점)까지 전부 반영하고 있었음 - 그 사실을 사용자가 인지 못 하고 있었던
것으로 보임(레벨 판정과 점수가 서로 다른 폭으로 evidence를 쓰고 있었던 게
진짜 문제). 그래서 이번 v2는 **레벨 판정(`ConditionLevel`) 쪽을 점수 수준으로
끌어올리는** 작업 - 점수 쪽 가중치는 건드리지 않음(이미 사용자 요청대로
설계돼 있었고, 사용자가 나중에 직접 재조정할 축이라는 원칙도 그대로).

**5장 표 개정**: 기존 4-rule(`RULE_SHORT_RECOVERY`/`RULE_LONG_SHIFT`/
`RULE_COMPOUND_HIGH_LOAD`/`RULE_EXTENDED_STREAK_SHORT_BREAK`)에 2개 추가:

| Rule ID | 조건 | 결과 | Evidence |
|---|---|---|---|
| `RULE_WEEKLY_OVERTIME` | 최근 7일 누적 초과근무 ≥ 8시간 | Finding 추가, 최소 `ATTENTION` | EVIDENCE-012 |
| `RULE_BACKWARD_DIRECTION` | 현재 교대가 역방향 | Finding 추가, 최소 `ATTENTION` | EVIDENCE-001 |

`RULE_COMPOUND_HIGH_LOAD`도 "회복시간부족 AND 장시간근무"라는 고정 쌍에서
**"ATTENTION급 신호(위 4개: 회복시간부족/장시간근무/주간초과근무/역방향교대)
중 2개 이상 동시 성립"**으로 일반화됨 - 기존 쌍은 이 조건의 특수 케이스로
그대로 포함(회귀 테스트로 확인, 아래 참고).

**근거 강도에 따른 취급 차등** - "빠짐없이 활용"하되 전부 동일 무게로
안 씀: EVIDENCE-004/005(정량적 권고, 원문 숫자)는 `RULE_EXTENDED_STREAK_SHORT_BREAK`로
여전히 그 자체만으로 단독 `HIGH_LOAD`. EVIDENCE-012(법제 기준, 정량적)도
독자적인 ATTENTION 신호로 격상. EVIDENCE-001(비교연구, "정방향이 상대적으로
유리한 경향"이지 절대 위험수치가 아님)은 혼자서는 ATTENTION까지만 - 다른
신호와 겹쳐야 HIGH_LOAD. 연속 야간근무 일수 자체는 대응하는 Evidence 항목이
없어(5장 표에 "근거 없음"으로 명시돼 있던 항목) 여전히 판정에 안 씀 - 다만
3일 이상부터 EVIDENCE-006(야간근무 후 수면 우선)을 상기시키는 쪽으로
워딩만 스마트화(context Finding에 조건부 문구 추가).

**"조합=심각" 자체의 정직성**: "ATTENTION급 신호 2개 이상 = HIGH_LOAD"라는
조합 규칙 자체는 특정 논문이 제시한 임계값이 아니라 이 앱의 설계 판단(여러
연구가 개별적으로 뒷받침하는 부담이 겹치면 누적된다는 상식적 추론) -
`RULE_COMPOUND_HIGH_LOAD`의 Finding 메시지에 이 점을 그대로 명시함
(condition_score.dart의 `_kMaxProblemDeduction` 같은 "설계값임을 숨기지
않는" 기존 관행과 동일선상).

**API 변경**: `ConditionRuleEngine.evaluate(date)` → `evaluate(date,
{otMinutesByDate = const {}})`. 기본값이 빈 맵이라 기존 호출부(테스트 등)는
전부 그대로 컴파일되지만, 그 상태로는 `RULE_WEEKLY_OVERTIME`이 항상 꺼진
채로 평가됨 - 실제 앱 배선(`condition_provider.dart`의
`todayConditionResultProvider`)만 `recentOvertimeMinutesProvider`를 watch해서
실제 값을 넘김. 이 provider는 원래 `sleep_condition_provider.dart`에
있었는데, 이제 `ConditionRuleEngine` 자체도 필요해져서 더 기반 레이어인
`condition_provider.dart`로 옮김(순환 import 방지 - `sleep_condition_provider.dart`가
이미 `condition_provider.dart`를 import하고 있어서 반대 방향은 순환이 됨).

**condition_score.dart는 의도적으로 안 건드림** - OT/direction을 이미 자체
로직으로 계산하고 있어서(19장), `baseResult.levelFindings`를 읽어 재사용하도록
리팩터할 수도 있었으나 이번 세션 범위에서 제외함(시간 대비 이득이 낮다고
판단 - 두 파일 다 "최근 7일/8시간" 같은 동일한 상수를 쓰지만 물리적으로는
독립 정의라 나중에 하나만 바뀌면 어긋날 잠재 위험이 남아 있음 - 다음에 이
축을 만지면 함께 정리할 것).

**테스트**: `test/condition_rule_engine_v2_test.dart`(신규, 5개 시나리오) -
역방향 단독(ATTENTION), 정방향은 레벨 무관(context만), 주간초과근무 단독
(otMinutesByDate 기본값이 빈 맵이라 안 넘기면 항상 꺼짐도 같이 확인),
역방향+회복시간부족 조합(HIGH_LOAD 승격 확인), 그리고 v1 원래 케이스(회복시간
부족+장시간근무)가 일반화 후에도 그대로 HIGH_LOAD로 나오는 회귀 확인. 기존
스위트(`today_forecast_engine_test.dart`/`_scenarios_test.dart`/
`condition_score_test.dart`)는 전부 2교대(주간/야간)뿐인 스케줄만 써서
`analyzeDirection()`이 항상 null이었고 `otMinutesByDate`도 `evaluate()`에
안 넘겼던 터라, 이 v2 신호들을 전혀 검증하지 못하고 있었음(그래서 "전체
스위트 통과"가 새 규칙이 동작한다는 증거가 되지 못했음) - 새 테스트 파일이
그 공백을 메움. `flutter test` 전체(82개) 통과 확인.

**도움말**: `help_screen.dart`의 "컨디션 판정은 어떻게 정해지나" 문구를
6-신호 체계로 갱신하고, 12개 Evidence 전체를 나열하는 "컨디션 판정 근거"
항목을 신설함(사용자 요청).

**미검증**: 실기기 미확인(Dart 로직만 검증됨). `RULE_BACKWARD_DIRECTION`이
실제 3교대(주간/오후/야간) 사용자 스케줄에서 기대대로 뜨는지는 실사용
데이터로 확인 필요.

## 23. (2026-09-04 후속) RULE_CONSECUTIVE_NIGHT_SHIFTS 추가 - "야간은 더 강도있게"

**배경**: 22장 v2 직후 사용자가 도움말 근거자료 목록을 보고 재확인 요청 -
"연속 야간근무 자체에 대한 근거를 찾아봐, 12시간 3일근무가 2일 휴식을
요하면 12시간 '야간'근무 3일은 훨씬 더 심할 것 같은데 좀 더 강도있게
반영할 수 없나."

**조사**: WebSearch/WebFetch로 추가 조사해서 `컨디션매니저_근거자료.md` 3절이
이미 "NIOSH만으로는 정량적 임계값 없음"이라고 결론 내렸던 걸 뒤집는 자료를
찾음 - Folkard & Tucker(2003, *Occupational Medicine*)가 연속 야간근무
횟수에 따른 상대위험을 정량화(2일째 +6%, 3일째 +17%, 4일째 +36%, 야간근무
연속이 낮/오후 연속보다 더 가파름). Folkard & Lombardi(2006, EVIDENCE-004
출처)와 같은 "Risk Index" 모델 계열(근무종류/근무길이/연속횟수/휴식간격
4축 중 "연속횟수" 축)이라 이미 신뢰하는 문헌과 동일선상으로 판단, 2차
인용(Frontiers 2024 rapid evidence review)으로 수치 재확인해서
EVIDENCE-013으로 신설(`evidence_database.dart`/`컨디션매니저_근거자료.md`
양쪽 갱신).

**Rule 추가**: `RULE_CONSECUTIVE_NIGHT_SHIFTS` - 연속 야간근무 3일 이상이면
22장의 ATTENTION급 신호군(회복시간/장시간근무/OT/역방향)에 다섯 번째로
합류(2개 이상 겹치면 여전히 HIGH_LOAD). **"조금 더 강도있게"라는 요청은
4일 이상 구간에 반영** - 원 연구가 "이 시점부터 가속화"라고 명시하므로,
4일 이상은 `RULE_EXTENDED_STREAK_SHORT_BREAK`와 동일하게 다른 신호 없이도
그 자체로 단독 HIGH_LOAD로 승격(5장 표에 새 행으로 반영).

**점수 반영**: `condition_score.dart`에 `_wConsecutiveNightStreak`(-10, 3일
이상) + `_wConsecutiveNightStreakSevere`(-10 추가, 4일 이상) 신설 -
`RULE_EXTENDED_STREAK_SHORT_BREAK` 팩터와 동일하게 `baseResult.levelFindings`를
읽어서 임계값을 또 정의하지 않음. 캘리브레이션 재확인(`condition_score_test.dart`) -
"야간 3연속" 시나리오가 기존 52점 → 42점(😫 심각)으로 하향됨(의도된 변화 -
이 시나리오가 정확히 이 신호가 잡아야 하는 케이스라 점수가 내려가는 게
맞음). 최악 케이스 2건은 이미 `_kMaxProblemDeduction`(47) 상한에 걸려있어서
그대로 38점 유지(캡이 의도대로 작동).

**야간 전 낮잠(EVIDENCE-007)/야간근무 후 수면(EVIDENCE-006) 재확인**: 사용자가
같이 물어봐서 확인 - `RULE_NAP_BEFORE_NIGHT`(Tip)/`RULE_NIGHT_FOLLOWED_BY_OFF`
(context Finding)로 이미 각각 반영돼 있음을 재확인, 코드 변경 없음.

**테스트**: `condition_rule_engine_v2_test.dart`에 2개 시나리오 추가(3일째
ATTENTION 단독, 4일째 단독 HIGH_LOAD) - `flutter test` 전체(84개) 통과.

**도움말**: "컨디션 판정은 어떻게 정해지나"를 5-신호 체계로 재갱신, "컨디션
판정 근거"에 EVIDENCE-013 추가.

**함께 확인/정리한 부수 항목**: `컨디션매니저_근거자료.md`의 Evidence ID
매핑표에 EVIDENCE-012가 누락돼 있던 것도 이번에 같이 채움(2026-09-01에
EVIDENCE-012를 추가했을 때 이 표를 안 고쳤던 기존 갭 - 코드/`컨디션매니저_설계.md`엔
이미 반영돼 있었음).

**미검증**: 실기기 미확인. EVIDENCE-013은 1차 논문 원문(Folkard & Tucker
2003 PDF) 직접 접근은 이번 조사에서 못 했고 2차 인용(Frontiers 2024)으로만
수치를 재확인한 수준 - 다음에 여유 있을 때 1차 원문 직접 확인 권장.
