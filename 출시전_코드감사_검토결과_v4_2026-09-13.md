# 출시 전 코드감사 검토결과 v4 (확정본)

작성일: 2026-09-14 (Asia/Seoul) · 파일명 날짜는 원본 감사일(2026-09-13) 기준 유지
기준 문서: `출시전_코드감사_2026-09-13.md` (원본 감사, 39건 + 추가 확인 사항 + 필수 검증 시나리오)
이전 판: v1·v2·v3는 이 문서로 대체되어 삭제함. 변경 내용은 1장 참고.
상태: **7차 외부 재검토(2026-09-14)까지 반영한 최종본 - 구현 기준 문서.** 새 판(v5)은 만들지 않고, 이후 변경은 구현 중 발견 사항만 이 문서에 직접 갱신.

작업 방식: 코드 수정 없이 정적 대조. 테스트·빌드 미실행, 실기기 재현 없음.

---

## 0. 이 문서 사용 원칙

1. **원본 감사 보고서를 함께 기준으로 유지한다.** 이 문서는 원본을 요약·재분류한 것이라, 원본의 세부 위험·재현 방법·
   검증 시나리오를 전부 대체하지 못한다. 구현 시 해당 원본 절(C01, H05 등)을 반드시 같이 읽을 것.
2. **번호는 고정한다.** 새 항목은 뒤에 붙이고, 번호를 다시 매기지 않는다.
3. **구현 직전에 수정안이 건드리는 호출 경로 전체를 다시 확인한다.** 호출자, 같은 테이블을 쓰는 다른 경로, Dart/Kotlin 쌍,
   **이미 있는 방어 로직**, 설계 문서·체크리스트까지. 이전 판의 오류는 전부 이 확인을 건너뛴 결과였다(1장).
4. **"재발 ≈0%" 같은 수치 표현은 쓰지 않는다.** 항목마다 **대응 범위 / 잔여 위험 / 검증 상태**로 적는다.
5. **사용자 결정이 필요한 항목은 결정 전에 착수하지 않는다** (9장).

---

## 1. 변경 이력 요약

### 1-1. v1 → v2 (1·2차 재검토)
- 수정안 오류 정정: #9(불규칙=custom 전제), #13(`0 == 0` 실패 판정), #24(서버 삭제 후 플래그), #10(과거 시각 cancel 누락),
  #3(스누즈 같은 ID 재사용), #16/H09(자연 복구 논리), #22(화면 꺼짐 분기 미처리), #19(부분 복원), #1·#8(재시도·복구 경로)
- 원본 대비 누락 복원: M07→#28, M10→#2, M13→#29, M14→#30, M08→#24, H17→#19, 추가 확인 사항→V1~V7, 필수 시나리오→S1~S12
- 신규 발견: #26(불규칙 롤링 생성 부재), #8↔V4 결합(`main()`이 `runApp` 전 DB를 무방비로 엶), 개인정보 문구 추가 오류, V6(정확한 알람 권한 재허용 수신기 없음)

### 1-2. v2 → v3 (3차 재검토)
- #26: 설계 의도 근거(`TEST_CHECKLIST.md` 2025-12-05 "불규칙 모드에서는 자동 알람 생성 안 됨") 발견 → 조건부로 조정
- #3: 회차 토큰을 시각 → 단조 증가 번호로
- #27: 하트비트 재등록은 **이미 있음**(`AlarmGuardReceiver.kt:208`) → 판정 방식 보강으로 정정
- #22: "종료(시각 기록)"와 "확정(상태 변경)"은 다른 개념 → 단일 정책으로 명확화
- #24: 삭제 대기 중 업로드 금지·generation·tombstone 옵션 추가
- #29: 설정상 확인된 사실과 미확인 구분

### 1-3. v3 → v4 (4차 재검토 + 코드 재확인)

**재검토 지적을 받아들인 것**

| 번호 | v3의 문제 | v4 반영 |
|---|---|---|
| #26 | 정책 미확정 | **D1 = P1 확정** → High, A 필수. 창 기준·트리거·템플릿 0개·멱등성·보호 대상 명세 |
| #1·#8 | 마이그레이션 미완료 중 **쓰기 동작**(스누즈·끄기 이력·수면 위젯) 처리 없음 | 호환 모드 쓰기 정책 추가(OS 동작 우선 + 대기 큐) |
| #3 | 활성 토큰을 지울 때 누적 카운터까지 지우면 값 재사용, `commit()` 실패 처리 없음 | 카운터와 활성 회차 분리, 직렬화, 저장 실패 시 동작 명시 |
| #4 | 화면 표시 즉시 알림 제거 → 홈 이동 후 제어 수단 상실. "권한 조합과 무관" 표현이 잔여 위험과 모순 | 울림 종료까지 제어 알림 유지, 표현 정정 |
| #5·#10 | 같은 ID 일정의 시간이 바뀌면 옛 예약이 새 내용을 틀린 시각에 표시 | 예약 시각을 Intent에 담고 수신 시 DB 기준 재계산 값과 대조 |
| #9·#19 | `restore_in_progress` 하나로는 DB 커밋 여부 불명, 기존 예약 취소가 검증 전일 수 있음 | 단계 분리 + **각 단계 멱등화**, 취소는 검증 후 |
| #16·#27 | 쓰기 잠금은 커밋 후 OS 반영 순서까지 보호 못 함 (늦게 도착한 옛 예약·취소) | 울림 시점 DB 대조 검증 추가 (아래 반론 2 참고) |
| #22 | 기한을 별도 prefs에 두면 부분 저장·ID 재사용 | 기한을 저장하지 않고 **행에서 파생** (아래 반론 3 참고) |
| #24·#30 | generation 규칙 오류 - 공유 중 정상 수정(`1 > 1`)도 거절됨 | generation + revoked 상태 기반 규칙표로 재설계 |
| #25 | 기존 기기 덮어쓰기 복원 시 현재 공유 상태 처리 없음 | 신규 설치 / 덮어쓰기 구분 정책 |
| #28 | provider 최신 값도 DB와 다를 수 있음 | 트랜잭션 안에서 DB 최신 값 읽고 해당 키만 변경 |
| #2 | "영원히 스킵"은 과장. 해시 순서·성공 기록·실행 중 변경 처리 없음 | 원인 표현 정정, 정렬 기준·성공 후 기록·재확인 추가 |
| #17·#20·#21 | 표현·검증 범위 | 배포 국가로 기기 로케일 판단 불가 명시, 다음 알람 표시 검증 분리, 서버 확인과 연결 상태 구분 |
| H03 | "출시 후" 확정은 부적절 | **G1: 실기기 결과 기반 출시 판단 항목**으로 이동 |
| M03 | 원본의 DST 양방향 검증이 S7에서 빠짐 | 조건부 검증으로 S7에 복원 |

**재검토 지적 중 방향을 달리 정한 것 (반론)**

1. **#26 "10일 전"의 기준 - 배정일 D-10이 아니라 규칙 근무와 같은 "울림일 기준 10일 창"으로 확정.**
   - 재검토안은 "배정일 D-10에 그 근무의 모든 템플릿 생성"이라 다음날 템플릿은 오늘+11일까지 예약됨
   - 그러나 현재 **규칙 근무의 네이티브 엔진·Dart 재생성·기존 알람 정리가 모두 "실제 울림 날짜가 오늘~오늘+9일"** 기준으로 동작함
     (`AlarmRefreshEngine.computeDesiredAlarms`의 `DAYS_AHEAD` 루프, `alarm_provider.dart`의 `datesAffectedByShiftChange` 후 창 필터)
   - 불규칙만 다른 창을 쓰면 생성·정리 범위가 두 벌이 되고, 경계 버그 위치가 두 배로 늘어남
   - 사용자 관점 차이는 "창 끝 날짜의 알람이 하루 늦게 목록에 나타나는가"뿐이고, **배정된 날짜의 알람이 울리는지 여부에는 차이가 없음**
     (미리 배정한 날짜의 알람은 그 날짜가 "오늘을 포함한 앞으로 10일" 안에 들어오는 날 생성되고, 창 안의 날짜를 늦게 배정하면 배정 즉시 생성됨.
     초안의 "모든 알람은 울리기 최소 9일 전에 생성"은 늦은 배정에는 성립하지 않아 5차 재검토로 정정)
2. **#16·#27 - 전체 revision 체계 대신 "예정 시각 정확 대조 + 반영 직전 재확인 + 실패 기록·재시도"로 대응.** (5차 재검토로 정정)
   - 늦게 도착하는 옛 OS 작업 중 ① 이미 지워진 알람의 예약은 `alarmExistsInDb`가, ② 새 ID로 대체된 알람의 취소는 ID 자동 증가가 이미 막음
   - ③ **같은 ID의 시각이 바뀐 뒤 옛 시각으로 재예약**(예: 스누즈 직후 엔진의 재등록 루프): 초안의 "행 시각이 1분 이상 미래일 때만 보류"는
     **40초 뒤 울려야 할 알람을 지금 울리는 조기 울림을 허용**하므로 폐기 → 예약 Intent에 담은 예정 시각과 DB의 최신 예정 시각을
     **정규화 후 정확히 비교**하고, 다르면 울리지 않고 DB 시각이 미래면 그 시각으로 재예약 (허용 오차 없음)
   - **"울릴 때 검사하면 무해"라는 초안 결론은 성립하지 않음:** 수신 시 검사는 조기 울림만 막고, **수신 이벤트가 제시간에 오지 않는 지연·누락은 못 막음**.
     그래서 ④ 커밋 후 OS 반영 직전 DB 재확인 ⑤ OS 반영 실패를 영속 기록하고 다음 트리거에서 재시도 ⑥ Guard 재등록(제시간 복구 보장 없음, 보조 수단)을
     함께 두고, 지연·누락의 잔여 위험은 #27에 명시
3. **#22 - 기한을 저장하지 않는다.** 기한 = `start_time + 9시간`으로 **행에서 매번 파생**. 저장소가 없으니 부분 저장·ID 재사용·삭제 정리 문제가 원천적으로 없음.
   수면 창 종료 시각 조건은 뺌. 단 "다음 근무 시작 때 사용자가 화면을 켠다"는 보장되지 않으므로, **기록된 수면이 수면 창 종료(다음 근무 시작)를
   넘을 수 있다는 잔여 위험을 #22에 남김** (5차 재검토로 정정)
4. **#19 - DB 커밋 여부를 알아내려 하지 않고, 복원 각 단계를 멱등하게 만든다.** 같은 백업으로 DB 단계를 다시 실행하면 같은 결과
   (테이블 비우고 같은 행 삽입)이므로, 커밋 직후 단계 기록 전에 죽어도 재실행하면 됨
   - 단, 멱등 재실행은 **복원 중 다른 쓰기가 없고 같은 백업을 다시 쓸 수 있을 때만** 안전함 → 복원 잠금·작업용 사본·원본 유실 시
     안전 종료 절차를 #19에 추가 (5차 재검토)

**v4에서 새로 찾은 것**

- **#31 (신규, 운영 `main`에 존재):** 사용자가 개별 알람을 삭제하거나 타입(소리/진동/무음)을 바꿔도 **다음 자동 갱신이 되돌림.**
  재검토는 이를 "P1 도입 시 위험"으로 봤지만, **규칙 근무에서 지금 이미 발생**하는 버그임 (5장 #31)
- #26 관련 사실 정정: 알람이 0개여도 Guard는 자정 점검을 예약함(`AlarmGuardReceiver.kt:120`) → 하트비트 사슬은 유지됨.
  `readTemplates`는 자체 catch가 없어 조회 실패는 예외로 전파됨 → "빈 목록 = 정상적인 0개"는 이미 구분됨. 문제는 빈 목록일 때의 조기 `return`뿐

### 1-5. 2026-09-14 결정 확정 및 #1 재설계 (구현 착수 직전 최종 반영)

- **D1-a·D10·D2·D3 확정.** D1-a는 방식 1(울림일 기준, 규칙 근무와 동일 계산식). D10은 도입(alarm_overrides, DB v24).
  D2는 광고 실제 운영하되 실제 광고 ID·디버그 틴트 끄기·Manifest 앱 ID 교체는 **최종 배포 직전**. D3도 개인정보 문구·Play
  콘솔 작업 모두 **최종 배포 직전**(이번 릴리스 구현 범위가 다 확정된 뒤 써야 다시 안 고침). 9장 표에 반영
- **1번 업데이트 경로(앱 내 안내 → 플레이스토어)에서도 C01이 그대로 발생함을 코드로 확인.** `update_service.dart:368`이
  `LaunchMode.externalApplication`으로 플레이스토어를 별개 앱으로 띄우므로, 업데이트 완료 시점에 프로세스가 죽고 사용자가
  자동으로 앱에 복귀하지 않음. "이미 볼 건 봤으니 다시 안 엶"이 정상적인 사용 패턴이라 확률이 낮은 예외가 아님 (#1 본문에 반영)
- **#1을 헤드리스 Flutter → 네이티브 `onUpgrade` SQL 이식으로 재설계.** v19~v24 마이그레이션이 전부 순수 SQL(데이터 변환
  없음)임을 확인 → `SQLiteOpenHelper`가 `onUpgrade`+버전 갱신을 자동으로 하나의 트랜잭션으로 묶어 원자성을 플랫폼이
  보장하므로, 별도 헤드리스 엔진·대기 큐·호환 모드가 전부 불필요해짐. **D4(헤드리스 시제품 진행 여부)는 폐기.**
  트리거 지점도 `MY_PACKAGE_REPLACED` 하나에서 "네이티브가 DB를 처음 건드리는 모든 지점"으로 넓어져 노출 시간이 줄어듦

### 1-6. 5차 재검토 반영 (2026-09-14, 최종본 확정)

| 번호 | 지적 | 코드 재확인 | 반영 |
|---|---|---|---|
| #1 | 잠금 해제 전 마이그레이션을 안 하면 "업데이트 → 재부팅 → 잠금 해제 전 아침 알람" 누락이 남음 | DB는 Device Protected 저장소(`DatabaseHelper.kt:85`, Dart `getDeviceProtectedStoragePath`)라 첫 잠금 해제 전에도 접근 가능. **현재도 `DirectBootReceiver`가 잠금 해제 전에 갱신 엔진으로 DB에 씀** → 직전 초안 #1 8번의 "잠금 해제 전 쓰기를 최대한 피하는 기존 설계"는 사실 오류 | 잠금 해제 전(`LOCKED_BOOT_COMPLETED`)에도 네이티브 마이그레이션 수행 |
| #1 | v18 미만 DB, 빈 `onCreate`/`onDowngrade`의 버전 stamp, 네이티브가 먼저 여는 컬럼 누락 DB, 근거 없는 수치("몇 밀리초", "극히 드묾") | 스토어에 실제로 나간 DB 버전은 v12(1.0.1~1.0.11, 2025-12~2026-01), v14, v15, v17, v18 (`pubspec.yaml` 버전 bump 커밋별 확인) → v18 미만 사용자 존재 가능. v2~v18도 전부 SQL(DDL + v11의 `UPDATE` 2줄, Dart 로직 없음) | **v1→v24 전 구간 지원**(단일 SQL 원본 권장), `onCreate`·`onDowngrade` 예외화, 네이티브 `onOpen` 스키마 검증·복구, 측정 근거 없는 표현 삭제 |
| #16·#27 | 수신 시 검사는 지연·누락을 못 막음, 1분 허용이 조기 울림 허용 | - | 1-3 반론 2 정정 참고 |
| #5 | 같은 계산식으로 정규화하면 1분 허용 오차 불필요 | - | 트리거 시각 정확 비교 |
| #3 | 저장 실패 시 ID만 비교하는 폴백은 "이전 종료 신호가 새 스누즈를 끔" 재발 | - | 메모리 토큰 폴백. 화면·오버레이 타이머 제거는 S4 확인 후 확정 |
| #9·#19 | 복원 중 동시 쓰기, 원본 유실 시 부분 상태, 구버전 백업의 `alarm_overrides` | `restoreAll`은 **백업에 들어 있는 테이블만** 비우고 채움 → 백업에 없는 테이블은 기존 값이 남음 | 복원 잠금, 작업용 사본, 안전 종료 절차, 덮어쓰기 복원 시 백업에 없는 원본 테이블은 비움, 영구보존 이력은 병합 |
| #24 | D7 규칙표에 tombstone 재전송·문서 없는 상태의 중지·구버전 앱 `delete` 누락 | 현재 `stopSharing`은 `delete()` 호출 | 규칙표 3행 추가, 삭제는 `generation` 없는 구버전 문서만 허용, S18 3케이스 추가 |
| #26 | "최소 9일 전 생성"은 늦은 배정에 성립 안 함 | - | 안내 표현 "오늘을 포함한 앞으로 10일간" |
| #31 | 슬롯 키 시각 문자열 정규화, A→B→A로 되돌리면 옛 예외 재적용 | - | `Locale.US` 고정 형식 공유, 예외에 원 배정일·근무 저장, 정책 D12 |
| #22 | "다음 근무 때 화면을 켠다" 가정 | - | 잔여 위험 명시 |
| 9·10장 | D5~D9·D11은 권장인데 10장은 "남은 결정 없음" | - | "초기 마이그레이션 작업은 착수 가능, 해당 기능 구현 전 남은 정책 확정"으로 통일 |

### 1-7. 6차 재검토 반영 (2026-09-14)

| 번호 | 지적 | 코드 재확인 | 반영 |
|---|---|---|---|
| #27 | "행이 삭제됐으면 예약·취소를 버림"이면 정상 삭제의 OS 취소가 누락됨 | 엔진은 행 삭제를 커밋한 뒤 취소하므로, 취소 시점엔 정상적으로도 행이 없음 | 예약(행이 유효할 때만 실행)과 취소(행이 없으면 항상 실행)의 재확인 조건 분리 |
| #19 | 복원 잠금 중 스누즈를 허용하면 "다른 쓰기 없음" 전제가 깨짐. 시간 초과만으로 잠금을 무시하면 진행 중 복원과 충돌 | `AlarmActionHelper.snooze`가 `alarms` 행의 날짜·시각·`type`을 바꾸고 OS에도 예약. Manifest에 `android:process` 없음(모든 컴포넌트 단일 프로세스) | 진행 중 스누즈·울리는 알람 행을 DB 교체 트랜잭션 안에서 같은 ID로 이월(3-1). 잠금 유효성은 프로세스 메모리 토큰으로 판정(4-1) |
| #12·#31 | 근무명 변경에 `origin_shift` 누락 | - | `shift_type`·`origin_shift`를 같은 트랜잭션에서 rename |
| #1 | `onOpen` 복구가 과거 마이그레이션을 재실행하면 파괴적 | v17에 `DROP TABLE IF EXISTS friends`, v11에 `UPDATE` 존재 | SQL 원본을 `migrations`/`repair` 구역으로 분리, `repair`는 비파괴 SQL 화이트리스트만 실행 |

### 1-8. 7차 재검토 반영 (2026-09-14, 최종)

| 번호 | 지적 | 코드 재확인 | 반영 |
|---|---|---|---|
| #19 | ID 충돌 시 이월 행(진행 중 알람)에 새 ID를 주면 화면·제어 버튼·회차 추적·종료 예약이 옛 ID를 써서 다른 행을 대상으로 실행 | `activeRing`(#3)·알람 화면/오버레이 Intent extra·PendingIntent requestCode가 모두 알람 ID 기반 | 진행 중 알람 ID 유지, 충돌하는 백업 `custom` 쪽에 새 ID. {백업 ID → 새 ID} 매핑을 단계 기록에 저장해 재실행 결과 고정 |
| #19 | 이월 행이 참조하는 재생 설정이 복원된 `alarm_types`에서 달라지거나 없어짐 | `AlarmPlayer.playAlarmFromDB`(`AlarmPlayer.kt:104`)·`CustomAlarmReceiver.getDurationFromDB`가 `alarm_type_id`로 설정 조회. `alarm_types`는 백업 대상(제외 목록은 `alarms`뿐) | 이월 알람별 재생 설정 스냅샷(Device Protected prefs), 재생·지속시간 조회 시 우선 사용 |
| #9 | (추가 발견) 복원 후 OS 반영을 `forceRefresh`에 맡기면 `custom`·이월 `snoozed` 행이 예약되지 않음 | 갱신 엔진의 재등록 루프는 fixed 행만 다룸(#26 이후 불규칙도 동일) | 복원 OS 반영 단계·안전 종료 절차에서 미래 `custom`·`snoozed` 행 명시적 재예약 |

---

## 2. 감사 보고서가 놓친 핵심 사실

- **운영 `main`(v1.0.22)은 DB `v18`, dev는 `v23`.** C01은 업데이트하는 기존 사용자 전원이 대상. 스토어에 나간 DB 버전은
  v12·v14·v15·v17·v18이라, 오래 업데이트하지 않은 사용자는 v18보다 낮은 버전에서 올라옴.
- 업데이트 후 **앱을 한 번 열기 전까지** 네이티브 DB 게이트(`DatabaseHelper.kt:187`)가 막혀서:
  알람 **진동만**(`AlarmPlayer.kt:153`), **스누즈 실패**(`AlarmActionHelper.kt:94`), 끄기 **이력 누락**(`AlarmActionHelper.kt:39`),
  수면 위젯 기록 불가, 10일치 갱신 중단, 재부팅 시 재등록 0건.
- `onDiskVersion()` 실패 시 "최신이라고 보고 진행"(`DatabaseHelper.kt:170`) → **마이그레이션 없이 버전만 찍혀 영구 손상** 가능.
- **불규칙 근무는 10일 밖에 배정한 날짜의 알람이 영영 생성되지 않는다 (#26, 운영에도 있음).** P1으로 수정 확정.
- **개별 알람 삭제·타입 변경이 자동 갱신으로 원복된다 (#31, 운영에도 있음, 규칙 근무 포함).**
- **광고(AdMob)와 Analytics는 이번 릴리스에서 처음 들어간다.**
- C02(자동백업 스킵)는 2026-09-11 사용자 신고의 실제 원인으로 판단.

---

## 3. 전체 번호표

| # | 원본 ID | 분류 | 요약 |
|---|---|---|---|
| 1 | C01, H16(네이티브) | A | 업데이트 후 앱 미실행 시 알람 망가짐 |
| 2 | C02, M10 | A | 자동백업 스킵 + 비일관 스냅샷 + 중복 실행 |
| 3 | H01 | A | 알람 화면 이탈 시 자동 종료 소실 |
| 4 | H02 | A | 오버레이 권한 없을 때 끌 수단 없음 |
| 5 | H08, H06(수신 측) | A | 탭 숨김 후 재부팅 시 일정 알림 부활, 옛 예약 오표시 |
| 6 | L01 | A | 테스트 광고 ID·디버그 틴트 배포 (D2) |
| 7 | H18 | A | 개인정보 안내 불일치 (D3) |
| 8 | H16(Dart) | A | 마이그레이션 실패를 삼킴 (V4와 결합 필수) |
| 26 | (신규) | **A (High)** | 불규칙 근무 자동 롤링 생성 (P1 확정) |
| 31 | (신규) | **A (High)** | 개별 알람 삭제·타입 변경이 자동 갱신으로 원복 |
| 9 | H05, H06 | B | 백업/복원 시 custom 알람 누락, 덮어쓰기 시 옛 예약 잔존 |
| 10 | H07 | B | 시간대 변경 시 일정 알림 재계산 |
| 11 | H11 | B | 근무명 쉼표 입력 |
| 12 | H12 | B | 근무명 맞교환·중복 |
| 13 | H15 | B | 등록 실패 무통보 + 권한 확인 실패를 허용으로 처리 |
| 14 | H19 | B | 다른 알람 삭제가 울리는 알람 중지 |
| 15 | M01 | B | OT 수정이 컨디션 판정에 미반영 |
| 16 | H10 | B | 트랜잭션 중 OS 호출 |
| 27 | H09 | B | 네이티브 갱신과 Flutter 수정 경합 |
| 17 | M06 | B | DB 날짜 문자열 로케일 의존 |
| 18 | M11 | B | 불완전 백업 파일이 정상 백업을 가림 |
| 19 | H17 | B | 부분 복원 + 백업 내용 검증 |
| 20 | H04 | B | 기상 알람 `setAlarmClock` 통일 |
| 21 | M09 | B | 공유 중지 후 친구 캐시 잔존 |
| 22 | M05 | B | 자동 수면 후보 미종료 |
| 23 | M16 | B | MediaPlayer 실패 시 해제 누락 |
| 24 | H13, M08 | B | 공유 중지·시작·이름 변경·동기화 실패 처리 |
| 25 | H14(완화) | B | 복원 시 공유 상태 처리 |
| 28 | M07 | B | 출퇴근시각 두 테이블 저장 경합·부분 실패 |
| 29 | M13 | B | dev/prod Firebase 분리 |
| 30 | M14 | B | Firestore 쓰기·읽기 절감, rules 검증 |
| G1 | H03 | **출시 판단** | 알람 재생 프로세스 생존 (S4 결과로 출시 전 필요 여부 판단) |
| - | M02, M03, M04, M12, M15, L02, H14(근본) | C | 6장 참고 |
| V1~V7 | 원본 추가 확인 사항 | 확인 | 7장 |

**원본 39건 전체 매핑:** C01→#1 · C02→#2 · H01→#3 · H02→#4 · H03→G1 · H04→#20 · H05→#9 · H06→#9·#5 · H07→#10 ·
H08→#5 · H09→#27 · H10→#16 · H11→#11 · H12→#12 · H13→#24 · H14→#25·C · H15→#13 · H16→#1·#8 · H17→#19 · H18→#7 ·
H19→#14 · M01→#15 · M02→C · M03→C·S7 · M04→C · M05→#22 · M06→#17 · M07→#28 · M08→#24 · M09→#21 · M10→#2 · M11→#18 ·
M12→C · M13→#29 · M14→#30 · M15→C · M16→#23 · L01→#6 · L02→C

---

## 4. A. 출시 전 반드시 고칠 것

### #1 — C01 + H16(네이티브): 업데이트 후 앱을 열기 전까지 알람이 망가짐

- **1번 업데이트 경로(앱 내 안내 → 플레이스토어)에서도 그대로 발생함 (2026-09-14 확인):**
  `update_service.dart:368`의 "업데이트하기" 버튼은 `LaunchMode.externalApplication`으로 플레이스토어를 **별개의 외부 앱**으로 띄운다.
  플레이스토어가 실제로 APK를 교체하는 순간 교대시계 프로세스는 강제 종료되고(옛 코드를 메모리에 띄운 채 파일만 바꿀 수 없어
  Android가 항상 이 시점에 죽임 - `MY_PACKAGE_REPLACED`가 존재하는 이유), 플레이스토어를 닫으면 홈 화면이나 원래 화면으로
  돌아가지 교대시계로 자동 복귀하지 않는다. "저녁에 스케줄 확인 중 업데이트 → 이미 볼 건 봤으니 앱을 다시 열 이유 없음 →
  취침 → 다음 날 알람"이 전형적인 사용 패턴이라, 오히려 **이 앱을 정상적으로 잘 쓰는 사용자일수록** 이 경로를 밟는다.
  즉 드문 예외 조작이 아니라 **업데이트 후 앱을 다시 열지 않는 정상 사용 흐름**에서 생기는 경로(발생 비율은 측정하지 않음).
- **실기기 발생 경로:** v1.0.22 사용자 → 업데이트(경로 무관) → 앱 미실행 → 다음 날 알람이 진동만 울림, 스누즈 불가,
  이력 누락. 재부팅이 겹치면 알람이 전혀 안 울림. 앱을 열 때까지 10일치 갱신도 멈춤.
- **수정 계획 (2026-09-14 재설계 - 헤드리스 Flutter 폐기, 네이티브 SQL 마이그레이션. 5차 재검토 반영):**

  Dart `_onUpgrade`의 **v2~v24 전 구간이 SQL만으로 구성**됨을 확인(DDL + v11의 `UPDATE` 2줄, 기존 데이터를 Dart 로직으로 읽어
  변환하는 단계 없음). #31의 v24(`alarm_overrides` 신설)도 새 테이블이라 SQL만으로 표현됨. 따라서 Flutter 엔진 없이 네이티브가
  직접 마이그레이션할 수 있음.

  1. **지원 범위 = Dart가 지원하는 전 구간(oldVersion 1 이상).** 스토어에 실제로 나간 DB 버전은 v12(1.0.1~1.0.11, 2025-12~2026-01),
     v14, v15, v17, v18 (`pubspec.yaml` 버전 bump 커밋별 확인) → 오래 업데이트하지 않은 사용자는 v18보다 낮은 버전에서 올라옴.
     **`oldVersion` 다음 단계부터 목표 버전까지 모든 단계를 순서대로 실행**하고, 중간 단계를 건너뛰고 최신 버전을 찍는 경로는 두지 않음.
     지원 범위 밖 값(0 이하 등)이면 예외를 던져 롤백
  2. **단일 SQL 원본 (권장):** 버전별 마이그레이션 SQL을 하나의 자산 파일(예: `assets/db/migrations.json` = `{버전: [SQL...]}`)로 옮기고
     Dart(`rootBundle`)와 Kotlin(APK의 `flutter_assets/` 경로)이 **같은 파일을 읽어 실행** → Dart/Kotlin SQL 불일치를 구조적으로 제거
     - APK 자산을 release 빌드·잠금 해제 전에 네이티브가 읽을 수 있는지 S1에서 확인. 안 되면 차선책: 양쪽 코드에 같은 SQL을 두고
       빌드 시 문자열 동일성 검사(`build.gradle.kts`의 `checkDartKotlinSync`에 추가)
     - 실행기 공통 규칙: `ALTER TABLE ... ADD COLUMN`은 `PRAGMA table_info`로 존재 확인 후 없을 때만 실행. catch로 삼키지 않음(#8 원칙)
  3. **네이티브 `DatabaseHelper.onUpgrade`가 위 SQL을 실행.** `SQLiteOpenHelper`는 `onUpgrade` 호출과 버전 갱신을 하나의 트랜잭션으로
     묶고 예외 없이 끝나야 커밋하므로, **스키마 변경과 버전 번호가 함께 성공하거나 함께 롤백**됨(원자성은 플랫폼 보장, S1에서 실패 주입으로 확인)
  4. **버전만 바뀌는 다른 경로 차단:**
     - `onCreate`: **예외를 던짐.** 네이티브는 DB를 새로 만들지 않음(신규 설치는 Dart 담당, `databaseFileExists()` 가드 유지).
       파일은 있는데 `user_version = 0`인 경우에도 빈 `onCreate`가 최신 버전을 찍는 일을 막음
     - `onDowngrade`: **예외를 던짐.** 지금은 빈 함수라 더 높은 버전 DB를 네이티브가 열면 낮은 버전으로 찍힐 수 있음
  5. **`isDatabaseReady()` 게이트 교체** (현재는 "정확히 일치할 때만" - `DatabaseHelper.kt:187`):
     - 디스크 버전 < 네이티브 → 헬퍼로 열어 마이그레이션 수행
     - 디스크 버전 > 네이티브 → 사용하지 않음(다운그레이드 방지)
     - `onDiskVersion()` 확인 실패 → **최신이라고 가정하지 않음**(현재의 fail-open 폴백 제거). 헬퍼로 열게 두되 4번 덕분에
       실제 버전이 낮으면 마이그레이션, 이상 상태면 예외 → 해당 호출만 실패하고 기존 fail-safe(진동 폴백 등) 유지
  6. **잠금 해제 전에도 수행:** DB는 Device Protected 저장소(`DatabaseHelper.kt:85`)라 첫 잠금 해제 전에도 접근 가능하고, 현재도
     `DirectBootReceiver`가 `LOCKED_BOOT_COMPLETED`에서 갱신 엔진으로 DB에 씀 → **"DB v18 → 업데이트 → 재부팅 → 잠금 해제 전 아침 알람"
     경로도 부팅 수신 시 마이그레이션 후 알람 재등록**. 헬퍼로 여는 첫 호출 지점(재부팅, 알람 수신, Guard 하트비트, 일정 알림, 수면 감지,
     앱 실행) 어디서든 같은 방식으로 동작
  7. **네이티브 `onOpen` 스키마 검증·복구 - 업그레이드 실행과 분리:** 버전은 이미 최신인데 컬럼이 빠진 DB(#8의 과거 동작으로 생겼을 수 있음)를
     네이티브가 먼저 열면 `onUpgrade`는 실행되지 않음 → `onOpen`에서 누락분만 추가. **과거 마이그레이션을 통째로 재실행하면 안 됨**
     (v17에 `DROP TABLE IF EXISTS friends`, v11에 `UPDATE`가 있어 재실행 시 친구 데이터 삭제·알람 타입 설정 덮어쓰기)
     - SQL 원본 파일을 두 구역으로 나눔: `migrations`(버전별 업그레이드용 증분, 파괴적 SQL 포함 가능) /
       `repair`(**현재 스키마 기준 비파괴 SQL만**: `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, 존재 확인 후 `ALTER TABLE ... ADD COLUMN`)
     - 실행기는 `repair` 구역에 위 세 형태 외의 문(`DROP`·`UPDATE`·`DELETE`·`RENAME`·`INSERT` 등)이 있으면 **실행하지 않고 오류로 거부**(화이트리스트 검사)
     - 스키마를 바꿀 때는 `migrations`에 증분 SQL, `repair`에 최종 형태를 함께 추가. Dart `onOpen`도 같은 `repair` 구역 사용
  8. **Dart 쪽과의 관계:** 네이티브가 먼저 올렸다면 `sqflite`는 저장된 버전이 최신이라 Dart `onUpgrade`를 실행하지 않음(이중 실행 없음).
     Dart가 먼저 여는 경우엔 Dart가 같은 SQL 원본으로 실행. **어느 쪽이 먼저인지는 사용 패턴에 따라 달라지며 빈도를 가정하지 않음**
  9. **동시 접근:** 네이티브와 Dart가 거의 동시에 열면 SQLite 쓰기 잠금으로 한쪽이 대기하고, 뒤에 연 쪽은 최신 버전을 보고 건너뛰는지 S1에서 확인
  10. **문서 규칙 갱신:** `CLAUDE.md` ⚠️4 "DB 마이그레이션 순서"(Native는 Flutter 마이그레이션 후에만 DB 사용)와 `DB_스키마_변경_가이드.md`를
     새 방식(단일 SQL 원본에 추가 → 양쪽이 실행)으로 같은 커밋에서 갱신
- **v3의 호환 모드·대기 큐는 폐기:** 마이그레이션이 네이티브의 첫 DB 접근 안에서 실행되고 그다음 같은 호출에서 알람 처리(재생 설정 조회,
  스누즈 등록, 이력 기록)가 이어지므로 별도 대기 큐가 필요 없음. 마이그레이션 자체가 실패하면 해당 호출은 기존 fail-safe 동작으로 처리되고,
  다음 DB 접근에서 다시 시도됨(버전이 그대로 낮게 남으므로)
- **부가 리스크:** 중간 - 네이티브의 자산 파일 읽기 경로(release·잠금 해제 전), v12처럼 오래된 버전에서 한 번에 v24까지 가는 경로의 실제 검증,
  `onCreate`/`onDowngrade` 예외화로 그동안 조용히 통과하던 이상 상태가 네이티브 오류 로그로 드러남
- **대응 범위:** 앱 미실행·잠금 해제 전 마이그레이션, 네이티브 버전 stamp(`onUpgrade`·`onCreate`·`onDowngrade`·확인 실패 fail-open),
  v12~v17 구버전 사용자, 네이티브가 먼저 여는 컬럼 누락 DB, Dart/Kotlin SQL 불일치
- **잔여 위험:** 향후 데이터 변환이 필요한 마이그레이션은 SQL 원본만으로 표현하지 못할 수 있음(그때 별도 설계), 마이그레이션이 계속 실패하는 기기
  (저장공간 부족 등)에서는 알람이 기존 fail-safe 폴백으로만 동작
- **검증 상태:** 설계만. **S1 선행 필수** - Robolectric으로 v12·v14·v15·v17·v18·v23 fixture → v24 스키마를 Dart `onCreate`로 새로 만든 v24와
  `PRAGMA table_info` 비교, 중간 SQL 실패 주입 시 버전·스키마 롤백 확인 → 실기기에서 업데이트 후 앱 미실행·재부팅 후 잠금 해제 전 알람 확인

### #2 — C02 + M10: 자동백업 변경 누락, 스냅샷 비일관, 중복 실행

- **실기기 발생 경로:**
  - **원인:** 매 호출 새 연결로 `PRAGMA data_version`을 읽어 이전 호출(다른 연결)의 값과 비교함 (`backup_watcher.dart:53`).
    SQLite 문서상 이 값은 **같은 연결의 두 시점 사이에서만** 비교 의미가 있음. 원본 감사의 축소 실험에서 변경 후 새 연결 값이
    변경 전과 같게 나와 스킵이 재현됨. 설정값만 바뀐 경우는 애초에 감지 대상이 아님
  - 테이블을 트랜잭션 없이 하나씩 조회 (`backup_service.dart:74`) → 서로 다른 시점의 테이블이 한 백업에 섞일 수 있음
  - `backupNow()`가 콜드 스타트·resumed·paused에서 각각 불리는데 재진입 잠금 없음
- **수정 계획:**
  1. **변경 감지:** 내보낼 내용의 해시 비교
     - 해시 입력은 순서를 고정: 테이블명 정렬, 행은 기본키(없으면 rowid) 순, 설정 키 정렬
     - `exportedAt`, 백업 시각 관련 등 계속 바뀌는 설정 키는 제외
     - **파일 저장이 성공한 뒤에만** 그 해시를 "마지막 성공 해시"로 기록
  2. **스냅샷 일관성:** 모든 테이블 조회를 하나의 읽기 트랜잭션 안에서
  3. **중복 실행:** 진행 중인 `Future`를 재사용하는 single-flight. 실행 중 새 요청이 오면 "재확인 필요" 표시 → 끝난 뒤 한 번 더 해시 비교
  4. **성능:** 해시·JSON 인코딩은 isolate, 네이티브 파일 입출력은 메인 스레드 밖
- **부가 리스크:** 매 전환마다 export 비용(이력 영구 보존으로 계속 커짐), 제외 키 누락 시 "매번 씀"(손실 방향 아님),
  긴 읽기 트랜잭션의 WAL 체크포인트 지연. #18과 함께 처리
- **대응 범위:** 변경 누락, 설정 변경 누락, 시점 불일치, 동시 실행, 실행 중 변경
- **잔여 위험:** 대용량 이력 성능, 백업 파일 노출(V1)
- **검증 상태:** 원인은 원본 축소 실험으로 재현. 수정안은 설계만. S6

### #3 — H01: 알람 화면에서 홈으로 나가면 자동 종료가 사라짐

- **실기기 발생 경로:** 잠금화면 알람 → 홈 버튼 → `noHistory`로 Activity 파괴 → 종료 타이머 취소 (`AlarmActivity.kt:418`) → 끄기 전까지 계속 울림
- **수정 계획:**
  1. **`RingingAlarmTracker`(Device Protected 저장소)에 두 값을 분리 저장**
     - `ringCounter`: 누적 카운터. **절대 지우지 않고** 새 울림마다 +1 (앱 데이터 삭제 시에만 초기화)
     - `activeRing`: (알람 ID, 회차 번호) 쌍. 끄기·스누즈·인계·타임아웃 시 지움
  2. **토큰 수명 규칙**
     - 새 울림(스누즈 후 재울림 포함)마다 `ringCounter` +1 → 그 값으로 `activeRing` 설정
     - 끄기·스누즈·인계·타임아웃 처리 시 `activeRing` 폐기 (카운터는 유지 → 번호 재사용 없음)
     - 종료 예약 PendingIntent extra의 (ID, 회차)와 현재 `activeRing`을 비교해 **둘 다 일치할 때만** 처리
     - 불일치 요청은 무시하고 로그 기록
  3. **직렬화:** 증가·설정·폐기·비교를 하나의 `synchronized` 객체 안에서 수행 (리시버·서비스·Activity가 같은 프로세스의 메인 스레드에서 실행되지만 명시적으로 보장)
  4. **저장:** `commit()`(동기). **실패 시** 한 번 재시도 → 그래도 실패하면 **프로세스 메모리의 `activeRing`으로 회차 구분을 계속 유지** + 로그
     (ID만 비교하는 폴백은 "이전 종료 신호가 새 스누즈를 끔" 문제를 재발시키므로 쓰지 않음. 프로세스가 죽어 메모리 토큰까지 잃은 경우엔
     소리도 함께 멈추고, 저장된 마지막 토큰과 불일치한 종료 신호는 무시하는 기본 규칙이 그대로 적용됨)
  5. `CustomAlarmReceiver`가 울리는 순간 종료 예약(현재+지속시간) 등록, 끄기·스누즈·인계 시 취소
  6. 화면·오버레이의 자체 타이머는 **당장 제거하지 않고 같은 회차 토큰으로 보호한 채 유지**(먼저 오는 쪽이 종료, 나중 신호는 토큰 폐기로 무시).
     AlarmManager 종료 예약만으로 절전 상태에서도 지속시간에 맞춰 종료되는지 **S4에서 확인한 뒤** 제거 여부 확정
  7. `timeout` 이력 기록 전 알람 행 존재 확인 (중복 이력 방지)
- **부가 리스크:** 중간 - 인계 흐름과의 상호작용, 이력 중복, 폐기 누락 시 정상 종료가 무시될 위험(→ 로그로 탐지)
- **대응 범위:** 화면 파괴와 무관한 자동 종료, 스누즈·인계 회차 혼동
- **잔여 위험:** **프로세스 생존·소리 재생 유지는 해결 못 함 → G1**. 저장 실패 폴백 시 회차 혼동 가능성
- **검증 상태:** 설계만. S4

### #4 — H02: 오버레이 권한이 없으면 알람을 끌 수단이 없음

- **실기기 발생 경로:**
  - 잠금 상태: 백그라운드 Activity 실행이 예외 없이 조용히 차단 → 폴백 미실행 → 소리만 남음
  - 잠금 해제 상태: 폴백 알림에 끄기/스누즈 버튼·탭 동작 없음 (`CustomAlarmReceiver.kt:274`)
  - 알림 권한까지 거부하면 알림도 표시 불가
- **수정 계획:**
  1. 울리는 즉시 제어 알림 게시: 전체화면 인텐트 + contentIntent + 끄기/스누즈 버튼 (7777 알림 버튼 재사용)
  2. **제어 알림은 울림이 끝날 때까지 유지** (끄기·스누즈·인계·타임아웃에서만 제거). 알람 화면·오버레이가 떠도 제거하지 않음 → 홈 이동 후에도 제어 수단 유지
     - 화면이 떠 있는 동안 시각적 중복이 거슬리면 알림은 무음·ongoing으로 두고 내용만 간결하게
  3. 게시 전 전체화면 알림 허용 여부·채널 차단 상태 검사 + 로그
  4. 그다음 알람 화면/오버레이 시도
  5. 타임아웃은 #3이 보장. 알림 버튼의 끄기·스누즈도 #3의 회차 규칙을 따름
- **부가 리스크:** 중간 - 삼성 "시스템 스누즈" 회피용 플래그(CATEGORY_CALL 등) 유지 필수, 알림 상주로 인한 UX
- **대응 범위:** **알림 권한이 허용된 경우** 오버레이·전체화면 권한 조합과 무관하게 제어 수단 확보, 화면 이탈 후 제어 수단 유지
- **잔여 위험:** 알림 권한까지 거부 시 제어 수단 없음(#3의 종료만 동작)
- **검증 상태:** 설계만. **삼성 실기기 권한 조합별 검증(S2) 필수**

### #5 — H08 + H06(수신 측): 탭 숨김 후 일정 알림 부활, 옛 예약 오표시

- **실기기 발생 경로:**
  - 탭 숨김 → 예약만 취소, `notify_enabled` 보존 → 재부팅 → `DirectBootReceiver`가 전부 재예약 (탭 설정은 잠금 해제 전 못 읽음)
  - 수신기는 Intent의 옛 내용을 그대로 표시 (`ScheduleNotificationReceiver.kt:74`)
  - 같은 ID 일정의 시각을 바꿨는데 옛 예약이 남으면(예: DB 저장 후 채널 호출 실패) 옛 시각에 울림
- **수정 계획:**
  1. 탭 on/off 시 Device Protected 저장소에도 플래그 기록, 앱 시작 시 Flutter 값을 네이티브로 동기화
  2. `rescheduleAllFromDb`와 수신기에서 플래그 확인
  3. **예약 시 트리거 시각(ms)을 Intent extra에 포함**
  4. 수신 시 DB 행을 읽어 ① 행 존재 ② `notify_enabled = 1` ③ **DB 기준으로 같은 계산 함수(`triggerMillisFor`)로 다시 구한 트리거 시각(ms)이 extra의 시각과 정확히 일치**할 때만 표시(허용 오차 없음).
     내용은 DB 기준으로 표시. 시각이 다르면 표시하지 않고, DB 기준 시각이 미래면 그 시각으로 재예약
- **부가 리스크:** 낮음 - 기존 사용자는 플래그 기본값 "켜짐". 시간대 변경 직후엔 재계산 값이 달라지므로 #10 재예약과 함께 동작해야 함
- **대응 범위:** 부팅·탭 복원·옛 Intent 내용·시각 변경 후 옛 예약
- **잔여 위험:** 백업 복원 직후 앱 시작 동기화 전 짧은 틈
- **검증 상태:** 코드 경로 확인. 수정안 설계만. S11

### #6 — L01: 테스트 광고 ID·디버그 표시 배포 (이번 릴리스 신규, D2)

- **실기기 발생 경로:** prod 릴리스에서 테스트 광고 ID, `kAdSlotDebugTint = true`로 파란 틴트·테두리 노출, Manifest 앱 ID도 테스트 값
- **수정 계획:** D2 결정 → 운영 시 prod flavor에 실제 ID, dev는 테스트 ID, 틴트는 `kDebugMode` 연결 / 미운영 시 광고 영역 비활성화. 광고 실패·비표시 화면 확인
- **부가 리스크:** dev에 실제 ID가 섞이면 무효 트래픽 위험
- **대응 범위:** 설정 오류 / **잔여 위험:** 없음 / **검증 상태:** 코드 확인. S10

### #7 — H18: 개인정보 안내 불일치 (이번 릴리스 신규, D3)

- **실기기 발생 경로:**
  - Analytics가 공유 미사용자에게도 자동 수집되지만 `privacyNoCollectionBody`는 "외부 서버로 전송되지 않습니다"
  - `privacyFirebaseBody`: "저장되는 값은 근무 스케줄과 익명 식별자뿐" - 실제로는 `ownerName`도 저장. "중지 즉시 서버 데이터 삭제" - 삭제 대기·친구 캐시(#21, #24)와 불일치
  - `privacyDataRetentionBody`: "앱을 삭제하면 함께 삭제" - 다운로드 폴더의 백업 파일은 남음
- **수정 계획:** ko/en 문구 전면 수정. 실제 SDK 수집 항목(DebugView로 확인), 화면 안내, Play 데이터 보안 신고 세 가지를 한 번에 대조해 일치시킴 (신고는 사용자 콘솔 작업). 배포 국가별 동의 필요 여부는 별도 확인
- **부가 리스크:** 없음 (문구)
- **대응 범위:** 안내 불일치 / **잔여 위험:** 법적 적합성은 판단 불가 / **검증 상태:** 코드·문구 확인. S12

### #8 — H16(Dart): 마이그레이션 실패를 삼킴 — **V4와 반드시 함께**

- **실기기 발생 경로:** v19/v23 `ALTER TABLE`이 저장공간 부족 등으로 실패해도 "이미 있을 수 있음"으로 넘김 (`database_service.dart:643`) → 버전은 최신인데 컬럼 없음 → 재시도 안 됨
- **수정 계획:**
  1. `ALTER` 전 `PRAGMA table_info`로 컬럼 존재 확인 → 있을 때만 스킵
  2. `CREATE ... IF NOT EXISTS`는 catch 제거
  3. 그 외 예외는 다시 던져 업그레이드 롤백 → 다음 실행 때 재시도
  4. **V4 시작 실패 복구 화면과 함께 출시** (`main()`이 `runApp` 전 DB를 try/catch 없이 열기 때문)
  5. #1 이후 Dart·네이티브가 같은 SQL 원본을 실행하므로, 이 방어(존재 확인·예외 재전파)는 **실행기 공통 규칙**으로 한 번 정의해 양쪽에 적용.
     어느 쪽이 먼저 실행되는지는 가정하지 않음
- **부가 리스크:** 중간~높음 - 조용하던 오류가 시작 실패로 드러남. 과거 v23(`slot_minutes`) 시도 후 되돌린 dev 기기 DB 상태 점검 필요
- **대응 범위:** 손상된 최신 버전 DB 생성 방지, 재시도 가능화
- **잔여 위험:** 실제 저장공간 부족 사용자는 공간 확보 전까지 사용 불가 (안내 문구 중요)
- **검증 상태:** 설계만. S1·S6

### #26 — 불규칙 근무 자동 롤링 생성 (D1 = P1 확정, High)

- **코드 사실:**
  - Flutter는 달력 배정 시 울림 날짜가 창 안인 경우에만 템플릿 fixed 알람 생성 (`alarm_provider.dart` 창 필터, `alarm_generation_service.dart:195`)
  - 네이티브 엔진은 불규칙이면 "기존 알람 재등록만" (`AlarmRefreshEngine.kt:133`), `computeDesiredAlarms`도 빈 목록
  - → 10일 밖에 배정한 날짜는 창 안으로 들어와도 아무도 생성하지 않음. 운영 `main`에도 동일
  - 원래 의도는 "불규칙은 자동 생성 안 함"(`TEST_CHECKLIST.md` 2025-12-05)이었으나, 배정 시 템플릿 알람 생성과 충돌 → **P1로 정책 변경 확정**
- **P1 동작 명세:**

  | 항목 | 정의 |
  |---|---|
  | 창 | **규칙 근무와 동일: 실제 울림 날짜가 오늘 ~ 오늘+9일** (`DAYS_AHEAD` = `kAlarmRefreshWindowDays`). 1장 반론 1 참고. 사용자 안내·릴리스 노트 표현은 **"오늘을 포함한 앞으로 10일간의 알람이 자동으로 준비됩니다"**("10일 전부터" 아님) |
  | 기여 | 울림 날짜 R에 대해 배정일 R(당일 템플릿), R+1(전날 템플릿), R-1(다음날 템플릿)의 근무를 모두 계산 - 규칙 근무와 같은 우선순위(당일 > 전날 기여 > 다음날 기여) |
  | 원본 | `assignedDates`(배정 없음/`미설정`이면 그 날짜 기여 없음) + `shift_alarm_templates` + #31의 개별 예외 |
  | 과거 제외 | 울림 시각이 지금 이전이면 생성하지 않음 |
  | 보호 대상 | `custom`, `snoozed`, 현재 울리는 알람(`activeRing`)은 정리 대상 아님 - 엔진은 `type='fixed'` 행만 비교(현행 유지) |
  | 템플릿 0개 | **정상적인 "생성할 알람 0개"로 처리** → 창 안의 기존 fixed 알람을 정리. 현재의 `templates.isEmpty` 조기 `return` 제거 (규칙 근무에도 동일하게 적용 - 지금은 마지막 템플릿 삭제 시 옛 알람이 정리되지 않음). 템플릿 조회 실패는 이미 예외로 전파되므로(자체 catch 없음) 빈 목록과 구분됨 |
  | 멱등성 | 같은 입력으로 두 번 갱신하면 추가·삭제 0건, 알람 ID 불변, 생성 이력 추가 없음 (현행 diff 방식 유지, 테스트로 고정) |

- **트리거 (앱을 열지 않아도 생성):**
  1. 자정 Guard → 일일 갱신 (알람 0개여도 Guard는 자정 점검을 예약함 - `AlarmGuardReceiver.kt:120`, 확인됨)
  2. 앱 시작·재개, 재부팅(`LOCKED_BOOT`/`BOOT_COMPLETED`), 업데이트(#1), 정확한 알람 권한 재허용(V6), 백업 복원(#9)
  3. **정책 도입 1회 강제 갱신:** 네이티브 prefs에 `refreshPolicyVersion` 저장 → 저장값이 코드 값보다 낮으면 일일 완료 표시와 무관하게 강제 갱신 후 갱신
     (P1 배포 당일에 기존 코드가 이미 "오늘 갱신 완료"를 기록했을 수 있음)
  4. 달력 배정·변경·삭제: 기존 Flutter 경로(배정일 D → 울림 날짜 D-1·D·D+1 재계산, 창 안만) 유지. 불필요해진 알람만 취소
  5. 템플릿 변경: 저장 후 네이티브 강제 갱신 (현재 규칙 근무 경로와 동일하게 확인)
- **수정 계획:**
  1. 네이티브 `readSchedule`의 불규칙 분기와 `refresh`의 조기 종료 제거, `computeDesiredAlarms`가 `assignedDates`로 근무를 조회하도록 일반화 (규칙 근무는 패턴 계산 결과를 같은 "날짜→근무" 조회 함수로 감쌈)
  2. Dart `computeDesiredFixedAlarmsForDate`와 동일 알고리즘 유지 (파일 상단 주석에 쌍 명시)
  3. **날짜 주입 가능한 계산 테스트:** Kotlin(`computeDesiredAlarms`는 이미 `internal`)·Dart 양쪽에 같은 fixture로 창 경계(오늘+9일 다음날 템플릿, 오늘 이미 지난 시각), 전날/다음날 기여, `미설정`, 템플릿 0개, 멱등성 검사 → **두 결과를 비교**
  4. `TEST_CHECKLIST.md` 1.2절·3.6절의 "불규칙 자동 생성 안 됨 / 전환 시 갱신 중단"을 새 정책으로 갱신
  5. 릴리스 노트: 기존 불규칙 사용자에게 업데이트 후 알람이 자동으로 생기기 시작함을 안내
- **부가 리스크:** 중간 - Dart/Kotlin 불일치 시 중복·누락. **#31 없이 P1만 켜면 불규칙 사용자의 개별 변경도 원복됨 → #31과 반드시 함께**. #16·#27 동시성 보완과 함께 검증
- **대응 범위:** 10일 밖 배정 날짜 누락, 템플릿 전체 삭제 시 옛 알람 잔존, 복원 후 불규칙 알람 재생성(#9)
- **잔여 위험:** 이중구현 불일치, 갱신 사슬(Guard)이 끊긴 기기
- **검증 상태:** 코드·커밋 이력 확인. 설계만. S5·S15

### #31 — (신규) 개별 알람 삭제·타입 변경이 자동 갱신으로 원복 (운영 `main`에 존재, High)

- **코드 사실:**
  - 네이티브 diff 키 = `timestamp|shiftType|alarmTypeId` (`AlarmRefreshEngine.kt:69,81`), 비교 대상은 미래 `type='fixed'` 행만
  - 개별 타입 변경 UI: `calendar_tab.dart:3963~3987`(소리/진동/무음), `next_alarm_tab.dart:765` → `updateAlarmType`이 행의 `alarm_type_id`만 바꿈
  - 개별 삭제 UI: `deleteAlarm` → 행 삭제 + `cancelled_before_ring` 이력. 엔진은 이 이력을 전혀 참조하지 않음
  - Flutter 재생성(`regenerateFixedAlarmsForDatesTxn`)은 날짜의 fixed 행을 전부 지우고 템플릿으로 다시 만듦
  - 운영 `main`에도 같은 키·같은 UI 존재
- **실기기 발생 경로:**
  - 오늘 밤 내일 06:00 알람을 삭제 → 자정 갱신이 템플릿으로 다시 만듦 → **삭제한 알람이 울림**
  - 내일 알람을 무음으로 변경 → 자정 갱신에서 키 불일치 → superseded 처리 후 템플릿 타입(소리)으로 재생성 → **무음으로 바꾼 알람이 소리로 울림**
  - 인접 날짜의 근무를 바꿔 Flutter 재생성이 돌면 즉시 같은 원복
- **수정 계획 (D10):**
  1. **개별 예외를 별도 원본 데이터로 저장:** 새 테이블 `alarm_overrides` (DB **v24**)
     - 키: 슬롯 = (울림 시각 문자열, `shift_type`, `day_offset`)
     - **시각 문자열 정규화:** Dart/Kotlin이 같은 형식 `yyyy-MM-dd'T'HH:mm:ss`(`Locale.US`, 초는 `00`, #17과 같은 원칙)으로 생성. 양쪽 생성 함수를 같은 fixture로 비교 테스트
     - 추가 컬럼: `origin_date`(그 알람을 만든 배정일), `origin_shift`(그 배정일의 근무명), `created_at`
     - 동작: `skip`(이 슬롯 생성 안 함) / `set_type`(이 슬롯의 `alarm_type_id` 지정)
     - 템플릿 시각이나 배정 근무가 바뀌면 슬롯 키가 달라져 예외가 **일시적으로 매칭되지 않을 뿐 삭제되지는 않음** → 근무를 A→B→A로 되돌리면 옛 예외가 다시 적용될 수 있음
     - **되돌림 정책 (D12, 권장: 삭제):** 배정일 D의 근무가 바뀌면 `origin_date = D`인 예외를, 근무 S의 템플릿이 바뀌면 `origin_shift = S`인 미래 예외를
       **같은 트랜잭션에서 삭제** → 되돌려도 옛 "건너뛰기"가 조용히 부활해 알람이 누락되는 일이 없음. 스키마는 어느 정책이든 같으므로 D12는 #31 구현 전에 확정
  2. `updateAlarmType`·`deleteAlarm`(fixed 행일 때)이 행 변경과 **같은 트랜잭션**에서 override 기록
  3. 네이티브 `computeDesiredAlarms`와 Dart `computeDesiredFixedAlarmsForDate`가 계산 결과에 override 적용 (skip 제외, set_type 반영) → 그다음 diff
  4. 지난 슬롯의 override 정리 정책(D11) - 알람 이력 영구보존 규칙 대상은 아니지만, 정리한다면 보수적으로(예: 30일 이상 지난 것)
  5. 백업: 새 원본 테이블이라 백업 레이어가 자동 포함 (`백업복구_설계.md` 원칙 확인)
  6. DB 버전 변경 절차: `DB_스키마_변경_가이드.md` 준수, Kotlin `DATABASE_VERSION` 동시 변경(빌드 검사 있음)
- **대안 검토 (기각):** 스키마 변경 없이 `alarms` 행의 `type`을 `fixed_skip`/`fixed_user`로 바꾸는 방식 → 알람 목록·다음 알람·위젯·DirectBoot·Guard·`alarmExistsInDb` 등 `alarms`를 읽는 **모든 조회 지점**이 새 타입을 걸러야 해서 누락 위험이 큼. 반면 override 테이블은 생성 계산 두 곳만 읽음
- **부가 리스크:** 중간 - 스키마 변경이 #1 업그레이드 경로와 맞물림 (단, v23은 아직 미배포라 사용자 입장에선 18→24 한 번). dev 기기(v23)는 23→24 경로 검증 필요
- **대응 범위:** 개별 삭제·타입 변경의 원복 (규칙·불규칙 모두, 네이티브 갱신·Flutter 재생성 모두)
- **잔여 위험:** 개별 **시각 변경** 기능이 있거나 추가되면 같은 설계로 확장 필요 (구현 전 UI 전수 확인)
- **검증 상태:** 코드 확인(운영 main 포함). 설계만. S16

---

## 5. B. 출시 전 권장

### #9 — H05 + H06: 백업/복원 알람 처리 (#26·#31에 의존)

- **경로:**
  - `alarms` 테이블 전체가 백업 제외 → 사용자가 직접 추가한 `custom` 알람이 복원 후 사라짐
  - 불규칙 fixed 알람은 #26 P1로 원본에서 재생성됨. 개별 예외는 #31 테이블로 백업됨 → fixed 행 자체는 백업하지 않음
  - 설정 탭 덮어쓰기 복원 시 기존 알람·일정 알림 예약·실행 중인 알람이 남음
- **계획:**
  - 백업: `custom`(미래)만 포함. `snoozed` 제외(D5)
  - 덮어쓰기 복원 순서: **① 백업 검증(#19) + 작업용 사본 저장** → ② **복원 잠금 설정**(#19) → ③ 울리는 중인 알람 정리 →
    ④ 기존 `alarms` 전 행 OS 예약 취소 + `cancelAllScheduleNotifications` → ⑤ DB 반영(`alarms` 비움 포함) → ⑥ 설정 반영 →
    ⑦ `forceRefresh` + **미래 `custom`·`snoozed` 행 명시적 재예약**(갱신 엔진은 fixed만 다루므로 맡기면 누락) + 일정 알림 재예약(#5 플래그 확인) →
    ⑧ 잠금 해제. **검증 실패 시 ②~⑧ 실행 안 함**. ④의 일괄 취소에서 이월 대상 ID(#19 3-1)는 제외
  - **백업에 없는 원본 테이블:** 현재 `restoreAll`은 백업에 들어 있는 테이블만 비우고 채움 → 구버전 백업(`alarm_overrides` 없음)으로
    덮어쓰면 **이전 근무표의 알람 제외 설정이 새로 복원한 근무표에 적용**될 수 있음. 덮어쓰기 복원은 "백업 시점 상태로 교체"이므로
    **현재 스키마의 백업 대상 원본 테이블 중 백업에 없는 테이블은 비움**. 단 영구보존 이력 테이블은 #19 규칙(병합)을 따름.
    신규 설치 복원은 원래 비어 있어 영향 없음
  - 복원 안내 문구의 보장 범위를 실제 복원 대상에 맞춤
- **리스크:** 낮음~중간 - 백업 형식 변경, 구버전 앱의 새 백업 복원 확인. 구버전 백업으로 덮어쓰면 그 이후 생긴 기능의 데이터(예: 수면 기록)도
  비워지므로 복원 확인 화면에 백업 날짜·앱 버전을 표시
- **잔여 위험:** ③ 후 ④ 전 종료 시 알람 없는 상태 → #19 단계 기록으로 재시작 시 이어서 실행
- **검증:** 설계만. S5·S17

### #10 — H07: 시간대 변경 시 일정 알림 재계산

- **계획:** `AlarmGuardReceiver` 시각/시간대 변경 분기에서 `rescheduleAllFromDb`(#5 플래그 확인). 트리거가 과거면 **건너뛰기 전 `cancel`**.
  재예약 시 새 트리거 시각을 extra에 담아 #5의 수신 검증과 맞춤
- **리스크:** 낮음 / **잔여 위험:** DST 전환일(M03) / **검증:** 코드 확인. S7

### #11 — H11: 근무명 쉼표 입력

- **계획:** 입력 지점(`onboarding_screen.dart:632`, `settings_tab.dart:1993`, 구현 전 전수 확인)에서 쉼표·`미설정` 차단.
  **백업 import(#19)와 기존 저장 데이터에도 같은 검증 적용**(기존 데이터는 D6: 로그만). 근본 해결(식별자 분리)은 출시 후
- **리스크:** 거의 없음 / **잔여 위험:** 기존 손상 데이터 / **검증:** 코드 확인. S8

### #12 — H12: 근무명 맞교환·중복

- **계획:** 저장 전 결과 이름 중복 검사 + 임시 고유 이름을 거치는 2단계 rename. #31 override의 **`shift_type`과 `origin_shift`를 같은 트랜잭션에서 함께** rename
  (`origin_shift`가 빠지면 이름 변경 후 템플릿을 수정할 때 `origin_shift`로 찾는 D12 예외 정리가 실패). 백업 import에도 중복 이름 검증
- **리스크:** 낮음 / **잔여 위험:** 없음(이 경로 한정) / **검증:** 코드 확인. S8

### #13 — H15: 등록 실패 무통보 + 권한 상태 오판

- **계획:** 전체 실패 = `scheduled.isNotEmpty && failCount == scheduled.length`, 부분 실패 별도 안내, `ScheduleNotificationScheduler.schedule()` 성공 여부 반환,
  `checkExactAlarmPermission()`을 granted/denied/unknown으로 구분(`permission_service.dart:70`)
- **리스크:** 조용하던 실패가 노출됨(의도) / **잔여 위험:** 자동 재시도 없음(V6·다음 갱신 의존) / **검증:** 코드 확인. 빈 생성 목록·부분 실패 케이스 테스트

### #14 — H19: 다른 알람 삭제가 울리는 알람 중지

- **계획:** `isAlarmRinging` 대신 `activeRing`(#3)의 ID와 비교, 진동·무음 울림도 판정에 포함
- **리스크:** 낮음 / **검증:** 코드 확인. S4 (진동·무음 포함)

### #15 — M01: OT 수정이 컨디션 판정에 미반영

- **계획:** `recentOvertimeMinutesProvider`가 `overtimeProvider`를 `watch`. OT 변경 후 **실제 화면 갱신** 테스트
- **리스크:** 거의 없음 / **잔여 위험:** 자정 경과 캐시(M02) / **검증:** 코드 확인

### #16 — H10: 트랜잭션 중 OS 호출 (#27과 함께)

- **계획:** DB 커밋 후 OS 반영. 늦게 도착한 옛 예약의 **조기 울림**은 #27의 예정 시각 대조가 막고, **지연·누락**은 #27의 반영 직전 재확인·
  실패 기록 재시도·Guard 재등록으로 완화(제시간 복구 보장은 아님)
- **리스크:** 낮음 - `AlarmRefreshEngineH2Test` 기대값 수정 / **잔여 위험:** 커밋 직후 종료 시 하트비트 전까지 미등록 / **검증:** Robolectric 갱신

### #27 — H09: 네이티브 갱신과 Flutter 수정 경합

- **경로:** 엔진이 트랜잭션 전에 근무표·템플릿·기존 알람을 읽음 (`AlarmRefreshEngine.kt:127`) → 그 사이 사용자 변경 시 옛 근무표로 계산
- **계획:**
  1. **DB 경합:** 엔진의 읽기를 쓰기 잠금 트랜잭션 안으로 이동 → Flutter 저장과 엔진의 "읽기→계산→쓰기"가 순서대로만 실행
  2. **조기 울림 방지 - 예정 시각 정확 대조 (1장 반론 2):** 모든 기상 알람 예약 Intent에 **예정 시각(정규화한 ms)**을 extra로 담고
     (기상 알람 예약 지점을 하나의 헬퍼로 모아 일괄 처리 - #20도 같은 헬퍼 사용), `CustomAlarmReceiver`가 울릴 때 DB 행을 읽어
     - 행 없음 → 울리지 않음 (현행 `alarmExistsInDb`)
     - **extra의 예정 시각 == DB 행의 예정 시각** → 울림 (AlarmManager 정확 알람은 예정 시각보다 일찍 오지 않으므로 허용 오차 불필요)
     - **다름 + DB 시각이 미래** → 울리지 않고 DB 시각으로 재예약 (옛 예약이 늦게 반영된 경우 - 40초 뒤라도 앞당기지 않음)
     - **다름 + DB 시각이 과거** → 울림 (DB가 가리키는 알람을 이미 놓친 상태 - 늦게라도 울리는 쪽)
     - extra 없음(이번 수정 이전에 등록된 예약)·파싱 실패·DB 접근 불가 → 울림 (fail-open 유지)
  2-1. **반영 직전 재확인 - 예약과 취소의 조건을 분리:**
     - **예약:** 직전에 행을 다시 읽어 **행이 존재하고 예정 시각이 이 작업의 시각과 같을 때만** 실행. 행이 없거나 시각이 바뀌었으면 이 예약은 버림(최신 작업이 따로 반영)
     - **취소:** 행이 **없으면 항상 실행** - 정상적인 삭제도 커밋 후엔 행이 없으므로, 여기서 버리면 OS 예약이 남는 취소 누락이 됨.
       행이 **있으면**(같은 ID를 스누즈가 재사용한 경우 등 이 알람이 여전히 필요) 취소 대신 DB 시각으로 재예약
     - 취소는 여러 번 실행해도 안전(없는 예약 취소는 무해)하므로, 판단이 애매하면 "취소 후 행이 있으면 DB 기준 재예약" 순서를 택함
  2-2. **OS 반영 실패 기록·재시도:** 예약·취소 예외 시 (알람 ID, 작업 종류)를 Device Protected prefs의 실패 목록에 기록 → 다음 갱신·Guard·앱 실행·
     정확한 알람 권한 재허용(V6)에서 DB 기준으로 재시도 후 목록 정리
  3. **Guard 재등록 보강 (기존 메커니즘):** `updateTwentyMinuteNotification`의 `isAlarmScheduled`(`FLAG_NO_CREATE`)는 PendingIntent 존재만 봄 →
     20분 이내 다음 알람은 **판정 없이 항상 재등록**(같은 requestCode·data·`FLAG_UPDATE_CURRENT`라 같은 시각으로 덮어씀). 추가 복구 수단으로 유지
  4. Flutter 재생성을 근무표 저장과 같은 트랜잭션에 묶을 수 있는지 구현 전 확인
- **리스크:** 중간 - 엔진이 쓰기 잠금을 잡는 동안 Flutter 저장 대기. 시간대 변경 직후엔 extra의 절대 시각과 DB 문자열의 새 해석이 달라지므로
  2번 규칙이 "DB 시각이 미래면 재예약, 과거면 울림"으로 올바르게 동작하는지 #10과 함께 확인
- **잔여 위험:** **수신 이벤트 자체가 제시간에 오지 않는 지연·누락은 수신 시 검사로 막을 수 없음.** 실패 기록 재시도는 다음 트리거 시점에야 동작하고,
  Guard 재등록은 알람 20분 전 하트비트가 제시간에 발동해야만 복구됨(보장 없음) → 조기 울림은 막지만 지연·누락은 완화 수준.
  하트비트 Doze 지연, DB 행 자체가 잘못 삭제된 경우(1번이 막음)
- **검증:** 설계만. 원본 H09 재현(스냅샷 직후 중단 주입) 테스트 + S8·S13

### #17 — M06: DB 날짜 문자열 로케일 의존

- **계획:** DB/ISO 저장·파싱 지점만 `Locale.US` 고정(8개 파일 25곳 중 표시용 제외)
- **리스크:** 표시용 포맷 오변경 주의
- **잔여 위험:** **배포 국가로 사용자 기기 로케일을 판단할 수 없으므로**, 이미 비정상 형식으로 저장된 날짜 데이터 가능성은 남음 → 파싱 실패 시 해당 행을 건너뛰고 로그(크래시 금지)
- **검증:** 코드 확인

### #18 — M11: 불완전 백업 파일 (#2와 함께)

- **계획:** 쓰는 동안 `IS_PENDING=1`, 완료 후 해제, 실패 시 행 삭제. 읽기는 최신순으로 유효한 후보까지 순차 확인. 검증된 이전 백업 최소 1개 유지
- **리스크:** 낮음 - 재설치 후 이전 설치의 pending 행은 안 보임(V2) / **잔여 위험:** OEM MediaStore 차이 / **검증:** 설계만. S6

### #19 — H17: 부분 복원 + 백업 내용 검증

- **계획:**
  1. **변경 전 검증 (실패 시 아무것도 바꾸지 않음):** 설정값 자료형, 테이블·컬럼 존재, 패턴 비어있지 않음, `today_index` 범위, 시각 형식·범위,
     템플릿·override의 근무명 참조, 근무명 쉼표·중복(#11·#12), 수면 시작<종료
  2. **작업용 사본:** 검증을 통과한 백업 내용을 앱 전용 저장소(Device Protected)에 사본으로 저장하고 해시를 기록 → 이후 모든 단계는
     다운로드 폴더의 원본이 아니라 이 사본으로 수행(원본이 삭제·교체돼도 재실행 가능)
  3. **복원 잠금 (네이티브도 읽는 Device Protected prefs, 잠금 토큰 포함):** 잠금이 걸린 동안
     - 네이티브 갱신 엔진·Guard의 갱신·수면 자동 감지·일정 알림 재예약·자동백업(`BackupWatcher`)은 **쓰기를 건너뜀**(다음 트리거에서 재시도)
     - 알람 **제어**(재생·화면·끄기·스누즈·타임아웃)는 막지 않음 - 복원 중이라고 기상 알람을 끄거나 미룰 수 없게 하면 안 됨.
       단 **스누즈는 이력만 쓰는 게 아니라 `alarms` 행의 날짜·시각·`type`을 바꾸고 OS에도 예약함**(`AlarmActionHelper.snooze`) →
       "복원 중 다른 쓰기 없음" 전제가 깨지므로 이력 병합만으로는 부족하고, DB 교체 단계가 이 쓰기를 조정해야 함(3-1)
     - 영구보존 이력(`alarm_history`·`alarm_creation_log`)은 "비우고 교체"가 아니라 **자연키(알람 ID·예정 시각·실제 시각·처리 유형) 기준 중복 제외 병합**
       (구현 전 두 테이블의 실제 컬럼으로 자연키 확정. 기존 복원도 이력을 비웠으므로 이력 영구보존 규칙과도 맞춤)
     - Dart 쪽은 복원 화면을 모달로 유지해 사용자 조작에 의한 쓰기 차단
     - **구현 전 DB 쓰기 지점 전수 목록화** 필요(잠금 확인 누락 방지)
  3-1. **알람 제어와 DB 교체의 조정 - 기기 진행 상태 행 이월:** `alarms` 행 중 백업 내용이 아니라 **이 기기에서 진행 중인 상태**, 즉
     ① 미래 시각의 `type='snoozed'` 행 ② 현재 울리는 알람(`activeRing`, #3)의 행을 DB 단계 트랜잭션 **안에서** 먼저 읽어 두고,
     `alarms`를 비우고 백업 행을 넣은 뒤 **같은 ID로 다시 넣음**(OS 예약의 requestCode = 알람 ID이므로 예약과 계속 일치)
     - DB 단계 트랜잭션은 SQLite 쓰기 잠금을 잡으므로 스누즈 쓰기는 그 **이전 또는 이후에만** 커밋됨 → 이전이면 이월로 보존, 이후면 이월된 행 위에 정상 반영
     - 4단계의 OS 예약 일괄 취소에서 이월 대상 ID는 제외
     - **ID 충돌 시 이월 행(진행 중 알람)의 ID는 절대 바꾸지 않음:** 울리는 알람은 화면·제어 버튼·회차 추적(`activeRing`)·종료 예약이, 스누즈 중 알람은
       OS 예약이 모두 기존 ID를 쓰므로, 행만 새 ID로 옮기면 끄기·스누즈·타임아웃이 다른 행을 대상으로 실행될 수 있음 →
       **충돌하는 쪽은 가져오는 백업의 `custom` 행에 새 ID를 부여**(그 행의 OS 예약은 복원 OS 반영 단계에서 새 ID로 등록 - #9 ⑦)
     - **ID 매핑 영속화 (재실행 결과 고정):** 새 ID는 (이월 ID ∪ 백업 ID ∪ 현재 `sqlite_sequence` 값) 중 최댓값 다음부터 백업 ID 오름차순으로 할당하고,
       {백업 ID → 새 ID} 매핑을 DB 단계 **시작 전에** 단계 기록에 저장. 재실행 시 저장된 매핑을 먼저 적용하고 새로 생긴 충돌만 추가 할당해 기록 갱신
       (잠금 중엔 새 알람 행을 만드는 경로가 막혀 이월 대상 ID 집합은 늘지 않고 줄어들기만 함). 명시적 ID 삽입 후 `sqlite_sequence`가 그 이상으로 올라가는지 구현 시 확인
     - 백업 이력(`alarm_history`·`alarm_creation_log`)의 `alarm_id`는 과거 기록 참조라 매핑을 적용하지 않음. 두 기기 이력 병합 시 ID가 겹칠 수 있는 점은
       기존에도 같으므로, 이력 화면이 기기 간 `alarm_id` 고유성을 가정하는지 구현 전 확인
     - **진행 중 알람의 재생 설정 보존:** 이월 행의 `alarm_type_id`는 **현재 기기의** `alarm_types`를 가리키는데, 복원이 `alarm_types`를 백업 내용으로 바꾸면
       같은 ID가 다른 소리·진동·지속시간을 가리키거나 없어질 수 있음(`AlarmPlayer.playAlarmFromDB`·`CustomAlarmReceiver.getDurationFromDB`가 이 참조로 설정을 읽음)
       → DB 단계 **직전에** 이월 대상 알람별 현재 설정(`sound_file`·`volume`·`vibration_strength`·`duration`)을 **Device Protected prefs 스냅샷**으로 저장
       (재실행 시 이미 있는 항목은 덮어쓰지 않음 - 첫 스냅샷이 복원 전 설정). `AlarmPlayer`·지속시간 조회는 해당 알람 ID의 스냅샷이 있으면 우선 사용,
       그 알람의 끄기·타임아웃·인계 처리 시 스냅샷 삭제(행이 이미 없는 스냅샷은 앱 시작 시 정리)
     - 대안(현재 설정을 `alarm_types`에 새 행으로 복사해 매핑)은 설정 탭의 알람 타입 목록에 사용자가 만들지 않은 타입이 노출되므로 기각
     - 재실행해도 같은 규칙으로 이월하므로 중단 후 이어서 실행해도 진행 중 스누즈가 사라지지 않음
     - 끄기·타임아웃은 행 삭제 + 이력 기록이라 이월 대상이 아님(이력은 병합으로 보존). `AlarmActionHelper`가 행이 없을 때도 이력을 남기는지 구현 전 확인
  4. **단계 기록 + 멱등화 (1장 반론 4):** `validated` → `locked` → `os_cleared` → `db_applied` → `prefs_applied` → `os_reconciled` → 잠금 해제·기록 삭제.
     DB 단계는 같은 사본으로 "테이블 비우고 같은 행 삽입(이력은 병합)"이라 재실행해도 결과 동일, 설정은 같은 값 재기록, OS는 취소·재예약 반복 가능
     → 커밋 직후 기록 전에 종료돼도 마지막 기록 단계부터 재실행. 잠금으로 다른 쓰기가 없다는 전제에서 DB 커밋 여부를 알아낼 필요 없음
  4-1. **잠금 유효성은 시간 초과가 아니라 "복원 작업이 살아 있는가"로 판정:** 모든 컴포넌트가 한 프로세스에서 실행됨(Manifest에 `android:process` 없음, 확인됨) →
     복원 시작 시 무작위 잠금 토큰을 **prefs와 프로세스 메모리(정적 변수) 양쪽에** 기록
     - 네이티브가 잠금을 볼 때 메모리 토큰이 prefs 토큰과 **같으면** 진행 중인 복원 → 규칙대로 쓰기 건너뜀
     - **다르거나 메모리에 없으면** 프로세스가 재시작된 것 = 복원 작업은 이미 종료됨 → 진행 중 복원과 충돌할 수 없으므로 쓰기 차단을 풀고
       (DB 단계는 하나의 트랜잭션이라 DB는 복원 전/후 중 한 상태로 일관됨) 현재 DB 기준으로 갱신·재예약,
       "복원이 완료되지 않았습니다 - 앱을 열어 마무리해 주세요" 알림 게시. 단계 기록·사본은 건드리지 않음 → 앱을 열면 5번으로 이어서 처리
       (그 사이 생긴 스누즈는 3-1 이월 규칙으로 보존)
     - 오래 걸리는 정상 복원과 충돌하지 않도록 **시간만으로 잠금을 무시하지 않음**
  5. **중단된 복원 처리 (앱 시작 시 잠금·기록이 남아 있으면):** 사본이 있고 해시가 맞으면 이어서 완료.
     **사본이 없거나 손상됐으면 안전 종료 절차:** 잠금 해제 → **현재 DB 기준으로 `forceRefresh` + 미래 `custom`·`snoozed` 행 재예약 + 일정 알림 재예약**(예약이 취소된 채 남지 않게)
     → "복원이 완료되지 않았습니다" 안내와 함께 다시 복원 / 현재 상태 유지 선택
  6. 기록이 남아 있으면 "이미 데이터 있음" 차단 통과 허용. 화면의 `dbCommitted` 판정은 단계 기록 기준으로 교체
- **리스크:** 중간 - 잠금을 확인해야 하는 쓰기 지점이 여러 곳(네이티브 여러 리시버 + Dart 자동백업), 잠금 해제 누락 시 갱신 정지(→ 앱 시작 시 판정),
  이력 병합 자연키가 부정확하면 중복 이력
- **잔여 위험:** 잠금 확인이 빠진 쓰기 지점, 사본 저장 공간 부족, 중단된 복원 후 사용자가 앱을 열 때까지 백업 내용이 완전히 반영되지 않은 상태
  (그동안 알람은 4-1에 따라 현재 DB 기준으로 정상 동작)
- **검증:** 설계만. S17 (단계별 실패 주입)

### #20 — H04: 기상 알람 `setAlarmClock` 통일 (#3·#4 이후)

- **계획:** 등록 지점(MainActivity, AlarmRefreshEngine, AlarmActionHelper, DirectBootReceiver, AlarmGuardReceiver 재등록, #27 울림 시점 재예약)을 헬퍼 하나로 통일해 `setAlarmClock`. 수면 샘플링·일정 알림은 별도 정책
- **리스크:** 중간 - 상태바 알람 아이콘, 삼성 동작
- **잔여 위험:** 제조사별 전달 보장
- **검증:** 실측 근거 없음. S3로 도입 전후 비교. **`setAlarmClock`의 "다음 알람 보기" 표시(showIntent·잠금화면 표시)는 실제 울림과 별도로 검증**

### #21 — M09: 공유 중지 후 친구 캐시 잔존

- **계획:** 조회를 **서버 소스 강제 조회**(`Source.server`)로 수행해 결과를 서버 확인 notFound / 서버 확인 revoked(#24 tombstone 선택 시) / 서버 미확인(오프라인·오류)으로 구분.
  **네트워크 연결 상태만으로 판단하지 않음.** 서버가 확인한 경우에만 로컬 `data_json` 삭제. 전달된 데이터·스크린샷 회수 불가 안내는 #7
- **리스크:** 낮음 / **잔여 위험:** Firestore SDK 캐시 / **검증:** 설계만. S9

### #22 — M05: 자동 수면 후보 미종료

- **경로:** 화면 꺼짐 + 진행 중 후보가 있으면 무조건 `return` → 종료 처리 미실행. 이후 화면이 켜진 첫 판정 시점이 기상 시각으로 기록됨
- **정책 (단일):** **종료 시각은 기록하되 확정하지 않는다.** "종료" = `end_time`·신뢰도 기록, "확정" = `status`를 `CONFIRMED`로 변경.
  현재 코드도 자동 후보 종료 시 `status`는 `PENDING_CONFIRMATION` 유지 → 앱 상단 확인 카드(`sleep_record_provider.dart:131`)에서 사용자가 맞음/수정/삭제
- **계획:**
  1. **기한 = `start_time + 9시간` (행에서 매번 파생, 저장하지 않음 - 1장 반론 3)**
  2. 화면 꺼짐 분기: 진행 중 후보가 있고 `now >= 기한` → `end_time = 기한`, 신뢰도 `LOW`
  3. 화면 켜짐 분기: `end_time = min(now, 기한)`
  4. → 기한을 넘긴 후보는 **어느 분기에서 처리되든 같은 값**
  5. 기한 이전 화면 켜짐은 기존 로직(2시간 미만 폐기 등) 그대로
- **리스크:** 낮음 - 저장소 추가 없음
- **잔여 위험:** 샘플 기반 추정의 한계, 확인 카드 방치, 9시간 넘게 실제로 잔 경우 짧게 기록됨(사용자 수정 가능),
  **기록된 수면이 수면 창 종료(다음 근무 시작)를 넘을 수 있음** - 창 종료 조건을 뺐고, 그 시각에 사용자가 화면을 켠다는 보장이 없음
- **검증:** 코드 확인. S14

### #23 — M16: MediaPlayer 실패 시 해제 누락

- **계획:** 로컬 변수로 생성 → 실패 시 `release` → 성공 시에만 필드 대입. 폴백 전 이전 리소스 정리
- **리스크:** 거의 없음 / **검증:** 코드 확인

### #24 — H13 + M08: 공유 동기화 실패 처리 전반

- **경로 (코드 확인):**
  - `stopSharing`: 로컬 플래그 먼저 끄고 서버 삭제 실패를 삼킴
  - `startSharing`: 플래그 `true` 저장 **후** 업로드 → 실패 시 로컬 "공유 중" + 서버 문서 없음. 화면은 `try/finally`만 있음
  - `updateMyName`: 로컬 이름 먼저 저장 후 업로드 → 실패 시 불일치
  - `syncIfEnabled`: 실패를 삼키고 재시도 기록 없음
- **통제 범위의 전제:**
  - Firestore 오프라인 쓰기 큐는 **앱 재시작 후에도 유지**되고 큐 안에서는 순서대로 반영됨
  - 따라서 앱 측 핵심 보장은 **"삭제(또는 중지 표시)가 큐에 들어간 뒤로는 그 공유 회차의 어떤 업로드도 큐에 넣지 않는다"**
  - `Future.timeout`은 원래 작업을 취소하지 않음 (시간 초과 = 큐에 들어갔을 수 있음)
  - 이번 수정 배포 **이전** 버전이 이미 큐에 넣은 쓰기는 앱 측에서 통제 불가 → 서버 규칙(선택 D7)으로만 차단 가능
- **앱 측 계획:**
  1. **공유 상태 머신 (영속):** `off` / `active` / `stop_pending`
     - `stop_pending`이면 **모든 업로드 경로 금지** - 함수 진입 직전과 실제 `set` 호출 직전 두 번 확인
     - 서버 확인 전 **새 공유 시작 차단** (화면에 "이전 공유 정리 중" + 재시도)
  2. **`generation` (공유 회차, 단조 증가, 영속):** 공유 시작 시에만 +1. 업로드 작업은 시작 시 generation을 캡처하고 `set` 직전 현재 값·상태와 비교해 다르면 중단
  3. `stopSharing`: 즉시 `stop_pending` → 서버 요청 → 서버 확인 시 `off`. 확인 전 종료되면 다음 시작·복귀에서 재요청
  4. `share_sync_dirty`: `active`에서 업로드 실패 시 기록 → 시작·복귀 시 재업로드
  5. `startSharing`·`updateMyName`: 실패 시 로컬 원복 또는 "반영 대기" 명시
  6. 공유 작업 전체를 Dart 단일 직렬 큐로 (보조 수단)
- **서버 강제 옵션 (D7) - generation 규칙 재설계:**
  - v3 규칙(`새 generation > 기존 generation`)은 **같은 회차의 정상 수정(`1 > 1`)을 거절하는 오류**였음. `>=`로 바꾸면 중지된 문서의 부활을 못 막음
  - 중지는 문서 삭제 대신 **tombstone**(`revoked: true`, 같은 generation, 근무 데이터 필드 제거)으로 덮어씀. 삭제를 쓰면 이후 늦은 `set`이 "새 문서 생성"으로 통과하므로 삭제는 금지
  - 규칙표 (`resource` = 기존 문서, `request` = 새 상태, 소유자 검증은 공통):

    | 경우 | 기존 문서 | 요청 | 허용 조건 |
    |---|---|---|---|
    | 최초 생성 | 없음 | `revoked == false`, `generation >= 1` | 허용 |
    | 같은 회차 정상 수정 | `revoked == false` | `revoked == false` | `request.generation == resource.generation` |
    | 중지 | `revoked == false` | `revoked == true` | `request.generation == resource.generation` |
    | 중지 후 늦은 업로드 | `revoked == true` | `revoked == false`, 같은 generation | **거부** |
    | 재시작 | `revoked == true` | `revoked == false` | `request.generation > resource.generation` |
    | 옛 회차의 늦은 업로드 | `revoked == false`, 새 generation | 옛 generation | **거부** (`==` 불만족) |
    | 구버전 앱 문서 | `generation` 필드 없음 | `generation` 필드 없음 | 허용 (구버전 앱 호환) |
    | 구버전 문서 → 신버전 첫 쓰기 | `generation` 필드 없음 | `generation >= 1` | 허용 |
    | 중지 재전송 (서버엔 반영됐으나 앱이 완료 기록 전 종료) | `revoked == true` | `revoked == true` | `request.generation == resource.generation` → **허용**(멱등) |
    | 문서가 없는 상태의 중지 (최초 업로드 실패 후 중지) | 없음 | `revoked == true`, `generation >= 1` | **허용** (내용 없는 tombstone 생성, 이후 재시작은 더 큰 generation 필요) |
    | 구버전 앱의 공유 중지 (`delete()` 호출) | `generation` 필드 없음 | 삭제 | **허용** (구버전 앱 호환 - 현재 `stopSharing`이 `delete()`를 씀) |
    | 신버전 문서 삭제 | `generation` 있음 | 삭제 | **거부** (tombstone만 사용 - 삭제 후 늦은 `set`이 "새 문서 생성"으로 통과하는 것 방지) |

  - 구버전 앱(삭제 방식)의 늦은 업로드로 인한 문서 부활은 새 규칙으로도 막을 수 없음 → 구버전 사용자에 한한 잔여 위험으로 남김

  - 같은 회차 안의 수정 순서는 한 기기의 SDK 큐 순서로 보장되므로 별도 revision은 두지 않음 (공유 문서의 쓰기 주체는 소유자 기기 하나)
  - 웹 뷰어·친구 앱은 `revoked == true`를 "공유 중지"로 표시(#21), rules 변경은 V5와 함께 사용자가 배포
  - 규칙 검증은 Firestore 에뮬레이터 규칙 테스트로 위 표의 모든 행을 확인 (S18)
- **재설치·익명 UID 변경:** 새 설치는 옛 문서에 쓰기 권한이 없어 중지·삭제 불가 → #25로 상태 혼동 방지, 옛 문서는 C의 H14 근본 과제(Firestore TTL 등)
- **리스크:** 중간 - 상태 분기 증가. D7 선택 시 rules·웹 뷰어·친구 앱 동시 변경
- **잔여 위험:** D7 미선택 시 이전 버전이 큐에 넣은 쓰기, 인증 실패로 큐에조차 못 들어간 작업, 옛 UID 문서
- **검증:** 설계만. S9·S18

### #25 — H14 완화: 복원 시 공유 상태 처리

- **계획 (복원 종류별):**
  - **신규 설치 복원:** 공유 상태·이름·generation·dirty 플래그를 복원 대상에서 제외 → 공유 꺼짐으로 시작 (새 UID와 옛 설정 혼동 방지)
  - **기존 기기 덮어쓰기 복원:** 이 기기의 **현재 공유 상태를 그대로 유지** (UID가 같으므로 연속성 있음)
    - 현재 `active`면 복원된 근무표를 올리도록 `share_sync_dirty` 설정 → 복원 완료 후 업로드
    - 현재 `stop_pending`이면 그 상태와 대기 작업을 유지 (복원이 중지를 취소하지 않음)
    - 백업 안의 공유 설정 값은 무시
- **리스크:** 낮음 / **잔여 위험:** 서버에 옛 UID 문서 잔존(C) / **검증:** 설계만. S5·S9

### #28 — M07: 출퇴근시각 두 테이블 저장

- **경로:** `condition_shift_times` 저장 후 `shift_durations` 저장 실패 시 불일치. 화면의 `schedule` 스냅샷으로 `shift_durations` 전체를 다시 씀 (`work_hours_settings_screen.dart:300`) → "초기화" 연속 클릭 시 옛 값으로 덮어씀
- **계획:** 하나의 DB 트랜잭션 안에서 **DB의 최신 `shift_schedule` 행을 읽고** `shift_durations`의 해당 근무 키만 변경 + `condition_shift_times` 쓰기를 같은 트랜잭션에. 커밋 후 provider 갱신.
  버튼 비활성화는 UX 보조일 뿐 다른 쓰기 경로(근무명 변경 등)는 트랜잭션 직렬화가 막음. 장기적으로 근로시간을 출퇴근시각에서 단일 파생
- **리스크:** 낮음 / **잔여 위험:** 없음(이 경로 한정) / **검증:** 코드 확인. S8

### #29 — M13: dev/prod Firebase 분리

- **설정상 확인된 사실:** `google-services.json`의 `project_id`는 하나(`shiftbell-29f31`), prod·dev 패키지가 각각 다른 appId로 등록. `firebase_options.dart` Android options는 prod appId 하나, `projectId` 동일 → **dev 초기화가 성공하면** 같은 Firestore에 읽고 씀
- **미확인:** dev 빌드에 실제 적용되는 appId, Analytics 스트림 귀속(운영 DAU 혼입 가능성)
- **계획:** S12로 확인 → flavor별 `FirebaseOptions` → dev 전용 프로젝트 분리 여부(D8) → 최소한 운영 DAU에서 dev 제외 기준
- **리스크:** 중간 - 프로젝트 분리 시 dev 익명 인증·테스트 데이터 단절 / **잔여 위험:** 이미 쌓였을 수 있는 dev 테스트 문서 / **검증:** 설정 파일 확인. S12

### #30 — M14: Firestore 쓰기·읽기 절감, rules 검증

- **경로:** 공유하지 않는 `shiftDurations` 변경에도 업로드, `assignedDates` 전체 `set`, rules 필드 검증 없음, `health_tips` 공개 `list`, `refreshAll`이 친구마다 `load()` 반복. 1MB 한도는 당장 문제 아님
- **계획:** 공유 필드 비교 후 변경 시에만 쓰기(#24 상태·generation 확인 뒤), `refreshAll`은 조회 후 `load()` 1회, rules에 허용 필드·자료형·크기 제한(D7 선택 시 #24 규칙표와 통합), Firebase 예산 알림
- **리스크:** 낮음 - rules가 구버전 앱의 쓰기 형식(generation 없음)을 막지 않도록 #24 규칙표의 호환 행 유지
- **잔여 위험:** 공개 링크 반복 조회 비용 / **검증:** 코드 확인. S18

---

## 6. 출시 판단 항목 · C. 보류

### G1 — H03: 알람 재생 프로세스 생존 (출시 판단 항목)

- **왜 C가 아닌가:** 알람 소리가 지속시간 전에 끊기면 앱의 핵심 목적에 직접 영향. #3·#4·#20은 "끄지 못함"은 막지만 "일찍 끊김"은 막지 못함
- **판단 방법:** S4에서 화면 실행이 제한된 상태 + 메모리 압박/프로세스 회수 시 재생 유지 시간을 측정
  - 끊김이 재현되면 → **출시 전** foreground service(적합한 타입) 전환 착수
  - 재현되지 않으면 → 출시 후 과제로 기록
- **검증 상태:** 잠재적 위험, 실측 필요

### C. 보류 (출시 후 과제)

| 원본 ID | 판정 | 이유 |
|---|---|---|
| H14 근본 해결 | 출시 후 | 익명 계정 연결 또는 Firestore TTL 정책(업로드마다 `expireAt` 갱신 + 활성 사용자는 앱 시작 시 갱신) 설계 필요. #25로 증상 완화 |
| M02 (자정·탭 전환 시 오래된 화면) | 출시 후 | 표시 지연 수준. 공통 날짜 provider 도입 시 함께 |
| M03 (서머타임) | 조건부 | 한국 배포여도 해외 이동 기기에서 발생 가능 → **S7에 DST 양방향 검증을 조건부로 포함**(영어권 사용자 확인 시 또는 해외 이동 사용 사례가 있을 때 출시 전 확인). 수정은 날짜 순회를 `DateTime(y,m,d+1)`로 바꾸는 방식 |
| M04 (근무명 문자열로 휴무 판정) | 출시 후 | 근본 해결은 스키마 변경. 규칙만 바꾸면 기존 사용자 달력 색상 변경 위험 |
| M12 (대화면·폴더블 레이아웃) | 보류 | S10 선행. 복원 화면의 스크롤 없는 Column 우선 확인 |
| M15 (`web_main` 분석 오류) | 불필요(Android 출시 기준) | 웹 뷰어 전용. 웹 배포 전 `flutter build web -t lib/web_main.dart` 확인. D7 선택 시 웹 뷰어 변경과 함께 재검토 |
| L02 (Analytics 이벤트 계측) | 제품 결정 | 버그 아님. 원본 근무명·메모·수면 상세 미전송 원칙 |

---

## 7. 39건 외 추가 확인 항목

| ID | 내용 | 현재 코드 상태 | 필요한 조치 |
|---|---|---|---|
| V1 | 평문 백업의 개인정보 노출·삭제 후 잔존 | 다운로드 폴더에 암호화 없는 JSON. 앱 삭제 후에도 남음 | #7 문구 반영, 수동 삭제 안내. 암호화는 D9 |
| V2 | 재설치 후 이전 백업 자동 탐지 한계 | 이전 설치 파일은 새 설치 소유가 아님. 삼성 사례 확인. SAF 수동 경로 존재 | 도움말 표현을 실제 지원 범위로. #18 후 재확인 |
| V3 | Android 10 미만 백업 미지원 안내 | 실패 토스트("Android 10 이상 필요, 또는 일시적 오류")만 | 해당 기기에서 백업 메뉴에 미지원 상태를 미리 표시 |
| V4 | 앱 초기화 지연·실패 시 복구 UI | `main()`이 `runApp` 전 `initializeDateFormatting`·DB·`AlarmService().initialize()`·`initFirebase()`를 await, DB는 try/catch·타임아웃 없음. `ErrorWidget.builder`가 **릴리스에서도 예외 원문 표시** | **#8과 함께 필수.** DB 실패 복구 화면, 릴리스 오류 위젯 문구 정리, `GoogleFonts` unawaited 오류 처리 확인 |
| V5 | 운영 Firestore rules 실제 배포 상태 | 저장소 파일만 확인 | 사용자가 콘솔 대조. #24·#30 rules 변경 시 함께 배포 |
| V6 | 부팅·업데이트·권한 재허용 경로 | `LOCKED_BOOT_COMPLETED`만. `MY_PACKAGE_REPLACED`·`BOOT_COMPLETED` 없음(#1에서 추가). **정확한 알람 권한 상태 변경 수신기 없음** | 권한 상태 변경 수신 시 강제 갱신 + 일정 알림 재예약. S1·S2 매트릭스에 포함 |
| V7 | `SCHEDULE_EXACT_ALARM` + `USE_EXACT_ALARM` 동시 선언 | 중복 자체는 버그 아님 | Android 12 권한 거부 경로를 #13·V6와 함께 검증. Play `USE_EXACT_ALARM` 자격 신고 확인 |

---

## 8. 출시 전 필수 검증 시나리오

**알람 관련은 에뮬레이터만으로 불충분 → 삼성 등 실기기 필수. 날짜 경계는 날짜 주입 계산 테스트로 먼저 고정하고, 실제 자동 실행은 기기에서 별도 확인.**

| ID | 시나리오 | 관련 |
|---|---|---|
| S1 | **로컬 선행 (Robolectric):** v12·v14·v15·v17·v18·v23 DB fixture → 네이티브 마이그레이션 → Dart `onCreate`로 새로 만든 v24와 `PRAGMA table_info` 일치, 중간 SQL 실패 주입 시 버전·스키마 롤백, `onCreate`/`onDowngrade` 예외, 버전은 최신인데 컬럼 누락된 DB의 `onOpen` 복구(**복구 후 `friends` 등 기존 데이터 보존** - 과거 마이그레이션 재실행 없음, `repair` 구역에 화이트리스트 밖 SQL을 넣으면 거부되는지). **실기기:** v1.0.22 prod(DB v18) 설치 → 알람 저장 → 새 빌드 `adb install -r` → **앱 미실행** → ① 알람 시각까지 대기, 소리·스누즈·끄기·이력 정상 ② **재부팅 후 첫 잠금 해제 전** 알람(부팅 수신 시 마이그레이션 + 재등록) ③ 10일 경과 ④ 네이티브와 앱이 거의 동시에 DB를 여는 경우 ⑤ 자산 파일(단일 SQL 원본)을 release·잠금 해제 전에 읽는지. 수면 위젯 기록도 확인 | #1, #8, #31(v24), V4, V6 |
| S2 | 알림·오버레이·전체화면·정확한 알람 권한을 각각 허용/거부/재허용하며 잠금·해제 상태에서 수신·끄기·스누즈. 화면 표시 후 홈 이동 → 제어 알림 유지 | #4, #13, V6, V7 |
| S3 | Doze 강제 진입 상태에서 5분 간격 알람·5분 스누즈·수면감지·일정 알림 동시 예약 → 실제 수신 시각 비교 (#20 전후). 다음 알람 표시 별도 확인 | #20 |
| S4 | 알람 중 홈 이동·화면 잠금·다른 알람 삭제·진동/무음 알람 삭제·**프로세스 회수 시 재생 유지 시간 측정** → 종료 시각·이력 정합성. 스누즈 재울림·인계 직후 이전 회차 종료 신호 무시. **절전(Doze) 상태에서 AlarmManager 종료 예약만으로 지속시간에 맞춰 종료되는지**(화면·오버레이 타이머 제거 여부 판단), 토큰 저장 실패 주입 시 메모리 토큰으로 회차 구분 유지 | #3, #14, **G1** |
| S5 | 규칙/불규칙·custom·스누즈·개별 예외가 있는 백업 → 신규 설치 복원 / 덮어쓰기 복원 → 알람 목록·OS 예약·공유 상태 비교 | #9, #25, #26, #31 |
| S6 | 백업 쓰기 중 종료, 저장공간 부족, 잘못된 형식 import, 설정 쓰기 실패 → 재시도·정상 백업 보존. 수동 백업 후 메모 수정 → 홈 → 복귀 → 자동백업 갱신. 백업 실행 중 추가 수정 → 종료 후 재확인 | #2, #8, #18, #19 |
| S7 | 시간대 변경(새 기준에서 과거가 되는 경우 포함)·자정·연말, 전날/당일/다음날 근무 알람과 일정 알림 동시. **조건부: DST 양방향(America/New_York 2026-03-08, 2026-11-01) 날짜 순회·일정 알림 트리거** | #10, #26, M03 |
| S8 | 근무명 쉼표·`미설정`·맞교환·중복, 날짜 근무 빠른 연속 수정(갱신 엔진 실행과 겹치게), 출퇴근시각 "초기화" 연속 클릭 | #11, #12, #27, #28 |
| S9 | 두 기기로 공유 시작 → 수정 → 오프라인 → 중지 → 앱 강제 종료 → 재시작 → 근무표 수정 → 온라인 복구 → 서버 문서 부활 여부. 시작·이름 변경 실패, 재설치 후 옛 링크, 친구 캐시(서버 확인 vs 오프라인) | #21, #24, #25, #30 |
| S10 | prod release 360/600/840dp, 글자 크기 1.0/1.5/2.0, 폴더블 → 화면·광고·복원 화면·광고 실패 화면 | #6, M12 |
| S11 | 일정관리 탭 숨김 → 재부팅 → 알림 시각 대기, 탭 복원, 복원 후 탭 설정 동기화. 일정 시각 변경 후 채널 호출 실패 주입 → 옛 시각에 표시 안 됨 | #5 |
| S12 | dev 빌드 실제 appId·DebugView 스트림, 운영 rules 대조, 개인정보 문구·실제 SDK 수집·Play 신고 대조 | #7, #29, V5 |
| S13 | 알람 20분 전 하트비트 시점에 OS 예약만 취소(PendingIntent 유지) → 재등록 여부. 스누즈 직후 옛 시각 재예약 주입 → **DB 예정 시각 40초 전에 도착하는 경우 포함** 일찍 울리지 않고 DB 시각에 울림. OS 예약 예외 주입 → 실패 목록 기록 후 다음 트리거에서 재시도. 커밋 후 반영 직전에 행 삭제 주입 → **예약 작업은 버리고, 삭제된 알람의 취소는 실행**(OS에 예약이 남지 않음). 같은 ID를 스누즈가 재사용한 행에 늦은 취소 도착 → 취소 대신 DB 시각으로 재예약 | #27, #16 |
| S14 | 자동 수면 후보 생성 → 9시간 넘게 화면 꺼짐 → ① 화면 켜고 앱 열기 ② 화면 꺼진 채 트리거 → 두 경우 `end_time`이 같고 확인 대기 상태인지 | #22 |
| S15 | **P1 불규칙:** ① 한 달 뒤 날짜 배정 → 앱 미실행 → 해당 날짜가 창에 들어오는 날 자정 이후 알람 생성 ② 창 끝 날짜의 전날·다음날 템플릿 ③ 배정 삭제·변경 시 D-1·D·D+1 정리 ④ 마지막 템플릿 삭제 → 기존 fixed 정리 ⑤ 알람 0개 상태에서 자정 갱신 유지 ⑥ 재부팅 ⑦ 갱신 중 프로세스 종료 후 재실행 → 중복 없음 ⑧ 같은 입력 두 번 갱신 → 변화 0건 ⑨ 정책 도입 당일 이미 갱신된 상태에서 업데이트 → 강제 갱신 | #26, #27 |
| S16 | **개별 예외:** 규칙·불규칙 각각 내일 알람 삭제 / 무음 변경 → 자정 갱신 → 원복 안 됨. 인접 날짜 근무 변경(Flutter 재생성) → 원복 안 됨. 해당 날짜 근무 자체를 바꾸면 새 근무 알람 생성. **근무 A→B→A 되돌리기·템플릿 시각 변경 후 되돌리기 → D12 정책대로 처리**. Dart/Kotlin 슬롯 키 문자열 일치 | #31 |
| S17 | **복원 단계별 실패 주입:** 검증 실패(아무것도 안 바뀜), `os_cleared` 직후 종료, `db_applied` 커밋 직후 단계 기록 전 종료, `prefs_applied` 실패 → 재시작 후 이어서 완료, 결과가 한 번에 성공한 경우와 동일. **복원 중 알람 울림·Guard·수면 감지·자동백업 발생 → 쓰기 건너뜀·이력 보존(병합)**. 복원 중 원본 백업 파일 삭제 → 사본으로 완료. **사본까지 없을 때 안전 종료**(현재 DB 기준 알람·일정 알림 재예약). 구버전 백업(`alarm_overrides` 없음)으로 덮어쓰기 → 기존 예외 비워짐. **복원 중 스누즈 → 복원 완료 후에도 스누즈 유지(같은 ID, OS 예약 일치)**, 백업 `custom` 행과 ID 충돌 시 새 ID로 이월·재예약. **복원 도중 앱 프로세스 강제 종료** → 네이티브가 잠금 무효 판정 후 알람 정상 동작 + 안내 알림 → 앱을 열면 이어서 완료(그 사이 스누즈 보존). **백업과 현재 기기에서 같은 타입 ID의 설정이 다른 경우·백업에 그 타입 ID가 없는 경우 → 스누즈 재울림이 복원 전 설정(소리·진동·지속시간)으로 동작**. 울리는 알람과 백업 `custom` ID 충돌 → 울리는 알람 ID 유지(끄기·스누즈·타임아웃 정상), 백업 `custom`은 새 ID로 OS 예약. 매핑 저장 후 중단·재실행 → 같은 새 ID. 복원 후 미래 `custom`·`snoozed` 알람이 실제로 OS에 예약됐는지 확인 | #9, #19 |
| S18 | **Firestore 규칙 에뮬레이터 테스트 (D7 선택 시):** #24 규칙표 전 행 - 같은 회차 정상 수정 허용, 중지 허용, 중지 후 늦은 업로드 거부, 재시작 허용, 옛 회차 거부, 구버전 문서 호환, **중지 재전송 허용, 문서 없는 상태의 중지 허용, 구버전 앱 `delete` 허용**, 신버전 문서 삭제 거부 | #24, #30 |

---

## 9. 사용자 결정 목록

| ID | 결정 사항 | 관련 | 상태 / 권장 |
|---|---|---|---|
| D1 | 불규칙 근무 알람 정책 | #26, #9 | **확정: P1 자동 롤링** (2026-09-14 재검토 반영) |
| D1-a | P1 창 기준: 울림일 기준(규칙 근무와 동일) vs 배정일 D-10 기준 | #26 | **확정: 울림일 기준(방식 1)** (2026-09-14) - 규칙 근무와 동일 계산식 재사용 |
| D2 | 이번 릴리스 광고 실제 운영 여부 | #6 | **확정: 운영함.** 단, 실제 광고 ID·디버그 틴트 끄기·Manifest 앱 ID 교체는 **최종 배포 직전에 작업**(2026-09-14). 그 전까지는 테스트 ID를 유지하고 dev/prod 코드 분리 구조만 먼저 만들어둠(#6 계획 그대로) |
| D3 | 개인정보 안내·Play 데이터 보안 양식 갱신 (콘솔) | #7 | **확정: 진행함.** ko/en 문구 수정과 Play Console 콘솔 작업 모두 **최종 배포 직전에 작업**(2026-09-14). 그 전까지는 실제 SDK 수집 항목이 확정(Analytics·공유·백업 등 관련 항목 전부 구현 완료)된 뒤에 문구를 써야 다시 안 고침 |
| D4 | ~~#1 헤드리스 마이그레이션 시제품 진행~~ | #1 | **폐기 (2026-09-14)** - #1을 네이티브 `onUpgrade` SQL 이식 + `SQLiteOpenHelper`의 트랜잭션 원자성으로 재설계하면서 헤드리스 Flutter 자체가 불필요해짐. 번호는 이력 보존을 위해 남기고 결번 처리 |
| D5 | 백업에 `snoozed` 포함 여부 | #9 | **확정: 제외** (2026-09-14, 권장안 채택) |
| D6 | 기존 근무명 쉼표·sentinel 시작 시 검사 | #11 | **확정: 로그만** (2026-09-14, 권장안 채택) |
| D7 | 공유 중지를 tombstone + rules로 서버 강제 | #24, #30, #21 | **확정: 앱 측 먼저, S9 결과로 결정** (2026-09-14, 권장안 채택 - S9 결과는 사용자에게 다시 보고해 tombstone/rules 채택 여부를 받음) |
| D8 | dev 전용 Firebase 프로젝트 분리 | #29 | **확정: S12 후 결정** (2026-09-14, 권장안 채택 - S12 결과를 사용자에게 보고, 분리 시 Firebase 프로젝트 생성·Console 작업은 사용자가 수행) |
| D9 | 백업 파일 암호화 | V1 | **확정: 출시 전엔 안내로 대체** (2026-09-14, 권장안 채택) |
| D10 | #31 개별 예외 테이블 도입 (DB v24, 이번 릴리스) | #31, #1 | **확정: 도입** (2026-09-14) - v23 미배포라 사용자는 18→24 한 번 |
| D11 | 지난 슬롯 override 정리 여부·기간 | #31 | **확정: 30일 이상 지난 것만 정리** (2026-09-14, 권장안 채택 - 영구 이력 테이블에는 적용 금지) |
| D12 | 근무·템플릿을 되돌렸을 때 옛 개별 예외 처리 | #31 | **확정: 배정일·템플릿 변경 시 관련 예외 삭제** (2026-09-14, 권장안 채택. 스키마는 정책과 무관) |

**착수 가능 범위:** D1~D12 전부 결정됨(2026-09-14, D5~D9·D11·D12는 사용자 지시 "권장안대로"로 일괄 확정). D7·D8은 결정 내용 자체가 S9·S12 결과를 보고 후속 판단하는 절차이므로 그 결과는 다시 사용자에게 보고한다. 작업 분담·교대 실행 방식은 `출시전_수정작업_그룹별_실행계획_및_세션인계_2026-09-14.md` §11 참고.

---

## 10. 권장 작업 순서

1. **결정 상태:** D1·D1-a·D2·D3·D10 확정, D4 폐기 (2026-09-14, 9장). **초기 마이그레이션 작업(아래 2·3)은 지금 착수 가능.**
   D5~D9·D11·D12는 **해당 기능 구현 전에 확정**
2. **#31 스키마 설계(v24) + 마이그레이션 SQL 단일 원본화** - v1→v24 SQL을 자산 파일로 정리(#1 계획 2), `alarm_overrides` 컬럼 확정(`origin_date`·`origin_shift` 포함)
3. **#1(네이티브 `onUpgrade`·`onOpen` 복구·`onCreate`/`onDowngrade` 예외화·게이트 교체) + #8 + V4** → **S1 (Robolectric 다버전 선행 → 실기기 18→24, 잠금 해제 전 포함)**
4. **#27 + #16** → **#26(P1) + #31 엔진·Dart 적용** (계산 테스트 선행) → S13·S15·S16
5. **#9 + #19 + #25** → S5·S17
6. **#2 + #18** → S6
7. **#3 → #4** → S2·S4 (**G1 판단**)
8. **#5 + #10** → S7·S11
9. **#6·#7 구조만 먼저:** dev/prod 코드 분리, 문구 초안 - 실제 광고 ID·최종 문구 확정은 **최종 배포 직전에 별도로** (D2·D3) → S10·S12는 최종 문구·ID 반영 후 실행
10. B 나머지 → V1~V7 → S1~S18 전체 통과
11. **배포 직전 최종 체크(별도 항목, 나머지 전부 통과 후):** #6 실제 광고 ID 교체 + 디버그 틴트 끄기 + Manifest AdMob 앱 ID 교체 / #7 최종 개인정보 문구 반영 + Play Console 데이터 보안 양식 갱신

---

## 11. 구현 시 공통 체크리스트

- [ ] 해당 원본 감사 절을 다시 읽었는가
- [ ] 수정안이 건드리는 호출 경로(호출자·같은 테이블을 쓰는 다른 경로·네이티브 쌍)를 끝까지 읽었는가
- [ ] 이미 있는 방어 로직(Guard 재등록, `alarmExistsInDb`, Guard 자정 예약 등)을 새로 만든다고 착각하지 않았는가
- [ ] 관련 결정(9장)이 확정됐는가
- [ ] Dart/Kotlin 이중구현 쌍(알람 생성·override 적용, **DB 마이그레이션 SQL**, 수면 창, 일정 알림 트리거)을 둘 다 바꾸고 같은 fixture로 비교했는가
- [ ] DB 스키마를 바꾼다면 `DB_스키마_변경_가이드.md`를 읽고 Kotlin `DATABASE_VERSION`을 같은 커밋에서 올렸는가, 마이그레이션 SQL을 단일 원본의 `migrations`(증분)와 `repair`(비파괴 최종 형태) 양쪽에 추가했고 v12·v18·직전 버전에서 올라오는 테스트를 돌렸는가
- [ ] 새 원본 데이터라면 `백업복구_설계.md` 원칙을 깨지 않는가
- [ ] 알람 이력 영구보존 규칙을 어기지 않는가
- [ ] 멱등성: 같은 작업을 두 번 실행해도 결과·이력이 한 번과 같은가
- [ ] 설계 문서·체크리스트(`TEST_CHECKLIST.md`, `CLAUDE.md`)에 정책 변경을 반영했는가
- [ ] 대응 범위 / 잔여 위험 / 검증 상태를 이 문서에 갱신했는가
