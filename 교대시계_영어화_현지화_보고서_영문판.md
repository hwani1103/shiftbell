# 교대시계(Shiftbell) 영어 현지화(i18n) 구현 보고서

작성일: 2026-08-15
브랜치: `claude/i18n-english-localization` (기준 커밋: `claude/dark-mode-fNhdW`의 `65867d6`)
※ 이 문서는 `교대시계_영어화_현지화_보고서.txt`(2026-08-14, 조사 전용) 이후, 실제 코드
구현을 마친 결과 보고서입니다. 조사 문서에 나온 "권장 순서" 1~6번을 실제로 전부 진행했습니다.

---

## 0. 요약

- **인프라**: `flutter_localizations` + `intl` + `.arb`(`lib/l10n/app_ko.arb`/`app_en.arb`) +
  자동생성 `AppLocalizations`(`lib/l10n/generated/`, `flutter gen-l10n`으로 생성, git 추적
  안 함) 구조를 정식 도입했습니다. 모든 화면에서 `context.l10n.somekey` 형태로 접근합니다
  (`lib/l10n/l10n_extensions.dart`).
- **한글 → 코드 분리 리팩터링 범위**: `lib/screens/*.dart` 16개, `lib/widgets/*.dart` 2개,
  `lib/services/update_service.dart`, `lib/main.dart`, `lib/web_main.dart`, 그리고
  한국어 문자열을 반환하던 모델 getter 다수(`AlarmType`, `AlarmHistory`, `CalendarThemeIdX`
  등) - 사용자에게 보이는 하드코딩 한국어 문자열을 전수 조사해서 교체했습니다.
- **ARB 키 개수**: 375개 (한/영 완전히 1:1 대응, 누락 없음 - 자동 스크립트로 검증함).
- **검증**: `flutter analyze` 기준 **에러 0건**(경고/정보성 린트는 대부분 이 작업 이전부터
  있던 것이며 아래 8번에 근거와 함께 정리). 실기기가 연결되어 있지 않아 육안 확인은
  못 했고, 코드 레벨(정적 분석 + 빌드)로 가능한 검증은 전부 했습니다.
- **안전장치**: 이 작업 시작 전 상태를 `claude/dark-mode-fNhdW` 브랜치에 커밋(`65867d6`)
  하고 push해뒀습니다. 언제든 아래 한 줄로 지금 dev 상태로 복귀 가능합니다:
  ```
  git checkout claude/dark-mode-fNhdW
  ```

---

## 1. 판단이 필요했던 항목 - 결정 사항 (조사 보고서 2부 대응)

### 1-1. 근무명 글자수 제한: 4자 → **10자**
`lib/constants/shift_name_limits.dart`의 `kMaxShiftNameLength = 10`.
- 왜 15가 아니라 10인가: "Afternoon"(9자)까지는 커버하고, 근무명 뱃지가 들어가는
  자리(달력 셀, 전체근무표 그리드)가 원래 "짧은 라벨" 전제로 디자인돼 있어서 완전히
  풀면 거의 항상 축소 표시 상태가 되어 오히려 가독성이 떨어질 것으로 판단.
- 동시에 뱃지 텍스트를 하드컷(`substring(0, 4)`)하던 부분(calendar_tab.dart 2곳,
  calendar_theme_lab_screen.dart 2곳)을 전부 `overflow: TextOverflow.ellipsis`로 교체해서,
  10자 제한을 넘는 극단적 케이스가 와도 정보가 뚝 잘리지 않고 자연스럽게 "..." 처리되게
  했습니다. all_shifts_view.dart(좁은 그리드, 3자 컷)는 별도로 폭 제약이 훨씬 빡빡해서
  3자+ellipsis로 절충.

### 1-2. 요일 순서: 두 언어 모두 **일요일 시작** 유지
`lib/utils/weekday_util.dart`로 5~6곳에 중복돼 있던 요일 배열을 통합. en-US 관습(일요일
시작)이 한국 관습과 일치해서 별도 로케일 분기 없이 고정. en-GB/유럽식(월요일 시작)은
이번 범위에서 다루지 않음 - 필요해지면 `weekdayLabel()` 내부에 countryCode 분기만
추가하면 되는 구조로 만들어뒀습니다.
- 단, 일부 달력 테마(다크 그리드, 미니멀 언더라인)가 원래부터 "항상 영어 요일 약자로
  보이는" 디자인이었던 부분(`_weekdayEn3`/`_weekdayEn1`)은 로케일과 무관한 디자인
  선택이라 그대로 뒀습니다(한국어 사용자에게도 원래 그렇게 보였음).

### 1-3. 날짜 표기: intl `DateFormat`으로 전면 교체, 단 **숫자만 있는 슬래시 포맷은 로케일
분기 안 함**
`'${year}년 ${month}월'` 같은 수동 조립을 `DateFormat.yMMMM(locale)`/`DateFormat.MMMd(locale)`
등으로 교체했습니다(예: "8월 2026" ↔ "August 2026"). 다만 `"7/21 ~ 8/20"`처럼 **단어
없이 숫자만** 있는 표기는 한국/미국 둘 다 월/일 순서가 같아서(M/d) 로케일 분기 없이
고정 숫자 포맷으로 통일했습니다(예: work_hours_settings_provider.dart의
`periodRangeShort`, next_alarm_tab.dart의 날짜 라벨). 이 판단의 이유: 병렬 작업 중 한
에이전트가 `DateFormat.Md(locale)`을 썼는데, 이게 로케일에 따라 구분자가 달라져서
(ko → "8. 15." 점 구분, en → "8/15" 슬래시) **한국어 사용자에게 없던 시각적 회귀**가
생기는 걸 발견 → 병합 단계에서 직접 고정 숫자 포맷으로 되돌림.

### 1-4. 공휴일 표시: **영어 로케일에서는 끄기** (토글/다국가 데이터셋 아님)
`lib/utils/holiday_util.dart`의 `getHolidayName(date, {required bool isKorean})` -
`isKorean=false`면 무조건 `null`. 조사 보고서의 3가지 선택지(끄기/토글/다국가 데이터셋)
중 "끄기"를 택함 - 다국가 공휴일 데이터셋은 나라마다 다르고 매년 갱신이 필요해 작업량이
너무 크고, 토글은 UI를 새로 만들어야 하는데 실기기 검증 없이 새 설정 UI를 추가하는 건
리스크가 큼. "한국 고유 공휴일이 남의 나라 달력에 뜨는" 어색함을 없애는 가장 안전하고
정직한 선택.

### 1-5. 시간 표시(AM/PM): 새 `commonAm`/`commonPm` 키 추가, 근무 타입 오전/오후와 분리
"오전"/"오후"가 (1) 근무 타입 이름(Morning Shift/Afternoon Shift, 기존 `shiftMorning`/
`shiftAfternoon` 키)과 (2) 실제 12시간제 AM/PM 선택기 두 가지로 쓰이는 걸 코드에서
확인하고, 후자 전용으로 `commonAm`("AM")/`commonPm`("PM") 키를 새로 분리했습니다(섞어
쓰면 영어에서 "Morning"/"Afternoon"이 시간 선택기에 잘못 뜨는 버그가 됨). 알람 시간
자체의 24시간제/12시간제 표시 방식은 기존 구조(HH:mm 저장 + 화면마다 자체 포맷)를
유지하고 손대지 않았습니다 - 이건 "번역"이 아니라 전체 앱의 시간 표시 정책을 바꾸는
훨씬 큰 작업이라 이번 범위에서 제외.

### 1-6. 카운터/복수형(개수): ICU plural로 처리
"3개 선택", "N일 N시간 남았습니다", "N일 근무 변경" 등 개수가 포함된 문구는 ARB의 ICU
plural 문법(`{count, plural, =1{...} other{...}}`)으로 처리했습니다. 알람 OT 시간
표시(`formatOvertimeMinutes`)만 예외적으로 plural 대신 **"3h 30m" 압축 표기**로
통일했습니다 - "hour(s)/minute(s)" 단복수 분기 자체가 필요 없어지는 실용적 선택이고,
영어권에서 흔히 쓰이는 시간 표기 관습이기도 합니다.

### 1-7. 근무명 자체(사용자 입력 데이터)는 번역 대상이 아님, 단 "휴무 감지" 로직은 보강
근무명("주간", "야간" 등)은 사용자가 자유롭게 입력하는 데이터라 번역 대상이 아닙니다.
다만 온보딩의 **기본 제안값**(예전엔 무조건 한국어 5개)은 로케일에 따라 영어
기본값(Day/Night/Morning/Afternoon/Day Off)을 보여주도록 `onboarding_screen.dart`에서
처리했습니다. 그리고 "휴무" 근무를 자동으로 고정 빨강색으로 칠하는 로직이
`shift.contains('휴')`로 하드코딩돼 있던 걸 `lib/utils/shift_name_util.dart`의
`isRestShiftName()`로 확장해서 "Off"/"Rest"/"Holiday" 같은 영어 표현도 인식하게 했습니다
(완벽하진 않음 - 사용자가 전혀 다른 단어를 쓰면 여전히 못 잡음, 한국어도 원래 같은
한계가 있었음). **최종 병합 단계에서 실제 색상 배정 엔진(`_assignFromPalette`,
calendar_tab이 실사용하는 함수)에도 이 로직을 적용**했습니다 - 처음엔 온보딩에만
적용되고 실제 캘린더 엔진은 여전히 `contains('휴')`만 쓰는 상태로 남아있던 걸
`flutter analyze` 후 직접 발견해서 고쳤습니다.

### 1-8. 앱 표시 이름: "교대시계" → **"Shiftbell"**
스플래시 화면, MaterialApp title 둘 다 로케일에 따라 전환됩니다(`onGenerateTitle`).
패키지 ID/도메인은 원래 영문(shiftbell)이라 문제 없었습니다.

### 1-9. 죽은 코드(미사용)는 로컬라이즈하지 않고 그대로 둠
아래는 실제로 어디서도 호출되지 않는 코드라, 사용자에게 절대 보이지 않습니다 -
번역해도 검증 불가능하고 위험만 늘어서 그대로 뒀습니다:
- `main.dart`의 `AlarmTestScreen`(라우팅 안 됨, 개발용 테스트 화면)
- `AlarmNotifier.addAlarm()`(alarm_provider.dart) - 호출부 없음
- `AlarmService.scheduleTestAlarm()`의 기본 파라미터 `'테스트 알람'` - 위 AlarmTestScreen
  에서만 쓰임
- `settings_tab.dart`의 `_showAlarmListDialog`/`_showEditShiftColorsDialog`/
  `_showAllTeamsSetupDialog`(파일 내부에서도 미사용으로 확인됨) - **이 3개는 예외적으로
  로컬라이즈까지는 해뒀습니다**(작업한 에이전트가 "나중에 재활성화될 수도 있다"고
  판단해 텍스트만 교체) - 다만 여전히 호출되지 않는 상태입니다.

---

## 2. 아키텍처 변경 사항 (다음에 문자열 추가할 때 참고)

- **새 문자열 추가 방법**: `lib/l10n/app_ko.arb`에 원문(한국어) 키 추가 →
  `lib/l10n/app_en.arb`에 동일 키의 영어 번역 추가 → `flutter gen-l10n`(또는
  `flutter pub get`/`flutter run`이 자동 실행) → `context.l10n.새키이름`으로 사용.
  두 arb 파일의 키 집합이 어긋나면 gen-l10n이 빌드 시점에 경고합니다(이번에 365개
  키가 정확히 1:1로 맞는지 스크립트로 검증 완료).
- **모델의 문자열 getter는 전부 `BuildContext`를 받는 메서드로 전환**:
  `AlarmType.vibrationLabel(context)`, `AlarmHistory.dismissLabel(context)`,
  `CalendarThemeIdX.label(context)`, `formatOvertimeMinutes(context, minutes)`,
  `getHolidayName(date, isKorean: ...)`. 순수 데이터 모델(`Alarm`, `ShiftSchedule` 등)에
  직접 `context`를 심지 않고, "표시용 라벨을 만드는 함수/getter"만 이렇게 바꿨습니다.
- **내부 센티널 값은 번역하지 않음**: `kUnsetShiftSentinel`(값: `'미설정'`,
  shift_schedule.dart) - "근무 미설정"을 나타내는 내부 비교용 값이고 절대 그대로 화면에
  노출되지 않게(항상 `context.l10n.commonNotSet`으로 변환 후 표시) 각 화면에서
  처리했습니다. 값 자체를 영어로 바꾸지 않은 이유: 이 값과 비교하는 코드가 10곳 넘게
  퍼져 있어서, 값을 바꾸면 그 모든 비교부를 동시에 정확히 맞춰야 하는 리스크가 있는데
  비해 실익이 없음(사용자는 이 값을 절대 직접 보지 않음).
- **요일/날짜 유틸 공용화**: `lib/utils/weekday_util.dart`(요일 라벨),
  `lib/utils/shift_name_util.dart`(휴무 감지), `lib/constants/shift_name_limits.dart`
  (근무명 글자수 상수) - 전부 이번 작업 중 새로 뺀 공용 파일입니다.

---

## 3. 새로 추가된 ARB 키 (조사 보고서 용어집에 없던 것들)

조사 보고서(2026-08-14)의 A~O 용어집은 "화면을 눈으로 훑어보고 만든 목록"이라 실제
코드를 전수 조사하며 진행하다 보니 그 문서에 없던 문구가 많이 발견됐습니다(다이얼로그
안내문, 도움말 본문, 개인정보처리방침 전문, 릴리즈노트, 친구공유 메시지 등 - 총
180개 이상). 전부 `app_ko.arb`/`app_en.arb`에 반영 완료했고, 주요 카테고리만 요약하면:

- 설정 탭 도움말 4종 전문(알람 갱신 주기/근무 변경/조 변경/데이터 저장) - 장문
- 개인정보처리방침 전문
- 앱 업데이트 안내 다이얼로그 + 릴리즈노트 본문
- 근무시간 설정 화면의 세부 문구(기본 근무시간 편집, 급여산정일 설명 등)
- 전체 교대조 설정 다이얼로그(3단계 마법사)의 각 스텝 안내문
- 알람 이력 화면의 삭제 확인/토스트 문구
- 친구공유 화면의 에러/성공 메시지, 공유 텍스트(카카오톡 등으로 나가는 메시지)

---

## 4. 사람이 한 번 더 검토하면 좋은 부분 (자동 번역 신뢰도가 낮은 영역)

병렬로 작업한 에이전트들이 공통적으로 표시한 항목입니다:

1. **개인정보처리방침 전문 번역**(`settingsPrivacyPolicyBody`) - 법적 성격의 텍스트라
   실제 배포 전 원어민/법무 검토 권장.
2. **설정 탭 도움말 장문 4종** - 자연스러운 영어 문장으로 번역했지만 실제 화면에서
   줄바꿈/길이가 잘 맞는지 확인 필요(실기기 없어서 육안 확인 못 함).
3. **친구공유 공유 메시지**(`friendShareMessage`, 카카오톡 등 외부 공유 시트로 나가는
   텍스트) - 이모지/줄바꿈이 많아 실제 공유 시트에서 어떻게 보이는지 확인 권장.
4. **`workHoursUnitMinuteLabel`**("minutes") - 좁은 숫자피커 옆 라벨이라 실기기에서
   "min"으로 축약하는 게 나을 수도 있음.
5. **`alarmDurationMinutes`**(알람 지속시간 1/3/5분 선택지) - plural 처리했는데, 다른
   짧은 숫자 라벨들과 스타일이 다를 수 있어 통일성 재검토 여지 있음.

---

## 5. 코드 레벨 검증 결과 (실기기 없이 진행)

- `flutter pub get` - 성공 (intl을 flutter_localizations 요구사항에 맞춰 ^0.19.0 →
  ^0.20.2로 상향, 그 외 기존 의존성 버전은 안 건드림)
- `flutter gen-l10n` - 성공, 375개 키 정상 생성(한/영 1:1 완전 일치, 스크립트로 검증)
- `flutter analyze` (전체 프로젝트) - **최종 에러 0건**.
  - 1차 검증 때 제 grep 스크립트의 들여쓰기 실수(`^   error`로 검색했는데 실제 출력은
    2칸 들여쓰기)로 실제 있던 에러 15건을 놓쳤던 걸, **`flutter build apk --debug`
    실제 빌드**로 다시 검증하다가 잡아냈습니다. 이 15건은 병렬로 작업한 에이전트 중
    하나(calendar_theme_lab_screen.dart 담당)가 세션 한도로 중간에 끊기면서 보고를
    남기지 못한 새 키 8개 + 다른 에이전트가 보고했지만 제가 병합 과정에서 실수로
    누락한 키 5개(all_shifts_view.dart) + `intl`/`material` 패키지 간 `TextDirection`
    이름 충돌 1건(calendar_tab.dart)이었습니다. 전부 원인 파악 후 직접 수정하고
    재검증 완료했습니다(과정 투명하게 남겨둡니다 - 실기기 없이 정적 분석만 믿었다면
    놓칠 뻔한 부분이라, 반드시 실제 빌드까지 돌려야 한다는 근거이기도 합니다).
  - 남은 경고 23건/정보성 린트 625건은 전부 이번 작업과 무관함을 `git diff
    claude/dark-mode-fNhdW`로 대조해서 확인했습니다(예: `database_service.dart`는
    이번 작업에서 아예 건드리지도 않았는데 같은 미사용 import 경고가 이미 있었음,
    `withOpacity`/`surfaceVariant` deprecated 경고는 Flutter SDK 자체의 최신 API
    권장사항).
- `flutter build apk --debug` - **성공**. 첫 실행 시 Flutter 툴이 "APK를 못 찾았다"는
  헷갈리는 메시지를 냈는데, 확인해보니 이 프로젝트가 dev/prod 플레이버로 나뉘어 있어서
  (`bb321d5` 커밋) 실제로는 `build/app/outputs/flutter-apk/app-dev-debug.apk`에
  정상적으로 191MB APK가 만들어져 있었습니다(빌드 도구가 플레이버 미지정 시 기본 경로를
  잘못 찾는 것으로 보이는 Flutter 자체의 알려진 사소한 버그 - 우리 코드 문제 아님).
- `flutter build web -t lib/web_main.dart` - **성공**(`√ Built build\web`). 진짜 앱과
  별도인 웹뷰어 entry point도 정상 컴파일됨을 확인.
- 근무명 색상 로직 추가 발견/수정: `flutter analyze`를 돌리다가 `calendar_theme.dart`의
  `_assignFromPalette`(실제 캘린더가 쓰는 색상 배정 엔진)가 여전히 `contains('휴')`만
  쓰고 있는 걸 발견해서 `isRestShiftName()`으로 교체했습니다(1-7번 참고) - 온보딩에만
  적용되고 실제 캘린더 엔진엔 적용 안 된 상태로 남아있을 뻔한 걸 검증 과정에서 잡음.

---

## 6. 테스트 남은 부분 (실기기 필요, 별도 진행 요망)

이번 요청대로 "테스트는 별도로 분리"했습니다. 실기기 연결 후 아래를 확인해주세요:

1. 기기 언어를 English(US)로 바꾼 뒤 앱 재실행 → 전 화면 영어로 뜨는지
2. 근무명을 영어로 10자 가까이 입력했을 때 달력 셀/전체근무표 뱃지가 안 깨지는지
3. "Off"로 근무명을 지었을 때 고정 빨강색이 정상 적용되는지
4. 영어 로케일에서 달력에 한국 공휴일이 안 뜨는지(반대로 한국어 로케일에서는 여전히
   뜨는지)
5. 온보딩 기본 근무명 제안이 영어로는 Day/Night/Morning/Afternoon/Day Off로 뜨는지
6. 알람 관련 화면(다음알람, 알람 이력) 카운트다운/복수형 표기가 자연스러운지
7. 설정 탭 도움말 4종 + 개인정보처리방침 전문의 줄바꿈/레이아웃
