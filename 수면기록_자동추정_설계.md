# 실제 수면 기록 및 자동 수면 추정 — 아키텍처 설계 (v1, "C번 요구사항")

> 2026-08-31 작성. Android 기술 조사는 `수면기록_자동추정_조사.md` 참고 - 이 문서는
> 그 결론을 전제로 한 실제 설계/구현 내용. 컨디션 매니저(B번, `컨디션매니저_설계.md`)
> 위에 얹는 **완전히 새로운 기능**이며, 기존 알람/근무패턴/캘린더 위젯 로직은 이번에도
> 0줄 수정한다(9장에서 실제로 나열해 확인).

---

## 0. 핵심 우선순위 (스펙 25장 그대로)

1. 사용자가 직접 기록한 수면(위젯 수동 토글) — 최우선, 자동 추정이 절대 덮어쓰지 않음
2. 근무 일정 + 사용자가 입력한 "근무별 평균 취침시각" + 휴대폰 장기 미사용 기반 자동 추정
3. 자동 추정 결과는 반드시 사용자 확인(맞아요/수정/기록하지 않기)을 거쳐야 함
4. **근무 중 수면도 정상 데이터** — validation으로 막지 않고, 분석 단계에서 "근무 중
   수면"으로 구분만 함

## 1. 데이터 모델

### 1-1. `SleepRecord` (실제 수면 1건)

```dart
class SleepRecord {
  final int? id;
  final DateTime start;
  final DateTime? end;      // null = 진행 중(수동 '수면 중' 상태 또는 자동 감지 진행 중)
  final SleepSource source; // manual | autoDetected
  final SleepStatus status; // confirmed | pendingConfirmation
  final SleepConfidence? confidence; // autoDetected만 사용, low|medium|high
}
```

- `end == null`은 "현재 진행 중"을 뜻하는 공용 상태 - MANUAL(위젯에서 취침만 누르고
  아직 기상 안 누름)과 AUTO_DETECTED(아직 화면 켜짐이 감지 안 돼 후보가 계속 연장
  중)가 같은 컬럼으로 표현됨.
- **분류 라벨(MAIN_SLEEP/NAP/WORK_SHIFT_SLEEP)은 DB에 저장하지 않는다.** 근무
  스케줄이 나중에 바뀌면 과거 수면 기록의 "근무 중 수면 여부" 판정도 같이 바뀌어야
  자연스러운데, 저장된 라벨은 그 변화를 못 따라감 - 그래서 스펙 6장이 말한 분류는
  **매번 조회 시점에 `ShiftPatternAnalyzer`와 대조해서 계산하는 파생값**으로 만든다
  (`sleep_shift_relation.dart`, 5장 참고). 기존 프로젝트 관례(work_hours_calculator.dart가
  근로시간 통계를 저장하지 않고 매번 계산하는 방식)와 동일한 원칙.

### 1-2. `sleep_expected_bedtime` (★ 가장 중요하게 강조된 요구사항)

사용자가 "근무별 평균적으로 실제 잠드는 시각"을 입력하면, 그 값이 자동 추정 트리거의
**1순위 기준**이 된다(근무 일정 기반 추정은 이 값이 없을 때만 쓰는 2순위 폴백).

```sql
CREATE TABLE IF NOT EXISTS sleep_expected_bedtime(
  shift_key TEXT PRIMARY KEY,   -- 실제 근무명, 또는 휴무를 뜻하는 상수 kOffDaySleepKey
  bedtime_minutes INTEGER NOT NULL,  -- 자정 기준 분
  updated_at TEXT NOT NULL
)
```

- `condition_shift_times`(B번에서 만든 "근무별 출퇴근 시각")와 **완전히 다른
  테이블** - 이건 "몇 시에 퇴근하는가"가 아니라 "그 근무를 한 날 보통 몇 시에
  잠자리에 드는가"라는, 실제로 이 기능을 위해 새로 받아야만 하는 정보.
- UI는 컨디션 탭의 근무시간 입력 섹션과 동일한 패턴(근무명별 시각 1개씩 입력하는
  리스트)을 그대로 재사용해서 사용자가 낯설지 않게 함.

### 1-3. DB 버전: v21 → v22

두 테이블 다 완전히 새 테이블 - 기존 테이블 0개 변경. `database_service.dart`의
`_onCreate`/`_onUpgrade`, `DatabaseHelper.kt`의 `DATABASE_VERSION`을 같이 올린다
(DB_스키마_변경_가이드.md 절차 그대로 - Kotlin은 이 테이블들을 직접 스키마
생성하지 않지만, 위젯/감지 코드가 **읽고 쓰기는 하므로** 버전 동기화는 반드시 필요).

## 2. 근무시간과 겹치는 수면 — 절대 막지 않는다 (스펙 5장)

`SleepRecord`를 저장하는 어떤 경로(위젯 토글, 자동 감지, 앱에서 수동 추가/수정)에도
"근무 시간과 겹치면 거부" 같은 validation을 넣지 않았다. 대신:

- 저장은 항상 그대로 허용.
- **읽을 때** `sleep_shift_relation.dart`가 그 수면 구간과 `ShiftPatternAnalyzer`가
  계산한 근무 인스턴스들을 대조해서 겹치는 시간(분)을 계산하고, 아래 3단계로 분류한다:

| 분류 | 조건 |
|---|---|
| `workShiftOverlap` | 근무 인스턴스와 1분이라도 겹침 |
| `mainSleep` | 겹치지 않고, 길이가 3시간 이상 |
| `nap` | 겹치지 않고, 길이가 3시간 미만 |

3시간 기준은 근거자료에서 온 값이 아니라 **순수 라벨링 편의상의 임계값**이다(코드
주석에 명시) - 컨디션매니저_근거자료.md의 Evidence와는 무관하고, 위험 판정에도
전혀 안 쓰인다.

## 3. Sleep Opportunity Detection (스펙 9장) — 우선순위 로직

`lib/services/condition/sleep_opportunity.dart` (Dart, 참조 구현) /
`SleepScheduleResolver.kt`(Kotlin, 백그라운드 실제 실행) 양쪽에 **동일한 알고리즘**을
유지해야 한다(알람 생성 로직의 Dart/Kotlin 이중구현과 동일한 선례 - 조사 문서 3장).

```
1순위: sleep_expected_bedtime에 오늘 관련 근무(또는 휴무)의 값이 있는가?
  있으면 → 창 = [bedtime - 30분, bedtime + 9시간], 단 다음 근무 시작 30분 전을 넘지 않음

2순위(폴백): ShiftPatternAnalyzer.recoveryWindowContaining(now)로 계산되는
  "회복구간"(컨디션 매니저 B번에서 이미 만든 로직 재사용) 전체를 창으로 사용

둘 다 없음(스케줄 미설정 등) → 오늘은 감지 안 함
```

- "지금이 이 창 안에 있는가"만 판정하고, 생체리듬 모델 같은 건 만들지 않는다(스펙
  9장 지시대로).

## 4. 자동 감지 파이프라인 (Kotlin, 신규 파일만)

`수면기록_자동추정_조사.md` 3장 다이어그램 그대로. 요약:

- `SleepDetectionScheduler.ensureScheduled(context)` — 지금이 창 안이면 20분 뒤 샘플링
  알람을, 창 밖이면 다음 창 시작(또는 최소 자정)에 깨어나는 알람을 하나만 예약(항상
  기존 예약을 취소하고 다시 거는 idempotent 방식).
- `SleepDetectionReceiver` — 그 알람을 받아서:
  1. MANUAL 진행 중인 레코드가 있으면 **아무 것도 안 하고 스킵** (수동이 항상 우선)
  2. `PowerManager.isInteractive()` 스냅샷 - 연속 2회 "꺼짐"이 확인되면 그때 비로소
     `sleep_records`에 `AUTO_DETECTED`/`PENDING_CONFIRMATION`/`end=null` 행을 생성
  3. 다음 샘플에서 "켜짐"이 확인되면 그 행의 `end`를 채우고 신뢰도 계산(조사 문서 4장)
  4. 끝에 `ensureScheduled()`를 다시 호출해 스스로 다음 깨어남을 예약

트리거 지점(전부 기존 파일에 한 줄 추가, 로직 자체는 새 파일):

| 위치 | 추가한 것 |
|---|---|
| `MainActivity.onResume()` | `SleepDetectionScheduler.ensureScheduled(this)` 한 줄(기존 `AlarmGuardReceiver.triggerCheck` 옆) |
| `AlarmGuardReceiver.triggerCheck()` | 같은 한 줄(기존 위젯 갱신 호출 옆에 나란히) |
| `DirectBootReceiver.onReceive()` | 같은 한 줄(재부팅 시 알람이 전부 사라지므로 필수) |

## 5. 컨디션 매니저와의 연결 — 완전 분리 (스펙의 "Claude에게 추가로 강조할 것")

```
Sleep Detection (Kotlin, 신규)
        ↓ (DB에 sleep_records만 씀)
Sleep Records (신규 테이블)
        ↓ (읽기 전용)
sleep_shift_relation.dart (신규, 분류만)
        ↓
sleep_condition_extras.dart (신규) ── ConditionRuleEngine(B번, 0줄 수정)과 나란히 존재
        ↓                                      ↓
   "실제 수면 기반 부가 설명"              "일정 기반 판정"(기존 그대로)
        └──────────────┬───────────────────────┘
                        ▼
                  condition_tab.dart(화면에서 둘을 나란히 보여줌)
```

`ConditionRuleEngine.evaluate()`는 이번 작업으로 **단 한 줄도 바뀌지 않는다.**
대신 `sleep_condition_extras.dart`가 별도로 실제 수면 기록을 분석해서(예: "최근 3일
야간근무 후 평균 수면시간") 화면의 별도 섹션에 얹힌다. 이렇게 분리해두면 나중에
감지 알고리즘이 통째로 바뀌어도(예: UsageStatsManager 방식으로 교체) 판정 엔진
코드는 전혀 안 건드려도 된다 - 스펙이 명시적으로 요청한 구조.

## 6. 위젯 (기존 캘린더 위젯과 완전 별개)

- `SleepWidgetProvider.kt`(신규) — 최소 크기(약 2×1 셀), 표시 내용 2줄만:
  1. 오늘 근무명 + 지금 근무 중인지 여부(가벼운 텍스트, `CalendarWidgetScheduleResolver`
     읽기 전용 재사용)
  2. 토글 버튼 1개 - 상태 3가지(평상시 😴 취침 / 진행 중 💤 수면 중 N시간 M분 /
     기상 직후 ☀️ 오늘 수면 N시간 M분)
- `SleepWidgetActionReceiver.kt`(신규) — 버튼 탭 브로드캐스트 처리: 진행 중인 MANUAL
  레코드가 없으면 새로 시작(insert), 있으면 종료(update) - **validation 없이 항상
  허용**(2장 원칙과 동일선상 - 위젯에서 근무 중에 눌러도 그대로 기록됨).
- 앱을 열지 않고 위젯 탭 한 번으로 끝나야 하므로(스펙 2~3장), 버튼 탭은 항상 그
  자리에서 즉시 처리하고 위젯 자체를 다시 그림(`RemoteViews` 갱신) - Activity를
  띄우지 않음.
- 기존 `calendar_widget_info.xml`/`CalendarWidgetProvider`는 **한 글자도 수정하지
  않음** - 완전히 새 리소스(`sleep_widget_info.xml`, `sleep_widget.xml`)와 새
  매니페스트 `<receiver>` 2개(Provider + ActionReceiver)만 추가.

## 7. UI (Flutter, `condition_tab.dart` 확장)

새 섹션 3개를 기존 컨디션 탭에 추가(기존 섹션 순서/내용은 그대로 두고 아래로 이어붙임):

1. **"근무별 평균 취침 시각"** — B번의 `_ShiftTimeSetupSection`과 동일한 UI 패턴
   (근무명별 TimePicker 1개 + 휴무 항목 1개), `sleep_expected_bedtime` CRUD
2. **자동 감지 확인 카드** — `PENDING_CONFIRMATION` 레코드가 있으면 최상단에 노출.
   "OO:OO ~ OO:OO, 약 N시간 M분 수면한 것으로 추정됩니다" + [맞아요]/[수정]/[기록하지
   않기]. "추정"이라는 단어를 반드시 씀(스펙 18장 - "측정"이 아니라 "추정").
3. **최근 수면 기록 리스트** — 최근 N일의 `SleepRecord`(수동+확정된 자동)를 나열,
   `workShiftOverlap`이면 "근무 중 수면" 배지 표시. 수동으로 새 기록(주로 낮잠)을
   추가하는 버튼도 여기에 둠.

## 8. 표현 원칙 (스펙 18장)

- `AUTO_DETECTED` + `PENDING_CONFIRMATION`: 항상 "수면 **추정**"
- `CONFIRMED`(사용자가 확인했거나 원래 `MANUAL`): "실제 수면" 또는 그냥 "수면"
- 절대 "정확한 수면시간"이라는 표현을 쓰지 않음(자동 감지 결과에 한해).

## 9. 기존 코드 영향 범위 (전부 나열)

**수정한 기존 파일 (모두 additive 한두 줄, 기존 로직 변경 없음):**

| 파일 | 변경 |
|---|---|
| `lib/services/database_service.dart` | `version: 21`→`22`, `_onCreate`/`_onUpgrade`에 두 테이블 추가, CRUD 메서드 추가 |
| `android/.../DatabaseHelper.kt` | `DATABASE_VERSION = 21`→`22` (한 줄) |
| `android/.../MainActivity.kt` | `onResume()`에 한 줄, `when(call.method)`에 `refreshSleepWidget` 케이스 1개 추가 |
| `android/.../AlarmGuardReceiver.kt` | `triggerCheck()`에 한 줄 추가 |
| `android/.../DirectBootReceiver.kt` | `onReceive()`에 한 줄 추가 |
| `android/app/src/main/AndroidManifest.xml` | `<receiver>` 3개 추가(SleepWidgetProvider/SleepWidgetActionReceiver/SleepDetectionReceiver) - 기존 항목 변경 없음 |
| `lib/screens/condition_tab.dart` | 새 섹션 3개 추가(기존 섹션 아래로 이어붙임, 기존 위젯 트리 변경 없음) |
| `CLAUDE.md` / `DB_스키마_변경_가이드.md` | 문서 갱신(v22 기록) |

**절대 건드리지 않은 것:**

- 알람 크리티컬 로직 전체(`AlarmRefreshEngine.kt`, `CustomAlarmReceiver.kt`,
  `AlarmActivity.kt`, `AlarmOverlayService.kt`, `AlarmPlayer.kt`,
  `NotificationHelper.kt`, `alarm_generation_service.dart`,
  `alarm_refresh_service.dart`, `alarm_service.dart`)
- `CalendarWidgetProvider.kt`/`CalendarWidgetScheduleResolver.kt`/
  `CalendarWidgetHolidays.kt`(캘린더 위젯 전체) - 읽기 전용으로 **호출**만 하고 수정 X
- `ConditionRuleEngine`/`ShiftPatternAnalyzer`/`evidence_database.dart`(B번 전체) - 5장 참고
- `backup_payload.dart`/`backup_service.dart` - 10장 참고(역시 수정 불필요 확인)

## 10. 백업 레이어 재확인

`sleep_records`/`sleep_expected_bedtime` 둘 다 **원본 데이터**(자동 감지 결과도
사용자가 확인을 거쳐야 확정되는 데이터라 원본으로 취급) - `BackupService.exportAll()`이
`sqlite_master`를 스캔하는 기존 설계 그대로 코드 변경 없이 자동 포함된다
(백업복구_설계.md 3장/7장과 동일 결론, 컨디션매니저_설계.md 8장에서 이미 한 번
검증한 것과 같은 패턴).

## 12. (2026-09-01 변경) "근무별 평균 취침 시각" 입력 기능 삭제

1-2장에서 "★가장 중요하게 강조된 요구사항"이라고 적었던 `sleep_expected_bedtime`
1순위 앵커 로직을 사용자 요청으로 완전히 걷어냈다. 이유: 2순위 폴백(회복구간 -
근무 종료~다음 근무 시작)만으로도 "근무 중엔 감지 안 함, 퇴근 이후만 트리거"
요구를 이미 만족하고, 별도 입력 없이 자동으로 그렇게 동작한다는 걸 재확인했기
때문(사용자 입력 부담을 줄이는 쪽을 선택).

- 삭제한 것: `condition_tab.dart`의 "근무별 평균 취침 시각" 섹션(UI),
  `sleep_expected_bedtime_provider.dart`(파일 자체 삭제),
  `database_service.dart`의 관련 CRUD 3개, `sleep_opportunity.dart`/
  `SleepScheduleResolver.kt`의 1순위 앵커 계산 로직, `SleepDetectionReceiver.kt`의
  "anchored" 신뢰도 가산점 규칙(더 이상 발생할 수 없는 조건이라 같이 삭제).
- **남겨둔 것**: `sleep_expected_bedtime` 테이블 스키마 자체(DB v22 그대로, 버전
  안 올림) - 빈 테이블로 방치. 정식으로 드롭하려면 DB_스키마_변경_가이드.md
  절차대로 버전을 올려야 하는데, 이 정도 죽은 테이블 하나 남기는 리스크가 그
  마이그레이션 작업보다 작다고 판단해 보류. `kOffDaySleepKey`/
  `SleepScheduleResolver.OFF_DAY_KEY` 상수는 더 이상 쓰는 곳이 없어 같이 삭제함.
- 자동 감지 신뢰도(LOW/MEDIUM/HIGH) 계산은 이제 "충전 중이었는가"와 "구간 길이가
  90분 미만인가" 두 규칙만 남음(4장 표에서 "anchored" 행 제거로 갱신 필요).

### 12-1. 샘플링 주기(20분) 관련 논의 - 유지로 결론

"20분씩 2번 연속 대신 더 길게(예: 2시간씩 2회) 잡아도 되지 않냐"는 제안이
있었으나, 아래 이유로 20분을 유지하기로 함(변경 안 함):

- `SleepDetectionReceiver.kt`는 **연속 2회 확인된 시점에 후보를 만들되, 실제
  저장하는 `start_time`은 그 중 "첫 번째" 오프 샘플 시각으로 소급**한다(코드
  참고). 즉 기록되는 시작/종료 시각의 오차 상한은 정확히 "샘플링 간격"이다 -
  20분이면 최악의 경우 실제 취침/기상 시각과 최대 ±20분 차이. 이걸 2시간으로
  늘리면 오차 상한도 그대로 ±2시간까지 커진다 - "시작/끝만 제대로 캐치하면
  된다"는 목표와 정반대로 그 캐치 자체의 정확도가 나빠짐.
- "연속 2회"라는 확인 방식 자체도 대가가 있다: 후보가 실제로 **생성되기까지**
  샘플링 간격의 2배가 걸린다(20분 방식은 40분, 2시간 방식은 4시간). 짧은 낮잠
  (조사 문서/설계 6장에서 이미 "낮잠"을 분류 대상으로 명시)은 대부분 이 최소
  4시간 문턱을 못 넘어 아예 감지되지 않게 됨 - 기능 목표와 정면으로 충돌.
- 배터리 관점에서도 20분은 이미 "유의미한 추가 소모 없음"으로 결론 낸 값(조사
  문서 5장) - Doze 심화 단계에서 시스템이 알람 빈도를 앱당 15분에 1회로 이미
  제한하므로, 20분보다 더 촘촘히 해도 실익이 없고, 더 길게 늘려도 절약폭이
  미미함(반면 정확도 손실은 위와 같이 큼). 즉 배터리를 아끼자고 늘릴 이유가
  없는 상황에서 정확도만 잃는 트레이드오프라 유지 쪽으로 결론.

## 11. 1차 MVP에서 실제로 구현하는 것 / 다음 단계

**이번에 구현(스펙 21장 범위 그대로):**
① 위젯 원터치 취침/기상 ② SleepRecord 모델+DB ③ 근무 일정과의 연결(겹침 분류)
④ 수면 가능 시간 분석(우선순위 포함) ⑤ 장기 미사용 자동 감지(20분 샘플링) ⑥ 사용자
확인 플로우

**다음 단계(스펙 22장 제외 목록은 계속 제외, 그 외 보류 항목):**
- 월간/주간 "야간근무 후 평균 수면" 등 통계 리포트의 전면적인 화면 통합(계산 함수는
  만들되 전용 리포트 화면은 보류)
- `UsageStatsManager` 고급 정확도 모드(조사 문서 3장 ③)
- 실기기에서 v21→v22 업그레이드 경로 + 위젯 실제 배치 확인(다른 미검증 항목들과
  동일한 상태)

## 13. (2026-09-01 후속) 실사용 피드백 반영 - 즉시판정 + 위젯 재설계 + 미니달력/전체보기

1차 MVP를 실제로 며칠 써본 뒤 나온 구체적 버그/UX 피드백을 반영. **DB 스키마는
전혀 안 건드림**(v22 그대로) - 전부 표시 레이어와 트리거 타이밍 수정.

### 13-1. 버그: 기상 후 앱을 열어도 한참 "OO시부터 수면 중"으로 남아있던 문제

**원인**: `SleepDetectionScheduler.ensureScheduled()`는 "다음 20분 샘플링 알람을
언제로 잡을지"만 계산할 뿐, 그 자리에서 즉시 판정을 돌리지 않는다. 그래서
`MainActivity.onResume()`/`AlarmGuardReceiver.triggerCheck()`가 이 함수만 불러온
지금까지는, 화면이 켜져서 앱을 여는 그 순간이 곧 "PowerManager.isInteractive() ==
true"인데도 그걸 그 자리에서 확인하지 않고 다음 20분 알람이 울릴 때까지 그냥
기다렸다 - 최악의 경우 20분간 진행 중(`end_time IS NULL`) 상태로 남아
`_PendingSleepConfirmationCard`가 "OO시부터 수면 중인 것으로 추정됩니다"만 보여줌.

**수정**: `SleepDetectionReceiver.handle()`(private)을 그대로 두고, 그 위에
`SleepDetectionReceiver.checkNow(context)`(companion, public)를 새로 얹어서
"즉시 판정 + 그 안에서 다시 ensureScheduled() 재예약"을 한 번에 하게 함.
`MainActivity.onResume()`/`AlarmGuardReceiver.triggerCheck()`/
`DirectBootReceiver.onReceive()` 세 트리거 지점 전부 `ensureScheduled()` 호출을
`checkNow()`로 교체 - 앱을 여는 순간 진행 중이던 자동 감지 후보가 즉시
종료(`end_time = now`)되고, 카드도 곧바로 "OO시~OO시, 약 N시간 수면한 것으로
추정됩니다" + [맞아요]/[수정]/[기록하지 않기]로 바뀐다. "수정"을 누르면 취침/기상
시각을 직접 다시 입력할 수 있음(기존 기능 그대로, 새로 만든 것 없음).

### 13-2. 위젯 재설계 - 토글 1개 → "오늘/상태" 두 줄 + 명시적 버튼 2개

**피드백**: "오늘: 야간"이라고만 뜨고 토글 버튼 하나로 취침/기상을 겸하니 지금
상태가 뭔지 헷갈림.

**변경**: `sleep_widget.xml`을 `오늘: OO`(근무명) / `상태: OO`(활동 중 / 수면 중
N시간 M분 / 오늘 수면 N시간 M분) 두 줄 + "🌙 수면"/"☀️ 기상" 버튼 2개로 재설계.
`SleepWidgetActionReceiver`도 `ACTION_TOGGLE` 하나 대신 `ACTION_SLEEP`/`ACTION_WAKE`
두 액션으로 나눠 각각 명시적으로만 동작(상태와 안 맞는 액션은 조용히 무시 -
예: 이미 수면 중인데 "수면"을 또 누르면 무시). 지금 의미 있는 버튼은 강조색
(`sleep_widget_button_bg.xml`), 반대쪽은 회색(`sleep_widget_button_bg_inactive.xml`
신규)으로 매 갱신마다 다시 칠해서 "지금 뭘 눌러야 하는지"를 항상 보여줌. 위젯
최소 크기도 2x1(110x40dp) → 3x2(180x110dp)로 키움(`sleep_widget_info.xml`).

### 13-3. "최근 수면 기록" 카드 재설계 - 리스트 → 가로 스크롤 미니 달력

**피드백**: 리스트형이 "진행중 - 자동확정"만 계속 나열되고, 확정/수정 동선도
없고, 상단 "+추가" 버튼의 흐름도 어색함.

**변경**: `sleep_day_slots.dart`(신규, 순수 함수) - `status=confirmed`인
`SleepRecord`를 **취침을 시작한 날짜**로 묶어서 하루당 최대 2개(시작 시각
오름차순)를 "슬롯①/②"로 뽑아낸다. ⚠️ DB에 슬롯 컬럼을 추가한 게 아니라 순수
표시/편집 편의 개념 - 이 화면들을 통해서만 기록이 생성되므로 하루 2건 이하로
자연히 맞춰지고, 컨디션 예측(today_forecast_engine.dart 등)은 이 개념을 전혀
모른 채 원본 레코드를 그대로 합산하므로 "두 슬롯의 합"과 실제 합계가 사실상
같아진다(완전 분리 원칙 유지 - 5장 참고).

`condition_tab.dart`의 `_SleepMiniCalendarCard` - 하루 1칸(4줄: 날짜+요일 /
근무명 / 슬롯① / 슬롯②)을 가로로 스크롤. **오늘까지만 렌더링, 미래는 없음.**
아래 두 슬롯을 탭하면 그 자리에서 취침/기상 시각을 입력(빈 슬롯) 또는
수정·삭제(채워진 슬롯)할 수 있음 - `lib/widgets/sleep_edit_dialog.dart`(신규)의
`showSleepSlotEditDialog`/`handleSleepSlotTap`이 미니 달력 카드와 전체보기 화면
양쪽에서 공유하는 공용 다이얼로그/액션 처리기.

### 13-4. "전체보기" 화면 신설 - 월 단위로 과거 기록 전부 훑어보기

가로 스크롤 카드로는 한 번에 3일 정도만 보이는 한계를 보완. `lib/screens/
sleep_calendar_full_screen.dart`(신규) - 상단에서 이전달/다음달(미래 달로는
못 넘어감) 이동. 처음엔 리스트형/격자형 두 레이아웃을 토글로 비교해볼 수
있게 만들었었는데, **사용자가 리스트형으로 확정**해서 격자형/토글/전용
바텀시트 상세화면은 전부 삭제함(13-6 참고) - 하루 1행(요일+근무명, 슬롯
3칸을 나란히)만 남음. 이 화면은 `sleepRecordProvider`(최근 60일 한정, 8주
컨디션 예측용)와 별개로 `DatabaseService.instance.getSleepRecords()`(기간 제한
없음)를 직접 읽어서 "저저번달"까지도 정상적으로 조회된다.

### 13-5. 다음에 확인할 것

- **실기기 검증 전무** - 이번 세션도 코드 트레이싱 + Flutter/Kotlin 컴파일
  성공까지만 확인. 특히 위젯 재배치(기존에 홈 화면에 추가해둔 위젯이 새
  레이아웃/크기로 잘 갱신되는지), `checkNow()`가 실제로 자정 넘긴 야간
  기록에서도 기대대로 즉시 종료되는지는 실기기에서 봐야 함.
- 하루에 우연히 같은 분류(주 수면/근무 중 낮잠/근무 외 낮잠)가 2건 이상
  쌓이는 경우(레거시 데이터, 위젯 연타 등) 미니 달력/전체보기에는 그중
  시작 시각이 이른 것만 보임(13-3 한계) - 실제로 자주 문제가 되면 그때
  "칸을 더 늘리기" 또는 "합쳐서 표시" 등을 검토.

### 13-6. (2026-09-01 후속2) 위젯 스마트 연동 + 3칸 분류 + 연타 방지 + 리스트형 확정

**위젯이 자동 감지도 반영하게 함**: 지금까지 위젯은 MANUAL(수동) 기록만 보고
"수면 중"을 표시했다("지금 실시간으로 벌어지는 일은 수동 토글만이 대변한다"는
원래 원칙). 사용자 요청으로 이걸 바꿔서, `SleepWidgetProvider.SleepingState`가
MANUAL/AUTO_DETECTED 진행 중 여부를 합쳐서 계산 - 자동 감지가 취침을 인식하면
사용자가 버튼을 한 번도 안 눌러도 위젯에 "상태: 수면 중으로 추정 N시간"이
뜬다("추정"이라는 단어는 8장 표현 원칙을 그대로 따름 - MANUAL은 "수면 중",
AUTO는 "수면 중으로 추정"으로 구분).

이때 "수면"/"기상" 버튼이 지금 상태와 안 맞을 때의 동작을 다시 정리함:

| 상황 | 버튼 | 동작 |
|---|---|---|
| AUTO 진행 중일 때 "수면"을 누름 | 무시(no-op) | 자동 감지가 잡아둔 더 정확한 취침 시작 시각(최대 ±20분 오차, 첫 꺼짐 샘플로 소급됨)을 "지금 누른 시각"으로 새로 덮어쓰면 오히려 부정확해지므로 |
| AUTO 진행 중일 때 "기상"을 누름 | **그 AUTO 후보를 그대로 CONFIRMED로 확정**하고 종료 | 앱을 열어 확인 카드에서 "맞아요"를 누르는 것과 사실상 동일한 효과를 위젯에서 바로 줌 - 앱을 열지 않아도 끝남 |
| MANUAL 진행 중일 때 "수면"을 다시 누름 | 무시(no-op) | 기존 동작 그대로(이중 시작 방지) |

**Claude의 판단(사용자가 의견을 요청함)**: "AUTO 진행 중에 수면을 누르면 무시"
쪽으로 정함 - 위젯 버튼이 더 명확하다고 덮어쓰게 하면 자동 감지가 소급해둔
정확한 시작 시각이 사라지는 게 더 큰 손해라고 판단함. 대신 이 경우 자동 감지가
잘못 판단했으면(예: 낮에 화면이 오래 꺼져 있었을 뿐인 오탐) 위젯에서 바로 고칠
방법이 없고, 앱을 열어 확인 카드의 "기록하지 않기"를 눌러야 함 - 실사용에서
이게 불편하면 다음에 "무시하지 말고 대체" 옵션도 고려 가능.

**연타/실수 탭 방지**: `SleepWidgetActionReceiver`에 "2분 미만인 수면은 저장하지
않고 버린다"는 규칙을 추가(`MIN_MEANINGFUL_MINUTES`, 근거자료 값 아님 - 순수
UX 예외처리). 위 no-op 규칙들과 합쳐서 "위젯을 반복해서 누르거나 5분마다
누르는" 시나리오에서 쓰레기 기록이 쌓이지 않게 함.

**"최근 수면 기록" 칸을 2개 → 3개로, "몇 번째" 대신 "성격"으로 분류**:
`sleep_day_slots.dart`의 `SleepSlotCategory`(main/workNap/offNap) - 하루 중
같은 날짜에 시작한 확정 기록을 (1) **주 수면**(4시간 이상, `kMainSleepSlotThresholdMinutes`)
(2) **근무 중 낮잠**(4시간 미만 + 근무 인스턴스와 겹침) (3) **근무 외 낮잠**
(4시간 미만 + 안 겹침) 세 칸으로 자동 분류한다(진행 중이라 길이를 모르면 취침
시작 시각이 밤 8시~새벽 6시대인지로 잠정 분류 후, 기상해서 길이가 확정되면
자동 재계산됨 - 저장은 그대로 두고 매번 다시 계산하는 파생 뷰이므로). 위젯/
자동감지로 들어오는 기록도 이 분류를 그대로 타므로 "몇 번째로 눌렀나"가 아니라
"어떤 성격의 수면인가"로 칸이 정해진다. 빈 칸을 탭했을 때의 기본 시각도 칸마다
다름 - 근무 중 낮잠은 그날 근무 시간대의 중간 지점(근무시간 미설정 시 13:00)을
기본값으로 잡아 조금 더 그럴듯한 시작점을 준다(`defaultSleepSlotTimes`).

**전체보기 화면 - 리스트형으로 확정**: 13-4에서 만들어뒀던 격자형/토글/바텀시트
상세는 전부 삭제. 리스트형만 남기고 3칸 분류에 맞춰 갱신.

**다이얼로그 UI 개선**: `AlertDialog` + `ListTile` 2개짜리였던 옛 UI("후지다"는
피드백)를 `showModalBottomSheet` 기반으로 다시 그림 - 취침/기상을 카드 2개로
나란히 보여주고 그 사이 화살표, 아래에 실시간 계산된 수면시간(또는 "기상 시각이
취침보다 빨라요" 경고)을 보여줌. 반환 타입/호출 계약(`SleepSlotEditResult`)은
그대로라 호출부 변경 없음.

### 13-7. (2026-09-01 후속3) 자동 감지 창을 "메인 잠 전용"으로 대폭 축소

**문제의식**: 기존 자동 감지 창("근무 종료 ~ 다음 근무 시작" 전체)은 근무 중
쪽잠은 애초에 안 잡히지만(근무시간 자체가 창에서 빠지므로), 그 외에는 범위가
너무 넓었음(연휴처럼 다음 근무가 며칠 뒤면 그 사이 내내 20분마다 계속 깨어나
샘플링). 사용자 요청으로 "자동인식은 메인 잠 트래킹만, 폭을 최대한 줄이자"는
방향으로 재설계.

**새 규칙**(`sleep_opportunity.dart`/`SleepScheduleResolver.kt` 양쪽 동일):

1. **야간 근무**(시작 시각 17:00~04:59) - 근무 **종료 1시간 전**부터 창 시작
   (`NIGHT_SHIFT_PRE_END_MINUTES = 60`). 근무 중엔 트래킹 안 하되, 칼퇴 오차나
   막판에 조는 경우를 대비해 1시간 여유를 둠. 예: 19~07시 근무면 06시부터.
2. **그 외(주간/오후 근무, 휴무일)** - 근무 종료 시각과 무관하게 **21시 이후**
   부터만 창을 연다(`FLAT_SLEEP_START_HOUR = 21`). 늦게 끝나는 근무(예: 23시
   퇴근)면 21시가 이미 지났으므로 실질적으로 근무 종료 직후부터 시작되는 셈 -
   별도 분기 없이 "두 값 중 나중"이 자연히 적용됨.
3. 두 경우 다 창의 최대 길이는 **9시간**(`WINDOW_MAX_HOURS`, 다음 근무 시작이
   더 이르면 그걸로 컷)로 캡을 씌움 - 예전처럼 다음 근무가 며칠 뒤라서 그 사이
   내내 계속 깨어나 샘플링하는 낭비를 없앰. (샘플링 스케줄만 멈출 뿐, 이미
   진행 중인 후보를 끝내는 건 여전히 `checkNow()`가 트리거 시점마다 처리하므로
   9시간 넘게 자도 최종 기록 자체는 정확함 - 13-1 참고)
4. **근무 중 짧은 낮잠/쪽잠은 이 창에 절대 포함되지 않는다** - 자동 감지는
   메인 잠 전용이고, 그런 수면은 위젯 수동 기록으로만 남긴다.

**날짜 경계 확인**(사용자가 명시적으로 요청한 검증): 20일 주간·21일 야간인
경우 - 20일 밤은 21시 플랫 규칙으로 정상 추적(다음날 21일의 야간 근무 시작
19시보다 훨씬 이르므로 겹치지 않음). 21일 야간(19시 21일~07시 22일)은 22일
06시(=07시-1시간)부터 최대 9시간(22일 15시까지, 다음 근무가 더 안 이르면)
추적 - "22일 아침에 잔다"는 사용자 설명과 일치.

**위젯이 자동 감지도 반영하게 함**: `SleepWidgetProvider.SleepingState`가
MANUAL/AUTO_DETECTED 진행 중 여부를 합쳐서 계산 - 자동 감지가 취침을 인식하면
사용자가 버튼을 한 번도 안 눌러도 위젯에 뜬다. AUTO 진행 중엔 "수면" 버튼은
무시(정확한 소급 시작시각을 덮어쓰지 않기 위함), "기상"을 누르면 그 자리에서
CONFIRMED로 확정(앱의 "맞아요"와 동일 효과) - Claude 판단으로 결정(사용자가
의견을 요청함): 위젯에서 덮어쓰게 허용하면 자동 감지가 소급해둔 더 정확한
시작 시각이 사라지는 손해가 더 크다고 판단. 대신 자동 감지가 오탐이면(예:
낮에 화면이 오래 꺼져있었을 뿐) 위젯에서 바로 못 고치고 앱을 열어 확인 카드의
"기록하지 않기"를 눌러야 하는 게 남은 트레이드오프.

**연타/실수 탭 방지**: `SleepWidgetActionReceiver`에 "2분 미만 수면은 저장하지
않고 버린다"(`MIN_MEANINGFUL_MINUTES`, 순수 UX 값)는 규칙 추가.

**위젯 문구/스타일 조정**: "오늘: OO"/"상태: OO" → "오늘 근무 : OO"/"상태 : OO"로
띄어쓰기 통일, 글씨 크기 소폭 확대(13→14sp/12→13sp), 상태 줄도 굵게. 수면 중
표시는 "수면중 (N시간 M분)"(수동)/"수면중(추정) (N시간 M분)"(자동)으로 - 시간은
항상 괄호로 따로 붙임.

**"기본 근무시간(개인 평소 근무 길이)"을 컨디션 예측에 반영**: 상세는
컨디션매니저_설계.md 14장 참고 - `condition_rule_engine.dart`의 `RULE_LONG_SHIFT`
(12시간 이상, EVIDENCE-004)는 실제 연구 근거값이라 절대 안 건드리고, "오늘의
컨디션 예측"(`today_forecast_engine.dart`) 문구 레이어에만 "이 사람 평소
근무시간 대비 오늘/어제가 긴가"를 추가함.

### 13-8. (2026-09-01 후속4) 실기기에서 발견된 오탐 버그 - 창 게이팅 누락

**실제 증상**(실기기, 야간 근무일 아침 09:15~09:20): "09:15~09:20, 약 5분
수면한 것으로 추정됩니다"라는 확인 카드가 뜸 - 명백히 잘못된 감지.

**원인**: 13-1에서 `SleepDetectionReceiver.checkNow()`를
`MainActivity.onResume()`/`AlarmGuardReceiver.triggerCheck()`에 연결해 "화면이
켜지는 순간 즉시 판정"하게 만들었는데, 이 트리거들은 **하루 종일 수시로**
발생한다(앱을 열 때마다, 어떤 알람이든 울릴 때마다). `handle()` 내부는 원래
`SleepScheduleResolver.computeWindowForNow()`(창)를 전혀 참조하지 않고
`PowerManager.isInteractive()` 전환만으로 후보를 만들었다 - 20분 주기
알람으로만 실행되던 시절엔 그 알람 자체가 창 안에서만 예약됐으니 문제가
없었는데, 창과 무관한 트리거가 추가되면서 "출근 준비하며 폰을 잠깐 내려놓는"
등 지극히 일상적인 화면 꺼짐/켜짐까지 전부 "수면"으로 오인식하게 됨.

**수정**(`SleepDetectionReceiver.handle()`): 화면이 꺼진 상태를 처음
관찰(`KEY_FIRST_OFF_SAMPLE_AT` 기록)하기 **전에** `computeWindowForNow()`로
"지금이 실제 추적 창 안인가"를 확인하고, 아니면 아무 것도 기록하지 않고
리턴한다. ⚠️ 이미 진행 중인 후보를 화면 켜짐 시점에 종료하는 로직(13-1)은
창 확인 없이 그대로 둠 - 후보가 존재한다는 것 자체가 "그 후보는 생성될 때
이미 창 안이었다"를 보장하므로, 끝내는 건 언제든(창을 벗어난 뒤라도) 허용해야
맞다.

**추가 안전장치 - "메인 수면 최소 2시간"**(사용자 요청, 창 게이팅과 별개의
2중 방어): 후보를 종료할 때 길이가 2시간(`MIN_MAIN_SLEEP_MINUTES`) 미만이면
저장하지 않고 그냥 삭제한다. 위젯의 "기상" 버튼으로 AUTO 후보를 바로
확정(promoteToConfirmed)하는 경로도 동일 기준 적용
(`SleepWidgetActionReceiver.MIN_MAIN_SLEEP_MINUTES` - MANUAL의
`MIN_MEANINGFUL_MINUTES`(2분)와는 별개 상수, 값은 반드시 동일하게 유지할 것).

**연동 개선 - `sleep_history.dart`의 매칭 완화**: 원래 근무 직후 매칭되는
수면을 `SleepRelation.mainSleep`(3시간 이상)만 인정했는데, 그러면 "근무 후
진짜로 짧게(2~3시간) 잔" 기록이 `nap`으로 분류되어 통째로 빠지면서 "데이터
없음"과 구분이 안 됐다 - 정작 "메인 수면이 너무 적다"를 보여줘야 할 바로 그
경우가 사라지는 문제였음. `workShiftOverlap`(근무 중 수면)만 제외하고 나머지는
전부 인정하도록 완화 - `today_forecast_engine.dart`의 "짧은 수면" 신호가
실제 값을 그대로 반영하게 됨(아래 14-1 참고).

**컨디션 예측 반영 지연 문제**: `sleepRecordProvider`는 실시간 push 구독이
없어서(원래 설계 의도 - 6장) 위젯/자동감지가 백그라운드에서 DB를 직접 바꿔도
앱이 이미 열려있던 세션은 그걸 모른다. `condition_tab.dart`의
`_ConditionBodyState`에 `WidgetsBindingObserver`를 추가해서 앱이 포그라운드로
복귀할 때(`AppLifecycleState.resumed`)마다 `sleepRecordProvider.notifier.refresh()`를
호출하도록 함 - 이 화면 자체 관심사라 `main.dart`는 안 건드림(완전 분리 원칙
유지, 이미 `main.dart`에 비슷한 패턴이 다른 목적으로 있음).

### 13-9. (2026-09-01 후속5) 낮잠 칸 단순화 + 창 중복 방지 + UI 다듬기

**"근무 중 낮잠"이라는 말 자체를 앱에서 없앰**: 13-6에서 만든 3칸 분류(주
수면/근무 중 낮잠/근무 외 낮잠)를 사용자 피드백으로 다시 단순화 - `근무
겹침` 기준을 완전히 빼고 **주 수면 1칸 + 낮잠 2칸**(둘 다 그냥 "낮잠")으로
바꿈. `sleep_day_slots.dart`의 `SleepSlotCategory`가 `{main, workNap, offNap}`
→ `{main, nap1, nap2}`로, 분류 기준도 "길이(4시간)"만 남고 `ShiftPatternAnalyzer`/
근무 겹침 의존성 자체가 빠짐(그래서 `defaultSleepSlotTimes`도 근무 시간대
기반 스마트 기본값을 버리고 낮잠 두 칸 다 단순 고정값으로 회귀). 같은 날
낮잠이 여러 건이면 시작 시각이 이른 두 개만 nap1/nap2로 배정.

**자동 감지 - "이 창은 이미 썼다" 중복 방지 추가**: 사용자가 "22일 아침에
자고 나서 그날 저녁에 또 자야 하는 경우"(예: 20일 주간·21일 야간·22일 휴무·
23일 주간)가 로직에서 빠졌는지 확인해달라고 요청 - 확인 결과 `computeWindowForNow`
알고리즘 자체는 이미 정상 동작함(21일 야간 창은 9시간 캡으로 22일 낮 즈음
만료되고, 22일 21시부터는 독립적인 플랫 규칙 창이 새로 열림 - 서로 다른 창이라
막을 이유가 없음). 다만 점검 중 관련된 실제 위험을 하나 발견함: **같은 창이
아직 열려있는 동안**(예: 9시간 캡 안에서 이미 아침에 한 번 자고 일어난 뒤,
낮 동안 폰을 잠깐씩 내려놓는 지극히 일상적인 행동) 자동 감지가 그걸 또
"낮잠"으로 잘못 잡을 수 있는 여지가 있었음 - `SleepDetectionReceiver.handle()`에
`hasRecordStartingInWindow()` 체크를 추가해서, 지금 창 범위 안에 이미
start_time이 있는 기록(수동/자동, 확정/대기 무관)이 있으면 그 창에서는 더 이상
새 후보를 만들지 않게 함. 창이 바뀌면(예: 그날 저녁 21시부터의 새 플랫 규칙
창) 당연히 다시 허용됨 - 창 단위로만 "한 번 씀" 표시가 되는 것.

### 13-10. (2026-09-01 후속7) 야간 근무 회복 수면의 날짜 귀속 + 검증/버그 수정

**미니 달력 카드도 "야간 근무 회복 수면은 근무 당일로" 귀속하도록 확장**:
`sleep_history.dart`의 `collectRecentWorkNights`(컨디션 예측용)는 원래부터
"근무 종료 후 가장 가까운 주 수면"을 그 근무일에 매칭했지만(이미 정확했음 -
재확인해서 트레이싱으로 검증), `sleep_day_slots.dart`(미니 달력 카드/전체보기)는
단순히 "수면이 시작한 날짜"로만 묶어서 - 예를 들어 21일 야간 근무(19시~다음날
07시) 후 22일 07시~13시에 잔 수면이 21일이 아니라 22일 칸에 표시되고 있었음.
`_attributedDay()`를 추가해서 "전날이 야간 근무였고, 그 근무 종료 후
16시간(`_kNightRecoveryWindowHours` - collectRecentWorkNights의 searchAheadHours와
동일값으로 맞춤) 이내에 시작하는 수면"이면 전날(근무 당일)로 귀속시킴 -
`ShiftPatternAnalyzer`를 다시 받게 됨(후속5에서 뺐던 걸 이 용도로만 다시 추가 -
주 수면/낮잠 "분류" 자체는 여전히 길이로만 판단, 이건 오직 "어느 날짜 칸에
보여줄지"만 바꿈).

**미래 시각 저장 방지**: "오늘 낮 12시인데 오늘 밤 22시~다음날 06시를 미리
저장"하는 게 가능했던 문제 - `showSleepSlotEditDialog`의 유효성 검사에
`!start.isAfter(now) && !end.isAfter(now)` 조건을 추가하고, 안 맞으면 저장
버튼 비활성 + "아직 지나지 않은 시각은 기록할 수 없어요" 안내 문구를 보여줌.

**미니 달력 카드 가로 스크롤이 "딱 붙는 애니메이션"처럼 보이던 버그**: 세로
리스트(컨디션 탭 전체)를 빠르게 스크롤했다 되돌리면 Flutter 슬리버 가상화가
이 카드의 State를 통째로 폐기했다가 다시 만들어서, "가로 스크롤을 끝까지
자동으로 밀어두는" 로직이 처음부터 다시 실행되며(jumpTo라 순간이동이지만
세로 스크롤 애니메이션 도중에 겹쳐서 "슥 붙는 애니메이션"처럼 보임) 반복
재생되던 문제 - 처음엔 `AutomaticKeepAliveClientMixin`으로 State를 유지시켜
해결한 줄 알았으나, **다른 탭에 갔다가 컨디션 탭으로 돌아오기만 해도
State가 다시 생성되는 경로가 있어 재발**함이 재현 확인됨(2026-09-01 후속8,
`백업_수면_컨디션_기능_검토_2026-09-01.md` B2 참고). 최종 수정: `jumpTo` 자체를
없애고 `reverse: true` + 데이터를 역순으로 뒤집어(`reversedDays`) "스크롤
안 해도 오늘이 이미 오른쪽 끝에 오는" 구조로 바꿔서 State가 몇 번을
재생성되든 애초에 이동시킬 게 없게 함 - `AutomaticKeepAliveClientMixin`은
더 이상 필요 없어져 제거함(`condition_tab.dart`의 `_SleepMiniCalendarCard`).

**일정관리 탭 일정생성 팝업이 키보드에 따라 미세하게 위아래로 움직이던 문제**:
시트 높이를 한 번만 계산해 캐싱해도 여전히 조금 움직였음 - 원인은
`showModalBottomSheet`가 Flutter 프레임워크 내부에서 builder가 반환한 위젯을
`MediaQuery.viewInsetsOf(context)`만큼의 바텀 패딩으로 자동으로 한 번 더
감싸기 때문(이 화면 코드 바깥에서 일어나는 래핑이라 안에서 뭘 해도 못 피함).
`showModalBottomSheet` 자체를 버리고 `showGeneralDialog` + `Align(bottom)` +
직접 만든 슬라이드 전환으로 하단 고정 시트를 새로 구현(`_showFixedBottomSheet`,
schedule_management_tab.dart) - 이제 시트 위치/크기가 키보드와 구조적으로
완전히 무관함(일정관리탭_UI_스펙.md에 상세 기록).

**미니 달력 카드/전체보기 UI 다듬기**:
- `sleep_edit_dialog.dart`의 편집 바텀시트에 `SafeArea(top: false)` 추가(제스처
  내비게이션 바와 안 겹치게) + 취소/삭제/저장 버튼을 임의 TextButton/ElevatedButton
  대신 앱 전역 공용 버튼(`AppSecondButton` - danger/neutral/success)으로 통일.
- 전체보기 화면(`sleep_calendar_full_screen.dart`): 근무명/슬롯 라벨/"+ 기록"
  텍스트가 전반적으로 너무 흐리다는 피드백으로 색 진하게(black54/black38 →
  black87 위주) + 날짜(예: "8/1 (토)") 폰트만 14→16sp로 키움(나머지 크기는
  유지). 미니 달력 카드(`condition_tab.dart`)도 동일한 흐림 문제가 있어 같이
  진하게 맞춤(날짜 크기는 그대로 - 사용자가 명시적으로 지목한 건 전체보기뿐).
