# 교대시계 출시 전 코드 감사 보고서

감사일: 2026-09-13 (Asia/Seoul)  
대상: HEAD `5fdbc88`, 앱 `1.0.23+25`, SQLite v23, compile/target SDK 36  
작업 방식: 소스·설정 수정 없이 정적 감사, 기존 Flutter 테스트, SQLite 축소 실험. 이 보고서만 새로 작성했다.

**판정: 현재 상태의 출시 승인은 보류하는 것이 타당하다.**

특정 조건에서 기상 알람이 복구되지 않고, 자동백업이 최근 변경을 놓치는 경로가 있다. 테스트가 통과했다는 사실만으로 이 앱의 핵심 보장인 '앱을 매일 열지 않아도 필요한 시각에 울린다'와 '복구 후 알람·근무표가 일치한다'를 확인할 수 없다.

| 등급 | 건수 | 판단 기준 |
|---|---:|---|
| Critical | 2 | 핵심 알람 복구 중단 또는 복구 불가능한 사용자 데이터 손실로 이어짐 |
| High | 19 | 알람 누락·잘못된 울림·데이터 불일치·공유 통제 실패 |
| Medium | 16 | 조건부 오동작·상태 갱신·날짜·성능·레이아웃·운영 위험 |
| Low | 2 | 출시 설정/계측 완성도 문제 |
| 합계 | 39 | 확정 결함과 잠재적 위험을 합한 수이며, 39건 모두 실기기 재현했다는 뜻은 아님 |

## 감사 범위와 증거의 한계

프로젝트 파일 목록, 최근 커밋, Flutter 진입점·Riverpod provider·모델·DB·백업·친구 공유·핵심 화면, Kotlin 알람 생성/실행/복구·위젯·수면 추정·일정 알림, Manifest·Gradle·Firebase rules/options 및 기존 테스트를 대조했다. 최근 HEAD는 86개 파일을 변경한 큰 커밋이므로 최근 추가된 일정 알림·탭 숨김·DB v23·Analytics를 기존 경로와 연결해 확인했다.

전체 구조와 주요 실행 경로를 대상으로 한 코드 감사다. 모든 UI 픽셀·ML 학습 데이터의 모든 항목·실제 Firebase 운영 설정을 검증한 인증 보고서는 아니다. 위치는 감사 시점 소스 기준이다.

- **축소 실험/정적 분석 재현:** 이번 감사에서 직접 실행한 결과가 있는 항목.
- **코드 경로 확인:** 특정 입력/순서를 주면 문제가 발생하는 로직을 확인했으나 모든 사례를 실기기에서 실행하지는 않음.
- **잠재적 위험:** OS/OEM·리소스 압박·비동기 실행 순서·외부 설정에 따라 결과가 달라져 추가 검증 필요.
- 각 항목의 **재현 방법은 앞으로 실행할 검증 절차**이며, 별도 표기가 없으면 이번에 완료한 실기기 테스트가 아니다.
- Firebase 운영 콘솔·실제 배포된 rules·Play Console Data safety/권한 심사 상태·실제 광고 설정에는 접근하지 않았다.
- 새 APK/AAB 배포, 앱 삭제/설치, 실제 사용자 데이터 변경, Firebase 서버 쓰기, 서명키 수정은 수행하지 않았다.

## 실제 실행한 검증

| 검증 | 결과 | 의미/한계 |
|---|---|---|
| `flutter test --no-pub` | **102개 통과** | 주로 순수 계산·분류 및 기본 위젯 검사. Android OS 알람 생존성·부팅·실제 Firestore 통합을 검증하지 않음 |
| `flutter analyze --no-pub` | **exit 1, 총 640개 진단** | `lib/web_main.dart:29`의 `dart:js_util` URI 오류 확인. 나머지 린트/경고를 각각 사용자 버그로 부풀려 세지 않음 |
| SQLite data_version 축소 실험 | **변경 전 1 / 동일 연결 변경 후 2 / 새 연결 변경 후 1** | 자동백업 비교 방식의 결함 재현. 실제 앱 DB는 변경하지 않음 |
| Git 상태 | 기존 추적 파일 변경 없음 | 보고서 추가 전 clean 상태 확인 |
| Kotlin/Robolectric 테스트 | 이번 감사에서 실행하지 않음 | 테스트 코드 검토만 수행. H2 테스트는 알려진 잘못된 동작을 재현하는 assertion임 |
| prod release AAB/실기기·OEM/Doze·재부팅 | 이번 감사에서 실행하지 않음 | 릴리즈 안전성의 잔여 검증 항목 |

Flutter 테스트/분석은 제한 실행에서 출력 없이 정체되어 해당 실행을 중단한 뒤 SDK·캐시 접근이 가능한 실행으로 완료했다. 기존 문서의 '분석 경고 0개' 등 과거 상태를 현재 검증 결과로 재사용하지 않았다.

## 구조상 핵심 위험

1. Flutter와 Kotlin이 같은 SQLite를 사용하지만 모든 변경을 직렬화하는 공통 revision/작업 큐가 없다.
2. SQLite commit과 AlarmManager 적용이 별도 시스템인데 원자적으로 성공한 것처럼 취급한다.
3. 알람의 생명주기가 화면 Activity/Overlay에 분산되어 화면이 없어지면 재생 제어·타임아웃 보장이 약해진다.
4. 백업에서 '파생'으로 제외한 알람 중 실제로는 사용자 원본이 포함돼 있고, 복원도 원본·OS 예약·설정 상태를 함께 전환하지 않는다.
5. '하루 1회 갱신', provider 캐시, 익명 공유 소유권이 시간·재설치·실패 경로에서 서로 맞물리지 않는다.

## 최근 변경과 회귀 우선순위

| 변경/연결 경로 | 우선 확인 항목 |
|---|---|
| DB v23 도입 | C01, H16 |
| 자동백업 변경 감지 수정 및 시작/복귀 트리거 추가 | C02, M10, M11 |
| 새 일정 알림 + 기존 시간대/부팅/복원 경로 | H06, H07, H08, H15 |
| 탭 생성/해제 및 컨디션 사전 로딩 | M01, M02 |
| 컨디션 v2에 OT 반영 | M01 — 엔진 단위 테스트는 통과하지만 실제 provider 갱신 경로는 누락 |
| 근무명 변경 시 condition_shift_times 추가 갱신 | H12 |
| Analytics/Google Services 추가 | H18, M13, L02 |
| 기존 알람 실행/복원 설계에서 계속 남은 문제 | H01~H05, H09~H10, H19 |

아래 항목은 새 커밋이 원인이라고 입증된 경우와 기존 구조에서 이어진 경우를 구분해 읽어야 한다. 최근에 건드린 파일이라는 이유만으로 그 변경이 버그를 새로 만들었다고 단정하지 않았다.

## 상세 발견 사항

### [Critical] C01 — 업데이트 후 앱을 열지 않고 재부팅하면 알람 복구가 막힘

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/DatabaseHelper.kt:187](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/DatabaseHelper.kt:187), [android/app/src/main/kotlin/com/hwani1103/shiftbell/DirectBootReceiver.kt:35](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/DirectBootReceiver.kt:35)
- **발생 조건:** DB v22 이하 사용자에게 v23 앱이 자동 업데이트되고, 새 앱을 실행하기 전에 기기를 재부팅.
- **사용자 증상:** 기존 기상 알람이 울리지 않으며 앱을 직접 열기 전까지 복구되지 않을 수 있다. 재부팅 전에도 알람 타입을 읽지 못해 진동 폴백으로 바뀔 수 있다.
- **원인:** 네이티브는 디스크 버전이 정확히 23일 때만 DB를 사용한다. 마이그레이션은 Flutter만 수행한다. 부팅 복원과 갱신 엔진 모두 동일한 거부 조건에 걸린다.
- **재현 방법:** 이전 출시 APK에서 알람을 저장 → 새 APK로 교체 설치하되 실행하지 않음 → 재부팅 → 예약 시각까지 대기. 업데이트 자체가 예약을 지운다는 가정 없이도 재부팅으로 재현 가능한 경로다.
- **수정 방법:** Flutter 실행 없이도 부팅 시 알람을 읽을 수 있도록 하위 스키마 호환 읽기 또는 공통 마이그레이션을 제공한다. 앱 업데이트 후 최초 실행 전 재부팅 테스트를 출시 필수 항목으로 둔다.

### [Critical] C02 — 자동백업이 데이터 변경을 계속 놓침

**판별:** 축소 실험 재현

- **코드 위치:** [lib/services/backup_watcher.dart:53](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_watcher.dart:53), [lib/services/backup_watcher.dart:118](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_watcher.dart:118)
- **발생 조건:** 한 번 백업한 뒤 근무표·메모·수면기록을 수정하고 자동백업에 의존.
- **사용자 증상:** 오래된 백업만 남는다. 앱 삭제·기기 고장 후 복구할 때 최근 데이터가 사라진다.
- **원인:** 매 호출 새 연결로 PRAGMA data_version을 읽고 연결을 닫은 뒤 이전 연결의 값을 비교한다. 이 값은 영속 변경 번호가 아니다. SharedPreferences만 바뀐 경우도 감지하지 않는다.
- **재현 방법:** 별도 SQLite 실험 결과: 변경 전 1 → 동일 연결에서 변경 후 2 → 새 연결에서 다시 1. 앱에서는 수동백업 후 메모 수정 → 홈 → 복귀 → 백업 파일 내용/시각 비교.
- **수정 방법:** 앱·네이티브 쓰기에 공통으로 적용되는 영속 revision/dirty 표시를 만들고 설정 변경도 포함한다. 백업 성공한 revision만 저장한다. 단순히 호출 횟수를 늘리는 수정으로는 해결되지 않는다.

SQLite 공식 문서도 data_version은 **동일 연결의 두 시점 값만 비교해야 한다**고 명시한다. [SQLite PRAGMA data_version](https://sqlite.org/pragma.html#pragma_data_version).

### [High] H01 — 알람 화면을 나가면 자동 종료 타이머가 사라짐

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActivity.kt:118](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActivity.kt:118), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActivity.kt:418](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActivity.kt:418)
- **발생 조건:** 알람 Activity가 떠 있는 동안 홈으로 나가 noHistory에 의해 Activity가 파괴되고 앱 프로세스는 살아 있음.
- **사용자 증상:** 설정한 지속시간을 넘겨 알람 소리·진동이 계속될 수 있다.
- **원인:** 종료 타이머를 Activity Handler에 두었으며 onDestroy에서 취소한다. AlarmPlayer의 루프 재생에는 별도 지속시간 제한이 없다. 알림만 남겨도 타이머는 이전되지 않는다.
- **재현 방법:** 알람 지속시간을 짧게 설정 → 잠금 화면 알람 수신 → 홈으로 이동 → 지속시간이 지난 후 재생 여부와 timeout 이력 확인.
- **수정 방법:** 재생·종료시각·타이머를 화면과 독립된 알람 실행 컴포넌트가 소유하게 한다. 화면 재생성 시에도 최초 종료시각을 유지한다.

### [High] H02 — 알림 폴백에 제어 버튼·타임아웃이 없고 화면 실행 실패를 감지하지 못함

**판별:** 코드 결함 확인, OS별 결과는 잠재적 위험

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:129](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:129), [android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:190](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:190), [android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:274](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:274)
- **발생 조건:** 오버레이 권한 없음, 전체화면 알림 제한, 알림 권한 거부 또는 백그라운드 Activity 실행 차단.
- **사용자 증상:** 소리는 나는데 끄기 화면이 없거나, 알람 자체가 보이지 않는다. 폴백만 실행되면 자동 종료 타이머도 시작되지 않는다.
- **원인:** 직접 startActivity 후 예외만 폴백 조건으로 삼는다. BAL 차단은 예외 없이 일어날 수 있다. 폴백 Notification에는 contentIntent와 해제/스누즈 action이 없고 fullScreenIntent만 있다. POST_NOTIFICATIONS 거부 시 이 폴백도 사용할 수 없다.
- **재현 방법:** Android 14~16에서 오버레이·전체화면 권한을 각각 끄고 잠금/해제 상태로 예약 수신. 이어 알림 권한도 끈 조합에서 조작 가능성과 종료시간 확인.
- **수정 방법:** 알람 실행 상태를 먼저 유지하고, 표시 가능한 알림에 명시적 contentIntent·해제·스누즈를 제공한다. 전체화면 가능 여부와 채널 상태를 검사한다. 화면 표시 성공 여부와 무관하게 타임아웃을 보장한다.

백그라운드 Activity 실행은 OS 제한을 받으며 차단 결과가 반드시 예외로 전달되는 것은 아니다. 이 플랫폼 설명과 위 코드의 예외 기반 폴백을 대조한 판단이다. [Android Activity security](https://developer.android.com/guide/components/activities/secure-bal).

### [High] H03 — Receiver 반환 후 알람 재생 프로세스 생존을 보장하지 않음

**판별:** 잠재적 위험

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:118](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:118), [android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:264](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/CustomAlarmReceiver.kt:264), [android/app/src/main/AndroidManifest.xml:92](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/AndroidManifest.xml:92)
- **발생 조건:** 앱이 백그라운드이고 Activity가 실제로 뜨지 않거나 기기의 프로세스 정리가 공격적인 경우.
- **사용자 증상:** 소리가 짧게 끊기거나 500ms 후 예정된 화면 표시가 실행되지 않을 수 있다.
- **원인:** Receiver에서 MediaPlayer를 시작하고 Handler로 후속 UI를 예약한 뒤 반환한다. 오버레이도 일반 startService이며 foreground service로 승격하지 않는다. Manifest 권한 선언만으로 실행 중 서비스가 foreground가 되지는 않는다.
- **재현 방법:** 화면 실행이 제한된 상태에서 알람 수신 후 메모리 압박/프로세스 회수를 유도하고 logcat·재생 길이를 비교한다. 단순 force-stop은 정상 생존성 검증과 구분한다.
- **수정 방법:** 알람 재생을 적합한 타입의 foreground service로 이전하고 즉시 알림을 게시한다. 서비스 시작 실패를 처리하고 재생 리소스·wake lock·타임아웃을 서비스에서 관리한다.

Receiver에서 시작한 장시간 작업은 프로세스 생명주기와 별개로 보장되지 않는다. [Android 앱 구성요소와 프로세스](https://developer.android.com/guide/components/fundamentals).

### [High] H04 — Doze에서 촘촘한 알람·5분 스누즈가 정확하지 않을 수 있음

**판별:** 플랫폼 제약에 따른 잠재적 위험

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:919](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:919), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:462](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:462), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActionHelper.kt:210](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmActionHelper.kt:210)
- **발생 조건:** 깊은 절전 상태에서 가까운 시각의 알람 여러 개, 5분 스누즈, 수면감지/일정알림 예약이 겹침.
- **사용자 증상:** 정시에 울리지 않거나 가까운 알람이 지연·몰아서 전달될 수 있다.
- **원인:** 일반 등록·자동 갱신·스누즈가 setExactAndAllowWhileIdle을 사용한다. 이 API는 idle에서도 호출 빈도 제한을 받는다. 부팅 긴급 예약만 setAlarmClock을 사용하고 이어지는 갱신은 다시 다른 방식으로 덮는다.
- **재현 방법:** 실기기에서 5분 간격 알람과 스누즈를 예약하고 Doze 강제 진입 후 실제 수신 timestamp를 비교한다. 앱을 켠 채 기다리는 테스트로 대체하지 않는다.
- **수정 방법:** 사용자 기상 알람은 setAlarmClock 사용을 검토하고 모든 등록 경로를 통일한다. 수면 샘플링·가벼운 일정 알림은 목적에 맞는 별도 정책으로 둔다.

정확한 while-idle 알람에도 전달 빈도 제한이 있다. '9분이면 무조건 충분하다'처럼 고정 임계값을 가정하지 않아야 한다. [AlarmManager API](https://developer.android.com/reference/android/app/AlarmManager#setExactAndAllowWhileIdle(int,long,android.app.PendingIntent)).

### [High] H05 — 백업에서 불규칙 알람·개별 알람이 복구되지 않음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/services/backup_service.dart:38](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:38), [lib/screens/restore_backup_screen.dart:115](C:/Users/Administrator/Desktop/shiftbell/lib/screens/restore_backup_screen.dart:115), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:133](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:133)
- **발생 조건:** 불규칙 근무 사용자 또는 개별적으로 수정한 알람을 백업하고 새 설치에 복구.
- **사용자 증상:** 달력은 복구되는데 알람은 비거나 개별 수정이 사라진다.
- **원인:** alarms 테이블 전체를 파생 데이터로 간주하여 제외한다. 불규칙 스케줄은 네이티브가 기존 alarms 재등록만 수행하므로 빈 DB에서 생성할 원본이 없다. 규칙 알람의 개별 변경도 템플릿만으로 복원할 수 없다.
- **재현 방법:** 불규칙 근무 날짜에 알람 설정 → 백업 → 별도 새 설치에서 복원 → 다음 알람 목록과 OS 예약 확인.
- **수정 방법:** 규칙 생성 결과와 사용자가 직접 만든/수정한 원본을 분리하여 백업한다. 불규칙 근무의 알람 재생성도 별도로 구현한다. 복구 안내의 보장 범위를 실제 데이터 모델에 맞춘다.

### [High] H06 — 덮어쓰기 복원 후 이전 알람과 이전 일정 알림이 남음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/services/backup_service.dart:155](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:155), [lib/screens/settings_tab.dart:203](C:/Users/Administrator/Desktop/shiftbell/lib/screens/settings_tab.dart:203), [android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationReceiver.kt:74](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationReceiver.kt:74)
- **발생 조건:** 현재 데이터를 쓰는 중 다른 백업으로 덮어쓰기. 특히 불규칙 스케줄·스누즈 또는 이전 일정 알림이 있는 경우.
- **사용자 증상:** 복구한 근무표와 관계없는 옛 알람이 울린다. 삭제된 일정의 내용이 알림에 나타날 수 있다.
- **원인:** 복원은 백업에 포함된 테이블만 지우며 제외된 alarms는 기존 상태로 남긴다. 네이티브 diff는 규칙 fixed만 정리한다. 이전 date_schedules의 PendingIntent도 취소하지 않고 Receiver는 현재 DB 유효성 대신 Intent의 옛 내용을 바로 표시한다.
- **재현 방법:** A 데이터에 가까운 알람·일정 알림 생성 → 이를 포함하지 않는 B 백업 덮어쓰기 → A 시각까지 대기. 새 설치 복원에서도 일정알림이 즉시 재예약되는지 확인.
- **수정 방법:** 복원 전 기존 예약 ID를 수집해 취소하고 실행 중 알람도 정리한다. 원본 교체 후 알람·일정 알림을 모두 재조정한다. Receiver에서 존재·활성·예약 revision을 검증한다.

### [High] H07 — 시간대 변경 시 일정 알림은 재계산되지 않음

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmGuardReceiver.kt:84](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmGuardReceiver.kt:84), [android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationScheduler.kt:99](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationScheduler.kt:99)
- **발생 조건:** 일정 알림 예약 후 여행·기기 설정으로 시간대를 변경.
- **사용자 증상:** 달력의 09:00 일정 알림이 변경 전 시간대 기준으로 울린다.
- **원인:** TIMEZONE_CHANGED/TIME_SET에서 근무 알람 엔진만 호출한다. 일정 알림 재등록은 부팅과 탭 재활성화 경로에만 연결돼 있다.
- **재현 방법:** 서울에서 미래 09:00 일정 알림 예약 → 시간대를 다른 지역으로 변경 → dumpsys alarm의 트리거와 새 현지 09:00 비교.
- **수정 방법:** 시간대 변경 경로에 일정 알림 전체 reconciliation을 연결한다. 이미 지난 시각의 처리 정책도 정하고 이전 예약을 남기지 않는다.

### [High] H08 — 숨긴 일정관리 탭의 알림이 재부팅 후 다시 켜짐

**판별:** 코드 경로 확인

- **코드 위치:** [lib/providers/tab_visibility_provider.dart:17](C:/Users/Administrator/Desktop/shiftbell/lib/providers/tab_visibility_provider.dart:17), [lib/services/schedule_notification_service.dart:89](C:/Users/Administrator/Desktop/shiftbell/lib/services/schedule_notification_service.dart:89), [android/app/src/main/kotlin/com/hwani1103/shiftbell/DirectBootReceiver.kt:71](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/DirectBootReceiver.kt:71)
- **발생 조건:** 일정 알림이 있는 상태에서 일정관리 탭 사용을 끄고 재부팅.
- **사용자 증상:** 사용하지 않기로 한 일정 알림이 다시 울리며, 누르면 숨김 상태인 탭으로 이동을 시도한다.
- **원인:** 탭을 끌 때 예약만 취소하고 notify_enabled는 보존한다. 부팅 시 네이티브는 schedule_tab_enabled를 보지 않고 notify_enabled=1을 전부 예약한다. Flutter 설정은 부팅 전 읽을 수 있는 네이티브 상태와도 분리돼 있다.
- **재현 방법:** 알림 예약 → 일정관리 탭 숨김 → 재부팅 → 알림 시각까지 대기.
- **수정 방법:** 알림 전역 활성 상태를 Direct Boot에서도 읽을 수 있도록 동기화하고 부팅 복원 및 Receiver 양쪽에서 적용한다.

### [High] H09 — 네이티브 갱신과 Flutter 수정 사이 공통 잠금이 없음

**판별:** 잠재적 위험: 실행 순서에 의존

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:127](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:127), [android/app/src/main/kotlin/com/hwani1103/shiftbell/RefreshLockManager.kt:39](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/RefreshLockManager.kt:39), [lib/providers/schedule_provider.dart:242](C:/Users/Administrator/Desktop/shiftbell/lib/providers/schedule_provider.dart:242)
- **발생 조건:** 앱 복귀·자정 갱신과 동시에 날짜 근무변경/템플릿 저장/초기화.
- **사용자 증상:** 변경한 날짜의 알람이 옛 설정으로 되돌아가거나 중복/누락되고 화면과 DB가 어긋날 수 있다.
- **원인:** RefreshLockManager는 네이티브 갱신끼리만 잠근다. 엔진은 트랜잭션 전에 schedule/templates/existing을 읽는다. SQLite 쓰기 직렬화는 그 이전에 읽어 둔 오래된 계산 결과까지 무효화하지 않는다.
- **재현 방법:** 엔진이 기존 스냅샷을 읽은 직후 중단 → Flutter에서 근무변경 완료 → 엔진 재개. 동일 날짜의 DB·OS 예약을 비교한다.
- **수정 방법:** 공통 revision을 읽고 commit 시 다시 검증하거나 모든 알람 원본 변경·reconciliation을 단일 실행 큐로 통합한다. 오래된 계산은 폐기 후 재계산한다.

### [High] H10 — SQLite 롤백과 AlarmManager 예약·취소가 원자적으로 묶이지 않음

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:157](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:157), [android/app/src/test/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngineH2Test.kt:154](C:/Users/Administrator/Desktop/shiftbell/android/app/src/test/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngineH2Test.kt:154)
- **발생 조건:** 여러 알람 갱신 중 N번째 OS 예약 실패 또는 프로세스 종료.
- **사용자 증상:** DB에는 원래 알람이 남아 있지만 OS 예약은 취소돼 있거나 DB에 없는 예약이 남는다.
- **원인:** 트랜잭션 내부에서 OS 취소·등록을 수행한다. DB 롤백은 OS 부작용을 되돌리지 않는다. 기존 H2 테스트도 현재 롤백 동작을 확인하는 테스트이며 OS 정합성 보장을 검증하지 않는다.
- **재현 방법:** 기존 알람을 제거하고 여러 개 추가하는 갱신에서 N번째 schedule 호출 실패를 주입 → DB와 실제/모의 OS 예약 목록 비교.
- **수정 방법:** DB에는 예약 작업/outbox를 먼저 원자적으로 저장하고 commit 후 OS에 적용한다. 실패 작업은 재시도하며 앱 시작·부팅 시 양쪽 상태를 재조정한다.

### [High] H11 — 쉼표를 포함한 근무명이 패턴을 파괴함

**판별:** 코드 경로 확인

- **코드 위치:** [lib/models/shift_schedule.dart:219](C:/Users/Administrator/Desktop/shiftbell/lib/models/shift_schedule.dart:219), [lib/screens/onboarding_screen.dart:624](C:/Users/Administrator/Desktop/shiftbell/lib/screens/onboarding_screen.dart:624), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:264](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:264)
- **발생 조건:** 사용자 정의 근무명에 'A,B'처럼 쉼표 입력 후 저장·재실행.
- **사용자 증상:** 근무 한 개가 둘로 나뉘고 패턴 주기가 달라지며 해당 근무 알람 템플릿이 매칭되지 않는다.
- **원인:** 근무명 입력에서 쉼표를 금지하지 않는데 pattern/shift_types/active_shift_types를 join(',')/split(',')으로 직렬화한다.
- **재현 방법:** 사용자 정의 근무 'A,B'를 생성하여 패턴에 넣고 알람 설정 → 재실행 → 패턴 길이와 알람 확인.
- **수정 방법:** 근무 식별자와 표시명을 분리하거나 JSON 배열 저장으로 migration한다. 즉시 완화 시 쉼표 및 예약 sentinel '미설정' 입력을 차단하고 기존 데이터도 검사한다.

### [High] H12 — 근무명 맞교환·중복 변경이 알람과 근무시간을 합쳐 버림

**판별:** 코드 경로 확인

- **코드 위치:** [lib/services/database_service.dart:1038](C:/Users/Administrator/Desktop/shiftbell/lib/services/database_service.dart:1038), [lib/screens/settings_tab.dart:1941](C:/Users/Administrator/Desktop/shiftbell/lib/screens/settings_tab.dart:1941)
- **발생 조건:** 이름 변경 다이얼로그에서 A→B, B→A로 교환하거나 두 이름을 같은 이름으로 변경.
- **사용자 증상:** 두 근무의 알람 템플릿이 한 이름에 몰리고 한쪽 출퇴근시간이 소실된다.
- **원인:** 이름 변경을 순서대로 WHERE oldName UPDATE하여 앞서 바꾼 행까지 다음 변경 대상으로 삼는다. condition_shift_times는 새 이름의 행을 먼저 삭제한다. 트랜잭션은 논리 오류를 막지 않는다.
- **재현 방법:** 서로 다른 알람/근무시간의 A·B를 만들고 한 번에 이름을 교환 → 각 테이블 결과 확인.
- **수정 방법:** 중복 결과명을 사전 검증하고 임시 고유 이름을 거치는 2단계 rename 또는 안정적인 shift_id를 사용한다.

### [High] H13 — 공유 중지 실패를 성공처럼 처리하고 진행 중 쓰기가 공유를 되살릴 수 있음

**판별:** 코드 경로 확인, race는 잠재적 위험

- **코드 위치:** [lib/services/friend_sync_service.dart:83](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:83), [lib/services/friend_sync_service.dart:113](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:113), [lib/screens/my_share_code_screen.dart:139](C:/Users/Administrator/Desktop/shiftbell/lib/screens/my_share_code_screen.dart:139)
- **발생 조건:** 네트워크/인증 오류 중 공유 중지 또는 근무변경 동기화와 공유 중지가 겹침.
- **사용자 증상:** 공유 중지로 표시돼도 기존 링크로 계속 조회되거나 삭제 후 문서가 다시 생길 수 있다.
- **원인:** 로컬 enabled=false를 먼저 쓰고 서버 delete 실패를 삼킨다. 이미 enabled=true를 확인한 syncIfEnabled는 취소되지 않으며 upsert 직전 재확인/세대 번호가 없다.
- **재현 방법:** delete를 실패시키고 UI 상태와 다른 기기의 서버 조회 비교. 별도로 sync를 플래그 확인 직후 멈춤 → stopSharing 완료 → sync 재개.
- **수정 방법:** 공유 중지 pending/confirmed 상태를 분리하고 서버 확인 전 '삭제 완료'라고 표시하지 않는다. 공유 작업을 직렬화하고 중지 세대 이후 쓰기를 막으며 삭제 재시도 상태를 보존한다.

### [High] H14 — 재설치·기기변경 후 기존 공유 문서의 소유권과 삭제 수단을 잃음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/services/friend_sync_service.dart:41](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:41), [lib/services/backup_service.dart:83](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:83), [firestore.rules:29](C:/Users/Administrator/Desktop/shiftbell/firestore.rules:29)
- **발생 조건:** 공유를 켠 채 앱 삭제·데이터 삭제·기기변경 후 복원.
- **사용자 증상:** 옛 링크는 옛 근무표를 계속 제공하지만 새 설치에서는 그 문서를 수정·삭제할 수 없다. 복원된 '공유 중' 설정은 새 UID와 연결되어 기존 친구 링크가 갱신되지 않는다.
- **원인:** 소유권이 익명 Auth UID에만 묶인다. 백업은 공유 플래그/이름만 보존하고 Auth 자격 증명은 복구하지 않는다. TTL·서버 정리·계정 연결·옛 링크 폐기 경로가 없다.
- **재현 방법:** 공유 링크 확보 → 앱 삭제/데이터 초기화 → 복원 및 이름/근무 수정 → 옛 링크 조회와 새 ownerId 비교.
- **수정 방법:** 계정 연결/복구 가능한 공유 소유권 모델 또는 만료·관리 가능한 링크 정책을 설계한다. 복원 시 공유 상태를 무조건 이전 설치와 동일하다고 취급하지 않는다.

익명 계정을 영속 계정에 연결하는 기능은 Firebase가 제공하지만 현재 앱에는 연결 경로가 없다. [Firebase 익명 인증](https://firebase.google.com/docs/auth/android/anonymous-auth).

### [High] H15 — 알람 등록 실패를 성공으로 숨기는 조건문과 API 계약

**판별:** 코드 경로 확인

- **코드 위치:** [lib/providers/alarm_provider.dart:200](C:/Users/Administrator/Desktop/shiftbell/lib/providers/alarm_provider.dart:200), [lib/providers/alarm_provider.dart:224](C:/Users/Administrator/Desktop/shiftbell/lib/providers/alarm_provider.dart:224), [android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationScheduler.kt:52](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/ScheduleNotificationScheduler.kt:52), [lib/services/permission_service.dart:70](C:/Users/Administrator/Desktop/shiftbell/lib/services/permission_service.dart:70)
- **발생 조건:** 정확한 알람 권한이 없는 Android 12 등, 예약 API 오류, 채널 호출 실패.
- **사용자 증상:** 알람/일정 화면에서는 저장된 것으로 보이지만 실제 예약은 없고 사용자에게 실패가 전달되지 않는다.
- **원인:** failCount는 scheduled를 순회할 때만 증가하므로 'failCount > 0 && scheduled.isEmpty'는 참이 될 수 없다. 일정 알림의 Kotlin schedule은 예외를 삼키고 채널은 success를 반환한다. 권한 확인 실패도 true로 처리한다.
- **재현 방법:** 예약 호출 전부가 예외를 던지도록 주입 → notifier 반환/토스트/DB·OS 상태 확인. 권한 거부 조건은 USE_EXACT_ALARM이 자동 허용되는 최신 OS와 구분해 시험한다.
- **수정 방법:** 예약 성공 개수/실패 목록을 명시적으로 반환하고 실패를 UI·복구 큐에 반영한다. 권한 상태는 granted/denied/unknown으로 구분한다.

### [High] H16 — 마이그레이션 실패를 무조건 무시하여 손상된 최신 버전 DB를 만들 수 있음

**판별:** 잠재적 위험

- **코드 위치:** [lib/services/database_service.dart:643](C:/Users/Administrator/Desktop/shiftbell/lib/services/database_service.dart:643), [android/app/src/main/kotlin/com/hwani1103/shiftbell/DatabaseHelper.kt:170](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/DatabaseHelper.kt:170)
- **발생 조건:** 저장공간 부족·손상·실제 SQL 오류가 migration 중 발생하거나 네이티브 버전 검사 연결이 실패.
- **사용자 증상:** 일정관리만 반복 오류가 나거나 다음 실행에도 migration이 재시도되지 않는다.
- **원인:** ALTER TABLE의 모든 예외를 '이미 존재 가능성'으로 삼키므로 sqflite는 업그레이드 성공으로 끝낼 수 있다. onOpen은 shift_schedule 존재만 본다. 네이티브 버전 확인 실패 시 최신 버전이라고 가정하는 폴백도 빈 onUpgrade를 통과시킬 위험이 있다.
- **재현 방법:** v22→23에서 ALTER 하나에 실패 주입 → user_version과 PRAGMA table_info 비교 → 재실행. 네이티브 버전 검사 실패도 별도로 주입.
- **수정 방법:** 중복 컬럼임을 실제 스키마 검사로 확인한 경우만 무시하고 나머지는 롤백한다. 종료 전 필수 테이블·컬럼 검증을 수행한다. 버전을 알 수 없으면 임의로 최신이라고 간주하지 않는다.

### [High] H17 — 백업 복구의 DB commit 후 설정 실패로 부분 복구 상태가 남음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/services/backup_service.dart:157](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:157), [lib/services/backup_service.dart:173](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:173), [lib/screens/restore_backup_screen.dart:69](C:/Users/Administrator/Desktop/shiftbell/lib/screens/restore_backup_screen.dart:69)
- **발생 조건:** 외형은 정상 JSON이지만 preferences에 숫자 리스트 등 허용하지 않는 자료형이 있거나 설정 쓰기 실패.
- **사용자 증상:** 복구 실패 안내 뒤 재시도가 '이미 데이터가 있음'으로 막히고 일부 테이블/설정만 교체된다.
- **원인:** DB transaction이 먼저 commit된다. 이후 value.cast<String>() 등이 실패해도 DB는 되돌리지 않는다. 화면의 dbCommitted는 restoreAll 전체 성공 후 설정되므로 이 부분 성공을 구분하지 못한다.
- **재현 방법:** 유효한 백업 복사본의 preferences에 [1,2] 값 추가 → 새 설치에서 복구 → 실패 후 DB와 재시도 결과 확인.
- **수정 방법:** 모든 테이블·컬럼·자료형·설정 값을 mutation 전에 검증한다. 복구 단계와 완료 marker를 보존해 중단 후 안전하게 완료/롤백할 수 있게 한다.

### [High] H18 — Analytics 자동 수집과 개인정보 안내가 일치하지 않음

**판별:** 코드·문구 불일치 확인; 정책 적합성은 별도 검증 필요

- **코드 위치:** [lib/services/firebase_bootstrap.dart:34](C:/Users/Administrator/Desktop/shiftbell/lib/services/firebase_bootstrap.dart:34), [lib/l10n/app_ko.arb:308](C:/Users/Administrator/Desktop/shiftbell/lib/l10n/app_ko.arb:308), [lib/l10n/app_ko.arb:312](C:/Users/Administrator/Desktop/shiftbell/lib/l10n/app_ko.arb:312)
- **발생 조건:** 공유를 사용하지 않은 사용자도 앱을 처음 실행.
- **사용자 증상:** 사용자가 안내에서 예상하지 못한 이용 분석 수집이 활성화된다. 공유 중지 즉시 삭제·앱 삭제 시 모든 데이터 삭제라는 설명도 실제 실패/백업 잔존과 다르다.
- **원인:** Analytics collection을 항상 true로 설정하지만 개인정보 안내의 외부 전송 설명에는 Analytics가 없다. ownerName을 서버에 저장하면서 저장 필드 설명도 불완전하다.
- **재현 방법:** 공유 없이 신규 실행 → Analytics DebugView 확인 → 개인정보 화면 설명과 SDK 설정 대조.
- **수정 방법:** 실제 SDK별 수집 항목·목적·보관·삭제 한계를 안내 및 Play Data safety 신고와 맞춘다. 배포 국가/광고 정책상 필요한 동의·철회는 별도 확인해 적용한다. 이 감사만으로 법 위반을 단정하지 않는다.

### [High] H19 — 다른 알람 삭제가 현재 울리는 알람까지 중지하고 진동 알람은 감지하지 못함

**판별:** 코드 경로 확인

- **코드 위치:** [lib/providers/alarm_provider.dart:98](C:/Users/Administrator/Desktop/shiftbell/lib/providers/alarm_provider.dart:98), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:402](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:402), [lib/screens/next_alarm_tab.dart:66](C:/Users/Administrator/Desktop/shiftbell/lib/screens/next_alarm_tab.dart:66)
- **발생 조건:** A 알람이 울리는 동안 앱에서 미래 B 알람을 삭제하거나 진동 전용 알람을 UI에서 삭제.
- **사용자 증상:** B를 지웠는데 A 소리가 멈추며 B 이력이 실제 울림 중 삭제로 기록된다. 진동 전용은 울림 상태를 false로 판단해 중지 경로를 놓친다.
- **원인:** isAlarmRinging은 MediaPlayer.isPlaying의 전역 bool이며 대상 ID를 받지 않는다. deleteAlarm은 이 bool만 보고 전역 stopAlarm을 호출한다. 진동/무음 실행 상태는 bool에 포함되지 않는다.
- **재현 방법:** 소리 알람 A 수신 → 앱의 다른 B 삭제 → A 재생 확인. 별도로 진동 A 수신 → A 삭제 → 진동과 tracker 확인.
- **수정 방법:** RingingAlarmTracker의 현재 ID와 재생 모드를 사용한다. 대상 ID가 실제 실행 중인 경우에만 해당 알람을 종료하고 모든 모드를 동일한 상태 머신으로 처리한다.

### [Medium] M01 — OT 변경이 컨디션 점수·판정에 반영되지 않음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/providers/condition_provider.dart:63](C:/Users/Administrator/Desktop/shiftbell/lib/providers/condition_provider.dart:63), [lib/providers/overtime_provider.dart:32](C:/Users/Administrator/Desktop/shiftbell/lib/providers/overtime_provider.dart:32)
- **발생 조건:** 앱이 컨디션용 OT를 미리 읽은 후 달력에서 최근 일주일 OT 수정.
- **사용자 증상:** 달력의 OT와 컨디션 점수·초과근무 경고가 다르고 앱 재실행 후에야 바뀐다.
- **원인:** recentOvertimeMinutesProvider는 다른 provider를 구독하지 않는 캐시형 FutureProvider다. OT 쓰기 경로에서도 이를 invalidate하지 않는다.
- **재현 방법:** 앱 시작 → OT 합계를 경고 경계 전후로 변경 → 컨디션 탭 비교 → 앱 재실행 후 비교.
- **수정 방법:** OT revision/provider를 의존성으로 연결하거나 OT 저장 성공 시 관련 집계를 무효화한다. 'OT 변경→화면 재계산' 통합 테스트를 추가한다.

### [Medium] M02 — 자정 및 탭 비활성 중 수면 변경이 캐시된 화면에 남음

**판별:** 코드 경로 확인

- **코드 위치:** [lib/providers/condition_provider.dart:71](C:/Users/Administrator/Desktop/shiftbell/lib/providers/condition_provider.dart:71), [lib/providers/health_tip_provider.dart:35](C:/Users/Administrator/Desktop/shiftbell/lib/providers/health_tip_provider.dart:35), [lib/screens/condition_tab.dart:125](C:/Users/Administrator/Desktop/shiftbell/lib/screens/condition_tab.dart:125), [lib/main.dart:530](C:/Users/Administrator/Desktop/shiftbell/lib/main.dart:530)
- **발생 조건:** 자정을 넘어 앱 프로세스를 유지하거나, 컨디션 외 탭을 보는 중 홈 위젯으로 수면 기록 후 앱 복귀.
- **사용자 증상:** 어제의 컨디션/팁·오래된 수면기록을 오늘 것처럼 표시할 수 있다.
- **원인:** DateTime.now()는 Riverpod의 반응형 의존성이 아니다. 컨디션 화면이 떠 있을 때만 resumed에서 수면 provider를 새로 읽는데 탭은 IndexedStack이 아니어서 다른 탭에서는 해당 observer가 없다. 네이티브 refresh 신호는 알람만 갱신한다.
- **재현 방법:** 컨디션 한 번 열기 → 달력 탭으로 이동 → 홈 위젯 취침/기상 → 복귀 후 컨디션 진입. 별도로 자정 전후 앱을 유지해 비교.
- **수정 방법:** 공통 날짜/시간대 provider와 수면 데이터 revision을 도입한다. 앱 재개 및 탭 진입 시 갱신하고 네이티브 수면 변경 완료를 명시적으로 전달한다.

### [Medium] M03 — DST 날짜를 24시간씩 순회하여 근무시간·OT·수면 달력이 틀림

**판별:** 코드 결함 확인; DST 지역에 한정

- **코드 위치:** [lib/services/work_hours_calculator.dart:29](C:/Users/Administrator/Desktop/shiftbell/lib/services/work_hours_calculator.dart:29), [lib/providers/overtime_provider.dart:67](C:/Users/Administrator/Desktop/shiftbell/lib/providers/overtime_provider.dart:67), [lib/services/condition/shift_pattern_analyzer.dart:111](C:/Users/Administrator/Desktop/shiftbell/lib/services/condition/shift_pattern_analyzer.dart:111), [lib/services/schedule_notification_service.dart:120](C:/Users/Administrator/Desktop/shiftbell/lib/services/schedule_notification_service.dart:120)
- **발생 조건:** 서머타임 시작/종료가 있는 시간대의 전환일을 포함한 기간.
- **사용자 증상:** 일자를 중복/누락 집계하거나 수면기록이 달력 칸에서 사라진다. 09:00 일정 알림이 전환일에 한 시간 어긋날 수 있다.
- **원인:** 날짜 이동에 Duration(days:1), 현지 시각 구성에 자정+Duration(minutes)를 사용한다. 가을에는 다음 반복이 같은 날짜 23시가 될 수 있고 봄에는 01시로 이동한다. 근무 패턴 본체의 Julian day 수정이 다른 집계에까지 적용되지는 않았다.
- **재현 방법:** America/New_York의 2026-11-01 전후 기간에 날짜별 다른 OT/수면 기록을 넣어 날짜별 방문 횟수와 합계를 확인. 2026-03-08의 09:00 일정 트리거도 확인.
- **수정 방법:** 날짜 순회는 DateTime(y,m,d+1), 시각은 DateTime(y,m,d,h,min)로 구성한다. 경과시간과 달력 날짜의 의미를 분리하고 양쪽 DST 전환 테스트를 추가한다.

### [Medium] M04 — 근무명 문자열 때문에 실제 근무를 휴무로 오인

**판별:** 코드 경로 확인

- **코드 위치:** [lib/utils/shift_name_util.dart:10](C:/Users/Administrator/Desktop/shiftbell/lib/utils/shift_name_util.dart:10), [lib/services/condition/shift_pattern_analyzer.dart:102](C:/Users/Administrator/Desktop/shiftbell/lib/services/condition/shift_pattern_analyzer.dart:102)
- **발생 조건:** '휴일근무', 'Office', 'Forest' 등 휴/off/rest 부분문자열이 들어간 실제 근무명.
- **사용자 증상:** 근무시간을 설정해도 컨디션에서는 쉬는 날로 분류되고 근무부담·수면 추정이 왜곡된다.
- **원인:** 명시적인 근무 종류 대신 표시명 contains로 휴무 여부를 판정한다.
- **재현 방법:** '휴일근무' 또는 'Office'를 사용자 근무명으로 만들고 출퇴근시간 입력을 시도한 뒤 컨디션 분류 확인.
- **수정 방법:** 휴무 여부를 명시적인 shift 속성으로 저장한다. 문자열 추정은 초기 기본값 제안에만 쓰고 사용자가 수정할 수 있게 한다.

### [Medium] M05 — 수면 추적 창이 끝나도 진행 중 자동 수면이 종료되지 않음

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionReceiver.kt:151](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionReceiver.kt:151), [android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionScheduler.kt:27](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionScheduler.kt:27)
- **발생 조건:** 자동 수면 후보 생성 후 수면 창이 끝날 때까지 화면이 꺼져 있고, 이후 한동안 앱을 열지 않음.
- **사용자 증상:** 9시간 제한 설명과 달리 매우 긴 수면 후보가 남거나 기상 시각이 훨씬 뒤로 잡힌다.
- **원인:** ongoing != null이면 창 종료 확인 전에 return한다. 창 밖 스케줄러는 다음 자정으로 넘어갈 수 있어 실제 기상 후 화면 사용도 한동안 관측되지 않는다.
- **재현 방법:** 자동 후보 생성 → 창 종료 이후까지 화면 꺼짐 유지 → 다른 앱 사용만 하고 교대시계는 열지 않음 → 나중에 후보 종료시간 확인.
- **수정 방법:** 진행 후보에 창 종료/최대 길이 deadline을 적용하고 창 끝에 마지막 확인을 예약한다. 센서가 아니라 샘플 추정이라는 한계를 UI에 유지하고 장시간 후보를 자동 확정하지 않는다.

### [Medium] M06 — 저장 날짜에 기기 로케일을 사용해 Dart와 형식이 달라질 수 있음

**판별:** 잠재적 위험

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:385](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmRefreshEngine.kt:385), [android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionReceiver.kt:111](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/SleepDetectionReceiver.kt:111), [lib/models/sleep_record.dart:110](C:/Users/Administrator/Desktop/shiftbell/lib/models/sleep_record.dart:110)
- **발생 조건:** 아랍어 숫자·다른 달력 표현을 사용하는 기기 로케일.
- **사용자 증상:** 날짜 문자열 비교/파싱 실패로 수면 기록 전체 로딩 실패, 근무 날짜 override 불일치, 알람 조회 누락 가능.
- **원인:** DB 저장용 SimpleDateFormat에 Locale.getDefault()를 반복 사용한다. Dart는 ASCII ISO 문자열을 저장·파싱하고 날짜 키를 정확히 비교한다.
- **재현 방법:** ar 계열 로케일의 Android에서 네이티브가 알람/수면 row 생성 → 저장된 문자열과 Dart 파싱 비교. 실제 지원 기기의 formatter 출력 확인이 필요하다.
- **수정 방법:** 저장 형식은 Locale.ROOT/US 및 Gregorian/epoch로 고정하고 화면 표시만 현지화한다. 기존 비정규 형식이 있으면 정규화 migration을 제공한다.

### [Medium] M07 — 출퇴근시간 저장이 두 테이블에서 부분 성공하거나 빠른 수정으로 덮어써짐

**판별:** 잠재적 위험

- **코드 위치:** [lib/screens/work_hours_settings_screen.dart:300](C:/Users/Administrator/Desktop/shiftbell/lib/screens/work_hours_settings_screen.dart:300), [lib/screens/work_hours_settings_screen.dart:371](C:/Users/Administrator/Desktop/shiftbell/lib/screens/work_hours_settings_screen.dart:371)
- **발생 조건:** 시간 입력/초기화를 연속 조작하거나 첫 저장 이후 두 번째 DB 쓰기가 실패.
- **사용자 증상:** 컨디션의 출퇴근시간과 달력 근로시간 합계가 불일치한다. 이전 근무의 설정이 다시 나타날 수 있다.
- **원인:** condition_shift_times 저장 후 오래된 schedule 스냅샷으로 shift_durations 전체를 다시 저장한다. 두 쓰기의 공통 transaction 및 저장 중 직렬화가 없다.
- **재현 방법:** 첫 테이블 저장 성공 후 두 번째 실패 주입. 별도로 두 근무를 빠르게 수정하며 쓰기 순서를 지연시켜 최종 두 테이블 비교.
- **수정 방법:** 두 값을 하나의 트랜잭션으로 저장하거나 근로시간을 출퇴근시간에서 단일하게 파생한다. 최신 DB 상태를 기준으로 부분 갱신하고 UI 중복 저장을 막는다.

### [Medium] M08 — 오프라인 공유 쓰기가 끝없이 대기하고 실패 후 재동기화가 보장되지 않음

**판별:** 잠재적 위험 및 누락 경로 확인

- **코드 위치:** [lib/services/friend_sync_service.dart:68](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:68), [lib/services/friend_sync_service.dart:126](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:126), [lib/screens/my_share_code_screen.dart:121](C:/Users/Administrator/Desktop/shiftbell/lib/screens/my_share_code_screen.dart:121)
- **발생 조건:** 이미 익명 로그인한 상태에서 네트워크 차단 후 공유 시작/이름 변경/중지. 또는 인증 오류로 동기화 실패 후 앱만 다시 열기.
- **사용자 증상:** 작업 중 상태가 오래 지속되거나 친구에게 과거 근무표가 계속 보인다.
- **원인:** Firestore set/delete 서버 완료를 timeout 없이 await한다. 실패를 삼킨 sync에는 영속 dirty/retry가 없다. 친구의 fetch는 상대 문서 읽기일 뿐 내 실패한 업로드를 재시도하지 않는다.
- **재현 방법:** 온라인에서 공유 시작 → 네트워크 차단 → 이름 변경/공유 중지 → UI 대기 시간 측정. 업로드 실패 후 근무 수정 없이 네트워크 복구·재개.
- **수정 방법:** 로컬 저장과 서버 확인 상태를 구분하고 유한 대기·재시도 UI를 제공한다. 영속 pending revision을 네트워크 복구 때 재전송한다. timeout이 Firestore의 이미 대기 중인 쓰기를 취소하지 않는다는 점도 처리한다.

Firestore SDK에 접수된 오프라인 쓰기는 온라인 복귀 후 동기화될 수 있다. **SDK 큐에 이미 들어간 쓰기**와 **인증 실패 등으로 접수조차 못 한 쓰기**를 구분해야 한다. [Firestore 오프라인 동작](https://firebase.google.com/docs/firestore/manage-data/enable-offline).

### [Medium] M09 — 공유 중지 후에도 친구 캐시에 근무표가 계속 남음

**판별:** 코드 경로 확인; 제품 보장 범위 문제

- **코드 위치:** [lib/providers/friend_provider.dart:84](C:/Users/Administrator/Desktop/shiftbell/lib/providers/friend_provider.dart:84), [lib/services/friend_sync_service.dart:143](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:143), [lib/providers/friend_provider.dart:47](C:/Users/Administrator/Desktop/shiftbell/lib/providers/friend_provider.dart:47)
- **발생 조건:** 친구가 한 번 근무표를 본 뒤 소유자가 서버 문서를 삭제하거나 공유를 중지.
- **사용자 증상:** 친구 앱에서는 예전 근무표를 계속 열 수 있다. '더 이상 볼 수 없음'이라는 보장과 다르다.
- **원인:** fetch는 문서 없음·네트워크 오류를 모두 null로 합치고 refreshFriend는 기존 data_json을 지우지 않는다. Firestore 자체 캐시 여부도 결과에 표시하지 않는다.
- **재현 방법:** 친구 기기에 캐시 생성 → 소유자 공유 중지 → 친구 온라인 새로고침 및 오프라인 재진입 → 표시 확인.
- **수정 방법:** notFound/revoked/offline/cached를 구분하고 서버에서 확인된 삭제는 앱 캐시 제거로 반영한다. 이미 전달된 데이터·스크린샷까지 원격 철회할 수 있다고 안내하지 않는다.

Firestore는 캐시 데이터를 오프라인에서 제공할 수 있다. 이 외에 앱 자체 SQLite data_json 캐시도 존재한다. [Firestore 오프라인 캐시](https://firebase.google.com/docs/firestore/manage-data/enable-offline).

### [Medium] M10 — 백업 스냅샷이 비일관적이고 대용량 백업이 UI를 막을 수 있음

**판별:** 잠재적 위험

- **코드 위치:** [lib/services/backup_service.dart:74](C:/Users/Administrator/Desktop/shiftbell/lib/services/backup_service.dart:74), [android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:524](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:524), [android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:705](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:705)
- **발생 조건:** 백업 중 근무명/패턴/수면 기록 변경, 장기간 쌓인 이력, 큰 백업 파일 선택.
- **사용자 증상:** 서로 다른 시점의 테이블이 백업에 섞이거나 UI가 멈춤/메모리 부족으로 복구 실패.
- **원인:** 테이블을 읽기 transaction 없이 순차 조회하고 전체 JSON을 메모리에 만든다. 네이티브 MethodChannel 핸들러에서 파일 전체 쓰기/읽기를 메인 스레드에서 수행한다. 자동백업 재진입 잠금도 없다.
- **재현 방법:** 테이블 조회 사이에 rename/수면 쓰기를 삽입해 참조 정합성 비교. 많은 이력의 백업·복원 중 프레임 시간/메모리 측정.
- **수정 방법:** DB 일관 스냅샷을 확보하고 백업 작업을 직렬화한다. 파일 I/O는 worker, 큰 JSON 처리는 isolate/stream으로 이동하며 입력 크기 한도를 둔다. 이력을 자동 삭제하는 방식으로 해결하지 않는다.

### [Medium] M11 — 백업 파일 쓰기 실패 뒤 불완전한 최신 파일이 정상 백업을 가림

**판별:** 코드 경로 확인

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:728](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:728), [android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:767](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/MainActivity.kt:767)
- **발생 조건:** MediaStore insert 후 stream 열기/쓰기 실패 또는 프로세스 종료.
- **사용자 증상:** 정상 이전 백업이 남아 있어도 자동 복원은 가장 최신 불완전 파일만 읽고 백업이 없는 것처럼 처리한다.
- **원인:** 실패 시 새 row/파일을 제거하지 않고 IS_PENDING/완료 검증도 없다. readBackupFile은 _ID가 가장 큰 후보 하나만 반환하고 이전 정상 후보로 내려가지 않는다.
- **재현 방법:** 정상 백업 생성 → 다음 백업을 insert 후 쓰기 중단 → 자동 검색·decode 결과 확인.
- **수정 방법:** 완료 전 pending 상태를 사용하고 실패 파일을 정리한다. 읽기는 최신순으로 검증 가능한 백업을 찾으며 최소 한 개의 검증된 이전 백업을 유지한다.

### [Medium] M12 — 큰 화면·폴더블에서 실제 콘텐츠 폭과 광고/화면 배율이 다름

**판별:** 코드 구조상 위험, 실기기 재현 필요

- **코드 위치:** [lib/main.dart:245](C:/Users/Administrator/Desktop/shiftbell/lib/main.dart:245), [lib/services/ad_service.dart:70](C:/Users/Administrator/Desktop/shiftbell/lib/services/ad_service.dart:70), [lib/widgets/banner_ad_slot.dart:111](C:/Users/Administrator/Desktop/shiftbell/lib/widgets/banner_ad_slot.dart:111)
- **발생 조건:** 500dp보다 넓은 화면, 폴더블 펼침/접힘, 분할 화면, 큰 글자 설정.
- **사용자 증상:** 광고가 잘리거나 배치가 맞지 않고 .w/.sp 기반 여백·문구가 과도하게 커질 수 있다. 일부 복구/업데이트 다이얼로그는 세로 공간이 부족하다.
- **원인:** MaterialApp 콘텐츠는 maxWidth=500이지만 ScreenUtil 및 광고 크기는 전체 화면 기준이다. 광고 크기는 최초 한 번만 계산한다. Restore 화면은 스크롤 없는 Column이다.
- **재현 방법:** 360dp/600dp/840dp, textScale 1.0/1.5/2.0, 폴더블 실행 중 크기 변경에서 화면·광고·복원 화면 검증.
- **수정 방법:** LayoutBuilder의 실제 가용 폭을 공통 기준으로 사용하고 크기 변경에 광고를 재요청한다. 긴 화면/다이얼로그에 스크롤과 적절한 높이 제약을 둔다.

### [Medium] M13 — dev/prod Firebase 설정이 분리되지 않고 Android appId도 일치하지 않음

**판별:** 설정 불일치 확인; 실제 집계 영향은 검증 필요

- **코드 위치:** [lib/firebase_options.dart:66](C:/Users/Administrator/Desktop/shiftbell/lib/firebase_options.dart:66), [android/app/google-services.json:1](C:/Users/Administrator/Desktop/shiftbell/android/app/google-services.json:1), [lib/services/firebase_bootstrap.dart:31](C:/Users/Administrator/Desktop/shiftbell/lib/services/firebase_bootstrap.dart:31)
- **발생 조건:** dev 빌드에서 Firebase 초기화 및 Analytics 검증.
- **사용자 증상:** 테스트 사용이 운영 지표/공유 DB에 섞이거나 Firebase 초기화·Analytics 스트림 식별이 예상과 다를 수 있다.
- **원인:** google-services에는 dev용 별도 appId가 있지만 Dart android options는 prod appId 하나다. 두 flavor는 같은 Firebase project를 사용하며 collection 이름도 같다.
- **재현 방법:** 각 flavor에서 기본 Firebase app options 및 Analytics DebugView의 stream/app 정보 비교. 운영 앱 없이 dev 사용만 발생시킨 날 지표 확인.
- **수정 방법:** flavor별 FirebaseOptions를 선택하고 테스트용 Firebase 프로젝트/컬렉션·분석 스트림을 분리한다. 운영 DAU 산출에는 debug/dev 제외 기준을 둔다.

### [Medium] M14 — Firestore 공유 문서 크기·유효성·비용 제한이 없음

**판별:** 잠재적 위험

- **코드 위치:** [lib/services/friend_sync_service.dart:126](C:/Users/Administrator/Desktop/shiftbell/lib/services/friend_sync_service.dart:126), [firestore.rules:29](C:/Users/Administrator/Desktop/shiftbell/firestore.rules:29), [lib/providers/health_tip_provider.dart:22](C:/Users/Administrator/Desktop/shiftbell/lib/providers/health_tip_provider.dart:22), [lib/providers/friend_provider.dart:105](C:/Users/Administrator/Desktop/shiftbell/lib/providers/friend_provider.dart:105)
- **발생 조건:** 오래 누적된 assignedDates, 잦은 근무·근로시간 수정, 많은 친구, 공유 URL에 대한 반복 요청.
- **사용자 증상:** 문서 크기 제한에 도달하면 공유 갱신이 중단된다. 운영 read/write 비용과 앱 조회 지연이 예상보다 커질 수 있다.
- **원인:** 모든 assignedDates를 한 문서에 전체 set하고 공유되지 않는 shiftDurations 변경도 upsert를 유발한다. rules는 owner만 검증하고 필드/크기 검증이 없다. 공개 get 및 health_tips list에 서버 측 남용 방어가 보이지 않는다. 친구 N명 갱신 중 매번 전체 로컬 친구 목록도 다시 읽는다.
- **재현 방법:** 공유 문서가 커지는 장기 데이터와 대량 친구 fixture로 요청 수/문서 크기 측정. 네트워크 재시도·반복 URL 조회 비용은 운영 콘솔에서 별도 확인.
- **수정 방법:** 공유 데이터가 달라졌을 때만 쓰고 debounce/revision을 적용한다. 날짜별/기간별 구조·입력 제한·보안 규칙 스키마 검증을 설계한다. 공개 읽기는 제품 특성이므로 일괄 차단 대신 캐싱·남용 대응·비용 알림을 둔다.

Firestore에는 문서 크기 등 한도가 있으므로 무제한 단일 문서 증가는 장기 운영 위험이다. 운영 비용의 실제 규모는 콘솔 접근 없이 산정하지 않았다. [Firestore 한도](https://firebase.google.com/docs/firestore/quotas).

### [Medium] M15 — 현재 SDK에서 웹 친구 뷰어 진입점의 정적 분석 오류

**판별:** 정적 분석 재현; 웹 release build 미실행

- **코드 위치:** [lib/web_main.dart:29](C:/Users/Administrator/Desktop/shiftbell/lib/web_main.dart:29)
- **발생 조건:** 현재 개발 SDK로 웹 친구 뷰어를 검사/빌드.
- **사용자 증상:** 전체 정적 분석이 실패하며 웹 배포 파이프라인에도 문제가 생길 가능성이 있다. Android 앱 빌드 실패로 확대해 해석해서는 안 된다.
- **원인:** dart:js_util import를 현재 analyzer가 찾지 못한다. flutter analyze에서 uri_does_not_exist가 실제 출력됐다.
- **재현 방법:** flutter analyze --no-pub 실행. 웹 빌드는 별도로 flutter build web -t lib/web_main.dart로 최종 검증해야 한다.
- **수정 방법:** 지원되는 dart:js_interop/package:web으로 전환하거나 해당 코드를 지원하는 SDK 기준을 명확히 고정한다. Android와 웹 진입점 검증을 분리한다.

### [Medium] M16 — MediaPlayer 초기화 실패 시 객체가 해제되지 않을 수 있음

**판별:** 잠재적 메모리·네이티브 리소스 누수

- **코드 위치:** [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:201](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:201), [android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:258](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/kotlin/com/hwani1103/shiftbell/AlarmPlayer.kt:258)
- **발생 조건:** 사운드 URI 접근·prepare·start가 예외를 던지며 알람 재시도가 반복.
- **사용자 증상:** 오디오 리소스가 누적되거나 후속 알람 재생도 실패할 수 있다.
- **원인:** mediaPlayer = MediaPlayer().apply {...}에서 apply가 실패하면 필드 대입 전에 새 객체를 잃는다. catch에서 그 로컬 객체를 release하지 않고 다른 플레이어로 폴백한다.
- **재현 방법:** MediaPlayer prepare/setDataSource 실패를 주입해 생성·release 횟수 비교. 실제 손상/접근 불가 URI에서도 반복 측정.
- **수정 방법:** 생성 객체를 로컬 변수로 소유한 뒤 실패 시 finally/catch에서 release한다. 성공한 객체만 필드에 넘기고 폴백 전에 이전 리소스를 정리한다.

### [Low] L01 — 배포 설정에 테스트 광고와 디버그 광고 배경이 남음

**판별:** 설정 확인

- **코드 위치:** [lib/constants/ad_config.dart:18](C:/Users/Administrator/Desktop/shiftbell/lib/constants/ad_config.dart:18), [android/app/src/main/AndroidManifest.xml:230](C:/Users/Administrator/Desktop/shiftbell/android/app/src/main/AndroidManifest.xml:230)
- **발생 조건:** 현재 설정 그대로 prod release를 배포.
- **사용자 증상:** 테스트 광고만 요청하고 광고 실패/비표시 시 '광고 영역 Ndp' 디버그 표시가 사용자에게 보일 수 있다.
- **원인:** kBannerAdUnitId가 테스트 ID이며 kAdSlotDebugTint=true다. Manifest AdMob app ID도 테스트 값이다.
- **재현 방법:** prod release에서 광고 로드 성공/실패 두 경우 확인.
- **수정 방법:** 출시 광고 운영 여부를 결정하여 prod 전용 ID/디버그 표시 조건을 설정한다. 실제 광고를 아직 운영하지 않을 계획이면 수익 미발생은 의도된 상태로 문서화한다.

### [Low] L02 — 핵심 기능 Analytics 이벤트가 없어 알람 실패·사용 흐름을 측정할 수 없음

**판별:** 계측 누락 확인; DAU만 목표라면 기능 결함 아님

- **코드 위치:** [lib/services/firebase_bootstrap.dart:20](C:/Users/Administrator/Desktop/shiftbell/lib/services/firebase_bootstrap.dart:20), [lib/main.dart:312](C:/Users/Administrator/Desktop/shiftbell/lib/main.dart:312)
- **발생 조건:** 출시 후 알람 생성·수신·해제·권한 거부·백업 실패·탭 사용을 분석하려는 경우.
- **사용자 증상:** DAU와 세션 지표가 있어도 실제 알람 성공률·복구 실패율·어느 탭이 쓰이는지 판단할 수 없다.
- **원인:** Analytics 초기화 외 logEvent/logScreenView 및 Navigator observer 호출이 없다. 네이티브 알람 이벤트도 없다. 자동 이벤트가 있다는 사실을 수동 이벤트 누락과 구분해야 한다.
- **재현 방법:** DebugView에서 생성→수신→스누즈→해제→백업 실패를 수행하고 이벤트 목록 확인.
- **수정 방법:** 제품에서 필요한 최소 이벤트 계약을 정한다. 원본 근무명/메모/수면 상세는 보내지 않고 익명화된 성공 여부·오류 범주만 사용한다. Dart/네이티브 중 이벤트별 기록 주체와 중복 제거 키를 정한다.

Analytics SDK의 자동 이벤트와 제품별 사용자 정의 이벤트는 별개다. 자동 DAU/세션 수집이 전혀 없다고 단정하지 않았다. [Firebase Analytics 시작 가이드](https://firebase.google.com/docs/analytics/get-started?platform=flutter).

## 요청한 26개 관점의 확인 결과

| 관점 | 주요 연결 항목 |
|---|---|
| 1. 실제 사용자 버그 | C01/C02, H01~H19 |
| 2. 최근 수정 회귀 | 위 최근 변경 표, H07/H08/H16/H18 |
| 3. 상태관리·비동기·race | H09/H10/H13, M01/M02/M07 |
| 4. lifecycle·background/foreground | H01/H02/H03, M02 |
| 5. notification·alarm | H01~H10, H15/H19 |
| 6. timezone·날짜·자정 | H07, M02/M03/M06 |
| 7. Firestore | H13/H14, M08/M09/M14 |
| 8. Analytics 누락·중복 | H18, M13/L02. 수동 이벤트 중복은 확인되지 않음 |
| 9. 로컬/서버 동기화 | H13/H14, M08/M09 |
| 10. 삭제·재설치·로그아웃·기기변경 | C02, H05/H06/H14. 일반 로그인/로그아웃 UI는 없고 익명 Auth 사용 |
| 11. 권한 거부·변경 | H02/H15, H06/H08 |
| 12. Android 버전 호환성 | C01, H02/H03/H04/H15. 아래 잔여 검증 참고 |
| 13. 메모리 누수 | M16. 반복 observer/animation 해제 경로는 여러 곳에서 확인됐으므로 '전부 누수'로 단정하지 않음 |
| 14. 예외·crash | H15/H16/H17, M10/M15/M16 |
| 15. null·empty·malformed | H11/H16/H17, M06/M11. 백업에 대한 모델 수준 검증 부족 포함 |
| 16. migration·기존 사용자 | C01, H05/H06/H16/H17 |
| 17. 디스플레이·레이아웃 | M12 |
| 18. 네트워크 불안정 | H13, M08/M09/M11 |
| 19. notification permission 없음 | H02/H15. 권한 없이도 모든 표시/제어가 가능하다고 가정하면 안 됨 |
| 20. 배터리 절약·background 제한 | H03/H04, M05 |
| 21. UI edge case | H01/H11/H12/H19, M07/M12 |
| 22. 성능 | M10/M12/M14 |
| 23. 보안·개인정보 | H13/H14/H17/H18, M09/M14 |
| 24. Firebase read/write·비용 | M08/M13/M14 |
| 25. release 전용 위험 | H02/H03/H04, M13/M15, L01. prod release 직접 검증은 미수행 |
| 26. 핵심 목적 충족 | C01/C02, H01~H10/H15/H19 — 기상·일정 알림 신뢰성 및 복구 후 일관성 |

## 버그로 과장하지 않은 사항과 추가 확인 필요 사항

- **SCHEDULE_EXACT_ALARM + USE_EXACT_ALARM:** 두 권한을 함께 선언했다는 이유만으로 중복 버그라고 판정하지 않았다. 실제 OS·target·설치 상태에 따라 exact permission 조건을 분리해야 한다. Android 13 이상에서 USE_EXACT_ALARM 경로가 있음을 고려하여 '모든 신규 설치가 exact alarm 거부 상태'라고 쓰지 않았다. [Android 알람 예약 안내](https://developer.android.com/develop/background-work/services/alarms).
- **부팅 이벤트:** Manifest는 LOCKED_BOOT_COMPLETED만 처리한다. 일반 BOOT_COMPLETED·MY_PACKAGE_REPLACED·정확한 알람 권한 허용 이벤트에 독립적인 복구 핸들러가 없다. 플랫폼에서 현재 동작하는 경로까지 전부 실패한다고 단정하지 않았지만, 기기별 부팅·업데이트·권한 재허용 매트릭스 검증이 필요하다.
- **공유 읽기 공개:** ownerId를 아는 사람에게 get을 허용하는 것은 링크 공유의 의도된 권한 모델이다. 그 자체를 인증 우회 취약점으로 세지 않았다. 다만 공유 중지·유출 링크 폐기·익명 소유권 상실은 별도 문제다.
- **Firebase 클라이언트 API key:** 앱에 포함된 Firebase client options를 곧바로 서버 비밀키 유출로 판정하지 않았다. 실제 보안은 rules/Auth/제한 설정과 대조해야 한다.
- **운영 rules:** 저장소 rules를 읽었을 뿐 실제 프로젝트에 동일한 rules가 배포돼 있다고 확인하지 않았다.
- **백업 파일:** 다운로드의 JSON은 암호화되지 않은 근무·메모·수면·친구 캐시를 포함한다. 임의의 모든 앱이 곧바로 읽을 수 있다고 단정하지는 않는다. 공유 저장소 접근 권한을 가진 앱·파일 관리자·사용자 파일 공유에 대한 노출 경로와 삭제 후 잔존을 안내해야 한다. [Android 공유 저장소](https://developer.android.com/training/data-storage/shared).
- **재설치 백업 자동 탐지:** 이전 설치가 만든 Downloads 파일은 현재 앱 소유 파일과 동일하게 자동 접근된다고 보장할 수 없다. 현재 SAF 수동 불러오기 경로는 유효한 완화책이다. 도움말의 '자동으로 나타난다' 보장은 실제 지원 범위에 맞춰야 한다. [Android 공유 미디어 접근](https://developer.android.com/training/data-storage/shared/media).
- **Android 10 미만 백업:** 코드가 명시적으로 미지원(false/null) 처리한다. 이를 새로운 crash 버그로 세지는 않았다. 지원 OS 사용자에게 백업 불가가 명확히 안내되는지 확인이 필요하다.
- **빈 패턴/손상된 시간:** ShiftSchedule의 modulo 전에 empty 검사, 알람 템플릿의 int.parse/toInt 앞 값 검증이 부족하다. 정상 UI에서 반드시 만들어지는 입력이라고 단정하지는 않았으며, 백업 import 검증(H17)에 패턴 길이·시각 범위·참조 ID·수면 시작/종료 순서를 포함해야 한다.
- **스타트업 실패:** main()은 runApp 전에 DB·Firebase·광고 준비를 수행한다. 일부 초기화에 상한 시간/복구 UI가 부족하고 GoogleFonts의 unawaited Future 오류 처리도 점검 대상이다. 이번에는 실기기 무한 스플래시/crash를 재현하지 않아 확정 건수에 추가하지 않았다.
- **오래된 이력:** 영구 보존 요구는 존중해야 한다. 성능 해결은 페이지 조회·인덱스·백업 처리 개선이며 임의 자동 삭제가 아니다.

## 수정 승인 후 권장 순서

1. **알람 유실·잘못된 울림 방지:** C01, H01~H10, H15, H19. DB 원본·OS 예약·실행 중 알람을 공통 revision/상태 모델로 맞춘다.
2. **백업·복구 신뢰성:** C02, H05/H06/H17, M10/M11. 기존 사용자 백업 파일을 보존한 채 검증하고 원본/파생 분리를 바로잡는다.
3. **공유 통제·migration·입력:** H11~H14/H16/H18. 이름 변경은 데이터 호환성까지 포함해 수정한다.
4. **상태·날짜·출시 설정:** M01~M16, L01/L02. 날짜·provider 통합 검증과 운영 Firebase 구성을 마무리한다.

개별 버그를 모두 'try/catch로 무시'하는 수정은 피해야 한다. 실패를 사용자에게 전달하고 재시도 가능한 상태로 남기는 것이 핵심이다.

## 출시 전 반드시 통과시킬 시나리오

1. 실제 이전 배포 DB → 최신 앱 교체 설치 → 앱 미실행 → 재부팅·첫 잠금 해제 전 알람.
2. 알림/오버레이/전체화면/exact 권한 허용·거부·재허용을 분리한 알람 수신·끄기·스누즈.
3. Doze에서 5분 간격 알람, 5분 스누즈, 수면감지·일정 알림 동시 예약.
4. 알람 중 홈 이동·화면 잠금·다른 알람 삭제·프로세스 회수 후 종료시간/이력 정합성.
5. 규칙/불규칙·개별 수정·스누즈가 있는 백업의 신규 설치 복원 및 기존 데이터 덮어쓰기.
6. 백업 중 종료·저장공간 부족·malformed import·설정 쓰기 실패 이후 재시도와 정상 백업 보존.
7. 시간대 변경·DST 양방향·자정·연말, 근무 '전날/당일/다음날' 알람과 일정 알림 동시 검증.
8. 근무명 쉼표·sentinel·맞교환·중복 이름, 빠른 날짜 근무/출퇴근시간 연속 수정.
9. 두 기기의 공유 시작→수정→오프라인→중지→복구, 재설치 후 기존 링크 상태.
10. prod release에서 360/600/840dp·큰 글자·폴더블 변경, Firebase 스트림·개인정보 안내·광고 실패 화면.

**최종 판단:** 단위 테스트 통과와 별개로 C01/C02 및 알람 실행·복원 High 항목을 해결하고 위 시나리오를 검증하기 전에는 출시 안전성을 보장하기 어렵다. 이 보고서는 수정 제안이며, 사용자 승인 전 앱 코드를 변경하지 않는다.

