# G1 handoff

```text
그룹 / 담당 / worktree / branch: G1 / Claude(원 구현), Codex(T11-FIX 사용자 승인 예외) / C:\Users\Administrator\Desktop\shiftbell-g1 / release/g1
공통 기준 SHA / 최종 소스 SHA: 94ff08f / e6e679a (CODE_FROZEN 2026-09-14; T10 대상 2b82adc 뒤 T11-FIX `438a9b9`, `e6e679a`)
상태: CODE_FROZEN `e6e679a` — T11 High 5·Medium 3 전부 FIXED. T11-FIX는 Codex 수정(사용자 승인 예외). Claude의 가벼운 diff 확인 뒤 T12 진행
수정한 감사 번호: #3 #4 #5 #10 #11 #12 #13 #14 #16 #17(G1 Kotlin 핵심) #20(예약 API setAlarmClock 통일 — S3 미실측) #23 #26 #27 #28 #31(실행·D11·D12) V6(수신 코드 — Manifest는 연결 요청) + contracts §3 변경 통지 연결. V7은 코드 변경 없음(검증 항목). H03-GATE는 실기기 S4 필수(코드 미변경).
실제 변경 파일(소유권 범위 검사 결과): 59개(94ff08f..e6e679a), 그중 T11-FIX 22개(2b82adc..e6e679a). §3.1 G1 목록 — Alarm*.kt·CustomAlarmReceiver·RingingAlarmTracker·DirectBootReceiver·ScheduleNotification*·MainActivity·NotificationHelper(contracts §7 메모상 G1)·alarm/schedule/date_schedule/tab_visibility/work_hours_settings/condition_shift_time provider·calendar/next_alarm/onboarding/schedule_management/settings/work_hours_settings 화면·alarm_generation/alarm/schedule_notification 서비스·database_service(CRUD/트랜잭션)·shift_schedule 모델·테스트. 목록 밖은 확정된 연결 요청만 사용: ARB 2파일(#2), permission_service·permission_warning_banner(#3), CalendarWidgetScheduleResolver(#4), overtime_provider(#6). release/g2·g3 변경 경로와 교집합 0건. DB 스키마·migrations·DatabaseHelper·Gradle·main.dart 미변경. Manifest는 T10 `2b82adc` 연결분이며 T11-FIX에서는 미변경.
공통 파일/API/ARB 연결 요청 및 반영 SHA: g1/integration_requests.md #1~#6(Manifest intent-filter 3개, ARB 키 5개, 소유권 3건, contracts §7.2 키 5개) — 반영 SHA는 T10에서 기록. G3 요청 #1(revision 성공 통지)은 9b8a586로 G1 쪽 구현 완료.
사용자 결정 확인(D 번호·선택·일시): D1~D12 전부 확정(2026-09-14, 실행계획 §4). 적용 — D1 P1(#26), D6 로그만(#11), D10 override(#31), D11 30일 정리, D12 원본 변경 시 삭제. REPAIR-REJECT=A(G0). PROVISIONAL / G0 실기기(T04-D) 미통과 상속
최종 동작과 회귀 금지 규칙 준수: 알람 이력·생성 로그 자동 삭제 없음(D11은 alarm_overrides만), DB 스키마 미변경(G0 v24 계약 사용), 트랜잭션 안 OS 호출 없음(contracts §1.4), 기상 알람 예약 경로 단일화, Dart/Kotlin 생성 계산을 같은 fixture로 비교, 기존 근무명 자동 변환 없음(D6), 가벼운 일정 알림은 기상 알람 정책과 분리.
추가/변경 테스트와 실제 실행 명령: 기존 G1 테스트에 T11-01~08 재현을 추가하고 `g1_alarm_regeneration_diff_test.dart` 및 양쪽 `review_fixture_additions.json` 어댑터를 추가했다. T11-FIX 수정 전 FAIL: 권한 unknown 2건 true, extra 없는 옛 일정 표시, `day_offset=0` 잔존, DB 조회 실패 뒤 stale 예약/스누즈 취소, 재생성 ID 1201→1202, 무음 전환 뒤 진동 잔존, 탭 sync 예외 은폐, revision 주입점 부재 컴파일 오류. 수정 후 같은 표적 테스트 전부 PASS.
테스트 결과(실행 빌드·환경·expected/actual·증적): `sh ./gradlew testDevDebugUnitTest` 전체 PASS, `flutter test` 전체 211/211 PASS, `flutter analyze`는 기존 warning/info를 포함해 exit 1이며 유일한 error는 허용된 `lib/web_main.dart`의 `dart:js_util` 1건, `flutter build apk --flavor dev --debug` PASS(`build/app/outputs/flutter-apk/app-dev-debug.apk`). 각 Flutter 명령 전에 `sh ./gradlew --stop` 실행. 에뮬레이터·실기기·설치·실행 없음. 공식 기기 판정은 T12에서 계속한다.
미실행 실기기/Console 항목: S1(V6 실제 OS 수신 — Manifest 반영 후), S2(#4·#13 권한 조합·제어 알림, 삼성), S3(#20 setAlarmClock 수신 시각·스누즈·다음 알람 표시), S4(#3·#14 홈/잠금/Doze 자동 종료·진동/무음 삭제, H03-GATE 재생 유지), S5·S15(#26 앱 미실행·자정·재부팅), S7(#10 시간대), S8(#11·#12·#28), S11(#5 탭 숨김·재부팅), S13(#27 경합), S16(#31 개별 삭제·타입 변경 유지). T04-D 삼성 ①② 상속.
잔여 위험·다음 그룹 주의사항: (1) 화면·오버레이 종료 타이머는 회차 토큰으로 보호한 채 유지 — S4 후 제거 판단 (2) 알림 권한까지 거부 시 제어 수단은 화면·오버레이·자동 종료뿐(#4) (3) 수신 이벤트 자체 지연·누락은 수신 검사로 못 막음(#27), 재시도는 다음 트리거 의존 (4) 엔진 쓰기 잠금 동안 Flutter 저장 대기 체감 미측정 (5) 제어 알림·스누즈 알림 문구는 네이티브 한국어 하드코딩(G5 현지화 판단) (6) #13 전부 실패 시 일괄 배정 catch 문구가 근무 저장 사실을 안 드러냄, 온보딩 초기 알람 등록 루프 실패 처리 없음 (7) 수면 계열(G3) Kotlin의 Locale은 G3 #17 범위 — T10 통합 뒤 확인 (8) 백업 복원(G4)은 alarm_overrides·alarm_state 신규 키·schedule_tab_enabled 동기화·변경 통지를 고려해야 함 (9) TEST_CHECKLIST.md 1.2·3.6절(불규칙 자동 생성 안 됨)·CLAUDE.md 알람 설명(예약 헬퍼·P1·override)은 후속 문서 단계에서 갱신 (10) 백업 import 근무명 검증은 G4(#19) (11) T11-02의 extra 없는 레거시 일정 예약은 시각 동일성을 증명할 수 없어 fail-closed로 폐기됨 (12) T11-04 DB 재조회 실패 때 유효할 수 있는 기존 OS 예약은 유지되고 실패 목록의 후속 retry에 의존 (13) 에뮬레이터·실기기 전달/Doze/OEM 동작은 T12와 S 시나리오 미검증.
```

## 착수 전 인계 메모 (T06 시작 전, Claude)

- **T04-E 케이스 ② 관찰(2026-09-14, 에뮬레이터 API 33):** 업데이트 후 재부팅 → 첫 잠금 해제 전 06:10 알람에서 같은 알람 ID 1의 `CustomAlarmReceiver 알람 수신`이 0.47초 간격으로 **2회**(재생도 2회 시작). 재부팅 없는 케이스 ①은 1회. 추정: `DirectBootReceiver`의 "가장 가까운 알람 즉시 등록"(`scheduleNativeAlarm`)과 `AlarmRefreshEngine.refresh` 재등록이 서로 다른 PendingIntent(extras/flags/requestCode 차이)로 같은 시각에 예약을 2개 만듦. 증거: `scratchpad/t04e/log_case2_after_boot.txt`, `g0/test_results.md` 8장. → **#20(기상 알람 예약 경로 setAlarmClock 통일)·#14/#3(회차) 작업 때 모든 예약 경로의 PendingIntent 동일성 확인·중복 수신 테스트 추가.**
- T11a 기대값: `docs/release_audit/g1/fixtures_draft_p1/` (Codex, contracts §2만 근거) — Dart/Kotlin 공통 계산 테스트 입력으로 사용.

## 진행 기록 (끊김 대비 — 하위 작업마다 한 줄)

| 일시 | 담당 AI | T | 감사 번호 / 하위 작업 | 커밋 SHA | 다음에 할 일 |
|---|---|---|---|---|---|
| 2026-09-14 | Claude | T06 | 착수: 기준 94ff08f, g1 pub get 완료. 첫 묶음 #3·#14·#23 코드 읽기 완료. **원인** #3 종료 타이머가 Activity/Overlay Handler에만 있어 홈 이동 시 소실, 트래커가 ID만 저장(회차 없음) / #14 isAlarmRinging=MediaPlayer.isPlaying 전역 bool(ID 무시, 진동·무음 false) / #23 MediaPlayer().apply 중간 실패 시 미해제. **설계** RingingAlarmTracker에 누적 ring_counter + active(ID,회차) + 메모리 토큰 폴백(commit 실패) · CustomAlarmReceiver가 울림 시 회차 담은 AlarmManager 종료 예약(AlarmActionReceiver ACTION_RING_TIMEOUT) · 종료/버튼은 회차 일치 시만 · 화면/오버레이 타이머는 토큰 보호한 채 유지(S4 전 제거 금지) · isAlarmRinging(alarmId) = tracker 기준 · MediaPlayer 로컬 소유 후 성공 시만 필드 대입 | - | Dart alarm_provider 삭제 흐름·MainActivity 핸들러 확인 → 구현 |
| 2026-09-14 | Claude | T06 | **#3·#14·#23 구현 완료(WIP).** #3: `RingingAlarmTracker` 누적 `ring_counter` + 활성(ID,회차), lock 직렬화, commit+1회 재시도 실패 시 메모리 회차 유지(ID 단독 비교 없음), 이전 버전 ID-only 값은 회차 0. 모든 종료 경로(Activity/Overlay 버튼·타이머, 7777 끄기, 인계, 종료 예약)가 소리/DB 전에 `AlarmActionHelper.claimRingEnd(ID,회차)` 통과. `CustomAlarmReceiver`가 울릴 때 `ACTION_RING_TIMEOUT`(data `shiftbell://ring-timeout/ID/회차`) exact 예약(권한 없으면 비정확 대체), 화면·오버레이 타이머는 토큰 보호로 유지(S4 후 제거 판단), 지난 회차 화면 요청은 즉시 닫음, 같은 ID 중복 수신은 이전 회차 예약 회수 후 새 회차. `finishUp`의 ID 기준 추적 해제 제거. #14: MethodChannel `isAlarmRinging(alarmId)`=활성 회차 ID 비교(진동·무음 포함), 신규 `stopRingingAlarm(alarmId)`=판정+종료 원자화 → Dart `deleteAlarm`이 반환값으로 swiped/cancelled_before_ring 결정, calendar_tab/next_alarm_tab의 선행 `dismissOverlay` 제거(선행 시 이력 오판). 부수 수정: 오버레이 외부 종료 신호가 새 알람 재생 뒤 도착해 새 소리를 끄던 경로 차단(활성일 때만 stop). #23: `startLoopingPlayer` 지역 생성·실패 시 release·성공 후 대입, `AlarmPlayer.isAlarmRinging()` 제거. **테스트** `G1RingRoundTest` 10건 + 기존 23건 = Kotlin 33/33 PASS(`sh ./gradlew testDevDebugUnitTest`, Robolectric SDK34). flutter analyze(변경 3파일) 신규 경고 0. **신규 Native 키(등록부 반영 요청)**: `alarm_state.currently_ringing_round`(long, G1, 백업 안 됨, 없으면 회차 0), `alarm_state.ring_counter`(long, G1, 백업 안 됨, 절대 초기화 안 함). **미검증**: 화면/오버레이 실제 동작·홈/잠금/Doze 지속시간·진동/무음 삭제 = S4 실기기. Dart deleteAlarm 단위 테스트 없음. **수정 전 FAIL 확인**: `AlarmPlayer.kt`만 94ff08f로 되돌려 G1RingRoundTest 실행 → `#23 재생 준비 실패 해제` FAIL(`expected:<END> but was:<IDLE>`), 복원 후 PASS. #3/#14 테스트는 수정 전 API가 없어 컴파일 단계에서 성립 안 함(동작 비교는 S4) | `97c72fc` | #4/#13 착수 |
| 2026-09-14 | Claude | T06 | **#13 구현(WIP).** 원인: ① `regenerateAlarmsAroundDates`의 전부 실패 조건 `failCount>0 && scheduled.isEmpty`가 항상 거짓(전부 실패해도 성공 안내) ② `ScheduleNotificationScheduler.schedule()`이 실패를 로그로만 삼키고 채널은 `success(null)` ③ `checkExactAlarmPermission` 채널 오류 → true(허용 둔갑) ④ `changeShiftWithAlarms` 첫 등록 예외가 나머지 등록 전부 중단. 수정: `AlarmScheduleOutcome(attempted, failed)`(alarm_service.dart)로 전부/일부 판정 단일화 → 전부 실패는 예외, 일부 실패는 결과 반환 → 달력 일괄 배정 스낵바 `alarmSchedulePartialFailed`. `schedule()` Boolean 반환 → 채널 `scheduleDateNotification`이 bool 반환, `scheduleNativeAlarm` 실패는 `result.error(SCHEDULE_FAILED)`. `syncForSchedule` → bool(알림 불필요·과거 시각은 true, 날짜 형식 오류·Native false/null·채널 오류는 false) → `dateScheduleProvider.create`는 `({saved, notifyScheduled})`, `update`는 bool → 일정관리 탭 스낵바 `scheduleNotifyRegisterFailed`. `PermissionService.checkExactAlarmPermissionState()` granted/denied/unknown 신설, 경고 배너는 unknown을 "정확한 알람(확인 불가)"로 표시. 기존 `checkExactAlarmPermission()`(bool)은 unknown=통과 유지 — 권한 소개/복원 화면이 막히지 않게(두 화면은 G1 목록 밖이라 수정 안 함). `changeShiftWithAlarms`는 개별 try·실패 수 로그(화면 호출부 `_changeShift` 미사용). **소유권/연결 요청(통합 담당자 확인)**: ① §3.1 목록 밖 `lib/services/permission_service.dart`, `lib/widgets/permission_warning_banner.dart` → G1 배정 요청(release/g2·g3 동결 변경 경로와 교집합 0건 확인: `git diff --name-only c0b2e73 release/g2·g3`) ② ARB 신규 키 3개(ko/en): `alarmSchedulePartialFailed{failed,total}`, `scheduleNotifyRegisterFailed`, `permissionStatusUnknown{name}` — G0 선례대로 그룹 브랜치에 추가, 통합 시 확인. **테스트** Dart `test/release_audit/g1/g1_schedule_failure_test.dart` 16/16 PASS(`flutter test test/release_audit/g1`), Kotlin `G1ScheduleResultTest` 2건(성공 true / `setCanScheduleExactAlarms(false)` → false) (첫 작성은 `ShadowAlarmManager.setCanScheduleExactAlarms(false)`로 거부를 흉내 냈으나 Robolectric은 예외를 안 던져 FAIL → SecurityException을 던지는 mock AlarmManager를 ContextWrapper로 주입해 수정). Kotlin 전체 35/35 PASS. 수정 전 FAIL 비교는 반환형이 바뀌어 컴파일 단계에서 성립 안 함. flutter analyze 변경 파일 신규 경고 0(불필요한 `!` 1건 즉시 제거). **잔여**: 전부 실패 시 일괄 배정 catch 문구가 "근무 할당 실패"라 근무는 이미 저장됐다는 사실이 안 드러남 / `onboarding_screen` 초기 알람 등록 루프는 개별 실패 처리·안내 없음(첫 실패 예외로 중단) — 다음 연결 단계에서 판단 / 권한 재허용 후 회복·스낵바 표시는 E/D S2 | `47909d3` | #4 |
| 2026-09-14 | Claude | T06 | **#4 구현(WIP).** 원인: 끌 수단이 화면뿐 — 잠금 상태 백그라운드 Activity 실행이 조용히 막히면 폴백 미실행, 오버레이 권한 없을 때 폴백 알림(alarmId+100000)엔 버튼·탭 동작 없음, 7777은 홈 버튼 때만 게시. 수정: `NotificationHelper.showRingControlNotification()` — `CustomAlarmReceiver`가 소리 재생 직후·화면 시도 전에 7777 게시(ongoing·silent·CATEGORY_CALL·그룹·localOnly·BigTextStyle 유지, 채널 `shiftbell_alarm_v3` HIGH, 전체화면+탭 = AlarmActivity(회차 포함), 버튼 "5분 후"/"알람 끄기", 치우기=끄기). 게시 전 `areNotificationsEnabled`·`canUseFullScreenIntent`(34+)·채널 IMPORTANCE_NONE 로그. 제거는 끄기·스누즈·인계·타임아웃에서만(오버레이 스누즈·외부 종료에 7777 제거 추가, 화면이 떠도 유지). `ACTION_SNOOZE_FROM_NOTIFICATION` 신설(회차 관문→stop→closeRingUi→snooze→8889), 알림 끄기는 `closeRingUi`로 화면·오버레이까지 닫음, 지난 회차 끄기는 7777을 지우지 않음(새 울림 알림 보호). 옛 `showNotification` 폴백 제거. `AlarmActivity.visibleRing`으로 전체화면 인텐트가 이미 띄운 회차 화면은 0.5초 뒤 재실행 안 함, 홈 버튼 시 같은 빌더로 재게시. **테스트** G1RingRoundTest #4 3건(게시 내용·버튼 회차 / 5분 후 지난 회차 무시·현재 회차 snoozed+이력+7777 제거 / 끄기 swiped 1건·중복 신호 무시) — Kotlin 전체 38/38 PASS. **미검증(S2 필수)**: 삼성 실기기 권한 조합(알림·전체화면·오버레이 on/off)별 표시·버튼·잠금 상태 FSI, 해제 상태에서 heads-up+오버레이 시각 중복 정도, Android 14+ ongoing 알림 스와이프 시 끄기 처리. **잔여**: 알림 권한까지 거부면 제어 수단은 화면·오버레이·#3 자동 종료뿐 / 알림 문구 "알람 울림 중"·"5분 후"·"알람 끄기"는 기존 7777처럼 네이티브 한국어 하드코딩(영어 미지원, G5 현지화 판단) | `15a48ad` | #16/#27 착수 |
| 2026-09-14 | Claude | T06 | **#16/#27 설계(코딩 전 기록).** 코드 확인 결과: ① `AlarmRefreshEngine.doRefresh`가 근무표·템플릿·기존 알람을 트랜잭션 **밖**에서 읽고, 트랜잭션 **안**에서 OS 예약 → N번째 예외가 앞선 insert 전부 롤백(H2 테스트가 고정해둔 현재 동작) ② `AlarmActionHelper.snooze/dismiss`도 DB 트랜잭션 전에 OS 취소·예약 ③ **착수 전 메모의 재부팅 중복 수신 원인 확정**: `DirectBootReceiver.scheduleNativeAlarm`만 Intent에 `setPackage()`를 넣어 `Intent.filterEquals`가 달라짐 → 엔진·MainActivity·Guard·스누즈의 PendingIntent와 **다른 예약**이 됨(같은 시각 2개 → 0.47초 간격 2회 수신, 엔진의 취소도 그 예약을 못 지움) ④ 예약 코드가 6곳(엔진·MainActivity·AlarmActionHelper·Guard 재등록·DirectBoot·엔진 불규칙 재등록)에 복제. **설계**: 신규 `AlarmWakeScheduler.kt`(Alarm*.kt, G1) 한 곳에서 Intent 생성(동일성 보장)·예정 시각 extra `expectedAtMillis`(초 단위 정규화)·예약/취소. 엔진은 잠금 트랜잭션 안에서 읽기→계산→DB 쓰기 → 커밋 → OS 반영. 반영 직전 재확인: 예약은 행 존재+시각 일치일 때만, 취소는 행 없으면 항상·있으면 DB 시각 재예약. 실패는 `alarm_state.os_sync_failed_alarm_ids`(StringSet, G1, 백업 안 됨, 성공 시 제거)에 기록 → 엔진 시작·Guard·앱 재개에서 DB 기준 재시도. `CustomAlarmReceiver`는 행 없음=안 울림, 예정 시각 일치=울림, 불일치+DB 미래=안 울리고 DB 시각 재예약, 불일치+DB 과거=울림, extra 없음·파싱 실패·DB 불가=울림. Guard는 20분 이내 다음 알람을 존재 여부 판정 없이 DB 시각으로 항상 재등록. 스누즈 새 시각은 초 단위로 정규화해 DB 문자열과 일치시킴 | - | 테스트 작성 → 헬퍼·엔진·수신기·호출부 교체 |
| 2026-09-14 | Claude | T06 | **#16/#27 구현(WIP) + #20 예약 API 통일 선반영.** 위 설계대로 `AlarmWakeScheduler.kt` 신설·엔진 `doRefresh` 재구성(읽기·계산·DB 쓰기를 한 트랜잭션, OS 반영은 커밋 뒤 `scheduleIfCurrent`/`cancelIfGone`, 실패 1건이라도 남으면 `last_alarm_refresh` 미기록)·`CustomAlarmReceiver`의 `alarmExistsInDb` → `decideOnReceive`·`AlarmActionHelper` dismiss/snooze OS 반영을 커밋 뒤로(스누즈 시각 초 단위 정규화)·MainActivity `scheduleNativeAlarm/cancelNativeAlarm`·DirectBoot·Guard 재등록(판정 없이 항상 DB 시각)·Guard/`triggerCheck`/엔진 시작에서 `retryFailed`. **#20 관련**: v4 #20 계획이 "등록 지점을 헬퍼 하나로 통일해 `setAlarmClock`"이라 헬퍼를 만들며 같이 적용(DirectBoot가 이미 setAlarmClock이라 헬퍼를 setExact로 두면 재부팅 경로가 퇴보), showIntent=MainActivity(openTab 0), 권한 거부 SecurityException은 비정확 예약으로 걸어둔 뒤 실패 보고. #20의 S3(수신 실제 시각·5분 스누즈·다음 알람 표시) 실측은 미실행. 옛 버전이 남긴 `setPackage` 변형 예약은 같은 ID 예약·취소 시 함께 제거(업데이트 직후 중복 대비). **테스트** `G1WakeSyncTest` 9건(행 없음·일치·불일치+미래 재예약·불일치+과거·extra 없음 / 예약 재확인 / 취소 재확인 / 실패 기록→재시도 / 재부팅 경로 동일성), `AlarmRefreshEngineH2Test`를 수정 동작으로 갱신(OS 실패에도 DB 전부 유지·실패 1건 기록·갱신 완료 미기록) — Kotlin 전체 47/47 PASS. **수정 전 FAIL 확인**: `DirectBootReceiver.kt`만 15a48ad로 되돌리면 재부팅 동일성 테스트 FAIL(`expected:<[t]> but was:<[t, t]>` = 같은 알람 OS 예약 2개, T04-E 케이스 ② 관찰과 일치), 복원 후 PASS. **신규 Native 키(등록부 반영 요청)**: `alarm_state.os_sync_failed_alarm_ids`(StringSet, G1, 백업 안 됨, 성공·할 일 없음 시 제거). **미검증**: 엔진 쓰기 잠금 동안 Flutter 저장 대기 체감(busy timeout)·H09 원본 경합 재현(스냅샷 직후 중단 주입)은 단위 테스트 없음 → S8·S13, 시간대 변경 직후 예정 시각 판정은 #10과 함께 확인. **잔여**: 수신 이벤트 자체 지연·누락은 수신 시 검사로 못 막음(재시도는 다음 트리거 의존), 날짜 문자열 파싱은 아직 `Locale.getDefault()`(#17에서 US 고정) | `2763e04` | #26/#31 |
| 2026-09-14 | Claude | T06 | **#26/#31 A(생성 쪽) 구현(WIP).** `AlarmRefreshEngine`: `computeDesiredAlarms`의 불규칙 조기 `emptyList()` 제거(`resolveShiftType`이 배정일→없으면 "미설정" = Dart `getShiftForDate`와 같음), `doRefresh`의 불규칙 "재등록만" 분기·템플릿 0개 조기 return 제거(0개 = 옛 fixed 정리, 조회 실패는 예외 전파→롤백), `alarm_overrides`를 같은 트랜잭션에서 읽어 계산에 적용(**우선순위로 시각별 알람을 고른 뒤 적용 — skip이면 그 시각을 다른 근무의 같은 시각 템플릿으로 채우지 않음**, set_type은 타입만), activeRing ID는 toRemove에서 제외, custom·snoozed 미래 알람도 커밋 뒤 재등록(예전엔 불규칙에서만 — 규칙 근무의 재부팅 후 누락 갭도 같이 막음), D11: `slot_time <= now-30일` 예외 삭제(이력 테이블 무관), `nowMillis` 주입. `REFRESH_POLICY_VERSION=1`/`alarm_state.refresh_policy_version`(contracts §2 등록 키): `AlarmRefreshUtil`이 낮으면 오늘 갱신 여부와 무관하게 강제 갱신 트리거, `markRefreshed`(성공 시에만)가 기록. Dart `computeDesiredFixedAlarmsForDate(overrides:)`·`regenerateFixedAlarmsForDatesTxn`도 같은 규칙, `alarmSlotTime`(숫자 직접 조립, 초 00)·`alarmSlotKey`·`readAlarmOverrides` 추가. **T11a fixture 반입**: `docs/release_audit/g1/fixtures_draft_p1/*` → `test/release_audit/g1/fixtures/`(SHA-256 동일 확인). **테스트** Kotlin `G1GenerationFixtureTest`(생성 5케이스 = fixture 기대값), `G1GenerationEngineTest` 7건(템플릿 0개 정리+custom/snoozed/activeRing 보존, 조회 실패 전파·무변경, 멱등 2회, 불규칙 창 밖 기여, 엔진 경로 skip/set_type, D11 경계 502 삭제·503 보존·이력 불변, 정책 버전 실패 시 미기록·성공 시 기록·낮으면 강제 트리거) — Kotlin 전체 55/55; Dart `g1_generation_fixture_test.dart`(같은 5케이스 + 창 크기 상수) — Dart g1 22/22, analyze 0건. 첫 실행에서 fixture 테스트가 plain JUnit이라 `org.json` 스텁 NPE → Robolectric 러너로 수정. **남은 #31 B**: 사용자 삭제·타입 변경 시 예외 기록, D12(배정일·템플릿 변경 삭제, rename 반영) — 다음 커밋 | `64a2130` | #31 B |
| 2026-09-14 | Claude | T06 | **#31 B(쓰기 쪽)·D12 구현(WIP).** `DatabaseService`: `deleteAlarm`이 행 삭제 전 같은 트랜잭션에서 skip 예외 upsert, 신규 `updateAlarmTypeWithOverride`(alarm_provider `updateAlarmType`이 사용)가 set_type upsert — 대상은 `type='fixed'`·미래 행만(custom·snoozed·지난 알람·행 없음은 기록 안 함), `slot_time` = `alarmSlotTime`(초 00), `origin_date` = 울림일 − `day_offset`, `origin_shift` = 행 근무명, UNIQUE 슬롯은 `ConflictAlgorithm.replace`. D12: `saveShiftSchedule`/`updateShiftSchedule`를 트랜잭션으로 바꾸고 `deleteOverridesForChangedAssignments`(예외가 가리키는 배정일마다 이전/이후 `getShiftForDate` 비교 → 다르면 삭제 — 날짜 변경·일괄 배정·규칙 패턴 변경을 한 규칙으로), `changeShiftWithAlarms` 트랜잭션에도 재생성보다 먼저 호출. 템플릿: `replaceAllAlarmTemplates`는 근무별 구성(시각·타입·오프셋) 비교 후 바뀐 근무의 미래 예외를 **템플릿 쓰기 전에** 삭제(실패 시 함께 롤백), `insertAlarmTemplate`·`deleteAllAlarmTemplates`도 같은 트랜잭션 삭제. `renameShiftAtomic`은 `alarm_overrides.shift_type`·`origin_shift`를 함께 변경. `resetSchedule`은 예외 전체 삭제. 테스트용 `debugNowOverride`. **테스트** `g1_override_state_test.dart` 11건(삭제 skip 기록·origin 계산 / custom·지난 알람 미기록 / set_type 교체 / D12_ASSIGNMENT_CHANGE·ABA / 근무 불변 저장 시 보존 / D12_TEMPLATE_CHANGE·ABA / 템플릿 저장 실패 주입 시 예외 삭제 롤백 / D12_SHIFT_RENAME / 구성 비교 함수) — `flutter test` 전체 179/179(기존 146 + G1 33), analyze 신규 경고 0(표시된 unused import 3건은 변경 전부터). **잔여**: 근무명 맞바꾸기 A↔B의 UNIQUE 충돌·임시 이름 2단계는 #12, 사용하지 않는 `deleteAlarmTemplate(id)`는 D12 미적용(호출부 없음), 예외가 가리키는 `alarm_type_id`가 삭제된 타입일 때의 재생 fallback은 기존 fail-safe(진동)에 의존 — S16 실기기 확인 | `bef9f58` | #11/#12/#28 |
| 2026-09-14 | Claude | T06 | **#11·#12·#28 구현(WIP).** #11: `shift_schedule.dart`에 `validateShiftName`(빈 이름·길이 10·쉼표·예약어 `kReservedShiftNames`={"미설정","없음"}·중복) — 쉼표는 pattern/shift_types 쉼표 직렬화를 깨고, "미설정"은 배정 없음 내부값, "없음"은 달력 일괄 배정의 해제 선택지 문자열이라 같은 이름 근무를 배정 불가. 입력 지점 전수 확인 결과 2곳(온보딩 추가 대화상자, 설정 근무명 변경 대화상자)에 적용. D6: `invalidStoredShiftNames`로 `_loadSchedule` 때 로그만(데이터 변환 없음). ARB 2개 `shiftNameCommaNotAllowed`, `shiftNameReserved{name}`(연결 요청 목록에 추가). #12: 설정 대화상자가 비활성 근무 포함 전체 목록 기준으로 "변경 후" 이름 중복 검사(맞바꾸기·순환 허용). `renameShiftAtomic` 재작성 — 저장 직전 형식·최종 중복 재검사(`ArgumentError`, 무변경), 트랜잭션 안에서 옛 이름→임시 이름(`__shiftbell_rename_tmp_N__`)→새 이름 2단계, 참조 7곳(alarms·templates·history·creation_log·condition_shift_times·overrides.shift_type/origin_shift) 공통 함수, 대상 이름의 고아 condition/override 행은 교체. 설정 화면은 실패 시 안내 후 상태 미반영. #28: `DatabaseService.saveShiftTimeRange(shift, range?)` — 한 트랜잭션에서 최신 `shift_schedule` 행을 읽어 `shift_durations`의 해당 키만 바꾸고 `condition_shift_times` upsert/삭제(null=되돌리기 → 0). `work_hours_settings_screen`의 저장·되돌리기가 이 함수 사용 후 `scheduleProvider.applyExternallyPersisted`·`conditionShiftTimeProvider.applyExternallyPersisted`(신규)·`dataRevisionProvider(shiftTimes/workHoursSettings)` 증가(contracts §3, 커밋 성공 뒤에만). **테스트** `g1_shift_name_test.dart` 20건(검증 7·D6 2·rename 맞바꾸기/3순환/최종 중복 거부/형식 거부/마지막 단계 실패 주입 롤백·#28 연속 두 근무 저장/되돌리기/실패 롤백/근무표 없음) — `flutter test` 전체 197/197, analyze 신규 경고 0(표시된 unused import 등은 HEAD부터). 첫 실행 4건 FAIL은 테스트 시드가 `alarm_history.scheduled_date` NOT NULL을 빠뜨린 것(제품 결함 아님) → 시드 보완. **수정 전 FAIL 확인**(맞바꾸기 테스트만 임시 파일로 분리, `database_service.dart`만 되돌려 실행 후 복원): ① 94ff08f(원래 코드) → `at location ['alarms']['A'] is 'A' instead of 'B'` = A↔B 맞바꾸기 후 두 근무의 참조가 한 이름으로 합쳐짐(운영 결함 재현) ② bef9f58(#31 B 직후) → `UNIQUE constraint failed: alarm_overrides...`로 rename 자체 실패. 복원 후 PASS. **잔여**: 백업 import 쪽 근무명 검증은 G4(#19) 범위 / 규칙 근무에 이미 쉼표로 쪼개져 저장된 이름은 로그로만 드러남(D6) / 근무명 변경 시 `dataRevisionProvider(shiftSchedule)` 증가 등 다른 저장 경로의 변경 통지 연결은 연결 단계에서 일괄 확인 | `7b56d3c` | #5/#10/#17/V6 |
| 2026-09-14 | Claude | T06 | **#5·#10·#17·V6 구현(WIP).** #5: `ScheduleNotificationScheduler` 예약 Intent에 `schedule_expected_at`(트리거 ms), 수신기는 Intent 내용 대신 `resolveOnReceive` — 탭 사용 중 + 행 존재 + `notify_enabled=1` + DB로 다시 계산한 트리거 == 예약 시각일 때만 **DB 내용**으로 표시, 불일치+DB 미래면 재예약, DB 불가·과거 불일치는 미표시(일정 알림은 fail-closed). extra 없는 옛 예약은 DB 트리거가 지났으면 표시. `alarm_state.schedule_tab_enabled`(Device Protected, 기본 true): 탭 숨김/복원 채널이 기록, 앱 시작 시 `TabEnabledNotifier._load` → `syncScheduleTabEnabled`(값이 바뀔 때만 회수/복원), `schedule()`·`rescheduleAllFromDb()`가 확인(숨김이면 재예약 대신 일괄 취소). #10: `AlarmGuardReceiver` 시간/시간대 변경 분기에서 `rescheduleAllFromDb`, 지난 일정은 **예약만** 취소(이미 표시된 알림은 유지). #17: G1 Kotlin(AlarmRefreshEngine·AlarmActionHelper·AlarmWakeScheduler·DirectBootReceiver·AlarmGuardReceiver·CalendarWidgetScheduleResolver)의 DB 날짜 `SimpleDateFormat`을 `Locale.US`로(20분 전 알림 표시 시각만 기기 로케일 유지). Dart 저장은 `toIso8601String`·숫자 직접 조립이라 변경 없음. 수면 계열(G3 소유)은 대상 밖 — 연결 단계 확인 필요. V6: `DirectBootReceiver`가 `BOOT_COMPLETED`(LOCKED_BOOT와 같은 처리), `MY_PACKAGE_REPLACED`·`SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED`(허용일 때만)에 엔진 즉시 갱신(실패 목록 재시도 포함)+Guard+일정 알림 재예약. **Manifest intent-filter 3개는 `integration_requests.md` #1**(수신 코드는 준비됨). **테스트** Kotlin `G1ScheduleNotifyTest` 8건(DB 내용 표시 / 행 없음·알림 끔·탭 숨김 미표시 / 옛 시각 예약 재예약 / 숨김 시 재부팅·신규 예약 없음 / 동기화 회수·복원 / 시간대 변경 재예약+지난 일정 예약 삭제 / 업데이트 수신 재조정 / 권한 변경: 비허용 무시·허용 재조정), `G1LocaleTest` 2건(th-TH-u-ca-buddhist·fa-IR·ar-EG에서 불규칙 생성 fixture 일치·스누즈 저장 문자열·수신 파싱), Dart `g1_schedule_tab_sync_test` 3건 — Kotlin 65/65, `flutter test` 200/200, analyze 0건. 첫 실행에서 권한 변경 테스트가 Robolectric 기본 `canScheduleExactAlarms()=false`로 재조정을 건너뛰어 FAIL → 테스트가 비허용/허용 두 경우를 명시하도록 수정(제품 동작은 설계대로). **수정 전 FAIL 확인(#17)**: 로케일 변경만 들어간 3파일(AlarmRefreshEngine·AlarmActionHelper·AlarmWakeScheduler)을 7b56d3c로 되돌려 `G1LocaleTest` → 2건 FAIL(`th_TH_#u-ca-buddhist` 불규칙 생성 `expected:<[3건]> but was:<[]>` = 배정일 키가 불기 연도로 만들어져 알람 0개, 스누즈 저장 연도 `2569`), 복원 후 PASS. #5·V6 테스트는 신규 API 기반이라 수정 전 비교 불가(옛 동작은 코드 경로로 확인: 수신기 Intent 내용 그대로 표시, LOCKED_BOOT 외 무시). **미검증**: S7(시간대 변경 실기기)·S11(탭 숨김·재부팅·채널 실패)·S1/S2(V6 실제 OS 수신 — Manifest 반영 후). **잔여**: 백업 복원 직후 앱 시작 동기화 전 짧은 틈(#5), DST 전환일(M03) | `18985a7` | 연결(변경 통지 contracts §3)·문서 → 동결 |
| 2026-09-14 | Claude | T06 | **연결 — contracts §3 변경 통지(WIP).** 원본 저장이 성공한 뒤에만 `dataRevisionProvider` 증가: `ScheduleNotifier`(saveSchedule/updateSchedule/applyExternallyPersisted/resetSchedule/changeShiftWithAlarms → shiftSchedule + workHoursSettings, 근무표 행에 shift_durations 포함), `WorkHoursSettingsNotifier` 설정 4종(prefs 저장이 true일 때만 → workHoursSettings), `ConditionShiftTimeNotifier.save/remove`(→ shiftTimes), `OvertimeNotifier.adjust`(→ overtime), 근무명 변경 성공 뒤 settings_tab이 shiftTimes 추가 통지, 출퇴근 시각 저장은 #28에서 이미 통지. 각 Notifier는 `Ref?`를 선택 인자로 받아(Provider가 전달) 기존 직접 생성 호출과 호환. **소유권 요청** `overtime_provider.dart` → `integration_requests.md` #6(g2/g3 교집합 0건). **테스트** `g1_revision_test.dart` 5건(근무표 저장·수정 통지 / DB 실패 주입 시 미통지 / 근로시간 설정 / OT 증감 / 출퇴근 저장·삭제) — `flutter test` 전체 205/205, analyze 신규 경고 0. **범위 밖·확인 필요**: 계산 쪽 watch는 G3(동결 98c4831) — T10 통합 후 실제 재계산 연결 확인, 백업 복원(G4)의 통지는 G4 | `9b8a586` | 문서·동결 준비(변경 파일 소유권 검사, 헤더 채우기) |
| 2026-09-14 | Claude | T10 | **연결(통합 담당자).** release/g1에 `2b82adc` — `AndroidManifest.xml` DirectBootReceiver intent-filter에 BOOT_COMPLETED·MY_PACKAGE_REPLACED·SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED 추가(요청 #1). contracts §7.2 G1 Native 키 5개 등록·소유권 결정(#3·#4·#6) dev `ac672fe`. ARB 키(#2)는 브랜치에 포함. **빌드 확인** `flutter build apk --flavor dev --debug`(shiftbell-g1) → `app-dev-debug.apk` 생성, exit 0(assembleDevDebug 228.5s). 설치·실행은 안 함. **T11·T12 대상 SHA = `2b82adc`**(9b8a586 + Manifest 연결) | `2b82adc` | T11(Codex 교차 리뷰) → T12 |
| 2026-09-14 | Codex | T11 | **교차 리뷰 완료 — `CHANGES_REQUESTED`.** 대상 release/g1 `2b82adc`(기준 `94ff08f`, 동결 `9b8a586` + Manifest 연결)를 `git diff`/`git show`로만 검토. High 5건: 권한 조회 unknown→허용, extra 없는 옛 일정 예약의 stale 표시, 생성 diff key의 `day_offset` 누락, 최종 DB 재조회 실패 시 stale 예약 진행·동일 ID snooze 취소 위험, Dart 재생성의 무조건 삭제·재삽입으로 멱등성/ID/이력 훼손. Medium 3건: 무음 알람 인계 시 이전 진동 잔존, 탭 숨김 native 동기화 실패 은폐, 근무시간 저장 revision 중복 증가·도메인 오통지. 상세 `review.md`; 보강 기대값 2건 `review_fixture_additions.json`. 빌드·테스트·pub get·analyze·에뮬레이터 미실행, shiftbell-g1 수정/체크아웃 변경 없음 | 대상 `2b82adc` | G1 CODING 재개 후 8건 수정·회귀 테스트·재동결·연결 SHA 갱신. 그 전 T12 보류 |
| 2026-09-14 | Codex | T11-FIX | **T11-FIX는 Codex 수정(사용자 승인 예외). CODE_FROZEN.** T11-01~08 재현 테스트를 먼저 추가해 `2b82adc`에서 각 결함의 FAIL을 확인한 뒤 G1 동결 목록과 승인된 연결 요청 #3·#4·#6 범위에서만 수정. Native: 레거시 일정 fail-closed, `day_offset` diff 키, DB 재조회 실패 보수 처리, 새 회차 vibrator 정리. Dart: unknown 권한 거부, fixed 멱등 diff/ID·이력 보존, 탭 sync 실패 전파·rollback, 출퇴근 저장 domain별 revision 1회. 공통 fixture 2건을 Kotlin/Dart 양쪽에서 대조. DB v24·history/creation-log 자동 삭제·D7/D8/S9/G2 주입점 미변경. 표적 테스트 수정 후 PASS; Gradle 전체 PASS, Flutter 211/211 PASS, analyze 허용 error 1건만, dev debug APK PASS. 변경 파일 22개, worktree clean, push 안 함 | `438a9b9`, `e6e679a` | Claude가 `2b82adc..e6e679a` diff 가볍게 확인 후 T12. 기기·Doze·OEM·잠금화면은 T12/S 시나리오에서 검증 |
