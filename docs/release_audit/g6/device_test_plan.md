# 실기기·Console 통합 테스트 가이드 (출시 전, 2026-09-14)

> 대상: dev 브랜치 통합본(G0~G5 코드 완료). 자동 테스트·에뮬레이터로 확인할 수 없는 항목만 모았다.
> 실행계획 §7 S1~S18과 G6 연결 시나리오를 **세션 단위**로 묶었다. 결과는 각 표의 "결과" 칸에 PASS/FAIL/NOT_RUN과 날짜·기기·메모를 적는다.
> 사용자 결정·AI 위임 결정은 `docs/release_audit/decisions_delegated_2026-09-14.md` 참고.

## 0. 준비

| 항목 | 내용 |
|---|---|
| 기기 | 삼성 실기기 1대(필수), 두 번째 기기 또는 에뮬레이터 1대(S9 친구공유용). Android 10 미만 확인은 에뮬레이터 API 28 |
| 설치 | **반드시 flavor 지정**: `flutter install --release --flavor dev` (flavor 없이 설치하면 스토어 앱이 지워질 수 있음). 운영 앱 데이터는 건드리지 않음 |
| 업데이트 테스트(S1) | 스토어 설치본과 서명이 같아야 하므로 **Play 내부 테스트 트랙**으로 새 버전 배포 후 업데이트. 로컬 서명 APK로 덮어쓰기 금지 |
| 공통 설정 | 알림·정확한 알람·다른 앱 위에 표시 권한 허용(권한 조합 테스트 제외), 배터리 최적화는 기본값 유지 |
| 로그 | 문제가 보이면 `adb logcat -s CustomAlarmReceiver AlarmWakeScheduler AlarmRefreshEngine RestoreGate RestoreOs MainActivity` 캡처 |
| 금지 | 알람 테스트 중 설정 → 앱 → 강제 종료 사용 금지(알람 예약이 OS에서 지워져 결과가 무효) |
| 프로세스 종료 확인 (2026-09-15 재검토) | **최근 앱 스와이프는 프로세스 종료 증거가 아님**(task 제거일 뿐 PID가 남을 수 있음). 중단 시험은 홈으로 나간 뒤 `adb shell am kill <패키지>` → `adb shell pidof <패키지>`가 비어 있는지 기록. 실사용 알람이 없는 에뮬레이터/전용 기기에서 |
| 충전 (2026-09-15 재검토) | 충전기를 연결한 대기는 Doze 조건이 아님 - 기본 전달 확인으로만 셈. 절전 확인은 충전 분리 상태에서 |

## 세션 A — 알람 핵심 (단일 기기, 반나절)

| ID | 시나리오(원 번호) | 절차 | 합격 조건 | 결과 |
|---|---|---|---|---|
| A1 | 권한 조합 (S2, #4, #13, V6) | ① 알림 권한만 끄고 1~2분 뒤 알람 → 잠금/해제 상태 각각 ② 오버레이 권한만 끄고 반복 ③ 정확한 알람 권한 끄고 근무 저장 ④ 정확한 알람 권한 다시 허용 | ① 화면이 뜨고 끄기/5분 후 동작 ② 잠금화면 알람 화면 또는 제어 알림(알람 울림 중 · 5분 후 · 알람 끄기)으로 끌 수 있음 ③ 설정 탭 경고 배너, 근무 저장 시 "일부 알람을 설정하지 못했어요" 안내 ④ 허용 직후 앱을 안 열어도 다음 알람이 예약됨. **③④는 기기별 적용 확인**: Manifest가 `USE_EXACT_ALARM`(자동 부여·사용자 취소 불가)과 `SCHEDULE_EXACT_ALARM`을 함께 선언하므로 설정에서 거부 상태를 못 만들 수 있음 → 그 경우 N/A와 사유 기록 | |
| A2 | Doze 전달 (S3, #20) | 화면 끄고 충전기 분리, 5분 간격 알람 3개 + 그중 하나 스누즈 + 같은 시간대 일정 알림 1개. 1시간 이상 방치(가능하면 `adb shell dumpsys deviceidle force-idle`) | 기상 알람은 예약 시각 ±1분 안에 울림, 상태바에 다음 알람 아이콘 표시. 실제 울린 시각을 적어 둠. **일정 알림은 예약 API가 달라 ±1분 기준을 그대로 적용하지 않고 지연 시간만 기록** | |
| A3 | 소리 유지·자동 종료 (S4, #3, #14, H03-GATE) | ① 울림 중 홈 버튼 → 방치 ② 잠금 상태에서 방치 ③ 진동 알람 울림 중 무음 알람 도착 ④ 울림 중 **다른** 알람 삭제 ⑤ 울리는 알람 자체를 앱에서 삭제 ⑥ 지속시간 5분 이상 알람을 끝까지 방치 | ①② 알람 타입의 지속시간이 지나면 자동으로 멈추고 이력이 "시간 초과" ③ 이전 진동이 남지 않음 ④ 울림 계속 ⑤ 즉시 멈추고 이력 기록 ⑥ **H03: 소리가 중간에 끊기거나 앱 프로세스가 죽지 않음** | |
| A4 | 스누즈·늦은 취소 (S13, #16, #27) | ① 5분 스누즈 → 재울림 ② 스누즈 대기 중 앱에서 그 알람 시각 변경 ③ 알람 삭제 직후 곧바로 앱 종료 | ① 5분 뒤 같은 소리로 재울림 ② 옛 시각에 울리지 않고 새 시각에 울림 ③ 삭제한 알람이 울리지 않음 | |
| A5 | 개별 알람 예외 (S16, #31) | 규칙 근무 알람 1개 삭제, 다른 1개를 무음 타입으로 변경 → 자정 넘기기(또는 앱 재시작·재부팅) → 해당 날짜 근무를 다른 근무로 바꿨다가 되돌림 | 자정·재시작 뒤에도 삭제/무음 유지. 근무를 바꾸면 예외가 정리되어 새 근무 기준 알람이 생김 | |
| A6 | 불규칙 자동 생성 (S15, #26) | 불규칙 근무에서 11일 이상 뒤 날짜에 근무 배정 → 앱을 열지 않고 날짜가 10일 창에 들어올 때까지 대기(또는 시스템 날짜 이동 후 원복) | 앱을 안 열어도 그날 알람이 생성·예약됨. 템플릿을 모두 지우면 남은 fixed 알람이 정리됨 | |
| A7 | 재부팅 (S1·S15, V6, T04-D) | 알람 10분 전 재부팅 → **잠금 해제하지 않고** 대기 | 잠금 해제 전에도 울림, **같은 알람이 두 번 울리지 않음** | |
| A8 | 앱 업데이트 (S1, #1, T04-D ①②) | 현재 스토어 버전으로 알람 설정 → 내부 테스트 트랙 새 버전으로 업데이트 → **앱을 열지 않고** 다음 알람 대기 / 업데이트 뒤 재부팅 → 잠금 해제 전 알람 | 두 경우 모두 울림, 앱을 열면 데이터·알람 목록 정상 | |
| A9 | 시간대 변경 (S7, #10) | 알람·일정 알림이 있는 상태에서 시간대를 다른 나라로 변경 → 원복 | 알람·일정 알림이 새 시간대 기준 시각으로 다시 걸림, 이미 지난 시각이 된 일정 알림은 울리지 않음 | |
| A10 | 근무명 (S8, #11, #12, #28) | 쉼표·"미설정" 이름 입력, A↔B 이름 맞교환, 같은 이름 중복, 근무시간 설정에서 출퇴근 "초기화" 연타 | 쉼표·예약어·중복 차단 안내, 맞교환 후 달력·알람·예외 모두 정상, 연타해도 근무시간 값이 되살아나지 않음 | |
| A12 | 백그라운드 울림 지속 (AUD-04, H03) | 충전 분리·배터리 기본값에서 지속시간 5분 이상 알람: ① 잠금 상태 ② 해제 + 오버레이 허용 ③ 해제 + 오버레이 거부(제어 알림만) ④ 알람 화면에서 홈 이동 후 방치. 각 경우 울리는 중 `adb shell pidof com.hwani1103.shiftbell.dev` 2~3회 | 지속시간 끝까지 소리 유지 후 자동 종료, 중간에 PID가 사라지지 않음. **불안정하면 재생을 foreground service로 옮기는 설계 결정 필요**(코드 미수정 상태). 한 기기 통과가 모든 OEM을 증명하지 않음 | |
| A13 | 미리듣기 중 실제 알람 (AUD-06) | 시스템 알람 볼륨을 낮게(예: 1단계) → 설정 → 알람 타입 소리 미리듣기 재생 중 "지금 + 1분" 실제 알람 도착 → 미리듣기 종료 → 실제 알람 끄기 | 실제 알람이 무음이 되지 않음, 알람을 끈 뒤 시스템 알람 볼륨이 처음 값으로 돌아옴 | |
| A11 | 일정 알림·탭 숨김 (S11, #5, T11-07) | 일정 알림 켠 일정 추가 → 일정관리 탭 숨김 → 재부팅 → 탭 복원 / 일정 시각을 바꾼 직후 옛 시각 대기 | 숨김 동안 알림 없음(재부팅 후에도), 복원하면 다시 예약, 옛 시각 알림 없음 | |

## 세션 B — 수면·컨디션 (하룻밤)

| ID | 시나리오 | 절차 | 합격 조건 | 결과 |
|---|---|---|---|---|
| B1 | 자동 수면 후보 종료 (S14, #22) | 수면 감지 창에서 휴대폰을 오래 사용하지 않아 후보가 생기게 함 → **화면을 켜지 않고** 9시간 이상 방치 → 기상 후 앱 열기 | 후보가 "시작 + 9시간" 시각으로 끝나 있고 확인 대기 카드 표시(자동 확정 아님). 화면을 켠 경우와 꺼진 채 알람 경로의 종료 시각이 같음 | |
| B2 | OT 반영 (#15) | 달력에서 OT 추가·삭제 → 수면·회복 탭 | 앱 재시작 없이 "최근 7일 초과근무" 사실·추천 행동이 바로 바뀜 | |
| B4 | 자정 후 오늘의 컨디션 (AUD-05) | 서로 다른 근무가 이어지는 날 23:50쯤 수면·회복 탭을 연 채로 두기 → 00:01 이후 확인, 또는 앱을 배경에 둔 채 다음 날 재개(근무 수정·앱 재시작 없이) | 오늘 기준 판정·OT 7일 범위·오늘의 컨디션(상황 줄·어제 수면)이 새 날짜로 바뀜(점수는 2026-09-15 삭제) | |
| B3 | 복원 중 수면 위젯 (G3 #2) | 세션 C 덮어쓰기 복원 진행 중에 홈 화면 수면 위젯 버튼 누르기 | "백업 복원 중이에요" 토스트, 기록 안 생김, 복원 뒤 위젯 정상 | |

## 세션 C — 백업·복원 (단일 기기, 1~2시간)

| ID | 시나리오 | 절차 | 합격 조건 | 결과 |
|---|---|---|---|---|
| C1 | 자동 백업 변경 감지 (S6, #2) | ① 메모 추가 → 홈으로 나가기 ② 테마만 변경 → 홈으로 ③ 아무것도 안 바꾸고 여러 번 열고 닫기 ④ 설정 → 지금 백업 | ①② 다운로드/ShiftBell 파일 시각이 갱신 ③ 파일이 늘거나 매번 새로 써지지 않음 ④ 성공 안내, 폴더에 백업 파일은 최대 2개 - 최신 + 직전 정상본(교차 검토 X-07, dev는 `ShiftBell_Backup_dev_…`) | |
| C2 | 불완전 파일 (#18) | 이력이 많은 상태에서 "지금 백업" 직후 곧바로 홈 → `adb shell am kill` + `pidof`로 PID 소멸 확인(스와이프만으로는 종료 증거 아님) → 재실행 후 다시 백업 | 폴더에 크기 0이거나 깨진 백업 파일이 남지 않음, 이전 정상 백업이 유지됨 | |
| C3 | 신규 설치 복원 (S5, #9, #25, V2) | 불규칙/규칙 근무표·미래 사용자 알람(직접 추가)·개별 예외·일정 알림·수면 기록·친구 공유 켬 상태에서 백업 → dev 앱 삭제 → 재설치 → 권한 허용 | 복구 안내가 자동으로 뜸(안 뜨면 온보딩의 "이전 백업 불러오기"로 직접 선택 - 기기 한계 V2로 기록). 복구 후 근무표·예외·**직접 추가한 미래 알람**·일정 알림·수면 기록 복원, 알람이 실제로 울림, **친구 공유는 꺼진 상태** | |
| C4 | 덮어쓰기 복원 + 진행 중 알람 (S5·S17, #19) | ① 알람이 **울리는 중** 설정 → 백업 데이터 불러오기 → 복원 ② 알람을 **스누즈한 뒤** 5분 안에 다른 백업으로 복원(백업의 알람 타입 소리를 일부러 다르게) | ① 울림·끄기 버튼이 계속 동작하고 끄면 이력 1건 ② 5분 뒤 **복원 전 소리·지속시간으로** 재울림 - **잠금 해제 상태(오버레이)에서도 복원 전 지속시간까지 울림**(AUD-03: 예전엔 오버레이가 복원된 짧은 지속시간으로 먼저 끔), 복원 후 알람 목록·일정 알림 정상, 앱이 재시작됨 | |
| C4-2 | 재시작 경계의 새 알람 (AUD-02) | 에뮬레이터/전용 기기: 복원 완료 직전(진행 화면 표시 중) 도착하도록 "지금 + 1분" 알람을 만든 뒤 덮어쓰기 복원. 타이밍이 안 맞으면 여러 번. logcat `MainActivity`의 "재시작 요청 거절"/"재시작 보류" 확인 | 새로 울리기 시작한 알람이 재시작으로 끊기지 않음, 끈 뒤 앱이 재시작됨 | |
| C5 | 복원 중단 (S17) | 덮어쓰기 복원 "복구하는 중..." 표시 중 홈 → `adb shell am kill` + `pidof`로 PID 소멸 기록(데이터가 작으면 복원이 먼저 끝날 수 있음 - 그때는 N/A와 사유) → 기다렸다가 다음 알람 확인 → 앱 열기 | 알람은 끊기지 않음(현재 데이터 기준). "백업 복원이 완료되지 않았어요" 알림(또는 앱 시작 화면). "이어서 복원하기" → 백업 내용으로 완료 / 다시 한 번 중단 후 "지금 데이터로 계속 쓰기" → 알람·일정 알림 정상 | |
| C6 | 잘못된 파일 | 설정 → 백업 데이터 불러오기 → 백업이 아닌 JSON/근무 일정 없는 백업 선택 | 오류·안내만 뜨고 **지금 데이터는 그대로** | |
| C7 | Android 10 미만 (V3) | 에뮬레이터 API 28에서 지금 백업 | "Android 10 이상 필요" 실패 안내, 앱 정상 | |
| C8 | 친구공유 상태 복원 (#25, G2-03) | 친구 공유 켠 기기에서 다른 백업으로 덮어쓰기 복원 / 공유 중지 대기(오프라인에서 중지) 상태에서 복원 | 공유는 이 기기 상태 그대로(켜짐이면 복원된 근무표가 친구에게 반영, 중지 대기면 계속 대기 후 온라인에서 삭제) | |

## 세션 D — 친구공유 두 기기 (S9, #21, #24)

> AI 위임 결정(D8): dev 앱도 운영과 **같은 Firebase 프로젝트**를 씀. 테스트가 끝나면 두 기기 모두 공유 중지.

| ID | 절차 | 합격 조건 | 결과 |
|---|---|---|---|
| D1 | 기기 A 공유 시작 → 기기 B가 코드로 친구 추가 → A 근무 수정 | B 새로고침에 반영 | |
| D2 | A 비행기 모드 → 근무 수정 → 공유 중지 → 앱 스와이프 종료 → 재실행 → 근무 수정 → 온라인 | A 화면에 "공유 중지를 서버에 보내는 중" 배너, 온라인 뒤 사라짐. **B에서 A 근무표가 다시 나타나지 않음(부활 없음)** - 나타나면 D7 재검토 사유로 기록 | |
| D3 | B 비행기 모드에서 친구 목록 새로고침 | 친구·마지막 근무표 유지, "확인 불가" 표시(삭제로 단정 안 함) | |
| D4 | A 공유 중지 확인 후 B 온라인 새로고침 | B 목록에서 A 제거 | |
| D5 | (a) A가 **공유 중지 확인 후** 재설치 → B의 옛 링크 열기 (b) A가 **중지 없이** 재설치 → B의 옛 링크 열기 | (a) "불러올 수 없어요" 안내 (b) **옛 문서가 서버에 남아 마지막 근무표가 계속 보일 수 있음이 현재 구현의 정상 기대값**(재설치 감지·TTL 없음, 개인정보 안내의 "회수 불가" 설명과 일치하는지 확인). 2026-09-15 재검토로 기대값 정정 | |
| D6 | A 이름 변경을 오프라인에서 → 온라인 | "아직 서버에 반영되지 않았어요" 안내 후 자동 재전송 | |

## 세션 E — 출시 빌드·Console (사용자, 최종 배포 직전)

| ID | 항목(원 번호) | 절차 | 합격 조건 | 결과 |
|---|---|---|---|---|
| E1 | 운영 광고 ID (#6, D2, X-13) | 앱 ID는 사용자 홈 `~/.gradle/gradle.properties`의 `ADMOB_APP_ID`로 두고 `flutter build appbundle --release --flavor prod --dart-define=ADMOB_BANNER_ID=<실제 배너 ID>` → 산출물 SHA·versionCode 기록 → **Play 내부 테스트 트랙**에 올려 테스트 계정으로 설치/기존 버전 업데이트 | 광고 영역에 파란 틴트 없음, 테스트 광고 라벨 없음, **본인 광고 클릭 금지**. ID를 빠뜨리거나 테스트 ID면 **prod release 빌드가 실패하는 것이 정상**(`checkProdAdIds`) - "빈 광고 자리로 빌드 성공" 기대값은 2026-09-15 재검토로 삭제 | |
| E2 | 레이아웃 (S10, M12, AUD-07) | 기본 폰 360dp + 큰 글꼴, 키보드 열린 상태, 넓은 화면 1종(폴드 펼침 또는 600dp 이상 에뮬레이터) | 저장·복원·끄기 버튼에 닿을 수 있음, 넓은 화면에서 광고가 콘텐츠 폭(최대 500dp) 안에 표시. 전 조합(360/600/840×글꼴×방향) 전수는 필수 아님 | |
| E3 | Firebase 앱·통계 목적지 (S12, #29, D8) | Firebase Console → Analytics DebugView/실시간: prod 앱 사용 시 prod 앱 스트림에 표시, dev 앱은 수집 안 됨 | dev 설치가 운영 DAU에 섞이지 않음 | |
| E4 | 운영 Firestore rules (V5) | Console의 rules와 저장소 `firestore.rules` 비교 | 동일(다르면 저장소 기준으로 사용자가 배포) | |
| E5 | Play 데이터 보안·권한 신고 (#7, D3, V7) | 앱 내 개인정보 안내(설정 → 개인정보처리방침)와 Play 데이터 보안 양식 대조: 기기 식별자·앱 활동(Analytics), 광고 ID(AdMob), 이름·근무 일정(친구공유, 사용자 선택), 암호화 안 된 로컬 백업. 정확한 알람·전체 화면 알림 권한 신고 | 세 곳(앱 안내·SDK 실제 수집·Play 양식)이 일치. 안내문 효력일을 배포일로 맞춤 | |

## G6 연결 시나리오 (최종 빌드 1회)

1. 업데이트 → 앱 미실행 → 잠금 해제 전 울림 (A8)
2. 불규칙 근무 자동 생성 → 개별 예외 → 근무 변경 → 백업 → 복원 (A6·A5·C3)
3. 복원 중 스누즈 → 앱 스와이프 종료 → 재실행 → 이어서 복원 (C4·C5)
4. 공유 중지 대기 → 덮어쓰기 복원 → 온라인 (C8·D2)
5. OT·수면 기록 → 자정 → 시간대 변경 (B2·A9)

## AI가 코드·자동 테스트로 이미 확인한 범위 (참고)

- G0 DB 마이그레이션 Flutter/Kotlin 자동 테스트, 에뮬레이터 S1 ①② (T03/T04-E)
- G1 알람 로직 Kotlin/Flutter 단위 테스트(교차 리뷰 T11 수정 포함), G2 상태 머신·Firestore rules 에뮬레이터(T13)
- G4/G5는 컴파일·기존 자동 테스트 회귀까지만(사용자 지시로 전용 테스트 보류) - **세션 C·E는 이번 코드의 첫 실제 검증**
- **자동 테스트·AI 리뷰는 OS 생명주기(Doze 전달 지연, 권한 제한, 프로세스 회수, 복원 중단)를 대신하지 않는다.** 여기 표에 NOT_RUN으로 남은 항목을 코드 리뷰 결과로 PASS 처리하지 말 것(2026-09-15 재검토 `recheck_2026_09_15/release_readiness_review.md` §5·§6, 조치 기록 `recheck_2026_09_15/fix_record.md`)
- 사용자용 1대 기기 절차는 바탕화면 `교대시계_출시전_필수_실기기테스트.txt`(이 표의 A·B·C·D·E 중 핵심을 묶음)
