# 실제 수면 기록/자동 추정 — Android 기술 조사 (구현 전 필수 조사)

> 2026-08-31, "C번 요구사항"(실제 수면 데이터 수집 및 자동 추정 모듈) 착수 전 조사.
> 결론부터: **지속적인 화면 감시(BroadcastReceiver 상시 등록)도, Foreground Service도,
> 특수 권한(사용정보 접근)도 1차 버전에는 쓰지 않는다.** 이미 이 앱이 쓰고 있는
> "정확한 알람(AlarmManager) 기반 주기적 깨어남" 패턴을 재사용하는 것으로 충분하고,
> 그게 배터리/OS 정책 양쪽에서 가장 안전하다.

## 1. 현재 프로젝트의 제약 조건 확인

- `compileSdk = 36`, `targetSdk = 36` (Android 16 대상), `minSdk = flutter.minSdkVersion`
  (`android/app/build.gradle.kts`)
- 이미 보유한 권한: `SCHEDULE_EXACT_ALARM`, `USE_EXACT_ALARM`, `WAKE_LOCK`,
  `RECEIVE_BOOT_COMPLETED`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_SPECIAL_USE`
  (`AndroidManifest.xml`) — 정확한 알람 권한은 이미 있음. 새 권한 없이 시작 가능.
- 이미 쓰고 있는 "주기적으로 깨어나는" 인프라: `AlarmGuardReceiver`(자정 또는 다음
  알람 20분 전 중 더 이른 시점에 `AlarmManager`로 깨어남) + `CalendarWidgetProvider`가
  그 하트비트에 얹혀서 위젯을 갱신하는 패턴(`AlarmGuardReceiver.triggerCheck()`).
  **이 패턴을 그대로 재사용**하는 게 이번 조사의 결론이다.
- `DirectBootReceiver`가 재부팅 시 `AlarmGuardReceiver.triggerCheck()`를 호출해 알람을
  다시 예약함 — `AlarmManager` 알람은 재부팅하면 전부 사라지므로, 새로 추가하는 수면
  감지용 알람도 반드시 여기 편승해서 재예약해야 함.
- `CalendarWidgetScheduleResolver.kt`가 이미 "근무 스케줄을 읽기 전용으로 Kotlin에서
  해석"하는 로직(패턴 순환 + assignedDates 우선순위, DST 안전 Julian Day 계산)을
  가지고 있음 — **이 객체를 그대로 호출**해서 재사용하면 같은 로직을 또 복제하는
  위험을 크게 줄일 수 있다(완전히 못 피하는 부분만 조사 3장 참고).

## 2. 조사한 방법과 기각/채택 이유

### ① `ACTION_SCREEN_ON`/`ACTION_SCREEN_OFF` 상시 리시버 — 기각
- 이 두 브로드캐스트는 **매니페스트에 등록할 수 없음**(암묵적 브로드캐스트 제한,
  Android 8.0+). 반드시 "살아있는 프로세스"에서 `registerReceiver()`로 동적 등록해야
  하는데, 그러려면 앱을 계속 살려두는 무언가(Foreground Service 등)가 필요함.
- Android 개발자 문서/커뮤니티 공통 결론: "화면 on/off를 상시 감시하려고 앱을 계속
  띄워두지 말 것"이 명시적으로 권장되지 않는 패턴. 배터리 소모 + Doze 정책과 정면
  충돌.
- **기각.**

### ② Foreground Service로 상시 감시 — 기각
- 기술적으로는 가능하지만, Android 공식 가이드가 "시스템이 앱을 idle로 판단하는 걸
  막으려고 Foreground Service를 쓰지 말라"고 명시함. 알림 아이콘이 항상 떠 있어야
  하고(사용자 경험 저하), 배터리 소모도 큼.
- 이 앱은 이미 `AlarmOverlayService`(알람이 실제로 울리는 짧은 시간에만 동작)와
  `FOREGROUND_SERVICE_SPECIAL_USE` 권한을 그 용도로만 씀 — "상시 수면 감시"용으로
  전용하면 그 권한의 성격이 완전히 달라져 스토어 정책 리스크도 커짐.
- **기각.**

### ③ `UsageStatsManager` + "사용정보 접근" 특수 권한 — 1차 보류, 2차 후보
- `UsageEvents`의 `SCREEN_INTERACTIVE`(15)/`SCREEN_NON_INTERACTIVE`(16) 이벤트를
  조회하면 화면 on/off **이력**을 나중에 한 번에 조회할 수 있어(Pull 방식이라 상시
  감시가 필요 없음) 정확도가 가장 높음.
- 그러나: (a) `PACKAGE_USAGE_STATS`라는 **특수 권한**이 필요하고, 이건 일반적인
  런타임 권한 다이얼로그가 아니라 사용자가 설정 앱에서 수동으로 켜야 함(마찰 큼).
  (b) Android R(11)+부터 **기기가 잠금 해제 상태가 아니면 `queryEvents()`가 null을
  반환**할 수 있음 — 하필 "수면 중"은 기기가 잠겨있을 확률이 매우 높은 상황이라 이
  한계가 뼈아픔. (c) 이벤트 보관 기간이 며칠로 제한됨.
- **결론**: 정확도는 더 좋지만 권한 마찰 + 잠금 상태 null 이슈가 겹쳐서 1차 MVP엔
  안 씀. 추후 정확도를 높이고 싶을 때 "선택적으로 켤 수 있는 고급 옵션"으로 검토
  (스펙 19장 "신뢰도" 체계와 자연스럽게 연결 가능 - 이 권한이 있으면 신뢰도 상한을
  더 높게 잡는 식).

### ④ `AlarmManager.setExactAndAllowWhileIdle()` 기반 주기적 샘플링 — **채택**
- 이미 이 앱이 `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM` 권한을 갖고 있고,
  `AlarmGuardReceiver`가 같은 방식(`AlarmManager`)으로 안정적으로 동작 중임이 실증됨.
- Doze 상태에서도 `setExactAndAllowWhileIdle()`은 전달되며, 다만 Doze 심화 단계에서는
  앱당 약 15분에 1회로 빈도가 제한됨 — 이번 설계의 샘플링 주기(20분)가 정확히 이
  제약과 맞아떨어짐(더 짧게 잡아봐야 어차피 시스템이 15분 이하로는 안 줄여줌).
- 특수 권한 불필요, 잠금 상태와 무관하게 동작(알람 자체가 잠금 여부와 무관), 상시
  프로세스 불필요(브로드캐스트 수신 시점에만 짧게 깨어났다가 종료).
- **매 순간이 아니라 "수면 가능 시간대"에만** 이 알람을 예약해서(9장 이하 설계 참고)
  하루 종일 도는 게 아니라 하루 중 몇 시간 구간에서만, 그것도 20분 간격으로만
  깨어나게 함 — 사실상 알람 몇 개 추가되는 정도라 기존 알람 시스템 대비 배터리
  부담이 거의 없음.
- **채택.**

### ⑤ "화면 켜짐 여부"를 무권한으로 확인하는 방법 — `PowerManager.isInteractive()`
- 별도 권한 없이 호출 가능. 다만 이건 **그 순간의 스냅샷**만 알려주지, 이력을 안
  줌 - 그래서 ④의 20분 간격 샘플링과 묶어서 "연속 2회 이상 꺼짐 샘플"로 판단해야
  "장기간 미사용"을 추정할 수 있음(단발성 스냅샷 하나로는 방금 껐는지 1시간 전에
  껐는지 구분 불가).
- 배터리 상태(`Intent.ACTION_BATTERY_CHANGED` sticky, 무권한)도 같은 방식으로 그
  순간만 확인 가능 — 신뢰도 보정에만 사용(4장 참고).

## 3. 채택한 아키텍처 요약

```
[AlarmGuardReceiver 하트비트] ─┐
[MainActivity.onResume]      ─┼─▶ SleepDetectionScheduler.ensureScheduled()
[DirectBootReceiver(재부팅)]  ─┘        │
                                        ▼
                      "지금이 수면 가능 시간대인가?"
                      (SleepScheduleResolver - 3장 하단)
                                        │
                     아니오 ──▶ 다음 체크 시점만 예약(창 시작 or 자정)
                                        │
                     예 ──▶ 20분 뒤 AlarmManager.setExactAndAllowWhileIdle 예약
                                        │
                                        ▼
                          SleepDetectionReceiver.onReceive()
                          - 이미 수동(MANUAL) 수면 진행 중이면 스킵(수동 항상 우선)
                          - PowerManager.isInteractive() 스냅샷
                          - 연속 2회 꺼짐 확인되면 sleep_records 후보 생성/연장
                          - 켜짐 감지되면 후보 종료(end 기록) + 신뢰도 계산
                          - 항상 끝에 ensureScheduled() 재호출(자기 자신을 계속 예약)
```

- **Sleep Detection(감지)과 Shift Health Engine(판정) 완전 분리**: `SleepDetectionReceiver`는
  오직 `sleep_records` 테이블에 쓰기만 하고, `ConditionRuleEngine`(기존 B번 작업)은
  단 한 줄도 수정하지 않는다. 실제 수면 데이터를 컨디션 분석에 연결하는 코드는 별도
  파일(`sleep_condition_extras.dart`)로 분리해서, 나중에 감지 방식이 통째로 바뀌어도
  (예: ③번 방식으로 교체) 분석 엔진 쪽은 전혀 손댈 필요가 없게 함(수면기록_자동추정_설계.md
  6장).
- **schedule 읽기 로직 중복 최소화**: `SleepScheduleResolver.kt`(신규)는
  `CalendarWidgetScheduleResolver.kt`(기존, 수정 없음)를 그대로 호출해서 "오늘 근무가
  뭔지"는 재사용하고, 거기 없는 것(근무별 출퇴근 시각 `condition_shift_times`, 근무별
  평균 취침시각 `sleep_expected_bedtime` — 둘 다 B번/C번에서 새로 만든 테이블)만
  추가로 읽는다. 그래도 "수면 가능 시간대 계산" 자체의 알고리즘은
  `lib/services/condition/sleep_opportunity.dart`(Dart, 사람이 읽는 기준 구현체)와
  Kotlin 양쪽에 각각 존재할 수밖에 없음(백그라운드 감지는 앱이 꺼져 있어도 동작해야
  하므로 Native가 자체적으로 계산해야 함) — **알람 생성 로직(Dart
  `alarm_generation_service.dart` ↔ Kotlin `AlarmRefreshEngine.kt`)과 완전히 동일한
  선례**이고, CLAUDE.md에 이미 이런 쌍을 어떻게 관리하는지 원칙이 있어 그대로 따름
  (두 파일 상단에 "동일하게 유지할 것" 경고 주석 + 이 문서를 근거 문서로 링크).

## 4. 신뢰도(LOW/MEDIUM/HIGH) — Rule 기반, 최소 버전

머신러닝 없이 아래 규칙만 사용(스펙 19장 지시대로):

| 조건 | 효과 |
|---|---|
| 기본값 | MEDIUM |
| 사용자가 설정한 "근무별 평균 취침시각"을 기준으로 창이 열렸음 | MEDIUM → HIGH |
| 근무 일정 기반 추정(패턴상 회복구간)만으로 창이 열렸음 | 그대로 MEDIUM |
| 확정된 구간 길이가 90분 미만 | 한 단계 낮춤(그냥 잠깐 안 만졌을 가능성) |
| 후보 구간 동안 충전 중이었음(`ACTION_BATTERY_CHANGED` 스냅샷) | 한 단계 올림(취침 중 충전은 흔한 패턴) |

## 5. 배터리 영향 결론

- 알람은 "수면 가능 시간대"에만 20분 간격으로 예약됨 — 하루 중 무제한이 아니라
  보통 6~10시간 구간 한정, 하루 약 20~30회의 정확 알람. 이미 이 앱이 알람마다
  등록하는 `CustomAlarmReceiver`/`AlarmGuardReceiver`와 같은 급의 부하라 유의미한
  추가 배터리 소모가 없음.
- 각 깨어남에서 하는 일은 DB 쿼리 1~2회 + `PowerManager` 스냅샷 1회 + (필요시)
  `AlarmManager` 재예약 1회뿐 — 네트워크 요청 없음, 지속 실행 없음.
- 사용자가 위젯에서 수동으로 취침을 눌러 MANUAL 기록이 진행 중이면, 자동 감지
  하트비트 자체가 즉시 스킵되므로(3장) 수동 기록을 쓰는 사용자는 사실상 자동
  감지 오버헤드가 거의 0에 가까움.

## 6. 이번 조사에서 다루지 않은 것(다음 단계 후보)

- `UsageStatsManager` 기반 고정밀 모드(3장 ③) — 사용자가 원할 때만 켜는 옵션으로.
- `BatteryManager`를 활용한 더 정교한 충전 패턴 분석.
- WorkManager로 전환 여부 — 지금 방식(AlarmManager 직접 사용)이 이미 이 앱의
  기존 알람 인프라와 100% 동일한 신뢰도를 가지므로, 굳이 새로운 백그라운드 실행
  프레임워크(WorkManager)를 추가로 들여올 이유가 없다고 판단(의존성/복잡도 최소화).
